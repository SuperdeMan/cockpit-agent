# 车书手册 RAG：车主口语问法召回（2026-09-26）

> 状态：**已发布 `0a4f626a`，真栈验收通过（§6）**
> 交付对象：manual-rag Agent、Cloud Planner 知识层（guide / exemplar / 能力描述）
> 关联：`agents/manual_rag/src/providers/local_index.py`（检索 v3）、`agents/manual_rag/src/toc.py`、
> `agents/manual_rag/src/toc_router.py`、`agents/manual_rag/src/agent.py`、`agents/manual_rag/resources/retrieval.yaml`（schema v2）、
> `agents/manual_rag/manifest.yaml`（0.4.0）、`agents/scene_orchestrator/manifest.yaml`（`scene.list` 反界定）、
> `skills/guides/manual-help-boundary.yaml`（v4）、`skills/exemplars/manual.yaml`、`test/eval_corpus/manual_rag_colloquial.yaml`、
> `scripts/probe_manual_rag_full_coverage.py --kind colloquial`
> 前序：[v2 实施](2026-09-03-xiaomi-su7-manual-rag-v2-implementation-plan.md)、[整本覆盖验证](2026-09-04-xiaomi-su7-manual-rag-full-coverage-validation-plan.md)

## 0. 结论

用户点名的「xxx 功能有哪些模式 / 怎么开启 / 有几个档位、后备箱能放几个行李」在生产上几乎全部召回失败。
**根因不在手册内容，也主要不在排序**：正确页往往排第一，却被零命中闸拦下——问句壳切出来的双字
（「有几 / 几个 / 个档 / 有哪」）整本手册一次都没有、IDF 最高，把覆盖率压到 0.42 以下。此外还叠着
「档 / 挡」异体、车主叫法与手册用词错位、Agent 丢掉规划器已补全的指代问题、规划器把「空调有什么模式」
当成场景列表、执行超时只有 2.5 s 五层问题。

修法分两层：**确定性词法层**重写查询理解（问句壳词表取自 `runtime.question_shape` 唯一实现、三类词分开计分、
四道范围闸）；**目录路由**在词法零命中 / 没把握 / 含手册不认识的实词时，让 LLM 在手册目录（封闭集合）里选章节，
LLM 只产目录编号、不见正文、不作答。

离线读数（同一份获准索引，逐条可复算，§5）：

| 尺子 | 规模 | 生产 HEAD | 新词法层 | 词法 + 目录路由 |
|---|---:|---:|---:|---:|
| collector 真实问法语料（仓库） | 42 | 15 | 40 | **42** |
| 留出集（盲写 #2，首次打开读数） | 135 | 22 | 65 | **129**（终版复跑 130） |
| 开发集（盲写 #1，调参用） | 165 | 31 | 118 | 159 |
| 原 36 题 / 章节 187 / 路径 160 / 页锚 269 / 视觉 35 | — | 全满 | 全满，Top-1 与基线逐条一致 | — |

负例（别家车型、燃油车部件、手机 / 闲聊）两套盲写集 30/30 零命中。

## 1. 取证：云端 collector 的真实问法

只读导出 collector 全部轮次（2026-07-10 → 2026-09-26，6 305 轮、514 句不同话术；存档
`.artifacts/manual-rag-colloquial/collector-turns-20260926.json`）：

- 落到 `manual.query` 的 **562 轮里 488 轮是「手册里没有查到」（86.8%）**。高频：「座椅有哪些调节功能」53、
  「车窗有哪些开启方式」35、「座椅有哪些加热档位」32、「座椅加热有几个档位」30、「大灯有几个模式」28、
  「打开后备箱，再告诉我空调有哪些模式」及其倒装各 27、「这车有几个座位」27、「后备箱能放几个行李箱」26、
  「空调有几个风量档」18、「氛围灯有哪些颜色可以选」7、「座椅加热在哪里打开」5。
  评审三轮把「座椅有哪些调节功能」记成「手册库没有这一项，路由对、内容缺」——实际第 29–33 页就是座椅调节，**是检索闸误判，不是内容缺**。
- 「空调有什么模式」**57 轮里 56 轮被规划成 `scene.list`**，答的是「回家 / 露营 / 午休」场景。
- 规划器写的 `question` 槽与原话：562 轮里 440 轮一致；不一致的主要两类都是规划器做对了——多诉求句只抽本步
  （54 轮「空调有哪些模式」）、指代补全（「它有几档」→「座椅加热有几档」，W19-c 历史视窗探针），
  但 Agent 取的是 `raw_text or slot`，原话优先，补全被丢掉。少数补全指错对象（「它锁上以后…」→「车锁上以后…」），仍比光秃秃的「它」强。

## 2. 根因（逐层）

| # | 层 | 证据 | 性质 |
|---|---|---|---|
| R1 | 零命中闸 | 「座椅加热有几个档位」第 37 页排序第一（质量 7.46，第二名 4.21），覆盖率 0.21 < 0.42：「热有 / 有几 / 几个 / 个档」全书 df=0、IDF≈6.3，主题词只有 1.6–4 | 主因 |
| R2 | 异体 | 手册统一写「挡」，用户写「档」 | 规范化缺口 |
| R3 | 答案类型词 | 「车窗有哪些开启**方式**」：车窗页列「手动模式 / 自动模式」，不写「方式」 | 覆盖率口径 |
| R4 | 句法产物 | 「怎么**把车**窗**打**开」的「把车 / 窗打」、「亮**了是**什么意思」的「了是」 | 覆盖率口径 |
| R5 | 叫法错位 | 大灯 / 外灯、定速巡航 / 自适应巡航、车架号 / 车辆识别代号、小电瓶 / 低压电池、空悬 / 空气悬架… | 同义词缺口 |
| R6 | 描述式问法 | 「停车时有人刮车能录下来吗」= 哨兵模式、「车顶上凸出来那个」= 激光雷达 | 词法天花板 |
| R7 | 检索用哪句话 | 原话优先，丢掉规划器已补全的指代 / 已拆分的本步问题 | Agent 缺陷 |
| R8 | 规划落域 | `scene.list` 描述「列出可用的场景模式」+ 例句「我能用什么模式」与「空调有什么模式」字面重合 | 知识缺口 |
| R9 | 执行超时 | 多步计划经执行器派发时 `latency_budget_ms/1000` 是硬超时，手册声明 2500 ms，而一次作答要 2–5 s（生产手册命中轮端到端 p50 4.9 s、p90 7.2 s） | 过去零命中不调 LLM 才没暴露 |
| R10 | 逐页闸 | 质量最高的页因覆盖率 0.38 被闸掉，几张覆盖率刚过线、质量很低的无关页反而入选；「充电上限设多少合适」基线能过是这个偶然（212 页恰好被闸掉） | 排序与闸交织 |

## 3. 设计

### 3.1 检索用哪句话（`agent._retrieval_question`）

原话是权威；只有原话自己解析不出时才用规划器写的槽（同 2026-07-19「规划器改写是不可信指代通道，解析不出才保留」的既有判据）：

- 原话主语是回指代词（判据唯一实现 `runtime.anaphora.has_anaphoric_subject`）→ 用槽；
- 原话多分句、槽只落在其中一个分句里（规划器把多诉求句拆成了多步）→ 用槽；槽横跨多个分句（规划器把多问句合并成一步）→ 仍用原话；
- 安全分级与告警信号**始终只看原话**；用槽检索时答案提示词同时给出【用户原话】与【问题】。

### 3.2 查询理解：三类词（`local_index.py`）

每个检索变体拆成三类词，各管各的事：

| 类 | 来源 | 计分 | 计入零命中闸 | 计入排序覆盖率 |
|---|---|---|---|---|
| 主题词 | 用户自己的内容（换过同义词、剥过壳） | ✓ | ✓ | ✓ |
| 证据词 | 意图扩展追加（参数 / 定期保养 / 整车尺寸参数…） | ✓ | ✗ | ✓ |
| 只排序词 | 答案类型词（方式 / 功能…，`facet_terms`）、症状（关不上 / 打不开）、词间接缝 | ✓ | ✗ | ✗ |

- **问句壳**：列举 / 定义 / 原因 / 位置 / 能力 / 属性 / 参照问法与疑问尾词直接取 `runtime.question_shape` 的词表；
  计数壳与属性壳按问句判据的结构（头 + ≤2 字动词 + 几量词 / 多大多高）整段剥（判据为「认出」计数问允许隔 4 字，剥离若也隔 4 字，
  「能量回收有几档」会从「能量」的「能」起跳吃空整句）；请求开头词（请问 / 告诉我 / 介绍一下 / 查一下）只剥句首——全句遮罩会吃掉章节名词「车辆介绍」。
  所有短语与模式**按字符并集遮罩**，与剥离顺序无关（逐个 replace 时「怎么」吃掉「怎么回事」的头，等长短语先后取决于集合迭代序、而字符串哈希每进程随机）。
- **异体**：档→挡，语料与查询同一次规范化，对称折叠。
- **句法产物**（不计分不计闸）：段首介词 / 指代虚字、段尾语气 / 助词虚字组成的双字；全书不存在且含虚字的双字；全书不存在、夹在两个真实双字之间（或句尾悬挂）的双字。
  整段都不在手册里的双字（机油、油箱、海豹、天窗）不是产物——零命中闸靠的正是它们。
- **词间接缝**：夹在两个已知双字之间、df 不到两侧较小者 1/4、且跨它的四字串全书至多出现一次（「车载冰箱」的「载冰」）；四字串条件排除「车道保持」这种由高频词组成的真复合词。
- **排序**：质量 = BM25 × 排序覆盖率^1.5（线性乘时，缺一个关键概念但主题词重复更多的页会反超「概念全部在场」的页）。闸仍是「质量 ≥ 1 且主题覆盖率 ≥ 0.42」，口径不变，变的是覆盖率只算用户自己的词。
- **意图扩展**（schema v2）：`standalone` 的规格扩展在内容剥空时自己就是问题（「这车有多长」），必须配 `require_any`（否则「这个多少钱」会凭空检出参数页）；
  `drop_any` 去掉指整车本身的主语词（「车身有多长」）。

### 3.3 范围闸（确定性，LLM 越不过）

| 闸 | 声明 | 例 |
|---|---|---|
| 外车型标记 | `foreign_vehicle_markers`（不收「理想 / 汉 / 唐」这类歧义词） | 比亚迪海豹的电池容量 |
| 本车没有的对象 | `absent_subject_markers`（纯电车无燃油系统部件；无可开启天窗） | 油箱能加多少升油、这车能加95号汽油吗、天窗为什么关不上 |
| 未知专名 / 型号码 | 三字符以上未知 ASCII（既有）+ 字母数字两字符型号码 | CarPlay、理想L9、问界M9 |
| 声明自检 | 启动时核对每个标记确实不在手册里，矛盾即拒绝启动 | 首版的「海豚」撞上了「qborn 小海豚儿童安全座椅」 |

ASCII 闸改为逐变体判：受控同义词换掉的专名（NOA → 智能领航辅助、TypeC → USB 接口）按换过之后的变体判。

### 3.4 目录路由（`toc.py` + `toc_router.py`）

- 目录来自获准索引本身：合并页路径（「座椅 > 座椅通风 / 车辆安全 > 安全带」）按「后段替换前路径同级层」还原，与 PDF 大纲 187 个叶子逐条一致，不需要 PDF 或第二份目录。
- **何时路由**：词法零命中；或最高主题覆盖率 < 0.7；或问句含手册不认识、同义词也换不掉的实词（奇骏 / 刮车 / 小冰箱）。
- **何时不路由**：范围闸拦下的；剥壳后没有实字的（「这是什么」曾被路由到「外部介绍」）；带仪表灯 / 图标语境的（认图标只认受控视觉目录，把「黄色感叹号」路由到告警表等于让生成模型在相邻行之间猜）；受控视觉目录已命中的。
- **LLM 的产出**只有 `{"scope", "sections"}`：编号由代码校验存在（未知 / 格式错一律丢弃，至多 3 个），按章节轮转取页（每节先取最相关的一页，保证每个选中章节至少进一页）。
  与词法结果合并时，**两路都选中的页排最前**，其余路由页与词法页交替，合计至多 4 块（`_merge_routed`；路由页一律排前曾把「三元锂电池平时充到多少」正确的词法首页挤到第二）。
  路由失败 / 超时 / 解析失败 = 没说话，回落词法结果；**零命中仍不调生成模型**（Q9 护栏①的本意——没有材料不让 LLM 作答——不变）。
- **否决只认一种**：含手册不认识的实词、且路由明确判 `other_vehicle` 时作废词法近似（「奇骏的电池容量」不能拿本车 73.6 kWh 作答）。「没找到章节」「不是用车问题」都不否决——实测 MiniMax 在温度 0 下对同一句的判空也不稳定（首版判空即否决，三套集各误伤 1–2 条）。

### 3.5 规划侧

- `manual.query` 描述写明「部件与功能有哪些模式 / 挡位 / 颜色、怎么开启与设置、储物容量与整车规格」；`scene.list` 补反界定「部件自带的工作模式与挡位不是场景，归车型手册问答」。能力目录 13 946 → 14 009 字，余量 1 991，零裁剪。
- `manual-help-boundary` v4：补一条部件自带模式 / 挡位 / 容量问法 → `manual.query`、`scene.list` 只列场景；删去与上一条重复的「如实弃权」句；常驻 policy + 本指南 2 549 / 2 600。
- 范例追加三条（实质改写、不抄 collector 原句）：空调工作模式、座椅加热分级、前备箱容量。
- 没有加 route hint：先看知识层在真栈上能否把「空调有什么模式」拉回手册（§6）。

### 3.6 执行超时

`manual-rag` `latency_budget_ms` 2500 → 15000（与 vision / nearby 同档）：词法毫秒级 + 路由（仅需要时，p50≈1 s，超时 8 s）+ 生成 2–5 s（瞬时失败重试一次）。

## 4. 实施清单

| 文件 | 改动 |
|---|---|
| `agents/manual_rag/src/providers/local_index.py` | 查询理解 v3、三类词、范围闸、`scope_veto` / `route_block_reason` / `unknown_subject_terms` / `table_of_contents` / `retrieve_sections` |
| `agents/manual_rag/src/providers/base.py` | `Chunk.coverage`（主题覆盖率） |
| `agents/manual_rag/src/toc.py`、`toc_router.py` | 目录还原；封闭集合路由与结论解析 |
| `agents/manual_rag/src/agent.py` | 检索用哪句话、路由编排、`data.retrieval / retrieval_basis / toc_sections` |
| `agents/manual_rag/resources/retrieval.yaml` | schema v2：`facet_terms`、`foreign_vehicle_markers`、`absent_subject_markers`、扩展 `drop_any / standalone`、同义词 +100 余条（目标词逐条核对手册） |
| `agents/manual_rag/manifest.yaml` | 0.4.0、描述 / 例句、`latency_budget_ms` |
| `agents/scene_orchestrator/manifest.yaml` | `scene.list` 反界定 |
| `skills/guides/manual-help-boundary.yaml`、`skills/exemplars/manual.yaml` | v4；+3 范例 |
| `test/eval_corpus/manual_rag_colloquial.yaml`、`manual_rag_full_coverage.yaml` | 口语语料 42 条；`colloquial_corpus` |
| `scripts/probe_manual_rag_full_coverage.py` | `--kind colloquial` |
| 测试 | `test_colloquial_retrieval.py`（34）、`test_toc_routing.py`（29）、探针语料预检、目录预算数字 |

## 5. 验证

### 5.1 尺子

- **collector 真实问法语料**（仓库，42 条）：A 组 collector 原句 24（含负例 3）、B 组规划器补全后的指代问句 7、C 组按四类补的问法 11；期望页逐条对照手册正文核过；
  `route: expected` 两条表示词法层可能零命中。整份语料通过真栈发送前预检（问句判定 + 端侧不截获），由单测守。
- **盲写集**：两个子 Agent 只看目录清单、不看手册正文，按「有哪些模式 / 几档 / 怎么开 / 是什么 / 能放多少 / 能不能 / 为什么 / 什么情况下」写口语问法；期望页按所选目录叶子自动标注（存在少量过窄，§5.3）。
  #1（165 条）用于调参，#2（135 条）在实现定稿前未打开，首次打开的读数即留出读数。存档 `.artifacts/manual-rag-colloquial/`（#1 问句 SHA-256 `b0b01d3d…a10`，#2 `b2caec0b…0e3e`）。
- 路由的离线读数直连 MiniMax-M3（与生产同型号，关思考）；密钥只从根 `.env` 读入进程，不打印不落盘。

### 5.2 离线读数

| 尺子 | 生产 HEAD | 终版词法 | 终版词法 + 路由 |
|---|---:|---:|---:|
| 口语语料 42（负例 3） | 15 | 40 | 42（终版，路由 6 次） |
| collector 训练集 53（负例 20，含别家车型；scratch 尺子，未进仓库） | 10 | 51 | 51–52 |
| 开发集 165（负例 15） | 31 | 118（第一版冻结原型 89） | 159（路由 81 次，49%） |
| 留出集 135（负例 15） | 22 | 65（首次）/ 67（终版） | 129（首次）/ 130（终版，路由 91 次，67%） |

- 路由延迟 p50 0.97–1.07 s，最长 2.4–7.9 s；三轮全量评测零调用错误（更早一轮有 1 次读超时，按 SDK 口径回落词法）。
- 整本覆盖（离线评测器，含源 PDF 重建审计）：页锚 269/269（Top-1 267）、路径 160/160（152）、章节 187/187（158）、视觉 35/35、原 36 题 36/36、显式手册来源 route hint 222/222——**与改前逐条一致**。
  中间版一度 Top-1 各少 1–2 条（「车辆介绍 外部介绍」的「介绍」被当请求词剥掉、同义词「自动锁 → 自动闭锁」吃掉「自动锁车」前半截），均已修回。

### 5.3 留出集剩余失败（终版 5 条）

「停车监控那个功能在哪打开」「运动模式到底在哪切换」（词法自信命中错页，覆盖率 ≥ 0.7 不路由）、「方向盘手感有哪几种可以选」（路由选到相邻的驾驶模式一节）、
「后视镜上那个小灯亮了是啥意思」（图标描述按设计不路由）、「后备箱都有哪几种打开方式」（命中应急开启 / 远程开启页，缺后备箱开闭页）。
「等红灯不用一直踩刹车那个功能怎么开」两次运行一对一错（路由方差）。
开发集另有标注过窄：「电池能每次都充到百分百吗」命中充电限值页（正是答案）、「导航播报有哪几种模式」命中语音命令表（手册只在那里写了「切换播报模式」）。

### 5.4 反向验证

在进程内逐条打掉规则（不改源文件），对应测试均变红：去问句壳、去档挡折叠、去答案类型词、去句法产物、证据词计入闸、去 standalone、
去本车没有的对象、去外车型标记、去型号码闸、去声明自检、并集遮罩换回逐个 replace、去未知实词上报、去视觉语境不路由、去章节轮转。
合成手册扩到约 60 页（填充页只放操作套话），否则缺席双字的 IDF 复现不了真实手册的偏斜——12 页时去掉问句壳剥离端到端测试照样绿，改为同时加查询理解的直接断言。

### 5.5 门禁与全量

四道门禁（skills / exemplars 域错配 1.8% / L0 strict 2/2 / capability integrity）、端侧 smoke 13/13、route hints 119/119、目录预算 11 条。
全量固定口径（提交前工作树，`-n 8 --dist worksteal`，TZ=UTC0）：**9908 passed / 0 failed / 32 skipped / 11 warnings**，518.58 s；较 `a7bf3eb1` 的 9843 多出的 65 条即本批新增测试。
日志 `.artifacts/manual-rag-colloquial/full-suite-pre-commit.log`，SHA-256 `42f1732f…fbe397`。

## 6. 真栈验收

release `0a4f626a52ad541b1ab1029bd686202ba9e16415`（2026-09-26 11:1x apply；推送 `d337e62b..0a4f626a` 四个提交）：dry-run `blocking_changes=[]`、bootstrap ready、
共享模型就绪（索引包未变）；独立 status `ok`、5/5 endpoint healthy、零 warning，`release_sha` = `running_release_sha`；统一 verify `verified`
（`.artifacts/dev-stack-verifications/20260926T031906Z-0a4f626.json`，provider/model `minimax:MiniMax-M3`）。

| 验收 | 结果 | 说明 |
|---|---|---|
| 口语语料 `--kind colloquial`（42 条，每条独立会话，单一手册卡 / 真实出处 / 预期页 / 零动作 / 零确认 / 车态零差异） | **41/42** | 唯一红「方向盘加热怎么开」：规划器两次调用后给 `system.clarify` 意图澄清卡，零动作；同句新会话复跑 **3/3** 落手册第 85 页（合计 3/4，规划器方差）。p50 6.3 s、p95 8.6 s、max 13.2 s |
| 指代两段式（「座椅加热在哪里打开 → 它有几档」「无线充电板支持多大功率 → 它对手机壳厚度有要求吗」「车道保持辅助怎么打开 → 它在多少速度以下会失效」）×3 | **9/9** | 追问轮均取到补全后的主题页（37 / 117–118 / 124–125） |
| 「空调有什么模式」×3（修前 57 轮里 56 轮落 `scene.list`） | **3/3** | 单一手册卡，空调控制第 197–201 页 |
| 「打开后备箱，再告诉我空调有哪些模式」×3，只「取消」不确认 | **3/3** | 首轮话术「根据手册，空调主要包括这些模式：制冷/制热（A/C）、自动模式（AUTO）、吹风模式…、内外循环…。要打开后备箱吗？…请确认是否继续」，确认挂起；「取消」后零动作。脚本按卡片页判 0/3 是尺子错——需确认的混合轮最终消息不带手册卡，答案在话术里（修前这半句 27 次都是「没有查到」） |
| 原 36 题中能通过发送前预检的子集 | **31/31** | 5 条旧表述被预检拦下、未发送（请查 SU7 用户手册冷态胎压 / 充电口怎么手动打开 / 后备箱怎么应急打开 / 刹车油几年换 / 仪表上小人身上有条斜线一直亮）；负例 7 条均零命中 |

全部轮次车态零差异、零 probe error。工件：`.artifacts/manual-rag-colloquial/live-colloquial-0a4f626a.json`（SHA-256 `5955ba35…f5c0`）、
`live-extra-0a4f626a.json`（`81e05256…9370`）。真栈整本章节 187 / 视觉 35 本次未重跑（离线逐条不变，§5.2）。

## 7. 已知边界与未做

- **复合问句**只取到一半：「胎压黄灯亮了，还能继续开吗？应该补到多少？」命中报警处置页（低于 2.3 bar），推荐值 2.9 bar 在另一页；带「灯亮」按设计不路由。分句检索（话题承接）先不做。
- **问句判据既有取舍**：「自适应巡航的跟车距离怎么调」被共用问句判据判成指令（`HOW_TO_ACTIONS` 刻意不含「调」，「温度如何调高」要当指令，合同有测试钉着），端侧解成 `setting.control`。本批不动，语料里未收这一句。
- **LLM 路由有方差**：同一句两次运行可能选不同章节（「车顶上凸出来那个」一次选雷达和摄像头、一次只选外部介绍）；只补不否决的策略把方差的代价限制在「多给或少给一页」。
- **词法自信命中错页不路由**：主题覆盖率 ≥ 0.7 且没有未知实词时不问路由（「运动模式到底在哪切换」命中特殊驾驶场景页）。开发集上「一律路由」（除视觉目录命中外）比阈值策略多对约 4 条，
  代价是路由调用从约五成升到近十成（每次约 1 s、占 MiniMax 配额）；先按阈值上线，拿真栈分布再定。
- 频度 / 语气副词（平时 / 通常 / 经常 / 到底…）进检索噪声，不再被当成「手册不认识的实词」触发路由；「一般」不收（章节名「车辆一般信息」）。
- **未知车系名**只能靠路由的 `other_vehicle` 判断兜住；品牌表只收无歧义的品牌 / 车系名。
- 「这车有几个座位」：手册没有直接写座位数，零命中是如实结果。
