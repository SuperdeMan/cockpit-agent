# docs/design/ — 前瞻设计与问题分析

> 本目录放**面向落地的设计提案与问题分析**，区别于：
> - `docs/architecture/` —— 架构唯一真相源（已定型的设计）
> - `docs/reviews/` —— review 修复交接清单
>
> 这里的文档是"给后续执行者（人或 Agent）照着做"的蓝图：先讲清现状与问题，再给目标、方案、分阶段落地步骤与验收标准。**一旦某提案落地并稳定，应把定稿内容并入 `docs/architecture/` 并在此处标注「已归档」。**

> 当前 QA release、生产证据和剩余活项不在本索引维护，统一看
> [`docs/reviews/2026-08-30-qa-closeout-handoff.md`](../reviews/2026-08-30-qa-closeout-handoff.md)。
> 已完成的 QA implementation plan 是历史实施证据，不是当前待办入口。

## 命名与结构约定

- 文件名：`YYYY-MM-DD-<topic-kebab>.md`
- 每篇开头声明：`状态`（草案/评审中/已批准/落地中/已归档）、`交付对象`（谁来实现）、`关联`（相关代码与文档）
- 推荐骨架：**现状/证据 → 问题 → 目标 → 方案 → 分阶段落地 → 验收 → 风险**
- 证据要可核验：引用 `文件:行号`、命令、实测结果，不空谈。

## 当前文档

| 文档 | 主题 | 状态 |
|---|---|---|
| [2026-09-03-xiaomi-su7-manual-rag-v2-implementation-plan.md](2026-09-03-xiaomi-su7-manual-rag-v2-implementation-plan.md) | 真实手册 RAG v2：无标点方法问句安全落域、PlanningGuide/exemplar 泛化、受控图标俗称、deterministic `.mrag` 图文包、HMI/Android manual 证据卡 | ✅ 本地实现/验证完成，候选 `f2dcb46`；retrieval 36/36、低并发全量 7824 passed；未 push、未 deploy，生产仍是 `a406e22` 文本版 |
| [2026-09-03-xiaomi-su7-manual-rag-implementation-plan.md](2026-09-03-xiaomi-su7-manual-rag-implementation-plan.md) | Xiaomi SU7 真实车型手册 RAG：PDF 哈希绑定、deterministic 私有索引、中文 n-gram BM25/重排、页码章节引用、错车型/低相关/数值 fail-closed 与 mock 豁免退出 | ✅ 已归档并发布 `a406e22`：main 23/23 + holdout 8/8、exact 全量 7796 passed、5/5 healthy、verify verified、生产 WS 正例 3/3 + CarPlay 负例 1/1、零动作 |
| [2026-06-13-vehicle-control-command-architecture.md](2026-06-13-vehicle-control-command-architecture.md) | 车控域升级到「公版语音指令表」统一 schema | P1 已落地：知识库三件套（62对象/150意图）+ VAL + fast_intent + 飞书全量导入脚本；P2/P3 待做，P2 缺口已量化见下方 2026-07-03 意图覆盖分析 |
| [2026-06-13-multi-intent-and-context.md](2026-06-13-multi-intent-and-context.md) | 多意图拆分 + 对话上下文/指代消解 | 已全部落地：上下文 + M1云侧DAG + M2端侧切分 + M3黄金用例 |
| [2026-06-13-asr-pipeline-analysis.md](2026-06-13-asr-pipeline-analysis.md) | ASR 收音失败根因分析与修复链 | 已全部落地：前端竞态 + 后端转码 + E2E + HTTPS提示 |
| [2026-06-13-open-domain-latency.md](2026-06-13-open-domain-latency.md) | 开放域响应慢：模型分层 + 流式贯通 + 即时反馈 | 已落地：流式+模型分层+chitchat兜底+即时反馈；降规划延迟待做 |
| [2026-06-14-cloud-central-orchestrator.md](2026-06-14-cloud-central-orchestrator.md) | 云端中枢：理解→规划→异构调度（车端快思考/Agent/工具）；T0/T1/T2 分级 + 有界 Agentic 循环 | 已落地：P0-P3、DispatchToEdge、T2、工具、权限/可观测；已补混合意图语义分组、句子级增量 TTS 与慢意图完整性回归 |
| [2026-06-15-observability-dashboard.md](2026-06-15-observability-dashboard.md) | NATS 可观测出口 + collector + 独立 Dashboard：车辆 diff、端云链路、Agent 运行态与 debug 对照实验 | 已归档：P0-P3 全部落地并完成 20 服务全栈验收；**2026-07-10 被 badcase 排查重构扩展**（会话/轮次/日志贯通 + SQLite 持久化 + 四视图，见下方 2026-07-10 行）|
| [2026-06-20-info-agent-expansion.md](2026-06-20-info-agent-expansion.md) | info 扩展：联网搜索/新闻/股票（只读聚合）+ 票务独立成交易 Agent | 草案：`info.weather` 已落地，余下规划中 |
| [2026-06-20-standalone-agents-roadmap.md](2026-06-20-standalone-agents-roadmap.md) | 独立 Agent 扩展路线完整设计：充能规划/场景编排/路况安全/行程增强/交易类——含 manifest、Provider 接口、交互流程、代码骨架、打通契约 | 已落地（P0-P3：charging-planner/scene-orchestrator/road-safety + trip-planner 增强）；§8 两处缺口已闭环（scene 命令对齐 VAL；road-safety 主动播报 Agent 侧已实现，**仅余 HMI 投递一跳**）|
| [2026-06-20-new-agents-detailed-design.md](2026-06-20-new-agents-detailed-design.md) | （已被 standalone-agents-roadmap.md 合并，保留作为历史参考） | 已合并 |
| [2026-06-20-search-news-redesign.md](2026-06-20-search-news-redesign.md) | WS6 补充：搜索/新闻从"罗列链接"重设计为"LLM 结论+摘要卡片"——Agent/HMI/Provider 三层方案 | 已落地但**卡片方案被 2026-06-22 取代**（search_answer 复读结论问题） |
| [2026-06-21-current-location.md](2026-06-21-current-location.md) | 当前定位能力：用户授权后以精确位置服务天气/导航/周边；位置不进会话记忆、不持久化、不进日志 | 已落地；后随上下文重构收敛为 manifest `context_scopes: location` 最小化下发 |
| [2026-06-22-search-quality-and-card-redesign.md](2026-06-22-search-quality-and-card-redesign.md) | 联网搜索质量重构（Exa 正文级检索 + 接地合成/引用/诚实弃权 + api-football 赛事 + 新闻 TTS 播报式速览）+ 信息卡片重设计（气泡给结论、卡片给证据，消除重复） | 已落地（P1-P5 + 二/三/四轮修复，真实 API 端到端验证） |
| [2026-06-22-charging-route-planning.md](2026-06-22-charging-route-planning.md) | 充电规划：高德沿真实路线取途经充电点（出发地→途经点→目的地）+ 泛地点高德候选二次确认（dest_choice）+ charging_route 时间线卡 + 聚合器卡片择优 + advisory（不车控/不发导航） | 已落地（多轮迭代，真实高德端到端验证） |
| [2026-06-20-ws2-registry-production.md](2026-06-20-ws2-registry-production.md) | WS2 Registry 生产化：PostgreSQL 持久化/多实例/语义路由/AgentClient 动态解析 | 已落地（P0+P1：PgStore/pgvector/AgentClient 动态解析/多实例） |
| [2026-06-20-ws8-security-permissions.md](2026-06-20-ws8-security-permissions.md) | WS8 安全与权限：权限动态解析/third-party 沙箱/LLM 注入防护/网络白名单/车控安全门控 | 已落地（P0+P1：注入检测集成/VAL 4 项门控/third-party 沙箱/HTTP_PROXY） |
| [2026-06-23-named-places.md](2026-06-23-named-places.md) | 常用地点（家/公司）：导航别名解析 + 未设置时二次交互引导设置 + Redis 持久化（memory 服务 profile.places）+ HMI 回显/修改 | 已落地 |
| [2026-06-23-navigate-landmark-and-charging-waypoint.md](2026-06-23-navigate-landmark-and-charging-waypoint.md) | 视觉地标经共享件解析地图官方名（name_matches 校验拒邻近无关 POI）+「导航去地标附近充电」按目的地搜站、聚合器并入 navigate.waypoints + 类目搜索不被整句多意图劫持 | 已落地（真实高德验证） |
| [2026-06-23-restaurant-waypoint-and-topscorers.md](2026-06-23-restaurant-waypoint-and-topscorers.md) | 顺路用餐途经点（navigate_to.stop_category→waypoint_choice 候选二次选择→route_plan 路线卡，navigation 接管因 food 恒 mock）+ 赛事射手榜 | 已落地 |
| [2026-06-23-sports-match-detail.md](2026-06-23-sports-match-detail.md) | 赛事进球详情/射手榜：api-football /fixtures/events（进球射手与分钟，剔除罚丢点球）+ topscorers 赛季回退标注 + 历史总榜走搜索接地合成 | 已落地 |
| [2026-06-24-trip-planner-multiturn-and-confirm-robustness.md](2026-06-24-trip-planner-multiturn-and-confirm-robustness.md) | 行程规划多轮闭环（规划→改某天→确认→第一站导航）：确定性 trip.plan/trip.modify 兜底（覆盖降级路径）+ trip-planner 有状态多轮 + 确认收尾取行程首日景点搜 POI + 确认词「占据整句」修复（行/可以/不要不误判）+ 孤儿确认护栏 + 跨 Agent meta 透传与 Struct→dict 修复 + 电量一致性 | 已落地（783 passed 实测 + 端到端验证） |
| [2026-06-24-complex-task-thinking-and-process-region.md](2026-06-24-complex-task-thinking-and-process-region.md) | 复杂任务动态思考（按统一 is_complex 判据对 LLM 开 thinking，Planner JSON 恒关，reasoning 不下发）+ 可折叠过程区（ProcessUpdate 事件 → 气泡内嵌折叠条「步骤+思考摘要」，行车/泊车双态门控；普通车控/闲聊零过程零延迟） | 落地中 |
| [2026-06-25-memory-system-redesign.md](2026-06-25-memory-system-redesign.md) | 记忆系统分层重构：L0 会话 + L1 车辆 + L2 语义画像(pgvector,自动抽取偏好) + L3 情景 + L4 主动雏形；多用户就绪(occupant)/时序-lite(superseded_by 不覆盖)/provenance+置信度/隐私端云分割/导出遗忘；复用 registry 的 bge-small-zh+pgvector，proto 向后兼容加 Remember/Recall/ForgetUser/ExportUser。配套 [research 调研](../research/2026-06-25-cockpit-and-agent-memory-systems.md) + [实施计划](../superpowers/plans/2026-06-25-memory-system-redesign-implementation.md) | 草案待评审 |
| [2026-06-25-context-system-redesign.md](2026-06-25-context-system-redesign.md) | 上下文系统重构：统一 `ContextManager` 脊柱——working/core 装配层(token 预算 + catalog 语义预筛) + 结构化焦点态(跨轮指代) + 上下文生命周期门面收敛 + 敏感上下文按 manifest `context_scopes` 下发。承接 memory 重构后裸着的 working/core 层 | 已落地（Phase 0-4，883 passed/6 skipped，零回归；§8 记两处取舍） |
| [2026-06-25-comms-link-hardening.md](2026-06-25-comms-link-hardening.md) | 通讯链路量产级加固：全链路 gRPC keepalive(共享 `runtime/grpcio.py` 工厂) + 优雅停机 + HMI 韧性(退避重连/断线发送队列/看门狗) + 熔断接线 + LLM 网关连接池/流式 stall + 依赖连接加固(Redis/PG/NATS)；含一处危险车控确认退化修复 + Go 网关换 IP 显式重连补强(dns:/// 单独不可靠) | 已落地（P0-P2，真栈自愈 2/2，不重启依赖方即恢复） |
| [2026-06-26-trip-planner-redesign.md](2026-06-26-trip-planner-redesign.md) | 行程规划结构化重构：LLM 提议骨架→确定性接地真实 POI/求解每日车程+按真实 SoC 沿路线编织充电点/出话术四段流水线(对症 TravelPlanner 纯 LLM 0.6% 通过率) + 每停靠点可导航 + 在途状态查询/「时间不够」自动精简 + 状态落 memory；护城河=车辆接地+在途编排 | 已落地（P0/P1/P2 全合并 main，真栈 e2e 6/6 真实 POI） |
| [2026-06-26-trip-planner-p0-implementation-plan.md](2026-06-26-trip-planner-p0-implementation-plan.md) | 行程规划重构 P0 实施计划：结构化模型 + 四段流水线 + 充电编织纯函数 + `trip_itinerary` 卡 + 落 memory 的逐项落地清单与验收标准 | 已落地（见 redesign 设计文档） |
| [2026-06-26-info-agent-deep-research-redesign.md](2026-06-26-info-agent-deep-research-redesign.md) | 信息域重构：新建**独立 `deep-research` Agent**（四段流水线：LLM 提议多视角子问题→确定性有界并行迭代检索→分节接地报告→渐进语音简报+可读报告卡，对症单轮检索多跳天花板）+ 检索/接地内核抽到 `_sdk` 共享（化解独立 Agent 的 provider 重复）+ info 联网查询编排层分层（quick/deep 路由）+ 接地「我」（位置/电量/行程/画像）+ 新闻个性化/深挖桥接；护城河=接地车辆+渐进语音+可落地产物，非「车机版 Perplexity」。用户拍板：独立 Agent / 语音简报+报告双态 / 先 P0 评审。P1=接地「我」(位置反查+画像召回)+多轮深挖(落 memory，「展开第N点」聚焦不重跑)+存记忆；增量=异步分钟级深调研(受理即返回+后台 deep 流水线越过同步 90s 上限+agent.proactive 推报告卡)+信源质量加权(域名权威分层重排+全局权威编号+学术兜底)+名单扩展(采纳信源研究报告)+新闻质量/时效/展示 | P0-P2 + 异步分钟级深调研 + 信源质量加权 + 名单扩展与新闻质量/时效/展示全落地（2026-06-27，全量 973 passed；真栈 e2e：固态电池调研→展开第1点聚焦上节、看新闻→详细讲讲第2条深挖桥接、异步「不急查完告诉我」→秒级受理→分钟后主动推送 9 节/3031 字报告卡、信源权威重排前5平均档位 1.00→2.80、综合新闻 Google News 头条 10 来源/今日/农场0/话题多样/繁体转简体；含端侧「电池」误匹配收窄 + 上线后实测修复 R1-R6：合成关思考防超时退化/去电量约束防漂移/去 livecrawl+简短子问题防 Exa 超时/清网页噪声/info.search 同源关思考/报告深度 985→2153字，见 §9；P2=新闻个性化+深挖某条桥接+主动早报雏形）|

| [2026-06-28-figma-hmi-dashboard-redesign-brief.md](2026-06-28-figma-hmi-dashboard-redesign-brief.md) | 座舱 HMI + Dashboard 视觉重设计 Figma 交接简报：产品语境/用户场景/设计原则与安全红线 + 横屏 1920×1080 信息架构骨架（左对话右"上下文舞台"场景机制）+ 全量组件与约 20 种卡片清单（字段/状态）+ design tokens 族 + 无障碍/车规可读性 + Figma 组织约定与移交 DoD + 实现映射。已锁定：横屏中控大屏 / **Aurora Glass 极光液态座舱**（液态玻璃+AI极光炫光+光球化身，提炼特斯拉/鸿蒙/小米/蔚来NOMI/理想/MBUX/iDrive+iOS Liquid Glass）/ HMI+Dashboard 同做 / 含建议 IA 骨架 | 已交接 → 落地中（见下方实施计划） |
| [2026-06-29-figma-hmi-dashboard-prompt.md](2026-06-29-figma-hmi-dashboard-prompt.md) | 分步喂给 Figma Make 的提示词块（A-1 设计系统 → A-2 主屏 → A-3~A-5 卡片族 → A-6 对话动态态 → A-7 设置；B-1~B-4 Dashboard）+ 每步「figma 输出物」（design/make 链接与版本号） | 工具输入（提示词索引，非落地文档） |
| [2026-06-29-figma-hmi-implementation-plan.md](2026-06-29-figma-hmi-implementation-plan.md) | 座舱 HMI Aurora Glass 重构实施计划：现状→目标 gap、token 迁移表、P0 设计系统 / P1 两栏外壳+右上下文舞台 / P2 卡片重建 / P3 对话动态态 / P4 设置侧栏 / P5 行车浅色 / P6 Dashboard 分阶段 + 验收 | 落地中：P0-P5（浅色）+ A-4 信息卡按源重建 + A-5 右舞台数据驱动地图 + A-8 图标库（39+21 图标，emoji 全替换，推回 Figma）全落地+push（commit 2ad83e3→39e65a4，按 Figma 源逐张重建非换皮）；**✅ 已重建容器 + 真后端全栈 e2e 验证（8 卡族真数据 + 过程区/确认条 + 地图舞台）+ 语音按钮换小舟光球 + 剩余 emoji 全替 A-8 图标 + ASR 流式上屏**；待做 P5 行车态变体、P6 Dashboard（均待 Figma 出帧）|
| [2026-06-30-asr-streaming-design.md](2026-06-30-asr-streaming-design.md) | ASR 流式识别上屏：WS 传输层（`/api/asr/stream` + 网关流式 ffmpeg）一次建好、引擎可换 + DashScope 实时（qwen3 走 `/realtime` OpenAI 协议、fun 走 `/inference` run-task 协议）+ MiMo 分块回退 + HMI 引擎/模型可切；边说边上屏、松手定稿自动发送、失败回退批处理 | 已落地（commit 184d48f→41fdd85；真栈 e2e：qwen3 5partial / fun 4partial / mimo 均出转写 + fake-mic 浏览器上屏→助手出天气卡。坑：qwen3 id 须**全小写**、fun 与 qwen3 端点/协议不同）|
| [2026-07-02-r2.2-permission-single-track.md](2026-07-02-r2.2-permission-single-track.md) | R2.2 权限单轨化：三处权限实现（planning 内联过滤/dispatch 内联校验/PermissionEngine 死壳）收敛为唯一决策 `security/permission.py::check_permission`（规划期+执行期同源）；fail-open 加 env `PERMISSIONS_FAIL_OPEN` 门控+结构化审计；纠偏「接 effective_scopes 会因扁平交集误拒 scene-orchestrator」→取零行为变化单轨 | 已落地并合并 main（`8999cba`/`0be9991`，全量 1014 passed + 真栈 e2e_ws 4/4） |
| [2026-07-02-r2.3-edge-cloud-persistent-channel.md](2026-07-02-r2.3-edge-cloud-persistent-channel.md) | R2.3 端云持久长连：`orchestrator/edge/cloud_client.py` 逐请求建流→进程内单条持久 bidi + corr_id 多路复用 + 15s 心跳 + 指数退避重连（dns:/// 重解析换 IP 自愈）+ 在途断连快速失败降级；删 Go 死代码 ChannelClient（~250 行）| 已落地并合并 main（`c7cdc01`/`ae8638d`，真栈 3 请求仅 1 hello + 换 IP 自愈探针）|
| [2026-07-02-r2.4-info-agent-split.md](2026-07-02-r2.4-info-agent-split.md) | R2.4 info agent 拆域：1269 行 `InfoAgent` 巨类按域拆 `handlers/{weather,search,sports,news,stock,briefing}` mixin + 共享 `_util`，`agent.py` 123 行；域方法经 self 靠 MRO 逻辑逐字不变 + 文件尾向后兼容重导出 | 已落地并合并 main（`def815a`/`18e6f73`，pytest agents/info 136 passed 零回归）|
| [2026-07-02-r3.1-session-auth.md](2026-07-02-r3.1-session-auth.md) | R3.1 会话鉴权最小闭环：静态 token 两层校验（HMI WS `?token=`→edge-gateway 解析身份+`meta.granted_scopes`、去 `user_id="u1"` 硬编码；Hello channel token→cloud-gateway），env 门控 `AUTH_REQUIRED` 默认关；未改编排核心/proto（R2.2 已备好 context.py 消费）| 已落地并合并 main（`f38b4db`，真栈无 token→401 + e2e_auth ALL PASS）|
| [2026-07-02-r3.2-service-mtls.md](2026-07-02-r3.2-service-mtls.md) | R3.2 服务间 mTLS：gRPC 双向 TLS，`GRPC_TLS` 门控默认关；单张共享 mesh 证书 + name override（`ssl_target_name_override`/`ServerName`）适配 agent 动态容器 hostname；Python `runtime/grpcio.py`（aio_channel secure + bind_port）+ Go `gateway/tlscfg`；`scripts/gen-certs.*` 生成证书（gitignore）| 已落地并合并 main（`37817c8`，全量 1030 passed + mTLS 模式 e2e_ws 加密链路 + insecure 探针被拒）。**至此 T3.1+T3.2 齐，安全链路无已知缺口** |
| [2026-07-03-r3.3-e2e-ci-gate.md](2026-07-03-r3.3-e2e-ci-gate.md) | R3.3 e2e 入 CI 门禁：nightly workflow 跑裁剪过的确定性 mock-safe 子集（`--case` 过滤 + `e2e_memory.py` 三处 SKIP 补丁，绕开 Mock 模式下 route_hints 缺失导致的必挂用例）+ `make e2e` 改为本地全量清单执行器（`scripts/run_e2e.{sh,ps1}`），零 GitHub secrets | 已落地并合并 main（`e54a914`/`cb70239`/`25b85aa`）：GitHub `workflow_dispatch` 二次实跑全绿（run 28639607108） |
| [2026-07-03-r3.5-degrade-matrix-e2e.md](2026-07-03-r3.5-degrade-matrix-e2e.md) | R3.5 降级矩阵自动化：新 `test/e2e_degrade.py` 刻画架构 §3.3 四行真实现状（断网/云Planner故障/单Agent故障/LLM超时，docker stop/pause 级故障注入 + `LLM_MOCK_DELAY_MS` 测试钩子），过程中发现 4 处真实缺口（研究阶段 3 处 executor 丢 error 信息/planner中途崩溃静默挂起/CompleteStream 无备用模型重试 + 真实跑又发现 1 处 edge-orchestrator pause/unpause 后不自愈）按用户决定只记录不修；Row 4 原设计经实测推翻重来 | 已落地并合并 main（`0355b1b`/`02a4896`）：GitHub `workflow_dispatch` 一次实跑全绿（run 28643924654） |
| [2026-07-03-r3.4-intent-eval-baseline.md](2026-07-03-r3.4-intent-eval-baseline.md) | R3.4 意图路由评测基线：`test/eval_fast_intent.py`（端侧 `classify_structured`）+ `test/eval_route_hints.py`（云侧 `RouteHintEngine`，复用生产同款 `PlanBuilder._validated_steps` 装配路径）产出准确率/召回率报告，基线入 `docs/reviews/eval/`；`ci.yml` 新增非阻塞 `intent-eval-baseline` job。飞书 1465 意图库标注语料不可得，改用现有 corpus + 历史回归转录；`route_hints_cases.yaml` 经 `--dump` 对真实 manifest 实测发现一处跨 hint 交互（"导航去第2天换一个"实际被 `trip.modify` 接管） | 已落地并合并 main，GitHub Actions `intent-eval-baseline` job 实跑确认全绿 |
| [2026-07-03-intent-coverage-gap-analysis.md](2026-07-03-intent-coverage-gap-analysis.md) | 意图覆盖缺口量化：飞书全量 1465 条意图/8683 条真实例句实测 `classify_structured()`，整体识别率 72.0%，给出分域/分对象明细与典型漏判样本，供「扩规则/上NLU模型/维持现状」三条后续路径决策参考 | 📋 纯调研，无代码改动；后续方向未定 |
| [2026-07-03-r3.6-observability-prometheus-otel-export.md](2026-07-03-r3.6-observability-prometheus-otel-export.md) | R3.6 Prometheus/OTel 导出：collector `GET /metrics`（手写 Prometheus 文本格式，零新依赖）+ `otel_bridge.py`（复用此前从未调用的 `tracing.py::setup_tracing()` 桥接真实 OTel span）+ compose 首次引入 `profiles` 机制门控 prometheus/grafana + 三面板 dashboard JSON。真栈数据链路已验证；Grafana 可视化因本机网络环境限制未验证（镜像/大文件下载不稳定，交叉验证确认是环境问题非代码问题） | ✅ 代码+真栈数据链路已验证；**Grafana 可视化已于 R4.0（2026-07-04）网络恢复后补验（三面板经数据源代理真实出数）**；R3 量产硬化至此全部完成 |
| [2026-07-04-r4.0-residual-cleanup.md](2026-07-04-r4.0-residual-cleanup.md) | R4.0 收尾包（清验收复审 §4 残留 + §3b 边界）：**K1** 端云持久通道 pause/unpause（同 IP 冻结再解冻）不自愈——真根因=app 心跳强制重连时 `_cancel_stream()` 令 `read()` 抛 `CancelledError`、被 `_run` 当任务取消 re-raise 打死重连循环（换 IP 走 grpc keepalive 的 `AioRpcError` 故不中招）→ `_run` 用 `_closing` 区分「流被取消/任务被取消」+ `_open()` 有界超时；**K2** `e2e_process_region.py` 默认泊车态断言在长期共享栈上因 VAL 调试态污染失败→断言前经 collector 复位泊车态使测试自足；**N1** `PermissionEngine` 死注入彻底删除；**K3** Grafana 面板本次网络恢复完成验证 | ✅ 全部完成并真栈验证（K1 解冻后 ~2s 自愈 + e2e_degrade 改回自愈断言 + 2 双向守护单测；K2 污染 fail→复位 pass；全量 1050 passed）；未改 proto/编排核心/架构 |
| [2026-07-04-r4.1-routing-quality.md](2026-07-04-r4.1-routing-quality.md) | R4.1 路由质量主题（T4.1+K6+D7-lite）：**P0** Registry 真语义路由（sha256 伪向量→llm-gateway→百炼真向量，按 capability 粒度，顺带修 §1.1 低频误路由 bug）+ **语义重排**修「语义被中文关键词打分遮蔽」（完整 ResolveAgents 路径 15/20→20/20）+ **P1** Resolve 评测基线 + **P2** 飞书 8590 条语料资产化+覆盖率报告（72.04% 复现自校验）+ **P3** 纯 pattern 扩规则（气象并入天气类 / 设置页族+修既有劫持）→ fast_intent 覆盖率 72.04%→75.74% | ✅ P0-P3+重排全落地并 push（`d596da9`…`317af48`）；真栈 registry 语义 20/20 + 全量 1069 passed；两真栈发现（语义遮蔽已修 / 82% 需对象化立 R4.1b 卡）见 §10 |
| [2026-07-04-r4.1b-edge-objectification-and-nlu-decision.md](2026-07-04-r4.1b-edge-objectification-and-nlu-decision.md) | R4.1b 端侧对象化 + K6 路径决策：量化「82% 目标缺口=辅助驾驶 ADAS 功能长尾等对象密集型工作」，摆开 A（继续扩规则对象化）/ B（端侧 NLU）两路。**关键洞察=NLU 只解决识别不解决执行，端侧对象化是 A/B 两路共用地基**。P0 补 3 个低成本对象（空气净化/导航播报/按键音，真栈 edge→VAL→action 6/6，覆盖率 75.74%→76.17%） | 🚧 P0 已落地并 push（`a094914`）；P1 ADAS（碰 ws8 安全门控）须 Plan Mode、建议独立聚焦会话 |
| [2026-07-04-r4.2-streaming-tts-bargein.md](2026-07-04-r4.2-streaming-tts-bargein.md) | R4.2 服务端流式 TTS + barge-in：DashScope CosyVoice 流式（复用 fun-asr 已破解的 `/api-ws/v1/inference` run-task 协议壳）+ WS `/api/tts/stream`（文本增量入→PCM 分片出）+ HMI AudioContext 调度播放（`pcmPlayer.mjs` node 可测）+ 失败无感回退句级批处理 + 打断 v1 确定性；P0 探针硬 gate（账号/增量回流/格式/首帧延迟四项全过才集成）；不改 proto/编排核心 | 📋 设计定稿待实施；§3.4 v2 语音打断已移交 R4.3 |
| [2026-07-04-r4.3-wake-vad-fullduplex.md](2026-07-04-r4.3-wake-vad-fullduplex.md) | R4.3 语音交互前端：唤醒词 + VAD + 全双工-lite——补 Phase 1 WS7 遗留「唤醒与打断」。全双工拆级钉死范围（L1 免唤醒连续对话 / L2 唤醒词 hands-free / L3 播报中打断；L4 真全双工=非目标，理由=绕开规划/执行分离车控安全链）；sherpa-onnx WASM 浏览器本地 KWS（「小舟小舟」pinyin 运行时配置，免训练）+ silero-vad 端点检测（fun/mimo 引擎首次获得端点能力），**唤醒前音频零上传**；`voiceLoop.mjs` 纯逻辑 FSM node 可测；两条必守边界=误唤醒静默回收 + 本地 dismiss 与云端 F1「取消」的分界（HMI 按 need_confirm 判定，承接 R4.1 §5.3 红线）；模型资产 gitignore+双源下载脚本（吸取 R3.6 大文件教训）；后端零改动、全部默认关 | ✅ **P1 大脑 + VAD 集成 + KWS 从源码构建全落地合 main**（`voiceLoop.mjs` FSM 20 例 + `sileroEndpoint.mjs` 10 例 node 绿，72/72）：**VAD=onnxruntime-web 单线程**（silero，无需跨源隔离）做端点+打断；**唤醒词「小舟小舟」=自建 sherpa-onnx KWS WASM**（生态无预构建→emscripten 从源码编，运行时 pinyin 免训练，`scripts/build-kws-wasm.sh` 重现）；**唤醒真麦验收通过**。🚧 **KWS 接进真 App** 在分支 `feat/r4.3-p2-kws-in-app`（主 app 开 `COOP`+`COEP:credentialless` 让 pthread KWS 能跑且不破坏跨源）；**已合 main**。**收尾（2026-07-05，真栈 Docker 容器端到端验证）**：唤醒人声播报（多提示音随机，TTS 关回退 beep）+ hands-free partials 实时上屏（ghost 气泡）+ 自定义唤醒词预设（4 词，token 对模型词表核验）+ 三路 mic 收敛单路共享流 + KWS 播报态 D6 抑制 + 流式 ASR 失败批处理兜底 + off→dashscope model 修复；后端契约护栏 `test/e2e_voice_loop.py`（ASR 流式 + TTS round-trip，真栈 PASS）；声学质量留真麦（§9）。取代 R4.2 §3.4 v2 |
| [2026-07-05-nearby-discovery-redesign.md](2026-07-05-nearby-discovery-redesign.md) | 周边发现 Agent 重构（food-ordering → nearby）：把 mock 点餐升级为**基于高德 POI 2.0 的通用周边发现** Agent——`nearby.search`（类目参数化，覆盖餐饮/酒店/景点/影院/停车/充电/加油，支持菜系·品牌·评分·人均·营业中过滤）+ `nearby.detail`（富详情：评分·人均·电话·营业时间·特色·图片）+ `nearby.order`（诚实预留桩，不假下单）。自持富数据 `AmapPlaceProvider`（补 navigation 薄 provider 丢掉的 business.cost/tel/opentime/photos），不动 navigation；与 navigation 按「**发现 vs 出行**」经 manifest `route_hints` 声明式切分、不改编排核心；新增 `place_list`/`place_detail` 卡并在 HMI 渲染（现 `restaurant_list`/`reservation` 卡未渲染）；含重命名 ripple 清单与真栈验收 | ✅ **P0 已落地并真栈验证（真高德端到端，2026-07-05）**：重命名+富 provider+三 intent+两卡渲染+全仓 food.*→nearby.* 迁移；全量 1087 passed 零回归、HMI build 过、registry resolve 基线重算 15/15；真栈实测「附近好吃的/酒店/火锅」出真实天安门周边 POI（评分·人均·营业·图片，缺字段不编造）+「XX 怎么样」出富详情卡。真机实测三修：①「看第八个详情」非锚定序号提取②充电/停车设施发现归 nearby 富卡（沿途充电规划仍归 charging）③价位/排序从原话兜底解析。安全：真栈发现 ws8 第三方出站代理是空壳（envoy 非可用正向代理）→ 换 `egress-proxy.py`（stdlib CONNECT 正向代理+域名白名单，实测 amap 200 / google 403），nearby 出站真正受白名单约束。**已合并 main（merge `b0ffac9`）**；余 P2 真实点单适配器（麦当劳/瑞幸 pre-order） |
| [2026-07-05-r4.3b-voice-loop-hardening.md](2026-07-05-r4.3b-voice-loop-hardening.md) | R4.3b 语音回路体验硬化：泓舟真麦反馈 5 问题根因全定位（首唤醒双份=StrictMode×enable 竞态孤儿控制器 / 处理期不可交互=THINKING 四种 FSM 死锁+网关 WS 串行无 cancel / 无「退下吧」退出 / 唤醒即说漏字=KWS 检测窗+recorder 等 ws.onopen / 语气词误收+qwen3 服务端 800ms 硬编码盖过客户端静音尾）+ 4 项审计隐患（陈旧 ASR 回调劫杀下一轮/pendingId 单槽/引擎 start 并发穿透/D6 回声指纹死代码）。P0 正确性包（epoch 护栏+死锁解除+回调代际+FIFO）/ P1 体验包（exit 词本地退场+filler 双门槛+端点宽限合并+recorder 先行）/ P2 链路包（PCM 直传+前滚缓冲根治漏字、vad_silence_ms 透传、网关 cancel 真打断）/ P3 obs+文档；后端仅 3 处加法（§7 清单制约束） | ✅ **P0-P4 全落地并真栈验证，已合并 main（`17e388e`）并 push**（P0 epoch 护栏/死锁解除/回调代际/FIFO；P1 退出词/filler/端点宽限合并/recorder 先行；P2 3 处后端加法**真栈 e2e 通过**——B1 PCM 直传 partial→final→done、B2 vad_silence_ms 透传、B3 网关 cancel「发 cancel→收 cancelled→新请求正常」，Go 编译过；P3 obs 计数+设置文案+D6 A4 勘误；**P4 两轮真麦反馈修复**——wake pre-roll=0 治「小周」误上屏/恢复唤醒提示音/退出词·filler 鲁棒化/语气词不退出聆听改进续问窗）；**110 node 例绿+tsc 零新错+build 过，三容器 --build 重建**；声学/真麦命中留 §10 泓舟验收 |
| [2026-07-07-r4.4-rejection-and-clarification.md](2026-07-07-r4.4-rejection-and-clarification.md) | R4.4 输入拒识 + 路由澄清：把「该不该处理、该给谁处理」显式建模为**置信度三段式**（高置信执行零改动 / 中置信路由级澄清 `intent_choice` 卡 / 低置信 hands-free 语音静默拒识）。受话判定与歧义申告**合并进 Planner 同一次 LLM 调用**（`addressed`/`clarify` 可选字段，fail-open、多 provider 通用）；拒识仅作用于 `meta.input_source=voice_*` 来源（push-to-talk/文本/补槽续接永不拒），fast_intent 端侧命中天然豁免；拒识轮不落对话历史不进画像；拒识压过 route_hints（电台「深入调研」碰瓷句代价不对称）、steps 压过 clarify（确定性优先）；澄清零会话状态（send_text 回发新请求，深度=1）+ `_fallback` 语义 top-1 加分数门槛（堵「低分硬执行 capabilities[0]」真 bug）；连续拒识端侧自适应收紧（续问窗减半→仅唤醒词→成功即恢复）；评测先行（rejection/clarify 语料照 R3.4 模式，先建尺再动 prompt） | ✅ **P0+P1+P2 全落地并各自 merge main（2026-07-08）**；真栈 e2e_rejection 2/2 + eval --live @ mimo 误拒 3.4%/拦截 88.9%、CLARIFY on 出卡+off 零变化+反例误澄清 0/17、rejectPolicy node 全路径；CLARIFY_ENABLED 默认 off 待真栈 CDP 验收翻 on，真麦嘈杂验收留泓舟 |
| [2026-07-07-r4.4-implementation-plan.md](2026-07-07-r4.4-implementation-plan.md) | R4.4 实施计划（可直接接手开工版）：P0 拒识主链（T0 语料先行→T1 planner schema/prompt→T2 engine D6 短路+落库跳过→T3 HMI 来源 meta+静默处理→T4 e2e/基线）/ P1 澄清主链（T5 fallback 门槛→T7 intent_choice 卡+序号选择→T8 语料）/ P2 自适应（T9 `rejectPolicy.mjs` 纯逻辑策略+FSM `setVadBargeInDisabled`）。含 §0 实施者必读 12 条坑（`_parse_and_validate` 空 steps 吞信号、build 重试条件、prefs 白名单制、拒识短路须在空计划分支之前、`_rejected` 内部键、HMI 必须补 `turnEnded()` 防 THINKING 死锁、prompt 按 env 拼接函数化等）+ 契约速查表（meta 键/卡 schema/env）+ 每期 DoD 命令块 | ✅ 已按计划全部执行（P0 merge → P1 merge → P2 merge，2026-07-08）|
| [2026-07-07-complex-intent-landmark-parking-fixes.md](2026-07-07-complex-intent-landmark-parking-fixes.md) | 复杂多意图真机修复：视觉地标解析超时（navigate 地标并发挤高德 QPS；Planner 错猜地标以原话官方名覆盖）+ 停车数据走 mock | 已落地（真机原句复验） |
| [2026-07-07-llm-asr-tts-multiprovider-and-sports-flags.md](2026-07-07-llm-asr-tts-multiprovider-and-sports-flags.md) | 多 LLM 源进程内注册表 + HMI 运行时热切换；TTS 增 MiniMax、MiMo 升流式（引擎→音色两级）；ASR 核对（MiMo 不构成实时增量上屏）；赛事卡国旗 | 已落地；ProviderLock / 请求级 pin / 跨厂商备份档在此基础上后续演进 |
| [2026-07-10-dashboard-badcase-observability-redesign.md](2026-07-10-dashboard-badcase-observability-redesign.md) | Dashboard badcase 排查重构（会话/轮次/日志贯通）：session_id 经 contextvar 贯通 + HMI 每轮自生成 trace_id（气泡角标复制→dashboard 搜索直达，零 proto 改动）+ 三个新 NATS 事件（`obs.turn` edge Handle 单点收口 / `obs.llm` 网关唯一出口 / `obs.log` 结构化日志激活+NatsLogHandler 防自激励）+ `cloud.planning` span 门控挂 plan/LLM raw + collector SQLite 持久化（重启不丢、badcase 豁免清理）+ dashboard 四视图重构（会话三级下钻默认页/总览/日志/badcase 收藏夹一键重放对照）+ 内容采集 `OBS_CONTENT_CAPTURE` 门控与统一脱敏 `redact.py` | ✅ P0-P2 全落地并合并 main（merge `e0795b9`，2026-07-10）：真栈 `test/e2e_obs.py` 16/16 + CDP 前端 12/12；实现差异与踩坑（llm-gateway 缺 nats-py 事件静默丢等）见 §12 |
| [2026-07-11-reminder-agent-design.md](2026-07-11-reminder-agent-design.md) | 智能提醒 Agent（reminder，50074）：自然语言创建日程/提醒/待办（确定性中文时间解析+LLM 兜底+回读确认）+ 到点主动触达（Agent 内 asyncio 调度轮询 PG `reminder_item` 原子领取 → 复用 NATS `agent.proactive`→HMI 播报+`reminder_card`）+ **右舞台 agenda 场景双形态**（单日=今日时间轴：当前时刻线/提醒节点/待办芯片；多日/全部=按天分组列表封顶一瞥，D7 拒周/月网格与舞台切换控件——维度切换在问句里）+ 序号/标题管理闭环（`REMINDERS_ACTIVE` 状态键）；独立 Agent 不并 info、不改编排核心/proto/网关；P1 update/snooze/重复/位置触发/任意日期区间，非目标=外部日历/端侧离线触发 | ✅ P0 + **P1a** 全落地（2026-07-11 同日：设计批准→P0→审查→泓舟批 P1a「直接开干」）：P1a=`reminder.update` 改时间（多条澄清+缺时间两轮续接）/ **snooze 改期原条目**根治 fired 尸体（e2e 4b 断言）/ **recur 重复规则**（每天/工作日/每周X，触发后滚动、完成不杀系列）/「下个月」区间 / 审查修复 B1 大后天截胡·B3 非法日期·N2 tz 守卫；reminder 单测 94、eval 26/26、真栈 e2e **7/7** + recur/update 探针；集成期修 `_POC_DEFAULT_SCOPES` 缺 profile.read/write 致被权限门剔除，详见 AGENTS.md §4 reminder 行；余 P1b 位置触发（独立启动）+ 错过补投 |
| [2026-07-11-reminder-cross-domain.md](2026-07-11-reminder-cross-domain.md) | 跨域提醒（真机 badcase trace 703f095f/b3ecd195：「第一场提醒我观看」路由/标题全对但反问时间，而开赛时间就在上一轮赛程数据里）：标准化「可提醒上下文」`REMINDABLE_ACTIVE` shared state——产未来事件的域按统一 schema opt-in 写入（本次 sports，trip/charging 即插），reminder 缺时间路径统一消费（序号/这场/开赛前指代 + `parse_lead` 提前量默认前10分钟 + 回读带事实锚）；A 类时间可推导（本设计）与 B 类事件触发（P1b）明确分界；不改编排核心 | ✅ P1c 已落地并真栈验证（2026-07-11 当日批+当日完成）：原 badcase 场景「第一场提醒我观看」从反问变一轮成单（开赛-10min）；reminder 单测 102 + sports 生产者 4 + eval 28/28；实现期修正=生产者写全部有 kickoff 场次保序号对齐、消费提到 LLM 兜底前（确定性优先） |
| [2026-07-11-reminder-agent-implementation-plan.md](2026-07-11-reminder-agent-implementation-plan.md) | 智能提醒 Agent P0 实施计划（可直接接手开工版）：Task 0-12 逐步 TDD——timeparse 黄金 40 例（固定时钟/固定 UTC+8 回退）→ store 双后端（claim_due 原子领取）→ scheduler（合并播报/失败不回滚）→ manifest+镜像（asyncpg 叠加层不动 _sdk）→ agent 四 handler（追问续接 REMINDER_PENDING/清空 meta.confirmed 契约）→ HMI types/纯逻辑 mjs/两卡/AgendaStage 双形态 → compose+conventions 登记 → 路由评测 7 例实测收敛 → e2e_reminder 6 断言真栈闭环；含实施者必读坑（依赖闭包/--build/时钟可测性） | ✅ 已按计划执行完毕（2026-07-11）：Task 0-12 全绿——reminder 单测 68 + HMI node 133 + eval_route_hints 17/17 + 全量 1268 passed + 真栈 e2e 6/6 |
| [2026-07-13-token-audit-config-and-badcase-fixes.md](2026-07-13-token-audit-config-and-badcase-fixes.md) | LLM 消耗排查（结论=栈内无闲置消耗入口，obs.db llm_calls 归属方法论）+ 两优化（合成会话跳过记忆抽取 session 前缀契约 §9.2、caller_service 归属补全）+ registry 长期不健康自动剔除（REGISTRY_EVICT_FAIL_COUNT，根治 food-ordering 残留刷告警）+ 批处理 ASR/TTS 去 MiMo 硬绑定（ASR_PROVIDER/TTS_PROVIDER auto 桥接流式引擎 + StreamBridge 双向适配）+ 真机 badcase 五修复（搜索兜底可读化/prompt 消毒/4xx 响应体可诊断、天气意图先答+地名收敛、HMI 右舞台车况动态化 vehicle_state 桥接、MiniMax new_sensitive 内容风控→权威 top-2 收窄重试+4xx→INVALID_ARGUMENT、「体感温度怎么样」误触空调→三层让路）| ✅ 当日全落地（2026-07-13）：全量 1406 passed/7 skipped、eval_fast_intent 57/57 零回归、真栈逐项复验（密钥轮换哈希比对/零抽取/vehicle_state 连上即推/意图先答/带引用真合成）|
| [2026-07-12-mode-routing-and-answer-quality.md](2026-07-12-mode-routing-and-answer-quality.md) | 四模式路由与回答质量重设计（直答/联网查询/新闻/深度调研）：模式进入判据全声明式（manifest 判别化 + Planner「时效与深度」通用段 + info.search/info.news 确定性 route_hints 护栏 priority 59）+ 新评测 `eval_mode_routing.py`（122 条五桶语料、`--live` 真 PlanBuilder 端到端 + 混淆矩阵）+ **引擎级 escalate 通用机制**（`AgentResult.data._escalate` 保留键，chitchat 误接时效题零播报自动改派搜索，conventions §9.1）+ chitchat 日期锚/depth 分档 + 搜索薄证据重试/新鲜度加权/top6 + 新闻 thinking 修复/话题 livecrawl + 深调研 cap 对齐/backtrack 放宽/深挖种子复用；§8 收尾续修（真机 badcase 四批）：MiniMax `<think>` 内联剥离、speech 出口统一剥 markdown（决策=不上渲染）、合成 JSON 截断/裸引号边界式抢救、赛事预测让路搜索+赛果带比赛阶段 | ✅ 9 切片 + 收尾续修全落地（2026-07-12，f210eb2 起连续提交）：基线 @mimo **175/177=98.9%**（受控对照 @MiniMax 84.2%→95.8%）、route_hints 47/47、确定性子集 57/57、全量 1360 passed/7 skipped、真栈四模式探针+badcase 原话复验全过；实施坑 8 条见 §7/§8；后续（2026-07-15）：日期锚扩为星期+时刻、纯钟点/日期/星期问句 chitchat `_clock_answer` 确定性直答 + 首个 route_hints 钉路由（墙钟 badcase，见 §9 与 journey 文档 §10） |
| [2026-07-14-scene-orchestrator-redesign.md](2026-07-14-scene-orchestrator-redesign.md) | 场景编排 Agent 重设计（静态预置激活器→「用户造场景 + 策略引擎」）：调研行业三代演进（预置模式/用户自定义 TCA[理想任务大师 120+动作·50+条件]/LLM 一句话生成[任务大师2.0·ChatIoT NL→TAP]）后定主线=**`scene.create` 一句话造场景**（LLM 仅创建期编译 NL→Scene DSL，白名单=构建期 COPY VAL `commands.yaml`，确定性校验+强制 require_confirm 改写+NEED_CONFIRM 回读；**激活/执行零 LLM**）+ PG `scene_item`（用户同名遮蔽预置）+ deactivate 真恢复（快照优先/反向默认表兜底）+ route_hints（guard 让路端侧 driving_mode/power_mode）；**v2 策略引擎（泓舟增补「按目标/环境/执行结果动态规划」）**=「聪明在编译期、可靠在运行期」两段式（D9）：Policy DSL v2（`guards/when/assert/on_fail` 平铺可选字段+key 白名单防幻觉）+ 激活期确定性 **Ground·Solve**（环境分支求值/幂等跳过已达成/空集诚实反馈，trip-planner 四段流水线同范式）+ **Verify-Repair** 失败闭环（后台对账 assert、诚实汇报/幂等重试建议卡/驻车补做 deferred，**repair 不新增执行通道**，对症 `_dispatch_cloud_actions` speech 逐条覆盖致多动作失败静默的真缺陷，D10）+ 与 Planner 边界「Planner 负责第一次的聪明，场景负责每一次的可靠」（D11）+ P3 询问式触发（触发/verify/补做共用一条 NATS 订阅）；**v2.1 评审修正（泓舟 4 指摘全成立）**：①DSL↔PG 字段一一同名对齐（补 goal/guards 列）②条件三态求值（unknown=exclude+告知·guard 缺数据降级 confirm，修互斥分支缺数据双发实 bug）③verify 竞态=activation_id 代际护栏（R4.3b epoch 同款）+对账清单闭包传参+单飞 cancel ④退出恢复基准改 `solved_actions`（when 跳过项不恢复，防覆盖用户手动调整） | ✅ **P0-P3 全部落地并真栈验证 + 全量代码评审收口**（`test/e2e_scene.py` **26/26**、全量 **1576 passed**、eval route_hints 70/70 + fast_intent 57/57、HMI CDP 真栈）：P0 造场景闭环 + P1 参数覆盖/会话沉淀/media 放开 + P2 Ground·Solve 三态求值与 Verify-Repair 后台对账 + P3 询问式触发（零执行权）。落地纠偏见文档 §0.5（4 处设计与代码事实不符 + 3 个被真栈揪出的既有真 bug：VAL 氛围灯 color 分支提前 return 导致亮度从未生效 / 端侧快路径劫持造场景句·激活句 / `_dispatch_cloud_actions` speech 逐条覆盖让失败静默）；**落地后全量评审六修复**见 §0.6（hour 时区 BUSINESS_TZ 同源 / once 触发器消费即熄 / 事件路径 10s 场景缓存 / verify 复合断言日志崩 / manifest 补 media.control / update·deactivate 两处 route_hints guard），随 `f436521` 入库、叙述 `4d77462` 补档 |
| [2026-07-14-journey-e2e-test-system.md](2026-07-14-journey-e2e-test-system.md) | 旅程级端到端验证体系（跨 Agent 自主执行 × 全场景连续对话 × HMI 二次交互）：在 L0-L2 存量测试之上新建 **L3 旅程层**（数据驱动 journey YAML 语料 + 统一 runner `test/e2e_journeys.py` + 能力记分卡：任务完成率/上下文保持率/二次交互成功率/诚实率/主动闭环率）与 **L4 HMI 交互层**（把历史临时 CDP 验证固化为 `test/hmi_cdp/`，`Network.webSocketFrameSent` 拦帧断言「点击→发出正确文本」）。两个关键协议事实定分层：HMI 二次交互=合成文本发送（`Cards.tsx` 五处，例外仅 `is_confirmation`/`meta.nearby_poi_id`）→ 协议层可高保真模拟；挂起确认遇无关轮被清（`engine.py:128`）→「中断-恢复」当前不存在。语料分**回归级**（必须绿，保护存量）与**目标级**（能力标尺，允许红——红灯清单=让系统具备两能力的工程 backlog）；测试集 A 组 14 条（单句多域/中间结果传递/交接改派/异步有始有终/失败诚实）+ B 组 13 条（跨域指代/中断恢复/域间信息迁移/快慢路径一致/20 轮通勤 showcase）+ C 组 12 条（CDP）+ G 全局门（时延基线/零泄漏/无断链话术）；失败自动 POST badcase 进 dashboard 收藏夹闭环 | ✅ **P0-P3 全落地 + 红灯修复收官（2026-07-14/15，§10 落地记录，泓舟拍板 9 项全按建议修+Q1-Q4 按建议）**：首跑抓到 scene custom_params 存量真 bug（`6470209`，路由变体依赖缺陷只有旅程层暴露）；目标级首跑 7/18 产出 9 项红灯工作项→当日三批修复（`96093ad`→`7e193c5`）——R1 POI 地标语义五连转正/R2 中断-恢复（含复验抓到的 wait_slot 疑问式黑洞次生 bug 三针修）/R3 焦点迁移/R7 导航 ETA→提醒一轮成单/R8 雨天确定性改室内/R9 故障层层诚实降级；**终态：回归级 15/15、目标级 16/18，B5-1「一次通勤」14 轮整旅程全绿**；L4 CDP 7 用例 6✅1⏭️（真帧实锤）；mock 车道挂 nightly（⚠ **2026-08-02 起 mock 车道为空、`e2e_journeys` 已退出 nightly**——原「mock-safe」判据写的是「route_hints 确定性路由」，那些 hint 已按数据退役，判据改写为「**不经过模型判断**」，见本文档 §4.3）、`make e2e` 已接入。**遗留卡清零（2026-07-15 收口，`3be7fdb`→`9c0843d`）**：A1-4=`_suspend` 增 prior 前缀前序步结论（防双播三道闸）+ timeparse 段位默认时刻（「明早」成单不追问），真栈理想形态「明天有雨…已设好明早8点带伞提醒」；B3-3=记忆三针（场景参数抽取黑名单/显式陈述立即抽取/谓词等价类 supersede），26 度落地终态校验过；B2-3 前提翻面照 B5-2 先例改语料（charging 例句形态，探针 6/6 选型）；**顺带修 B2-3 插话轮暴露的墙钟 badcase**（chitchat 锚缺时刻必编造 + planner 误路由 search→`_clock_answer` 确定性直答 + 锚补星期时刻 + manifest route_hints 钉路由）。全量 pytest 1627 |
| [2026-07-14-scene-orchestrator-implementation-plan.md](2026-07-14-scene-orchestrator-implementation-plan.md) | 场景编排重设计实施计划（可直接接手开工版）：P0 Task 0-9 逐步 TDD——基线+读实现依据（commands.yaml 结构/action_to_structured 判据/reminder 确认与续接样板）→ catalog 词表校验器（白名单/夹紧/DANGER 强制改写/RESTORE_DEFAULTS）→ store 双后端（**字段一一同名映射**）→ compiler（mock LLM/unsupported 剔除告知/全剔不可存/截断容错）→ agent 六 handler（create 确认与 SCENE_PENDING 续接/activate 写 activation_id+solved_actions+快照/deactivate 恢复基准=solved_actions/list 分组卡）→ HMI 两卡 → 镜像 COPY 词表+compose PG → manifest+route_hints eval 收敛+registry 基线 → e2e_scene ①②③④ → 回归登记；P1（update·delete/参数覆盖/会话沉淀/media 端侧分发单独确认）、P2 策略引擎（编译策略字段/solve 三态纯函数/NATS 多消费方订阅壳/verify 代际+单飞/e2e 行车拦截→驻车幂等补做）、P3 触发承接概要；实施者必读 14 条坑（词表唯一真相源/desc 长度即权重/重建等 10s/_POC_DEFAULT_SCOPES/裸 sys.path 禁插 src 等） | ✅ P0-P3 全部执行完毕（e2e_scene 26/26） |
| [2026-07-17-llm-runtime-hardening.md](2026-07-17-llm-runtime-hardening.md) | 多模型运行时硬化（外部建议清单核对后的采纳/改形/拒绝）：**评测 provider 锁定+漂移守卫**（eval_common `ProviderLock`，治「canonical 重跑与基线不可比」「重建回落 env」两次真实事故）+ active 选择 Redis 持久化 + 429/超时分类（RESOURCE_EXHAUSTED 语义、Retry-After、SDK 不再白做重连重试）+ CompleteStream 首 token 前降级（兑现 R3.5 只记不修的缺口）+ 被动健康统计/按需探针进设置页 + obs.llm 补 provider/档位/pinned 字段 + 请求级 `meta.llm_provider` pin（P2，供 badcase 重放 A/B）。**拒绝**：用户面逐请求选模型、周期主动探活、跨厂商自动 failover 默认开（单一大脑确定性；仅存设计待真实故障触发）、按任务自动选档新机制（@fast 档位+thinking+heavy 已覆盖） | 🚧 **P0+P1 已落地并真栈验证（2026-07-17）**：P0=active 持久化（restart 不回落）+ ProviderLock 漂移演练 PASS + collector provider 列；P1=429→RESOURCE_EXHAUSTED（Retry-After≤2s 同模型重试一次、否则跳 fast）+ 流式首 token 前档位降级（R3.5 缺口兑现）+ 被动健康窗口/`POST /api/llm/probe`/HMI 健康点（真栈 probe minimax ok 1373ms 记账）。**P2 请求级 pin 同日落地**：WS `meta.llm_provider` 全链路（Go 网关整包转发零改动+prefs 白名单+SDK/planner contextvar 双路透传+网关 `_serving` fail-closed），真栈同 trace 双腿 `cloud-planner@mimo`+`chitchat@mimo` 而 active=minimax 不受扰；全量 1695 passed。D7 failover 刻意仅存设计（真实事故才建）。**✅ 已归档**：定稿并入架构 **§8.1**（另 §3.3 注/§10） |
| [2026-07-17-data-authenticity-governance.md](2026-07-17-data-authenticity-governance.md) | 数据真实性治理（同批外部建议）：ui_card 保留键 **`_prov`**{mode: real/cached/degraded/mock, vendor, fetched_at, note}（Struct 免 proto，仿 `_escalate` 登记 conventions §9）+ **禁静默回退 mock 三层**（①显式 real 意图+构造失败→fail-fast 默认开 ②`REQUIRE_REAL_PROVIDERS` 严格栈开关默认关、CI mock 车道零破坏、parking-payment 设计即模拟豁免 ③启动期 provider 决议统一 log 审计）+ HMI 统一徽章（mock 琥珀醒目/degraded 灰/real 不打扰角标来源·更新时间）+ e2e 严格栈冒烟与 mock 泄漏探针；试点 weather/place_list/search_result 三族再推广。开放问题：embedding 无 key 伪向量与 `.env.example`「诚实降级 lexical」表述的张力待核实 | 🚧 **P0+P1 已落地并真栈验证（2026-07-17）**：P0=fail-fast+10 域决议日志一屏（8 real/2 mock 符合 .env）+news 运行期 mock 回退删除+embedding 疑点核实无害；P1=`attach()`+全工厂来源章+试点三族 `_prov`（weather/search_result/place 族）+HMI ProvBadge+**删 nearby 运行期 mock 回退**（search/detail 诚实 FAILED）——真栈 WS 探针天气卡 `{real,qweather}`/周边卡 `{real,amap}` 全链路到端。**P2 同日落地**：`REQUIRE_REAL_PROVIDERS` 严格闸双侧（agents 决议收口+llm-gateway llm/embed/asr/tts 四闸，豁免 parking,knowledge；真栈一次性容器实拒 `provider[stock]` mock）+`_prov` 推广至 13 卡族（trip/research/内部卡刻意不标——已有更强证据链）+`e2e_strict_stack.py` 泄漏探针进 run_e2e（三问全 real）；全量 1695 passed。**✅ 已归档**：定稿并入架构 **§9.5**（另 §10） |
| [2026-07-18-voice-interrupt-context-tts-batch.md](2026-07-18-voice-interrupt-context-tts-batch.md) | 真机反馈三轮批次：①播报中打断漏首字（barge-in 确认窗 300ms+VAD 延迟期间 ASR 未开、固定 200ms pre-roll 盖不住 → FSM 带 speech 起点动态回取）②demo-i9c92i 上下文五败系统修（weather 落地 date 槽位+超窗诚实、省略式追问延续上一轮[prompt 规则+Focus.last_intent]、预测让路补猜族+`_predictive_anchor` 指代锚点）③TTS 播报（唤醒提示音同引擎同音色经一次性 /api/tts/stream 合成、VAL 多意图名词式话术、audio.ts 段链修混合轮云端段无声/divergent 复读/主动播报丢失+queueTTS）；二轮=天气卡 focus 焦点日主视觉 + 「今年→2024」年份幻觉三层收口（planner `_date_line` 日期锚+`fix_relative_year` 确定性纠偏+预测词表放宽与 manifest route_hint d）；三轮=「提醒我冷萃咖啡」被体感共现规则劫持成开空调 → `classify_structured` 提醒话术全局让路（避开 ADAS「限速提醒」）+体感入口收窄为显式词形 | ✅ 三轮全落地并真栈复验（2026-07-18）：探针 4/4+4/4+原句 reminder_card 零车控；全量 1706+（见 AGENTS.md §4）、hmi 143、路由四护栏无回归；坑=winnat 保留区间挡 50070 宿主发布（临时 override，恢复需管理员重启 winnat） |
| [2026-07-19-sports-goal-detail-and-predictive-focus.md](2026-07-19-sports-goal-detail-and-predictive-focus.md) | 赛事域真机四 badcase（demo-aqv0mt，季军赛完赛日）：①「这场比赛都有谁进球」只回比分汇总——「都有」误判列表诉求 + 「这场」指代解析不到 → `_GOAL_DETAIL_HINT` 强详情词优先 + 当日唯一场次指代兜底，进 `_match_detail` 拉 events 射手/分钟；②「明天有什么比赛→这场你预测谁会赢」被缝成「决赛 法国vs英格兰」幻觉对阵且季军赛已完赛仍预测 → `_predictive_anchor` 重构为 `_resolve_predictive`（焦点日期链=本句日期>最近赛事轮用户句>今天→明天 + 阶段过滤 + **完赛短路直报结构化赛果** + 锚点带「尚未开赛/开球时刻」时效框定）+ `_predictive_redirect` 双入口收口（info.sports 与 planner 直路由 info.search）+ hint(d) 补「这场×预测词」召回；未开赛播报点名对阵（掐断下游缝合原料）；真栈复验抓到二层缺口=**planner 改写 query 是不可信指代通道**（「决赛」把季军赛阶段过滤清空），指代词/日期/阶段只从原话取 | ✅ 当日全落地并真栈复验（2026-07-19）：原句回放 5/5（进球十粒逐一射手分钟/决赛点名对阵/预测锚定真实决赛带时效框定/完赛直报 4-6 零检索）；info 200 + cloud 223 + 路由回归 +2 + eval_route_hints 87/87（+3 例） |
| [2026-07-24-eva-benchmark-intelligence-upgrade.md](2026-07-24-eva-benchmark-intelligence-upgrade.md) | **智能化升级母提案**（对标吉利超级 Eva 的架构评审与 Skill 化演进，v1.2 两轮外部评审已处置）：诊断天花板集中在「智能的三个供给侧」——模型接入形态（C3 无结构化规划输出）、知识扩展方式（C1 中央 prompt 单体 / C2 端侧 1727 行规则）、进化速度（C4-C6）；方案主线=**把每一种智能供给都声明式化**（Skill 层 §4.A / submit_plan §4.B / 记忆图谱 §4.D / 主动引擎 §4.E / 受控 MCP §4.F / 自进化 §4.G / Task Ledger+Verifier §4.I）；分期 M0a→M4，**M1 起每期先出子 RFC 再编码** | ✅ **M0a→M4 全部完成并通过总体验收（2026-07-26）**：各期落地记录见 §6 逐条；七路跨阶段组合验收抓出 2 个确认链 P0 与一批组合缺陷当日修复，架构文档推进 v1.9——验收报告与立卡清单 `docs/reviews/2026-07-26-acceptance-review-m0a-m4.md` |
| [2026-07-24-m1a-provider-toolcall-rfc.md](2026-07-24-m1a-provider-toolcall-rfc.md) | M1a 子 RFC：`submit_plan` 结构化规划输出 V1——四家 provider tool-calling 兼容矩阵 + 单一工具强制合法 Plan JSON（取代 `_extract_json` 脆弱截取）+ `PLANNER_TOOLCALL` 灰度双路径 | ✅ 已落地，默认 on（协议探针四家 16/16、arguments 畸形归零；**核心教训=tool schema 与输出指令三向改变模型输出分布，改 schema 必过旅程级行为对照**） |
| [2026-07-24-m1b-self-evolution-shadow-nlu-rfc.md](2026-07-24-m1b-self-evolution-shadow-nlu-rfc.md) | M1b 子 RFC：自进化 v1 流水线（badcase 挖掘→LLM 归因→补丁提案→eval 门禁→人审，含安全治理白名单）+ Cloud Shadow NLU 影子评测（8590 语料混淆矩阵→端侧切换建议） | ✅ 已落地（首份 nightly badcase 报告落库、闭环首案四层根因修复；Shadow NLU 全量 LLM 91.2% vs 规则 75.9%，切换建议 navi+setting 先行） |
| [2026-07-25-m2-task-ledger-outcome-verifier-rfc.md](2026-07-25-m2-task-ledger-outcome-verifier-rfc.md) | M2 核心件子 RFC：**Task Ledger**（跨轮持久任务账本，落点修正为 SDK+PG 层、cancel 拉模式、预算/deadline 强制、中断诚实报告）+ **Outcome Verifier**（声明式执行后对账，`capability.verification` 走 route_hints 同款 proto 全链，三态 UNKNOWN 不定罪）+ T2 分档放宽与重复副作用防抖 | ✅ **P0/P1/P2 全落地真栈验证 + 已归档**（定稿并入架构 **v1.4 §5.2.2**）；§9 落地记录含三处设计偏差编码期推翻并记账：「engine 已持有 NATS 镜像」是事实错误、挂点漏了 D0/T2 流式直通、report 增「已诚实降级不重复念」判据 |
| [2026-07-25-m2-memory-graph-rfc.md](2026-07-25-m2-memory-graph-rfc.md) | M2 后半子 RFC：记忆图谱——偏好加权与衰减（**加列不建 preference 新表**，对母提案 §4.D 的修正）+ 关系边（`memory_relation` 封闭 rel 词表 + 一跳解析，消费面=「去接孩子放学」）+ 生命周期强制项（证据溯源/consent/**GDPR 级联删除红线**）；emotion **不进记忆层**改会话级信号直供 TTS | ✅ **P0/P1/P2 全落地真栈验证 + 已归档**（定稿并入架构 **v1.5 §7.1**）；§10 落地记录含三决策点拍板依据与真栈三坑=亲属称谓自然语言变体/口语「接我妈」filler 漏「我」/播报要用自然称谓 |
| [2026-07-25-m3-proactive-engine-mcp-bridge-rfc.md](2026-07-25-m3-proactive-engine-mcp-bridge-rfc.md) | M3 子 RFC：**统一主动引擎**（六路主动收敛到一个治理器：情境断言投递期复核 / 跨生产方去重 / 免打扰 / 驾驶负荷延后 / 全局频控 / 同窗合并成一条；**fail-open**=治理器缺席即回落直发老主题）+ **reminder P1b 位置触发**（围栏边沿，首次观测只播种）+ **受控 MCP 桥**（allowlist + 版本锁定 + schema 指纹三重准入，写操作生命周期五项缺一不接）| ✅ **P0/P1/P2 全落地真栈验证 + 已归档**（定稿并入架构 **v1.6 §7.2/§7.3**；e2e_proactive 16/16、e2e_geofence 7/7、e2e_mcp 10/10、journeys regression 14/14）；§10 落地记录含五处设计偏差与**存量缺陷**「周期性全量快照把变更回调变成定时器」（road-safety 每 30s 查预警打开共享熔断器） |

| [2026-07-25-m4-s2s-fullduplex-rfc.md](2026-07-25-m4-s2s-fullduplex-rfc.md) | M4 核心件子 RFC：全双工 S2S 四件咬合——**统一 realtime adapter 双端契约**（对上事件协议永不随厂商变 / 对下 `BaseS2SProvider` 每厂商一实现）+ **三层状态机本侧权威**（下层事件只是上层输入；provider session=可丢弃缓存，重连=新 session+摘要重注入）+ **单工具 `escalate` 分工契约**（不注 capability 清单，执行全量回确定性链，车控无一条路径绕过 VAL/确认闸）+ **双链路域灰度=escalate 描述收放**（判定点在生成前，不做运行时拦截）；打断三层语义表（听感/任务/工具调用中——**打断≠回滚**）、文本副产品强制回灌防记忆黑洞 | ✅ **P0→P3 全落地真栈验证**（探针 12/12 + e2e 25/25 + 韧性 11/11 + 移交率 100%；pytest 2189）；§3.5 是**协议冻结基线**（含选型一票否决项：`qwen3-omni-flash-realtime` 静默丢弃 tools，官方文档看不出来）；§11 落地记录含五处设计偏差与真栈三缺陷（turn 悬挂无收口 / 重连泄漏 session / e2e 顺序读写自造背压）。P4 声纹+视觉见下一行（§8 边界速写已展开为独立子 RFC） |

| [2026-07-25-m4-p4-voiceprint-vision-rfc.md](2026-07-25-m4-p4-voiceprint-vision-rfc.md) | M4 P4 子 RFC：**声纹多用户**（提取与存储分家=网关出向量·memory 存模板与判定，**模板绝不下发无状态服务**；边说边识别 1.5s 即发、一次唤醒锁一次；**认不出一律回 primary**=退回能力上线前的行为；**首个注册者绑 primary** 以免主驾一注册就丢全部历史记忆）+ **视觉单帧入口**（**图像永不进对话链**，proto 只传 16 字节 frame_id、本体 TTL 120s 内存 LRU 不落盘；采集门控在端侧=命中触发词才抓一帧）；红线 **声纹不作鉴权因子**（身份识别与授权是两件事） | ✅ **P4a+P4b 全落地真栈验证**（e2e_voiceprint 8/8 + e2e_vision 6/6 + journeys 14/14；pytest 2270）；§11 落地记录含**六处被实测推翻的设计**（margin 才是主控制量而非 threshold / `qwen3.7-max` 对多模态直接 400 且档位解析静默回落 / 帧过期须显式失败否则模型假装看到 / `min_consistency` 原值会误拒合法注册 / `camera.read` 不能直接用 / 分布外音频风险比预想小）与**真机四批**（§11.6 三修=异步 onSend 破坏气泡不变量 / 名字只存声纹表没人读 / 注册交互缺过程可见与当场验证；§11.7 删除失效根因是 **CORS 白名单漏 DELETE**+名字是被重录冲掉的；§11.8 「谁说话都认成同一个」四层=探针里大半是静音 / 阈值拿合成音色标反了方向 / 判定不可见 / **注册与识别不同信道**（webm vs PCM 同人余弦差 0.2）；§11.8.2-4 识别对了答案仍错=**system 提示打不过对话历史**→身份确定性直答、**拒识误伤显式记忆指令**、§11.8.3 乘员维度盘点四处未接 occupant） |

| [2026-07-28-intent-accuracy-data-flywheel.md](2026-07-28-intent-accuracy-data-flywheel.md) | 意图理解/落域规划准确率的系统性升级（候选 M5）：诊断「badcase→改 manifest」治标循环的机制根因——**规则面 40:1 于知识面**（端侧 313 分支 + 云 32 条 replace hint vs skills 74 行）+ **五个断点**让「数据→智能」回路断路（evolve 提案半环因 `forbidden_hit` 子串误伤从未闭合 / 无 gold 标注载体 / `record_intent` 死代码线上无落域指标 / 8590 语料·Shadow NLU 结论·199 条 examples 三笔资产闲置 / 规则只进不出）；方案=**数据飞轮**：一次标注产三资产（评测用例+检索范例+训练标注），P0 修断点立尺子 → P1 **Exemplar Store 第三通道**（骑 skills hybrid 检索范式，修复即时泛化）→ P2 RoutingBench 周报+hint 影子裸跑退役+强模型影子分诊 → P3 端侧 NLU（R4.1b B 路启动判据已满足）→ P4 蒸馏条件触发；顺带抓出 catalog 8000 字符预算把 navigation 整域裁出 prompt（D1）等四个运行时缺陷 | ✅ **P0 已合入 main**（611351b：evolve 三修 / 落域指标接活 / gold 标注载体 / catalog 预算 16k / vision 触发面收窄）；✅ **P1 范例库落地**（`skills/exemplars/` 199 条 + `orchestrator/cloud/exemplars.py` 第三通道 + `test/eval_exemplars.py` CI 门禁 + evolve 第四类提案取代 regex 草案）——**修 badcase 的标准产物正式从正则换成数据**；阈值由 166 例探针扫描拍板（lex 0.34 / sem 0.65），A/B 记账口径修正为**只在注入子集算 Δ**（未注入两臂 prompt 逐字相同=采样方差）；✅ **P2 度量驱动治理落地**（`routing_bench.py` 立起 N1 尺子：首测 189/192=98.4%、paraphrase 96.5% 为主指标，但**语料仅 192 条且前三域占 70%**，隐藏分母与域偏斜每次都印；`hint_retirement.py` 给规则装上出口——双臂裸跑、provider 必须 pin、n=1 不做判定；`_always_include` 补 `category: core` 根治 D1；registry 打分换 bigram 归一，**解除 deep-research「desc 刻意不加长」那条倒果为因的约束**；端云 `_edge_nlu` 透传 + 分歧信号自动进日报；evolve 强模型影子分诊——**没有金标时不假装能判对错**）；**三处有意不做并附判据**：catalog TOP_K 不下调 / 端侧初判不进 prompt 且不留 env 开关 / 8590「可服务率」不可与 Shadow NLU 91.2% 直比；✅ **P3a 识别侧落地、执行侧刻意未接**（2026-07-30：先测天花板再训模型；两车道 holdout 95.8% vs transfer 64.9%，**校准的单调性是同分布性质**；DoD 未达线是产品决策——θ=0.8 才到 86.4%，代价约 1% 请求识别成错对象）；✅ **P0→P3a 全量评审通过**（2026-07-30，零 P0/P1）；✅ **P3 收尾**（2026-08-02：三张卡 + 评审两条 INFO 项；**证伪了 P3a 的一条归因**——判别化描述进 planner catalog 跨两档 Δ=0、收益在 registry 语义兜底；修掉**影子 `agree` 状态在生产里从未出现过**的比较口径缺陷，补对象等价类台账；影子挂满四条路径；parity 冻结 golden 进 CI）。P3b（operate 抽取 + 放量）开工判据未满足 |
| [2026-08-02-intent-routing-adversarial-testing.md](2026-08-02-intent-routing-adversarial-testing.md) | 意图理解与落域对抗测试体系：统一契约、发现/门禁双轨、L0-L3、最小对照与首偏离诊断 | ✅ 框架、语料、跨进程置信与资格硬闸已落地。当前 discovery 561 条 / 522 唯一输入；gate **139 stable / 129 唯一输入**，L0 strict 25/25。干净 `f0af9c0` 的 DeepSeek 对比/参考 all-layer **147/147、eligible=True**，正式 baseline 已按 L3 原始字节/摘要/时间/精确路径契约重取；MiniMax 主模型同口径 **141/147、eligible=False**；2026-08-10 修掉两条真缺陷后仍 141/147（raw 幻觉 8→3、未声明 fallback 4→2，但红灯换了一批）——**每批红灯都是从同一边界池重新抽的**，换具体红灯不提高整体资格概率。当前判定见最终 review §7 |
| [2026-08-02-intent-routing-adversarial-testing-implementation-plan.md](2026-08-02-intent-routing-adversarial-testing-implementation-plan.md) | 上述体系的逐任务 TDD 实施计划：契约/裁判/trace/runtime/report/CLI/450 条 discovery/140 条 gate/L0-L3/baseline | ✅ Task 1–15 全落地；Task 16–18 见落地记录中的实际范围与明确未达项 |
| [2026-08-02-intent-routing-adversarial-findings.md](2026-08-02-intent-routing-adversarial-findings.md) | 对抗测试产品缺陷、尺子硬化与修复批次的单一证据入口 | ✅ §5–§16 保留历史演进；**§17–§24 是当前批次入口**（2026-08-10）：§17 主模型 6 条 unstable 逐条取证与收敛（两条真缺陷 + 「加知识挤爆 SKILL_BUDGET」的回退）、§18 「写 guide 讲判据」四次读数**实测否掉**（有害且显著）、§19 路径 3 换出预选池**执行—验证—回退**全过程及两条新判据（不要为了某个模型的问题改尺子 / 动案例集前先问 baseline 认不认得）、§20 §4.1 剩余三项 P2 收口（三条候选跨进程取证＝**三个「不晋级」不是同一件事**、weather-outing 新 journey `A1-5` + 新 claim 枚举值 `adaptive_replan_continuity`、提醒三词形补齐，及「取代表形态是决定不是搬家」）、§21 §20.2 真缺陷的生产侧修复（范例 `chitchat#12`，A/B **5/12→11/12 p=0.027**、护栏零回归，及**「给一个对象写范例时两侧都要有对照物，只有一侧就是吸引子」**——对口不对口的判据）、§22 两条新 P2 收口（短句检索洞**是真的但不是病因**、adaptive replan 守卫及其作用域收窄、**A/B 前先证明两臂真的不同**的尺子漂移，以及「首轮把 adaptive 判成 simple」这个当天才显形的主形态）、§23 那条主形态的**真因在输出通道**（`plan_mode` 采集了却从没进报告 → 补上后 `toolcall` 91% vs `toolcall_salvage` 50%、p≈0.036，而 `complexity_declared` 全 True ⇒ **走 schema 作答与掉进自由文本作答输出分布不是一回事**；落地的是每批可见的 `meta.plan_modes`，不是修复）、§24 **换档 DeepSeek 对照**（走成 toolcall：MiniMax 48%/45% vs DeepSeek 100%/100%，p≈0.0002 ⇒ **是 provider 属性不是系统属性**；但**差异显著不等于它贵**——20 条 stable 上两档都 20/20，代价只在需要结构化字段的多阶段计划上兑现；附**协议层指标该跨档比、语义层不该**与**分类器要两个方向各验一次**）。聚合读数跨口径/模型不可直比，最终结论以 review §7 为准 |
| [2026-08-05-intent-adversarial-cross-process-confidence.md](2026-08-05-intent-adversarial-cross-process-confidence.md) | 对抗 gate 最终收尾设计：L1/L2 独立进程置信契约、逐样本 raw 证据、父报告合并与 baseline 硬闸 | ✅ 已批准并验收完成；未放宽 raw/fallback/L3/clean-SHA 门限，未用 route hint 追单趟全绿 |
| [2026-08-05-intent-adversarial-finalization-implementation-plan.md](2026-08-05-intent-adversarial-finalization-implementation-plan.md) | 上述收尾设计的逐任务 TDD 实施计划：suite/provenance、纯合并器、parent/worker、raw 幻觉、产品方差、完整真栈与文档收口 | ✅ Task 1–8 已完成并 fast-forward 合入 `main@d380353`：DeepSeek 对比 baseline 已写出且资格通过，MiniMax 主模型仍保留 `eligible=False` 的独立 P0，不以对比模型绿灯注销 |
| [2026-08-10-b1-execution-safety-stopline.md](2026-08-10-b1-execution-safety-stopline.md) | **B1 执行安全停止线**（外部评审采纳批次，裁决总览见 [`../reviews/2026-08-10-external-review-adoption.md`](../reviews/2026-08-10-external-review-adoption.md)）：封死 CLOUD-DEGRADED-LOCAL 危险动作确认旁路（`server.py:806` 无闸直执行 + `val.py:212` PoC 不拒绝，已逐行核实）+ VAL `confirmed` 参数 fail-closed（复用 `edge_call.py:268` 既有凭据模式下沉，5 个调用点全兜）+ T2 `elif streamed` 不可达 bug 修复（`loop.py:255`，部分输出后 unary 重跑）+ L2 VAL command probe 强制接入与反向突变探针 | ✅ **四步全部落地并合入 main**（`5b6980d`/`dd94950`/`55f3d3b`/`96f4f78`）：VAL `confirmed` fail-closed（+ legacy 面同闸，比方案多一处）/ 兜底三道挡板（含核实时发现、评审未提的「流式已播 + final 空」同族缺口）/ T2 `did_speak or did_action` + `_outcome_uncertain` / 探针接强制证据 + 反向突变 4 条 + `cloud_degraded` 语料 5 条。**每步都做了反向验证**（回退到 B1 前代码 15 条红）；§2.4.1 后半按实测退回（L0 有 35.8% 轮是合法端侧车控，照写会误红 29 条）。**冻结令已撤销**，验收 6/6 |
| [2026-08-10-b2-gate-ci-branch-governance.md](2026-08-10-b2-gate-ci-branch-governance.md) | **B2 意图门禁进 CI + 主干治理**：`scripts/check_intent_gate.py` 封装（Python 读退出码，根治管道吞码复发坑）+ ci.yml L0 strict 双 suite 阻断步骤（全离线零 LLM，与 skills/exemplar 门禁并列）+ CODEOWNERS + 分支保护两档（轻档禁 force-push/删除立即开；重档 require PR 待泓舟拍板，机制现实=required checks 只约束 PR merge 不约束直推） | ✅ **方案 A 全部 + CODEOWNERS 已合入**（`361dda1`）：门禁唯一入口 + ci.yml 阻断步骤 + `make gate-intent-l0`；**红灯验证已做**（注入 active intent 不补覆盖 → exit 1，还原 → exit 0）。✅ 轻档分支保护 2026-08-11 由泓舟在网页建 ruleset `light-branches-protection` 完成，经 API 核验 `enforcement=active`、`bypass_actors` 空、目标 `~DEFAULT_BRANCH`（§6.2）|
| [2026-08-10-b3-deploy-profile-fail-closed.md](2026-08-10-b3-deploy-profile-fail-closed.md) | **B3 DEPLOY_PROFILE 生产配置档**：dev\|demo\|prod 三档（dev 逐字现状零变化），prod 下 10 项强制表（AUTH_REQUIRED/PERMISSIONS_FAIL_OPEN/GRPC_TLS/OBS_CONTENT_CAPTURE/REQUIRE_REAL_PROVIDERS/口令非默认…）任一不满足**拒绝启动**；落点 `runtime/profile.py` 经 grpcio 工厂一处生效 + Go gateway 对应实现；Registry 静态 admission（token→agent_id 允许表，缺省关）；JWT/OIDC/Secret Manager/SBOM 记 prod-target 注释项不排期 | ✅ **2026-08-11 已实施合入**（`0055629`/`fdbee6e`/`dadd64f`）：三档 + 12 项强制表（比方案多 DEBUG_VEHICLE_CONTROL 与 GRAFANA 口令两项，第 5 项做实为层 2 通道身份、第 10 项改源码级断言、第 9 项由第 7 项联动）；**未知档位拒启不回落 dev**；每项校验复刻消费方解析（`AUTH_REQUIRED=1` 在 Go 侧是关）；报错不回显凭据。**容器演练当场抓出一次假绿**——三个服务没用 `x-python-env` anchor 而漏配，已补并加覆盖面结构断言。实施记录见方案 §6 |
| [2026-08-10-b4-capability-pack.md](2026-08-10-b4-capability-pack.md) | **B4 Capability Pack v1**：把「新增一个能力」从「同时记得改十处」（除雾实测 stat 为证）收敛为原子动作——① CI 能力完整性检查先行（联合声明源取清单，吸收「能力从哪里声明≠写在哪个文件」判据；执行/话术/等价类/风险/覆盖/验证六断言）② commands.yaml 扩为单一声明源（effect/risk/aliases 增量；`VEHICLE_INTENTS` 运行时派生退役手工集合；骨架生成器产待办不产成品）；**不另立新 yaml 格式、不做无消费方的 Contract v2 字段** | ✅ **2026-08-11 已实施合入**（`b9eb09e`/`7b68047`/`8ee3ea4`/`c6b14b2`）：门禁首跑 61 条，39 条进台账、**22 条真缺陷全部修掉**（含 `steering_wheel.height.set` 崩溃与 `lane_assistance_open/close` 同时为 True 的恒真空洞）；`effect` 落声明并被验证车道消费、**`risk` 改派生不落盘**（B1 已收敛危险判据，第二份声明会漂移）、**`aliases` 证伪后不加**（display_name 探针 20/28）；意图名单改**声明式**派生（机械派生实测 234 vs 76 且会复活已删同义名）；演练当场消掉 `_build_response_key` 那处同步点。实施记录见方案 §6 |
| [2026-08-10-b5-planner-retry-stream-refactor.md](2026-08-10-b5-planner-retry-stream-refactor.md) | **B5 Planner 重试策略表驱动 + D0/T2 流式判定统一**：RetryPolicy 声明化（trigger/attempt_limit/metric_tag/preserve_previous，plan_modes 口径不变保读数可比）+ StreamAttempt 状态枚举纯函数两处消费；评审原建议五层拆分/九段 Kernel 已降档；迁移纪律=先提取规则清单作行为快照、逐条等价测试 | ✅ **2026-08-11 已实施合入**（`191d3b4`/`d1fd704`）：`stream_state.py`（枚举 + `StreamTracker`——bug 活在**推进面**，只共享判定函数等于把出过事的一半留在原地）+ `retry_policy.py` 13 条策略表。**行为等价差分 21/21 逐字一致**（重问次数/回灌 prompt 全文/plan_mode/steps），且先补足语料到 **13/13 策略全覆盖**才读这个结论。四处字段不落（metric_tag/risk_class/preserve_previous/validation_errors，逐条理由见附录 A.1）。§4.2 幂等键**不新造 command_id**——复用 `_fingerprint`，B1 那笔「流断丢 final 没打指纹」的账就地补上。⚠ 触发条件当时**未命中**，由泓舟直接指示推进（已留痕）|
| [2026-08-10-b6-actionability-forward.md](2026-08-10-b6-actionability-forward.md) | **B6 可执行性判定与前瞻契约**：裸对象澄清族第四条路 ActionabilityClassifier（形态/句法特征判 EXECUTE/CLARIFY/REJECT，shadow 四元组记录，canary 另立卡）——前三条路尸检结论「检索是内容通道、裸对象澄清是形态判据」的具体化；端侧 NLU 放量顺序条款并入 P3b 准入（高危对象永不进放量序列）；Contract v2 远期字段逐字段触发表 | ✅ **2026-08-11 §2/§3 已实施合入**（`21794d0`，shadow 档）：`actionability.py` 纯形态判定，裸对象族召回 **2/2**、假阳性 **4/574（0.70%）**、planner 对照 11/20。「不是字面模式表」落成从 `commands.yaml` **派生**领域词表的断言，**首跑抓到「导航」**（它同时是谓词和 VAL 对象名）。三条读数纪律入库：分母挑得越干净假阳性越好看（首版 0/472 是把 `ei.*` 挡在分母外）/ 确定性判据的 p 分母是人为的 / 唯一漏判不属于本族不合并总分。REJECT 声明但 v1 不产出。§4 远期字段中 **`input_schema` 已于 2026-08-21 落地**（消费方=商户规格值域；触发命中了但**形态与本方案预想的不一样**——不是 planner 填错类型，是我们声明的值域本身是猜的，见方案 §4.1 与契约 §9.31、流水 history §65），其余仍逐字段触发；canary 需泓舟单独拍板 |
| [2026-08-11-payment-infrastructure-and-merchant-mcp.md](2026-08-11-payment-infrastructure-and-merchant-mcp.md) | **支付基础设施真实化**：统一 payment-gateway 双渠道自实现收单（支付宝当面付 + 微信 v3 Native）、worker 轮询推进终态、**Capture=确认后亮码**、`PAYMENT_REAL_SCENES` 场景白名单 fail-closed；商户 MCP 支付闭环接线 | ✅ 2026-08-11 四批全落地（流水 history §28，契约 conventions §9.17）；沙箱「支付→PAID→refund」段与微信真商户联调仍在余项（AGENTS §4.1 ③） |
| [2026-08-12-merchant-mcp-full-flow.md](2026-08-12-merchant-mcp-full-flow.md) | **麦当劳 / 瑞幸官方 MCP 全业务流程**（实施基线，承接上行「二期下单」余项）：自然语言选店/选品/规格 → 确定性预览计价 → 确认后创建未支付订单 → 受控支付入口 → 查单；瑞幸支持再次确认取消，**系统不执行最终付款** | ✅ 2026-08-12 落地（流水 history §29–§30）；badcase 收口与跨轮门店锚定见 2026-08-13-cross-turn-store-anchor 行 |
| [2026-08-13-cross-turn-store-anchor.md](2026-08-13-cross-turn-store-anchor.md) | **跨轮门店锚定**：真实商户的可信门店三元组**立卡时（2026-08-13）只在同一轮 plan 内**成立（`executor._resolve_slot_refs` 每轮 pop 旧 provenance，消费侧强制三槽同前缀同下标）——于是「先查附近的瑞幸」「在最近那家点一杯」两轮走不通，用户每轮都要把门店重说一遍。方案=把服务端焦点态扩一格 `focus.last_places`（只存 name/lng/lat 三标量、同 owner 同 session、TTL 到期即失效），executor 在**本轮 plan 内没有生产者时**才据它补槽并写同构 provenance；**延续的是「服务端记得取回过哪些门店」，不是让模型把坐标再说一遍** | ✅ **2026-08-13 已实施**（P0-P2，P3 语料待补）：落点接既有的 `_apply_focus_meta` 那条路，但 provenance 走 `PlanContext.focus_places`——`step.meta` 会被 `_resolve_slot_refs` 每轮 pop（那个 pop 是对的），而 PlanContext 是 LLM 写不到的服务端对象。**混拼反例 5 组全部被拒**（换下标／混来源／生产者不是 nearby／缺下标／换容器），这条是「修松了而测试还是绿的」那个风险的兑现物 |
| [2026-08-13-demo-mkemhn-merchant-hmi-hardening.md](2026-08-13-demo-mkemhn-merchant-hmi-hardening.md) | **商户链路与 HMI 收口（demo-mkemhn 十九轮全量复盘）**：逐轮 trace 取证归并出 8 条根因——nearby 无位置全国检索冒充「附近」（北京店三连）/ planner 线契约过严丢近合法计划→chitchat 兜底**编造门店列表与交易确认**/ 聚合 LLM 替系统许诺「我现在去获取…」/ 锚定三缺口（过度应用到 chitchat·粘性接力致永不过期·双过期后指名门店死路+「公开 POI 坐标」术语外泄）/ 挂起前缀 60 字符腰斩门店名 / mcd.nutrition 盲截断 / 范例与 guide 缺口。修法六批：A 编排层（容器键归一/锚定门控+限龄/聚合诚实约束/place_list 安全计数）、B nearby 诚实降级+指名门店按名查找、C 桥（escalate 选店自愈/拒绝话术去术语/菜单整句放宽/营养相关性打包）、D 知识（选店闭环范例三式×两商户+纯发现范例+guide 口径修正+chitchat 防编造）、E HMI（预览卡换店 chip+周边列表看菜单直达）、F 语料（两条新边界裁定+双向各 2 例，上界 571→579） | ✅ **2026-08-13 六批全部实施；08-14 二轮三方案（§5）+ 打磨与 searchType 勘误（§5.1）亦收口**：营业时段全旅程真栈通过（瑞幸聚合菜单/规格 chips/改规格生效；麦当劳附近门店官方检索打通——「仅碧海君庭」勘误为 searchType 写死收藏档）；聚焦回归 1261 绿、HMI 258、四条 blocking 门禁全绿（L0 strict 579 恰好用满）；坐标可信链与 S2S 红线零放松 ⚠ **其中③「预览卡只上 `_SPEC_GROUPS` 四族」的实现已于 2026-08-21 被推翻**——那张表的组名是**猜的**（`ice` 查的「冰量」组在瑞幸不存在），于是卡上真的画出了一个**必然失败的「少冰」按钮**；改由 `servers.yaml` 的 `input_schema` 声明（契约 §9.31，方案 §4 表内已就地校准）。**目标是对的，实现没做到。** |
| [2026-08-14-eva-round2-capability-gaps.md](2026-08-14-eva-round2-capability-gaps.md) | **超级 EVA 指令集第二轮对标缺口分析**：泓舟收集的 25 条竞品语料逐条比对（基本具备≈3 / 部分≈5 / 缺失≈17），定性=这轮考的不是「规划得对不对」而是「执行面有没有那个维度」——时间维（deadline/ETA 求解全链零表达）、路线几何维（「顺路」实为目的地附近，真沿途只有充电域）、记忆消费维（存储面完整但消费只有三个出口，§六 6 条长期记忆语料全灭）、生态维（订座/场次诚实桩）。缺口归 11 簇 G1-G11 并给优先级建议（P0=时间约束求解+记忆消费面）；§3 列明不照抄项（主动预设导航违反零执行权铁律，对标形态=询问式建议卡）；§5 顺带实锤 7 条存量问题（最重=nearby 假个性化：口味偏好搜索后才召回、只拼话术不改结果集） | ✅ 已批准（泓舟 2026-08-14 确认方向并纳入存量问题）；P0/P1/P2 + 存量问题已按下行实施计划五批合入，P3 簇（G4/G8/G9-trip）维持另立 RFC、G10 维持搁置 |
| [2026-08-14-eva-round2-implementation.md](2026-08-14-eva-round2-implementation.md) | **EVA 二轮实施计划（批 A-E）**：A 存量问题七条（评分先排后截/trip 序数/治理器 owner 分组/死槽位摘除/文档账）→ B 导航五刀（arrive_by 时限求解+ETA 量化+出发提醒反向环 / 沿途 45% 采样点 / landmark 俗称+自然地物 / route_pref→高德 strategy / 多途经点保序）→ C 记忆消费面（proto subject/polarity + 四个确定性消费出口，修 nearby 假个性化）→ D 事件记忆→询问式提醒建议（零执行权不破）→ E nearby 类目/诚实追问/氛围重排。关键设计决策 8 条（G7 询问式两段、死槽位摘除不接上、话术只许诺走得通的路等） | ✅ **五批全部实施合入 main**（`51c4992`/`04ff730`/`ad0f2e8`/`32fcdaf`/`7d47959`）；§7 实施记录含四处实施期修正（自埋 23点 错拆被自测抓住 / 饮食信号面过窄误伤主流路径 / 话术许诺未实现能力被改 / catalog 锚点 +62 留痕）与遗留清单 |
| [2026-08-14-dest-grounding-containment-card.md](2026-08-14-dest-grounding-containment-card.md) | **决策卡：目的地接地「就近包含误伤」家族（R1 第二期）**：EVA 真栈探针一天三见（虹桥机场→如家酒店停车场 / 外滩→星空艺术馆8层 / 滴水湖→雅悦酒店，trace 四例在卡）——R1 第一期包含式校验的隐含假设「名字包含查询词⇒是本体」被证伪：酒店/停车场借地标之名命名，在就近偏置距离序里天然排在本体前，校验放行则去偏置重搜/地标 LLM 两条救济全短路。**同词对照**是关键证据：search_poi 视觉地标分支 near=None 拿到滴水湖本体。四个候选方向（短名/精确优先重排、类目感知降权、去偏置对照裁决、「上次去过」直接用 episodic 坐标）+ 接手取证清单（先建红绿对抗迷你集再动代码）+ 三条不做 | ✅ 2026-08-14 当日收口（D2 类目锚词+主干级匹配+区县收窄、D4 轨迹坐标直用；迷你集 14/14 真高德全绿、真栈复验 5/5，history §34） |
| [2026-08-15-g8-navigation-route-session.md](2026-08-15-g8-navigation-route-session.md) | **G8 导航会话状态与增量改道（EVA 二轮 P3 簇）**：「导航中」此前不是会话状态——途经点/时限/策略在导航步结束即丢，「途经点不去了」无对象可指、「换条路」被兑现成全新导航。结果保留键 `_route_session`（navigation 全部 6 条 navigate 路径盖章）→ `Focus.active_route`（三条纪律复用门店锚定先例：接力不续期/prompt 只渲染名字绝不渲染坐标/消费方限龄 2h）→ `meta.focus_active_route` 按 location scope 下发 → 新 intent `navigation.reroute` 四操作（删/加途经点、换策略、改目的地走全套 R1 接地），未点名约束保持并重出 G1 时限判定。落域：台账 `navigation-trip.reroute-vs-modify` + L0 +5（suites 第六次适用） | ✅ 2026-08-15 实施合入（`74e6beb`）；cloud 802/navigation 124/四门禁全绿 |
| [2026-08-15-g4-trip-theme-retrieval.md](2026-08-15-g4-trip-theme-retrieval.md) | **G4 主题行程（EVA 二轮 P3 簇）**：trip 候选池加「主题检索步」——`extract_theme` 三族（书名号/跟着X游/同款取景地）并算出行 trigger；`build_theme_pool` LLM 只产名字候选（宁少勿错）→「{城市}{名}」优先高德接地 + `name_matches` 拒错名 → 通过者入池，**池的封闭纪律不变、入池来源多一路**；接地成功才标 `Trip.theme` 进话术。v1 刻意不做联网检索候选与方向性主题代选城市 | ✅ 2026-08-15 实施合入（`d26705e`）；trip 56/门禁全绿 |
| [2026-08-15-g9-trip-multi-city.md](2026-08-15-g9-trip-multi-city.md) | **G9 trip 跨城市（EVA 二轮 P3 簇）**：`Trip.cities` 保口述序 + `Day.city`；`extract_cities`（「先去A再去B」逐抓+「A、B」连写拆分）；propose 按城列池、day 标 city 收敛到城市集（缺标按序均摊）；ground 按城池接地；**solve 补跨天衔接 leg**——此前天与天之间不建驾驶段，充电编织对跨城长途是盲的、SoC 递推断开（对单城市同样成立的改进）。v1 刻意不做顺路重排序（城市序=口述序） | ✅ 2026-08-15 实施合入（`63a565b`）；trip 64（既有零改动通过）/门禁全绿 |
| [2026-08-27-minimax-qa-root-cause-fix-plan.md](2026-08-27-minimax-qa-root-cause-fix-plan.md) | **2026-08-26 MiniMax 云端长会话 QA 的根因分析与修复方案**：17 张症状卡（P0×2/P1×13/P2×2）收敛成 16 张根因卡 C1–C16，每卡机制定位到行 + 修法方向 + 验收 + 影响面。七处与 findings 定性不同的重判（P0-02「collector 无法回读」实为两个端侧真 bug + 探针诊断出口合并两种失败；「上海」来自 memory mock 车辆位置的唯一残留回退；等）+ 七个 findings 未记的新发现 N1–N7（后挡除雾**档/挡错字**致关错对象、media stopped 语音不可达、告警信号「机油机油灯」拼接、chitchat 编造改路线…）。四个根因族：会话事实无确定性读出口 / LLM 兜底零护栏+就近挑工具 / 多意图无覆盖度机器判据 / 单槽粘性焦点长会话缝。§4 六批实施顺序 + 三处待泓舟拍板 | ✅ **第 1–5 批已落地（2026-08-28）**：第 1 批 C1 安全告警链 + C2 端侧三 bug + C16 的 1·6·8（另修 N8/N9）；第 2 批 C4 会话事实读出口族（账本扩数据源维 + 三条确定性读出口 + 重列算子）；第 3 批 C3+C10 会话状态机族（`wait_slot` 方向反转成「槽值必须长得像这个槽」+ 声明式契约 `slot_shapes` + 补槽黑洞止损线；提醒域序数参照系统一 + 标题精确度阶梯 + 任务性准入 + 跨轮幂等，外加 C10-D 的两件接手残账）；第 4 批 C6+C5+C7+C8 复合句族（分句级路由锚定 `RouteHint.scope=clause` + 焦点让路 + 条件式约束陈述 shadow；覆盖度观测列 + **挂起不冻结兄弟步**；trip 城市锚/天数守恒/约束已满足直答/按天读；`navigation.reroute` 补 `origin` 维）；第 5 批 C9+C11+C12+C13+C14（拔掉全仓最后一条 mock 车辆位置回退 + 三卡补 `_prov`；防编造从禁语清单换成**类别否定** + 聚合器不再吞掉 Agent 自己的失败话术 + 执行性声明 shadow 观测列；会话内偏好有了载体 `Focus.session_constraints` + 挡板改问合并后的忌口事实；裸时刻双过时**不滚日改小时语义** + 荒谬余量闸；charging 落域走范例/描述/语料）——**C9-B 评估后不做**（那条机制按真栈五轮跑不复现）；第 6 批 C15 + C16 其余（**零生产代码改动**：provenance 裁决落探针的卡型 × 允许 mode 期望表 + 与契约 §9.3 之间的漂移闸；兜底闲聊说了没做 / 取消轮要点名标题 / 端侧拒绝串不得收场 + 扫全组元断言 / RECOVERY 断言确定性出口 / 城市·忌口·时限三条判据；新增**回放计分** `--replay`）。**六批全部落地并上线复验（2026-08-28/29）**：回放「修正后计分」= 33 红 → 38 红（转红 17 / 转绿 12 / WARN 5）；随后 `7ac2176` → `6a65e7a` → `8892431` → **`ed53f8f`** 连发四版，**迷你集三条原红全转绿、仍红 0**，长会话四 persona 完整 + information 56/58，六批**逐条有真栈实录**（城市漂移「上海」→「深圳」、三张卡从缺章到全部盖章、数据源从编造到念出 tushare、重列六次都答出真列表、mock 记 WARN）。⚠ **仍红两条不是新引入**（T20 执行性声明零动作＝六批没修它；T23 安全问句落 `system.clarify`＝拦住了危险执行但没答对）。⚠ 收尾时另抓到**我们自己第 5 批造的一个回归**（GPS 坐标串念进话术，**修了三次才修掉**——判据「同一个值有几个出口，就在它的入口处判一次」）。接手从 §0 接手须知 + §4 实施顺序（含六份「批次终态」）开始，**剩余待办看 `AGENTS.md` §4.1/§4.2** |

| [2026-08-15-eva-e2e-residual-cards.md](2026-08-15-eva-e2e-residual-cards.md) | **EVA 指令集验证遗留六卡（P1–P6）**：个人地点作途经点三级解析（人称→关系图谱/别名→画像/POI，未知诚实教学问）/ 多城 trip `must_visit` 点名 POI 第三路入池（直搜→landmark 俗称接地→按池质心归城→propose hint+确定性补插——「点了名的不许丢」与「池外不臆造」互补）/ 槽校验数量容错+话术人话（边界不松）/ 散句 relation 确定性前置抽取 + `resolve_person_place` 三类并集（只查 place_of 是「存得下用不上」第 N 例）/ 负偏好降权两根因串修（抽取丢店名 + **口味消费门禁不认咖啡类目的结构性漏洞**）/ 方差面范例四投不加 hint。过程抓修恶性缺陷：家人位置陈述被 set_place 改写本人公司地点——人称守卫绝不写画像 | ✅ 2026-08-15 六卡全部实施并端到端终验（报告 §5：✅17/⚠️5/🔒3）；真栈 Z6b 降权兑现/Z7 组合句两连/W2 散句链闭环 |

| [2026-08-15-eva-backlog-cards-e1-e5.md](2026-08-15-eva-backlog-cards-e1-e5.md) | **EVA 余项立卡 E1–E5**（§4.2「EVA 余项」除 G10 外全部处理）：E1 事件时刻**反推**用餐窗（`arrive_by` 的镜像半边；时刻解析下沉 `_sdk/timewindow.py` 作唯一实现，路上预留是**明说的假设**必须念出来，按入座时刻筛营业中）/ E2 停车便利·无障碍·不排队属性维（半径停车场计数**近似** + 记忆驱动触发 + 没有数据就说没有）/ E3 多城确定性归城校正（+ 顺延不得跨城、点名 POI 不当城市、多城没说天数按城数）/ E4 provider 方差面**给分母**的探针脚本（维持不加 hint）/ E5 谓词别名老账（**消费方误伤已命中**：`place.avoid`/`restaurant.no_queue` 因 scope∧谓词前缀是 AND 而永远召不回）→ 归一表扩容 + 消费面两路并集 + 存量清洗 | ✅ 2026-08-15 五卡全部实施并真栈验证；**三处过程抓修**：假个性化（「和老婆吃饭」命中「父母腿脚不便」）、探针自己抽掉位置前提造出「过度澄清 100%」、`_norm_days` 把「三天」读成 0。E3 连修四轮才够得着——**修好一个洞才看得见下一个洞**；末轮漏网点由日志实锤为「潍坊池被高德限流 ⇒ 该城无质心」的已知边界 |

| [2026-08-15-person-pickup-resolution-card.md](2026-08-15-person-pickup-resolution-card.md) | **决策卡：复合句里的「接送某人」人称解析失效**（存量，双档复跑取证后立卡）：「接爸妈去吃饭」两档都给不出教学问、「带我去接孩子放学…」MiniMax 导到济南同名小学 2004km，而同一套记忆下「去接我爸」两档都对 ⇒ **断的不是人称→地点这条链，是它的入口判据**——`_person_destination` 的「去掉人称词后还剩不剩实质内容」对**槽值**正确，套到**整句原话**上天然失效（复合请求必然有剩余内容）；一#1 另有独立成因=planner 转述记忆地点丢了城市。三个修法候选（接不到再回退人称 / 专名跨省兜底重搜 / 不推荐单独做的 prompt 路）+ **接手取证清单**（先建红绿迷你集再动码）+ 三条刻意不做 | ✅ **已落地并收口（2026-08-20，实施记录见卡 §6）**：真栈 **10/24 → 41/45**，一#5 与一#1 均由稳定红转绿，45 次取样里长距离误导航一次都没有。**连同 Q5 残余（`memory_item` 实体归一）一起做的**——两者本来就是同一件事。⚠ **卡上三处定性全被真栈取证推翻**（§2/§3 保留原文不改，两版并排看才知道「卡上怎么写」也要拿真栈验）：① 接错城**不是 provider 乱返回，是我们自己那条去偏置全国重搜捞上来的**（直连高德：同一个词带 near 只出深圳的学校，济南那条只在 near=None 时出现）；② `resolve_person_place` 对「女儿」「孩子」**早已永久返回 None**（匿名占位边与具名边被数成两个人）；③「planner 转述丢了城市」**不成立**，丢城市的是 `memory_item` 本身——2026-08-16 那个悬案到此才真正闭合：**召回的不是边**。架构 **v1.34 §5.2.8**、流水 history **§64** |

| [2026-08-15-qa-exploratory-root-cause-cards.md](2026-08-15-qa-exploratory-root-cause-cards.md) | **探索式真实用户 QA 轮根因分类与立卡 Q1–Q13**：533 turns × 5 persona × 两档跑出的 58 个问题收敛成 **12 个根因**（四个根因贡献 34 个问题：焦点与候选集每轮重建只存名字 13 条 / 会话状态单槽无绑定 5 条 / 能力缺席→就近误执行 6 条 / 安全与真实性只有 prompt 没有护栏 8 条）+ 四阶段计划。**九处与报告定性不同的重判**（逐条 psql/源码取证；重判 5–7 是 2026-08-16 Q12 落地时补的、**重判 8 是 08-17 Q7 残余批**（推翻的是卡自己写的落点）、**重判 9 是 08-22 I-030 批**（推翻的是**症状本身的严重档位**：卡记「跨组比较做不了」＝答非所问，实测是**跨组给出一个算错的确定性答案**）；5–7 分别是：I-029→**Q8 能力缺席**「navigate_to 没有 origin 槽」、I-025②→**Q2 残余**「序数绑错候选集，验不到规格」、I-008 稳定错的是「两条提醒被合成一条」而非时间）：I-044·I-028「幻觉」实为**真实存量记忆**（真病是关系图谱写入零约束——同一个孩子三个学校、四条 `X --family--> X` 自环、`superseded_by` 从未写过）/ I-036 胎压是**忠实转述了一份 mock 手册**（`mock.py:6` 逐字，而 manual-rag·road-safety·chitchat 三个 agent `_prov` 覆盖为 0）/ I-057 可观测**没丢**（已按实际 serving 厂商落 obs）降 P3 / I-021「预览即下单」**代码上不成立**（`create-order` 唯一调用点在 confirm 分支）须先做首偏离诊断。另含三条报告未记的新发现（画像被写成酒店且是复发 / `fire_at=0` 的 pending 提醒能落库 / 错误导航结果进记忆形成正反馈）与两条机制判据（**卡片是终点**：结构化结果渲染后不再是可消费的事实；**§37 复合取消只修了 `wait_slot` 没修 `wait_confirm`**——同一件事两条分支的漂移） | ✅ **四阶段全部完成**（2026-08-15 立卡 → **2026-08-22 I-030 跨组收官**；流水 history §41–§53 + §58/§59/§61/§62/§63/§64 + **§65/§66/§67**）。**I-030 跨组 2026-08-22 收口**（组指代保留键 `_candidate_label` + 跨组算子 + 下发面逐步按域选组；契约 §9.32、架构 **v1.36** §5.2.10）——⚠ 它把卡上的**症状档位**改了一档，见重判 9。**⇒ 轮级只剩 Q10 门店侧 I-024 一条。****Q12 规格维 2026-08-21 收口**——⚠ 卡上「等 B6 `input_schema` 远期字段」的定性被真机推翻四处（planner 没丢词；三个规格槽是**值域声明本身猜错了**，`ice` 查的「冰量」组在瑞幸不存在 ⇒ 结构性死亡两个多月）。**2026-08-22 真栈复验通过**（`--group spec` **9/9 全 `[det]`**）；复验又掀开并修掉「选品这一跳整条链是断的」——正是 I-025② 原文「卡片点击才生成生椰拿铁」那句话，history §66。**Q12 那一卡零残余**（这句话说的是 Q12，不是整轮——轮级残余见本行末尾）。**Q5 已全部交付**（写入闸 + 出处披露 + **实体归一**）。**Q8/Q11/P2 长尾已于 2026-08-19 全部交付**（六条能力上目录、offer 准入闸、P2 五条；架构 §5.2.7 / 契约 §9.29·§9.30）。已交付：Q1/Q3/Q4/Q13/Q2（第一批）/Q7（两个半维度）/**Q5（全部三块）**/Q11/**Q10（三块）**/**Q6**/**存量数据清洗**/**Q12（槽值原话回查 + 中文时间词三份实现收敛）**。**⚠ 进度真相源是 `AGENTS.md` §4.1（完成态叙述与逐卡终态 2026-08-27 起归档 history §72），不是本行。**⚠ **Q10 第四块「双入口不等价」的依赖经取证不成立**——`Focus.candidate_sets` 只活在云侧、**没有下发到 Agent 的通道**，须排在 Q2 残余之后（`AGENTS.md` §4.1 新第 8 步）。阶段 0 三重证据**推翻 I-021**（QA 轮零 `create-order`），并由迷你集牵出 **Q13**（两个分类出口 15/38 不一致）等四条报告没有的发现。**本轮沉淀的三条最贵判据**：注释挡不住第三次（「只在 handle 里加闸等于没加」那条注释就在原地，Q6 仍踩了本仓第三次 ⇒ 收敛成唯一入口 + 源码级守卫）／写位置启发式前先问「有没有一个字段就是干这个的」（`exchange_id` 早就在，按位置猜答出「暂停音乐、暂停音乐」）／**话术层判据验证不了「系统说的是不是真的」**（审计尺子四版前三版都假绿，这本身就是「必须做成确定性 handler」的论据） |

| [2026-08-18-redis-migration-identity-root-causes.md](2026-08-18-redis-migration-identity-root-causes.md) | **Redis 迁移 apply 失败的两条根因**（交接页 §3「不能宣称任何具体根因」的收敛）：**RC1** key-control 体积上限写死 1 MiB，而真实 manifest 是 **7.3 MB** ⇒ `_load_key` 抛错、`redis-restore` rc=1；云端回滚传的是自己那份小 backup-manifest 恰好在阈值内 ⇒ **「apply 失败而 rollback 成功」的全部原因就是这个阈值**。**RC2** 逻辑指纹取 `sha1(DUMP(key))`，而 `DUMP` 对 `hashtable` 编码的对象按 dict 桶序序列化、桶序取决于**每个进程随机的 hash seed** ⇒ 同一个 `redis.rdb` 两次独立加载**指纹三次三个值**（真实批次精确命中 2 个 `payment:` hash + 1 个 1340 成员的 `user_sessions:` set，listpack/quicklist 的 3296 个一条不差）——**不是数据问题，是校验不变量本身不成立**，数据一长必然踩。修法=上限按真实输入定（保留 symlink 守卫）+ 逻辑材料改**编码无关的规范化形式**（集合先排序、逐元素长度前缀、未知类型 fail closed） | ✅ 2026-08-18 两处修复 + 三条回归（反向验证全做）；**云端动作已完成**：受控基础设施重装 + 新 final 批次 `20260818T044944Z-34d72d7-final` 真实 apply = `APPLIED`（Redis DBSIZE 55→3302），过程中又掀开 RC3–RC7 五条，见 history §55.6–§55.9 |

| [2026-08-18-cloud-switch-verification-root-causes.md](2026-08-18-cloud-switch-verification-root-causes.md) | **切云验证通路的十条根因**（迁移成功、30 容器起齐、`status` ok 之后 `dev_stack verify` 仍一次没绿）：**RC8** endpoint 探针 3s 阈值照 localhost 写、云端真实往返 2.3–3.1s ⇒ 稳定 `degraded`；**RC9** 远端锁只读 stdout 不读 stderr，云主机每次 ssh 打整屏扫码横幅、Windows 管道缓冲 4 KiB 写满 ⇒ 远端发不出 `READY` 的**死锁**（并发抽干后 1.5s 就到）；**RC10** `_child_environment` 无条件剥掉 `AUDIO_API_URL` 而探针 import 期就要它——两头各一条**只验形式**的断言互相矛盾却都绿；**RC11** 云端 `AUTH_REQUIRED=true` 而本地 `.env` 没声明 `VITE_WS_TOKEN`，且 verify 不像 `frontend_command` 那样早报，三分钟后才 `child_failed`；**RC12** 探针等 `hello_ack`——那是 cloud-gateway 的 gRPC 帧，边缘 WS 从不发；**RC13** `meta` 传布尔而网关是 `map[string]string` ⇒ 整条消息 unmarshal 失败被**静默 `continue`**，现象是「60 秒零帧零日志」，读起来像云端挂了。**RC14** 切到 cloud 后 `scripts/tests` **17 条单测转红**而被测代码没动——`runner.main()` 从仓库根 `dev-stack.local` 解析 target，本地专属模式在 cloud 档下被合法拒绝 ⇒ **单测的读数不许依赖操作者的部署档**。**RC10/12/13 同在一个文件**＝这条用例从来没跑通过。四条新回归全部从被消费方派生契约（运行器实际交付的环境集合 / 网关能发的帧集合 / Go 结构体 json tag 的线上类型）。**RC15/RC16（接手批 §6c）**：验收项写着「HMI/Dashboard 能联调云端」，而文档给的入口 `dev_stack hmi` 在 Windows 上**必然失败**——argv[0] 是裸名 `npm` 而 `Popen` 不套 `PATHEXT`；同车道第二条是 dev server 输出被 pipe 吃掉、Ctrl-C 打不到它。两条旧守卫都只断言 `argv == ("npm", ...)` 字面量、**从没真起过进程**——**断言字面量等于没断言** **RC17（§6d）**：单测读操作者 shell 里的 `CAR_AGENT_*`——同一条用例只因 `export CAR_AGENT_SSH_IDENTITY=…` 就从绿变红（4 条），而 **CI 从不设这些变量所以永远绿，唯一会设它们的人正是唯一在用云端真栈的人**；这是尺子被环境串味的第三形态（RC10 环境变量被剥 / RC14 仓库状态文件 / RC17 操作者导出）⇒ **验收要在操作者的真实 shell 里做** | ✅ 2026-08-18 十处修复 + 十一条回归（反向验证全做）；`dev_stack verify` = **verified**（release `34d72d7`、minimax/MiniMax-M3、lock e2e），cloud 缺省 E2E **2/2 PASS** |

| [2026-08-23-hmi-android-app-plan.md](2026-08-23-hmi-android-app-plan.md) | **Android 陪伴端 App（`mobile/`）选型与实施计划**：HMI 与 App **共存、两个用户端一个后端大脑**（同 user_id 共记忆、各自会话、hmi/ 零改动）；生产标准 ⇒ 选型 **React Native + Expo**（v1 曾按「包壳 HMI」结论 Capacitor，被需求变更推翻，§0.1 留痕）——决胜维度是生产可演进 + TS 生态协同：`hmi/src` 纯逻辑会话语义层（ws 重连/请求归属/确认台账/pcmPlayer/voiceLoop）经 Metro monorepo 直引 + **白名单台账机器守**共享，不复制不搬家不动部署面；语音=PTT 流式 ASR（PCM 直传同协议）+ pcmPlayer 注入 react-native-audio-api；App 发独立 AUTH_TOKENS 条目（同 user_id、手机档不含 vehicle.control）、会话前缀 `app-`（不踩 memory/server.py:48 合成会话名单）；声纹注册**不做**（模板信道敏感 0.73→0.48 那条升级为约束）；M0 地基→M1 对话→M2 语音→M3 全卡族/车况/地图，免唤醒/S2S 条件触发，生产化（推送/账号/公网）单列挂账三条后端线 | **已批准（方向）**：2026-08-23 泓舟定三需求、五项拍板授权代决（§9 决策记录）；接手入口已登 AGENTS.md §4.1，开工从 M0；**逐任务实施计划见下行** |

| [2026-08-24-mobile-app-implementation-plan.md](2026-08-24-mobile-app-implementation-plan.md) | **Android 陪伴端 App 逐任务实施计划（执行真相源）**：M0–M3 逐任务展开（编号任务=做什么/产出物/验收含反向验证/坑），M4/M5 只给触发条件；§2 协议契约**指认源码行号不转述默写**（主链 WS 8 型下行帧语义=QA 轮硬化产物照抄、ASR `pcm16le` 直传、TTS start/text/finish 与 divergent 收尾、确认=字面 `'确认'/'取消'`+`operation_id`）；§2.7 共享白名单台账初版（准入判据+分阶段+`location.mjs` 仅纯导出的点名条款）；一次性环境准备 E1–E6（Android SDK/JDK=红线须泓舟；**中文+空格路径 ⇒ 原生构建必经 subst**）；里程碑 checklist 失败态与 happy path 同权重；坑账预载 6 条（meta 值全 string 否则整帧静默丢=RC13 原账、ASR 模型 id 全小写、单 final 守卫等） | 已批准（随主设计文档）；接手从 §0 开始，每任务完成补实施记录 |
| [2026-08-29-mobile-ux-v2-presence-redesign.md](2026-08-29-mobile-ux-v2-presence-redesign.md) | **Android 陪伴端交互设计升级方案（UX v2.2：以光球为锚的三层在场）**：盘点结论「贴了光球的聊天软件 vs 语音优先的随身座舱助手」——15 条读数带 file:line（光球不承载免唤醒态 `hf.orb` 算了没渲染 / 状态散在四条窄条 / S2S 轮零记录 / 确认条随列表滚走且 30s 静默消失 / Android 键盘避让空操作 / 形态只有一个布尔 / `driving` 存了没人读）；对标小艺（HarmonyOS 6.1，流光导航条 + 提示胶囊 + 两层容器 + Mate X7 自动分屏 + Pura X Max 侧拉伴随窗）与超级小爱 2.0（HyperOS 4 Beta，按住小白条说·抬手走 + 悬浮态 + 长任务上超级岛自动展开结果），逐条标版本与来源，**可搬的是机制、不可搬的是系统特权**（HarmonyOS 不跑 APK；超级岛三方接入是白名单审核制 ⇒ 用 Android 16 Live Updates / 默认助理角色替代）；五个升级点 U1–U5（在场模型 `derivePresence` 纯函数 + 光球三新态 attention/looking/muted + 状态胶囊 / 语音层 Voice Sheet 三入口一份记录 / Focus Dock 承诺面带倒计时 / 尺寸类 × 折叠姿态 × 行车档 / 出 App 在场跟 M5）；光球十条不变量与保留项清单；批次 B1–B5（只一次原生重建）+ 每批反向验证 + 「不负优化」八条可量判据 + 18 条假设可调点；**v2.1（同日）并入外部评审**：多轴 `PresenceSnapshot`（承诺永不被别的轴盖掉）、Composer 手势契约（轻点始终能说，`vad_silence_ms` 收尾）、**确认只投影 VAL**（`val.py::_safety_gate` 已是裁决点，删掉 UI 自定的车速门禁）、隐私在场栏 + S2S 开录即告知、产品身份（手持/支架/可信车载平板由 token scope + 设备角色定）、材质制度 G0/G1/G2、七种降级态、语音层增量沉淀、执行回执、追溯矩阵；§15 逐条采纳记录（不采纳「上滑锁定/左滑取消」与 App 端多用户 UX，各有理由） | **v2.2；B1 四批已收口，2026-08-30 落地评审完成（✅29 / ⚠8 / ❌1 / ⏭5 / 🔁5），五条 🔁 已回写；下一步 = 新会话拆 B2「语音层」，第一件事 D1**；后端挂账 Q16 `confirm_policy` / Q17 proactive 偏好 API / Q19 `missing_slots` |
| [2026-08-29-mobile-ux-v2-b1-implementation-plan.md](2026-08-29-mobile-ux-v2-b1-implementation-plan.md) | **UX v2.2 · B1「在场与锚」逐任务实施计划（已收口）**（17 任务，先测后码一任务一提交，**全 JS 不重建 APK**）：`core/presence/presence.ts` 纯函数 `derivePresence()`（六轴 + 唯一视觉主态，反向验证=交换 attention/looking 顺序即红）+ `commitment.ts` 稳定排序钉一项；账本侧剪枝按项到期精确调度 + 到期留痕 + 离线入队计数 + 「发送状态未知」；四路结构化信号（播报/抓帧可订阅、回声与链路降级回调、PTT 错误分类）；光球三新态；实验室两开关（v1 形态保留为回滚路径）；`usePresence` 收集器 / 状态胶囊 / Focus Dock（G0 实色、倒计时只读共享 TTL）/ 隐私栏；键盘先取读数再修；Onboarding 上品牌；`/state-gallery` 23 样本 + 覆盖度守卫；Maestro 06/08/09；真机 13 条验收表 | **v2.2 对应的 B1 已收口（2026-08-30）**；逐批读数与遗留出账见 §6.1–§6.4，落地评审见 [`2026-08-30-review-ux-v2-b1-vs-proposal.md`](../reviews/2026-08-30-review-ux-v2-b1-vs-proposal.md)；下一步 = 新会话拆 B2「语音层」，第一件事 D1；⚠ `slot`/`safety_blocked` 在 B1 只有类型与样本（协议无结构化字段） |
| [2026-08-30-qa-safety-confirmed-write-guard.md](2026-08-30-qa-safety-confirmed-write-guard.md) | **QA 收尾安全闸**：实现已闭合全部 dispatch-bound 出口、`response_only` 纵深契约与跨挂起 `safety_origin_text`。安全闸在 `e9fa602` 上专项 **15/15**；完整 information 同 release **57/59、1 warning**，清理与零商户 intent/draft 已证明，两红为 T24 安全问句错域但安全零动作、T47 safety focus 持续造成 charging 澄清。新闻轮 `limit:null` 被装成 `"None"` 的 internal error 已由 release `a729b98` 修复：Planner+Info **289**、Cloud **1278/1**、全量 **7770/32/13 warnings**，部署后新闻 3 会话/15 业务轮零 internal error，status 5/5 healthy、verify verified。⚠ T24/T47 与 TTS RPM/barge 残帧仍活跃，HEAD 的 test/docs 不是新 release | **生产已修主要安全与 internal-error 链；仍非 QA 全绿**（2026-08-30；见 §12.2） |
| [2026-08-30-mobile-ux-v2-b2-implementation-plan.md](2026-08-30-mobile-ux-v2-b2-implementation-plan.md) | **UX v2.1 · B2「语音层」逐任务实施计划（按方案 v2.2；15 任务四批，先测后码一任务一提交，全 JS 不重建 APK）**：T1 修 D1（承诺卡摘要取紧邻上一条用户原话 + ❌-1 标签让位）、T2 D2–D5 一组判据修正（armed 胶囊 3s 隐藏 / error 不被 armed 遮蔽 / 麦克风四档 `MicState` + `MIC_LABEL` 一处出口）；之后语音层三入口一张层（开合与 detent 是 `derivePresence` 的派生态、层零自有状态）、draft→final 增量沉淀（草稿气泡 / 打断不改红 / D9）、S2S 轮沉淀 + 「端到端」角标 + 开录即告知 + 首次显式同意、轻点即说（**写计划时核出 `vad_silence_ms` 只有 qwen3 消费、缺省 fun-asr 收不了尾 ⇒ 端侧 VAD 三层收尾**；**PTT 在免唤醒开着时今天是坏的 ⇒ AsrSession 领 micLease、jest 钉住**）+ Composer 手势契约 + D7、边缘极光、card_group 主卡/折叠、follow-up chips、视觉先落气泡、回声提示、播报三档、执行回执、在场轨迹页（🔁-1、D8）；末尾 B2→B3 闸（真人语音轮五项读数 + 5 人外部小样本 + Maestro 05 manual）；§1.1 追溯表把方案 / 评审每条要求指到任务 | **四批已收口；B2→B3 闸：过（泓舟 2026-09-01 逐项验收后裁定放行）⇒ B3/B4 可开工**。五项闸全部有读数且达标：G1 ✅ 首反馈时延 **3ms** + 不带唤醒词追问成功 + 追问窗 8.02s、G2 ✅（回声提示 0 次、无环，非预期值）、G3 ✅ 两半、G4 ✅ 两方向层与草稿都不丢、G5 ✅ 60.0fps 零掉帧 + 同屏循环动画 1 个。⚠ **5 人外部小样本是「裁定算过」不是测到**（实为泓舟自评 6/6，1 人非外部）——§11.4 状态可读性**仍无外部基线**；Accessibility Scanner 仍 ⬜。验收轮查出**三条缺陷记给 B3/B4**：S4 语音轮里 Composer 球看不出在播报（与 G5 的 0.00% 是一件事两面）、S6 muted 光球被 `filter` 裁成方框、**`plateGesture`「空输入框可长按录音」真机上从来不工作**。逐批读数/坑/遗留出账见 §6.1–§6.4，**§6.4 第一行是闸的结论** |
| [2026-09-01-mobile-ux-v2-b3-implementation-plan.md](2026-09-01-mobile-ux-v2-b3-implementation-plan.md) | **UX v2.1 · B3「原生一次重建」逐任务实施计划（按方案 v2.2；12 任务四批，先测后码一任务一提交）**：T1–T3 重建前先修 B2 验收轮三缺陷、各自独立提交且**每条配肯定式验收项**（S4 静态 speaking 双青环——动画仍停、G5 口径不破，`orbAnimated` 判据抽成 `orbPolicy`；S6 `filter` 挪进 oversize wrapper；`plateGesture` A-spike RNGH 竞争配置 → B 透明触摸层，真机正反例四格）；T4–T8 新原生一次落盘（expo-haptics + expo-blur + **自写 `modules/foldstate`**；keyboard-controller 按 B1 T12 + B2 G3 读数**裁定不装**）→ 一趟 `-Clean` 重建 → **注册验证按通道分流**（Expo 件看 ExpoModulesProvider + 运行时探针，PackageList 只管 RN 社区库——方案 §11.2 那行的字面对本批三件不适用）；T9 触感四种 / 姿态手折 / blur spike 三判据裁决（Q14）；T10 **AEC（`96a6830`）首次进包的语音读数重取段**（dumpsys `VOICE_COMMUNICATION` 核证、S2S 覆盖面终裁、唤醒率/VAD 重取、G2 回声口径换成「AEC 在场不可自然触发」、`lastUpdateTime` 进读数口径）；T11 B3′ 默认助理角色**独立 spike 分支**（不合主线不发版，只验 Q5 两问，§12.2 红线）；§1.1 追溯表把 §11.1/§11.2/B2 出账逐条指到任务 | **四批已收口（2026-09-02）**。三条带走的结论：① **AEC（`96a6830`）首次进包**（包锚 `2026-09-01 19:21:55`；B3′ 验完重装后 `2026-09-02 16:45:36`，APK 同源）⇒ 此前语音读数全部作废，**没记 `lastUpdateTime` 的语音读数一律无效**；dumpsys 的 `effects` 行直接列出 AEC/NS 实例，S2S 覆盖面终裁**销 B2 出账⑥**。② **blur 材质裁决：过**——③ 块梯度方差 **11.9** vs ①155.9／②328.7、最大梯度 **19.2** vs 100/140（低通签名；均值反而更高，能分开两者的是 max/p95 与方差），A/B 帧率**分不开**（同配置两趟噪声底就有 5ms/0.65pp 那么宽），20 次挂卸零崩 ⇒ B4 可换真模糊，接法必须 `<BlurTargetView>` + `blurTarget` 且 ref 先挂上（首帧为 null 会**静默回落成 none**）。③ **B3′ 默认助理角色：第一问过、第二问不过**——角色能选能生效（`query-services` 5→6、UI 列表、三条 secure/role、运行时 `isRoleHeld` 五条断言一致），但 HyperOS 三个触发手势（电源键长按解锁/锁屏、双击小白条）**一个都拿不到**，成因是 `com.miui.home` 与 `system_server` 都用**显式 ComponentName** 直接拉起小爱、不做 `ROLE_ASSISTANT` 解析（同机 AOSP `keyevent 219` 通路当场响应）；§12.2 红线两侧都有活证（锁屏 `onShow` 到达但 `refused`、`startActivity` 0、开麦 0）；spike 分支 `spike/b3p-assistant-role` 保留、**永不合并**。逐批读数/坑/遗留出账见 §6.1–§6.4；**仍开**：唤醒率 5–6/10 交裁（未触 N<5 红线，引擎对照已证「引擎能认」）、播报卡顿需一趟**无 AEC 对照构建**才能定因、`shutter`≡`wake`、B2 表#6 与「换一批」两格未取 ⇒ 全部 → B4/B5 |
| [2026-09-02-mobile-ux-v2-b4-implementation-plan.md](2026-09-02-mobile-ux-v2-b4-implementation-plan.md) | **UX v2.1 · B4「形态与行车档」逐任务实施计划（按方案 v2.2；15 任务四批，先测后码一任务一提交；零重建 / 零新原生依赖 / 零后端改动）**：第 1 批五份纯函数——尺寸类 × 姿态 → 布局 `layoutMode`（双栏 720 内容约束、姿态压过尺寸、分屏落单栏）、行车档 `drivingActive`（事实只有 Edge `process` 帧的 `driving` 标 + 手动开关、标 false 起 30s 退出，**客户端不拿 `speed_kmh` 再算一份**）、动效策略 `orbTempo`（reduce-motion 静帧 > 行车 ×0.5 > 全速）+ `EdgeGlow` 判据出 JSX、提示音 `OscillatorNode` 合成（同一 AudioContext；唤醒 ARMED→LISTENING 才响、PTT 不响）、舞台场景与 hmi `ContextualStage` 从源码逐字对账 + 通用列表兜底卡（`charging_list` 落点，型名归 hmi 侧）；第 2 批形态落地（`StagePane` / 舞台抽屉 / 双栏 / tabletop 分界 `measureInWindow` / book 铰链落 gap / 外内屏切换 PTT 松手 / 返回顺序、Maestro 07；语音层外壳换真模糊 `BlurTargetView` + `blurTarget`、同屏唯一 BlurView、`tintOverBlur` 待证；发送键圆形图标 + live region / partial 节流 / 卡片按钮 role）；第 3 批行车档（身份三选、说明行不承诺可控车、手动开关、建议胶囊只建议不切；B/C 语音层常驻 + 答后 3s 回落、120dp 球、一屏一卡 `DrivingCardSummary`、文本输入 A 常驻 / B 折叠 / C 隐藏、目标 56 用 `maestro hierarchy` 读实、过程区单行、横屏 40:60；主动消息仲裁接共享 `decideSpeech` + 行车档 critical 强制）+ 真机验收（§11.2 B4 四条——**③ 的 Dock `safety_blocked` 半无产出方，记 ⬜ 待 Q16**）；第 4 批 5 人外部小样本（做不做交裁）+ 收口。§0 单列两个**待裁项不进任务**：唤醒率 5–6/10 调阈值要独立 A/B 批、播报卡顿要一趟无 AEC 对照构建；§0 第 9 条把 `AGENTS.md` §4.2 每条「归 B4」指到任务 | **草案待批（2026-09-02）**；先裁 §0 第 3 条两个待裁项与是否分树 |

> 接真实 provider 的标准流程见常青指南 [`docs/guides/provider-integration.md`](../guides/provider-integration.md)。
