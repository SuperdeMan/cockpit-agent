# 探索式真实用户 QA（DeepSeek/MiniMax 对比轮）根因分类与立卡 Q1–Q12

- **状态**：待实施（2026-08-15 泓舟指示「分类总结原因并立卡，排计划分阶段处理」）
- **来源**：[`docs/reviews/2026-08-15-exploratory-real-user-qa-deepseek-minimax.md`](../reviews/2026-08-15-exploratory-real-user-qa-deepseek-minimax.md)（58 个去重问题：P0×3 / P1×44 / P2×10 / OBS×1）
- **性质**：**存量缺陷为主**，30 个问题族两模型同时复现 ⇒ 按 §4.3「两档是否同时错」判据全部是系统缺口，不是模型方差
- **卡号**：Q = QA 探索轮（与 B1–B6 外部评审、P1–P6 遗留、D1–D4 接地、E1–E5 EVA 余项、G1–G10 缺口均不冲突）
- **本文档做什么**：把 58 个**症状**收敛成 12 个**根因**，每个根因一张卡、一条首偏离链、一组验收；并排出四阶段计划。**不含实施**。

---

## 1. 一页结论

**58 个问题不是 58 件事，是 12 个根因的 58 种表现。** 其中四个根因贡献了 34 个问题：

| 根因 | 一句话 | 问题数 |
|---|---|---|
| 焦点与候选集**每轮重建、只存名字** | 卡片上的结构化事实不进上下文，下一轮只能靠 LLM 从话术里回忆 | 13 |
| 会话状态**单槽 + 无绑定** | 一个 `awaitConfirm` 布尔、一个 pending 记录、一句「确认」谁都能接 | 5 |
| **能力缺席 → 就近误执行** | 没有「估算距离」这个 intent，planner 就挑了 `navigate_to` | 6 |
| 安全与真实性**只有 prompt 没有护栏** | 「不要编造」是 system prompt 里的一句话，不是一道闸 | 8 |

**四处与报告定性不同的重判**（本次逐条取证在案，直接影响该修什么）：

| # | 报告定性 | 实测 | 影响 |
|---|---|---|---|
| **重判 1** | I-044/I-028「产生不存在的孩子/学校」「凭空生成咖啡店记忆」= 幻觉 | **是真实存量记忆**。`memory_item` 现有 `person.child`「用户的女儿在深圳市南山实验小学上学」、`taste.dislike_place`「以后不要推荐三立方(南山创维店)，觉得咖啡太酸」——u1 库里逐字在案 | 不是「防幻觉」，是**关系图谱写入无约束 + 答案无出处披露 + QA persona 只换 `session_id` 不换 `user_id`**。修法完全不同 |
| **重判 2** | I-036「未知车型时用通用常识覆盖手册真实性」 | **系统忠实转述了一份 mock 手册**。`agents/manual_rag/src/providers/mock.py:6` 逐字写着「前后轮均为 2.4–2.5 bar（冷胎）」，source=「第3章·轮胎保养」 | 不是「LLM 乱答」，是 **PoC mock 数据在消费面与真手册不可区分**（manual-rag 零 `_prov`）+ 零命中不短路 |
| **重判 3** | I-057「静默降级，可观测数据必须记录 actual provider」 | **已经记了**。`llm-gateway/server.py:270` 用实际 serving 厂商 `provider=a_aid` 落 obs `llm_calls` | 只剩 HMI 展示缺口，**降级为 P3**，不占 T2 名额 |
| **重判 4** | I-021「用户确认前已创建真实未支付订单」，**报告 §0 据此写下「麦当劳链路发生一次确认前创建真实订单」** | **不成立，已由阶段 0.1 三重证据坐实**（详见 §3-Q10）。那笔订单创建于 **2026-08-12 14:01、属 session `demo-7acjk0`**，比 QA 轮早三天；**QA 轮全程零 create-order** | I-021 **整项移出 T0**，与 I-026 合并成同一个缺陷：**查单/「刚才订单」不绑定当前 session/draft**，默认返回该 user 的历史订单 |

**三个报告未记、本次取证新发现的缺陷**（编号 N，并入对应卡）：

- **N1 画像地点被污染，且是复发**：`place.company` 现值 =「深圳湾万象城桔子水晶**酒店**」，相邻还有 `老婆 --works_at--> 深圳湾万象城`；另有一行 `place.frequent` 的正文就是字面量 `frequent`。§40 刚复位过一次同一字段。→ Q5
- **N2 无效数据能落库**：`reminder_item` 有一条 `fire_at=0`（1970-01-01 08:00）的 **pending** 提醒，标题是「现在这段对话里最便宜的瑞幸咖啡价格」——时间解析失败 + 非任务陈述被建成提醒，**两个缺陷叠加后仍然写进了库**。→ Q11
- **N3 错误结果进入记忆形成正反馈**：`2026-08-15 导航去过济南市南山实验小学` 两行在库——I-012 那次错误导航被记成了轨迹事实，会继续污染后续推荐。→ Q5

---

## 2. 根因分类表（58 → 12）

> 「首偏离层」= 该族问题**第一次偏离**发生在哪一层。这一列决定卡归谁、也决定别在下游层打补丁。

| 卡 | 根因 | 首偏离层 | 覆盖问题 |
|---|---|---|---|
| **Q1** | 确认/挂起/取消状态机：单槽 + 无绑定 + 两条分支判据不对称 | HMI + cloud engine | I-013、I-046、I-051、I-037① |
| **Q2** | 焦点与候选集：每轮重建、只存名字、无来源/版本/时效 | cloud context | I-011、I-018、I-019、I-023、I-030、I-052①、I-042、I-017、I-035、I-009①、I-026①、I-032② |
| **Q3** | HMI 响应归属：result 帧无 request id，靠 FIFO + 单看门狗 | HMI | I-048、I-053①、I-022、I-010、I-031 |
| **Q4** | HMI 位置前置闸：客户端正则子串 + 整句拦截 | HMI | I-007、I-032① |
| **Q5** | 身份·作用域·记忆写入约束 | memory + 产品口径 | I-044、I-028、I-045、I-056、I-015、I-058(OBS)、N1、N3 |
| **Q6** | 执行事实没有权威账本，审计由 LLM 从对话历史重构 | 缺失的一层 | I-047、I-038、I-002 |
| **Q7** | 端侧语义维度缺失：极性 / 顺序 / 省略 | edge fast_intent | I-039、I-040、I-006、I-003、I-005、I-001 |
| **Q8** | 能力缺席 → planner 就近挑工具（表现为误执行而非不支持） | 能力面 | I-016、I-004、I-049、I-050、I-037②、I-043① |
| **Q9** | 安全与真实性只有 prompt 没有护栏 | agent 实现 | I-036、I-054、I-043②、I-055、I-034、I-033、I-052②、I-057 |
| **Q10** | 商户：文本入口与按钮入口是同一件事的两份实现；确认前副作用待诊断 | mcp-bridge + HMI | I-021、I-027、I-020、I-024、I-025、I-026② |
| **Q11** | 提醒准入：没有「这句话是不是一个任务」判据，无效数据仍落库 | reminder + proactive | I-014、I-009②、N2 |
| **Q12** | 槽值保真：planner 转述丢限定词（城市/段位/语言/角色） | planner slots | I-008、I-041、I-029、I-025②、（I-012 已有卡） |

> I-012 已有独立卡 [`2026-08-15-person-pickup-resolution-card.md`](2026-08-15-person-pickup-resolution-card.md)，**本批不重复立卡**，但 §Q5 的取证给它补了一条新证据（见该卡「机制」段的修订建议）。

---

## 3. 逐卡

### Q1 · 确认/挂起/取消状态机（T1）

**覆盖**：I-013 全局确认命中旧请求 / I-046 取消后仍长期待确认 / I-051 商户补槽跨域劫持 / I-037① 无订单却进退款确认

**机制（已定位到行）**：

1. **HMI 侧确认是一个布尔 + 一个字符串槽**。`hmi/src/App.tsx:73` `awaitConfirm` 是全局布尔；`confirm()`（:794）在没有 `pendingLocationText` 时直接 `dispatch(reply, true)`，**只发「确认」二字，不带任何 operation_id / trace**。谁最后置位 `awaitConfirm`，这一下就打给谁。
2. **云侧挂起也是单槽**。`session.py:123` `_session_key(owner, session_id)` 一个 key 一条 `SessionState`，`_suspend` 覆盖旧挂起（engine.py:228 注释已自认「确认条 UI 也只有一个，语义一致」）。**这个语义在两个并行任务下就不成立了。**
3. **取消判据两条分支不对称**——本卡最关键的一处。`wait_confirm` 分支（engine.py:234）用 `_confirm_reply`，其「词占据整句」规则是 `len(t) <= len(k) + 3`；`wait_slot` 分支（engine.py:255）有 `_SLOT_CANCEL_RE` 子串匹配 **+ 复合取消余量 ≥6 续处理**（§37 那批的产物）。
   于是 **「取消刚才解锁」6 字 > 2+3，在 `wait_confirm` 下判不出取消**，落进「插话保留挂起」，pending 一直活着——正是 I-046 的原文现象（第三次单独说「取消」才清除）。
   > 判据：**§37 的复合取消修法只落了一半**。同一件事的两条分支，修了 `wait_slot` 没修 `wait_confirm`——「同一件事有两份实现，迟早有一份是错的」在**分支**上的形态。

**修法方向**（未拍板）：
- **A（必做）**：把 `wait_slot` 的取消判据（子串 + 复合余量续处理）**下沉成两条分支共用的唯一实现**，`wait_confirm` 改为调用它。同 B5 `stream_state.py`「判定抄两份正是 B1 那个 bug 的成因」。
- **B（必做）**：确认帧带 `operation_id`。`_suspend` 生成 id → 随确认卡下发 → HMI `confirm()` 原样回传 → engine 校验不匹配就诚实拒绝（「这条确认对应的操作已经不在了」），**不静默打给当前挂起**。
- **C（✅ 泓舟 2026-08-15 已拍板做）**：单槽 → 小容量挂起表（≤3，LRU 淘汰最旧）。HMI 确认条相应支持同时显示多条。**实施顺序：先 B 再 C**——`operation_id` 是挂起表的寻址键，没有它多槽根本没法定位；先做 C 会得到一个「有三个槽但仍然靠猜」的状态机。⚠ 淘汰要有话术（「刚才那条解锁确认已过期」），**静默丢弃就是 B3 那条「认不出就用默认值」的确认版**。

**验收**：确认 / 取消 / 重复取消 / 超时后取消 / 换话题后取消五组；跨任务串扰一组（旧导航挂起 + 新 scene 确认）；**每组两档各跑一次**。

---

### Q2 · 焦点与候选集：来源、版本、时效、结构化事实（T2，问题数最多）

**覆盖**：I-018 已有营业时间却答未查到 / I-023 已知两价不算合计 / I-030 跨商户序数比较失败 / I-011 失败搜索清空候选 / I-019 详情焦点粘滞 / I-052① 无候选编造 / I-042 取消不存在的任务 / I-017 取消后卡片残留 / I-035 取消的行程仍 active / I-009① 提醒焦点错位 / I-026① 「刚才订单」错绑 / I-032② 地点焦点切换

**机制**：

1. **`Focus` 是一组扁平单槽，每轮从当前 plan 重建**（`context.py:229` / `extract_focus:545`）。粘性接力**只有两格**：`last_places` 与 `active_route`（`update_focus:700-712`）——这两格是前两次被真栈烧到才加的。`last_choices` / `last_poi` / `last_destination` / `last_choice_purpose` 仍然每轮重建，**任何一轮不产生候选就把上一份抹平**。这就是 I-019。
   ⚠ **I-011 的根因经阶段 0.2 取证后改写**（§3.5-N5）：那次「失败的重搜」**根本没失败**——泛化兜底搜出了 10 家「美食」，于是它**合法地**覆盖了上一份川菜候选。所以要修的不是「失败不覆盖成功」，而是「**兜底/降级产生的候选集不得顶替用户点名的那份**」（候选集要带 `is_fallback` 之类的来源标记，序数解析优先绑定非兜底的最近一组）。
2. **候选集只存名字**。`last_choices` 是 `list[str]`（最多 5 条）、`last_places` 只留 `name/lng/lat` **三个标量**。于是卡片上明明渲染了营业时间、评分、电话、价格，**下一轮的上下文里一个字都没有**——I-018「称全部未查到营业时间」、I-023「不肯算 26.5+9.5」由此而来。
   > 判据：**卡片是终点**。结构化结果一旦渲染成卡片就不再是可消费的事实，后续比较/合计/序数只能交给 LLM 从话术里回忆——而它没有那些数。
3. **候选集没有来源与版本**。一个 `last_choices` 同时被 nearby POI 列表、商户菜单、途经点候选、充电目的地候选共用（App.tsx 侧同样是四个并列的 `lastXxxRef`）。跨域「第二个」于是无从判断问的是哪一份——I-030、I-025①、I-053②。

**修法方向**：把「候选集」升格成**一等对象**——`CandidateSet{source_intent, agent_id, version/ts, items:[{id, name, 结构化属性}]}`，Focus 持有**最近 N 组**而不是一格；序数解析显式绑定某一组；失败轮不覆盖成功轮（沿用 `last_places` 已经验证过的粘性纪律 + 限龄不续期）。

⚠ **先做 §5 的取证**：本卡最容易走成「给每个字段都加粘性接力」——那是打补丁，不是修根因，且第三次、第四次仍会漏。

**刻意不做**：不把候选集渲染进 prompt（`last_places` 那条注释已经写明：让模型看见只会诱导它自己编）。**它是给确定性消费方用的结构化事实。**

---

### Q3 · HMI 响应归属与并发（T1）

**覆盖**：I-048 上一轮生成中继续发送→响应错挂 / I-053① speech 与卡片不同家 / I-022 营养查询显示订单卡 / I-010 主动卡按钮未关卡片 / I-031 按钮回发反被拒识

**机制**：

1. **WS result 帧不带 request id**，归属靠 `pendingIdsRef` FIFO + 一条注释里的假设：「网关 WS 串行（一轮事件流 drain 到 EOF 才读下一条），故 fifo[0] 恒为当前正在收流的轮」（App.tsx:89-91）。**这个假设在用户抢发时不成立**：端侧快路径可以秒回，云侧还在流——两条流交错，FIFO 头就不是正在收流的那轮。
2. **看门狗只有一个计时器**。`armWatchdog`（:575）开头就 `clearTimeout(watchdogRef.current)` ——第二个请求一来，第一个请求的超时保护**被清掉了**。这解释了报告里「下一轮长期正在思考，需要同标签页刷新」：那轮既没有 final，也没有人再去救它。
3. **按钮 send_text 与后端状态解耦**：卡片按钮发的是一句自然语言，后端按新请求处理，与产生这张卡的那次操作没有任何绑定——I-010（点「不用提醒」→ 后端说没有待确认操作）、I-031（按钮句被判拒识）。

**修法方向**：result/action/stream 各帧回带 `request_id`（网关已有 trace 语境，属加字段不属改协议）；看门狗改**每轮一只**（`Map<id, timer>`）；发送期间要么禁用输入、要么明确支持并发并按 id 归属——**两者选一，不能靠 FIFO 猜**。

**验收**（照抄报告 §I-048，它列得对）：三请求快速连发 / 第一轮流式+第二轮 action+第三轮超时 / 中间取消一轮 / 每个用户气泡只接收自己 trace 的帧。**必须 CDP 真浏览器**。

---

### Q4 · HMI 位置前置闸（T1，改动面最小、收益最直接）

**覆盖**：I-007（整句被拦，含车控、取消、显式城市）/ I-032①（AQI 被拦）

**机制**（`hmi/src/location.mjs:22-31`，`App.tsx:773`）：

```js
LOCATION_DEPENDENT_TERMS = /(导航|带我去|…|充电站|停车场|…|充电|补电|续航|…)/
isLocationDependent = LOCATION_DEPENDENT_TERMS.test(t) || (WEATHER_TERMS && !EXPLICIT_PLACE)
```

四个各自独立的缺陷叠在一起：

1. **子串匹配没有意图概念**：「打开**充电**口」命中 `充电`、「**取消**当前**导航**」命中 `导航`、「别开始**导航**」命中 `导航`。
2. **`EXPLICIT_PLACE` 只对天气那半边生效**：「查深圳欢乐海岸周边停车场」显式给了地点，仍被 `周边|停车场` 直接判定为依赖当前定位。
3. **没有否定/取消排除**：闸只看「提没提到位置类词」，不看这句是要**开始**还是**停止**。
4. **闸在 `send()` 里、`dispatch()` 之前，拦的是整句**：多意图句里的天气/股票/车控全部陪葬。

> 判据：**客户端不该做意图判定**。这道闸真正要问的是「本轮**执行**是否需要当前坐标」，那个答案只有编排侧知道。

**修法方向**：闸降级为「**没有任何显式地点线索 + 命中位置依赖词 + 不是否定/取消句**」三条同时成立；否定/取消/车控对象走白名单；更彻底的方向是**后端在需要坐标时回一个 `need_location` 帧**由 HMI 弹授权——闸从「事前猜」变成「事后要」。⚠ 后者改动跨端云，二期评估。

**验收**：显式城市 / 当前位置 / 取消 / 否定 / `charging_port` / `charging_station` 六组双向；多意图句「关空调 + 查深圳天气 + 看股票」不得整句被拦。

---

### Q5 · 身份、作用域与记忆写入约束（T2，本批最深的一处）

**覆盖**：I-044、I-028、I-045、I-056、I-015、I-058(OBS)、N1、N3

**机制**（psql 实测，证据在库）：

1. **会话不是隔离维度，owner 才是**。`reminder_item` 表**根本没有 `session_id` 列**（有 `user_id` / `occupant_id`）；memory 同理。QA 的 5 类 persona 全部是同一个 `u1/primary`，只换了浏览器 `SESSION`。
   实测：`u1/primary` 有 **15 条 pending + 90 条 cancelled** 提醒——I-045「返回 13–17 条历史提醒」就是这 15 条，**不是泄漏，是默认查询范围 = 全时段全会话**。
2. **关系图谱写入零 schema 约束**。`memory/relation.py` 只归一了**谓词词表**（`REL_VOCAB` + 别名），没有主宾角色校验、没有自环守卫、没有 `(subject, rel)` 单值约束，`superseded_by` 列存在但**从未写过**。库里现状：

   | 实测行 | 病 |
   |---|---|
   | `深圳国家工程实验大楼A栋 --works_at--> 用户` | 主宾颠倒 |
   | `公司 --lives_at--> 深圳国家工程实验大楼A栋` | 把「公司」当人 |
   | `老婆 --family--> 老婆`、`女儿 --family--> 女儿`、`孩子 --family--> 孩子`、`爸妈 --family--> 爸妈` | 自环边，零信息 |
   | `深圳 --place_of--> 出发地` | 主宾颠倒 + 把「出发地」当实体 |
   | `女儿 --place_of--> 深圳市南山实验小学` / `女儿 --place_of--> 南山实验小学` / `孩子 --place_of--> 南山外国语学校` | **同一个孩子三个学校，无 supersede** |

   > **这直接改写了 I-044 的定性**：不是「用相似 persona 记忆补全」，是**图谱里真有三条互相矛盾的边，每轮召回哪条看运气**。
   > 同时它给 [`person-pickup 卡`](2026-08-15-person-pickup-resolution-card.md) §2 补了一条证据：那张卡写「关系边里存的是全名，是 planner 转述那一跳丢了城市」——**库里其实存着一条不带城市的边**。修那张卡前先重新取证是哪一条被召回。
3. **画像地点写入同样无准入**（N1）：`place.company` 现值是一家**酒店**，`place.frequent` 的正文是字面量 `frequent`。§40 刚复位过同一字段 ⇒ **复位是清数据，没修入口**。
4. **答案不带出处**：记忆命中后直接以「您……」陈述，没有「我记得您之前提过」这一层——于是真记忆在用户眼里就是幻觉。

**修法方向**（三层，缺一不可）：
- **写入闸**：自环拒绝、主宾角色校验（人→地点方向固定）、`(subject, rel)` 单值 supersede、置信度低于阈值不落库。**沿用 E5 的定式：归一表 + prompt 收敛源头，两头都做。**
- **消费面出处披露**：记忆驱动的结论必须说出「依据是哪条记忆、什么时候记的」（E2 已经有这个形态：「记得您提到过家人行动不太方便」）。
- **产品口径**：「进行中的任务」默认限**本会话 + 未来 N 天**，跨会话结果必须显式说明。
- **QA 方法论**（写进测试文档）：**persona 必须换 `user_id`，只换 `session_id` 证明不了隔离**。

⚠ **数据清洗需泓舟单独授权**（红线：数据迁移）。清洗前先修入口，否则清完再跑一轮又脏——§40 已经证明过一次。

---

### Q6 · 执行事实账本与审计出口（T2）

**覆盖**：I-047 执行审计不可信 / I-038 长会话末尾无法回忆 / I-002 speech 与车辆状态不一致

**机制**：`task_ledger` 实测只有两类 kind——`research`（106 条）与 `mcp_order`（17 条）。**车控、导航、提醒、场景一律不进账本**；QA 那 533 轮在账本里**一条记录都没有**（最后一条早于 QA 开始时间）。

于是「刚才实际执行了什么」这个问题**没有可查询的事实源**，chitchat 只能拿对话历史让 LLM 重构——它当然会答错（报告实测：声称车窗开了且音乐继续放，真实 action 是 `window.open` + `media.pause`）。

obs 侧 `turns` 表只有 `actions` **计数**，没有动作内容；动作细节在 `spans.attrs` 里，但 obs 是开发侧通道，运行时 agent 够不着。

> 判据：**「系统持有的事实绝不让 LLM 答」**（墙钟三件套的既有纪律）在**执行事实**上还没兑现。

**修法方向**：不新建第二套账本——先看 `task_ledger` 能不能承载即时动作（它已有 `session_id` / `agent_id` / `status` / `result_ref`），或让 executor 把每轮 action 结果落一条**会话级执行记录**，由确定性 handler 回答审计类问题（`chitchat` 只负责措辞，不负责事实）。I-002 同源：speech 必须由 VAL 真实结果合成，部分成功逐项披露。

⚠ **先量清楚写入量**：533 轮 × 每轮 N 个动作，落库频率与保留期要一起定，别做成一个只涨不清的表。

---

### Q7 · 端侧语义维度：极性 / 顺序 / 省略（T2）

**覆盖**：I-039 否定反向执行 / I-040 同对象顺序动作只执行第一步 / I-006 刚操作完「关掉」仍问对象 / I-003「再展开」指代到天窗 / I-005 前后风挡丢一项 / I-001 云边重复执行

**机制**：`fast_intent.split_and_classify*`（:1691/:1730）把整句按连词切段，**每段独立丢给 `classify_structured`**。这条链上：

- **没有极性维度**：「车窗别开」切出来就是「车窗别开」，对象词 `车窗` + 动作词 `开` 命中正向规则 → `window.open`。**「别」不是任何判据的输入。**
- **没有顺序维度**：返回的是一个 list，执行是并行聚合；「打开然后立刻关掉」即使切对了也表达不了「先 A 后 B」。
- **没有跨段省略消解**：「把空调打开**然后立刻关掉**」的第二段没有对象，`classify_structured` 判不出 → 整段标 `_needs_cloud` 上云 → 云侧拿到一个无对象的碎片「立刻关掉」。**第一步执行了，第二步蒸发了。**
- **I-001 是同一条链的云侧半边**：`route.mixed` 把「有点冷」当独立云侧片段规划成 `hvac.inc`，而端侧已经按 26 度执行过——**已被 local_actions 满足的语义跨度没有被剔除**。

> 这三条缺的都是**同一个东西**：端侧分类器是「对象 × 动作」的**正向、无序、无上下文**匹配。加规则治不了，因为极性和顺序不是「再多几条规则」，是**多一个维度**。

**修法方向**：分段时**先抽极性**（否定词 → 段级 polarity 标记），极性为负时映射到反向意图或**诚实上云**（宁可慢一点，不要按反）；同对象多段保序（复用云侧 `depends_on` 的表达，别在端侧发明第二套）；跨段省略先在**段内**回填对象（上一段的对象是最强候选），跨轮省略走 Q2 的焦点。

**验收**：正反两向 × 三个对象 × 并列句 × 纠正句；**必须做反向突变验证**（注入缺陷要红——§4.3「恒绿的断言比没有更糟」）；`test/eval_corpus/intent_adversarial/negation` 族同步补覆盖（⚠ 语料唯一输入 591/上界 591，**余量为 0**，加语料前先按 §4.1 的规矩说明占用理由）。

---

### Q8 · 能力缺席 → 就近误执行（T2/T3）

**覆盖**：I-016 纯距离查询触发导航 / I-004 方向盘加热自然语言不可达 / I-049 静音→`volume.dec` / I-050 双闪→大灯 / I-037② 无订单进退款 / I-043① 疲劳无能力面

**机制**：`navigation` 的能力面只有 `search_poi / navigate_to / reroute / set_place / reverse_geocode / locate / poi_detail`——**没有「估算距离/时长」这个 intent**。用户问「从深圳市民中心到家多远多久」，planner 只能在现有工具里挑最近的一个，于是挑了 `navigate_to`，真的开始导航。双闪→大灯、静音→音量减、无订单→退款，全是同一形态。

> 这是本仓**已经命名过**的病：`fast_intent.py:506` 的注释写着「云侧能力面也没有除雾意图，planner 只能在别的工具里挑」。B4 的能力完整性门禁保证了**「声明了的都齐全」**，但没有保证 **「没声明的会被诚实拒绝」**。

**修法方向**：
- **补能力**（走 B4 SOP）：`navigation.estimate`（只算不导）、`media.mute/unmute`、`hazard_light`；`steering_wheel_heating` 补端侧入口（它已在 `commands.yaml` 声明，属 §4.2 那 14 条欠账，**从那张表里挑并同步删台账条目**）。
- **补「不支持」这条路**：B6 的 `actionability` shadow 已经声明了 REJECT 但 v1 不产出——**这正是它的第一个真实消费场景**。⚠ B6 §5 写明 canary 需泓舟单独拍板，本卡只走「补能力」那半边，另半边挂 §4.2。
- I-037 单独还需要 order reference 校验（无订单号不得进 refund capability），归 Q10 一起做。

---

### Q9 · 安全与真实性：prompt 不是护栏（**T0**）

**覆盖**：I-036 胎压(P0) / I-054 红色机油灯(P0) / I-043② 疲劳与障碍物(P0) / I-055 资讯站冒充手册 / I-034 无来源精确数字 / I-033 失败不披露 provider / I-052② 编造营业时间 / I-057 静默降级(P3)

**机制（四条，逐条有码）**：

1. **`manual_rag` 的「不要编造」只是 system prompt 的一句话**（`agent.py:15`）。代码不检查 `chunks` 是否为空、不检查答案里的数字是否出自 chunks、零命中不短路——**照样把 LLM 输出当手册结论播出去**。
2. **mock 知识库与真手册在消费面不可区分**（重判 2）。`mock.py:6` 的胎压条目带着 `source="第3章·轮胎保养"`，一路传到 `ui_card.sources`，用户看到的就是「手册第 3 章」。同一会话里追问章节时因为关键词不再命中而回到诚实兜底——**两句自相矛盾的答案，是同一条检索链的两次不同命中**。
3. **`_prov` 真实性标记覆盖面漏了正好这三个 agent**：实测 `info` 7 处、`nearby`/`navigation`/`trip_planner`/`charging_planner`/`parking_payment` 各 2 处，而 **`manual_rag` 0、`road_safety` 0、`chitchat` 0**。
   > 判据：**数据真实性治理覆盖了「取回外部数据」的 Agent，唯独漏了「答案本身就是内容」的 Agent**——而后者恰恰是最不该没有出处的那类。
4. **安全域没有「不可让步的结论」这一层**：`road-safety` 的能力面只有天气/路况三条，`fallback: chitchat`；疲劳驾驶、警告灯分级**根本没有能力面**（Q8 同源）。于是红色机油灯的安全约束不跨轮保持，被下一轮的普通驾驶建议覆盖；用户说「别提醒我，继续开」时没有任何东西挡住「好的，安心开」。

**修法方向**（本卡是 T0，先做前两条）：
- **零命中短路**：`chunks` 为空或最高分低于阈值 → **不调 LLM**，直接诚实弃权。这是确定性判据，不是 prompt。
- **来源类型随资料一起传**：chunk 带 `source_type`（`manual` / `web` / `mock`），生成层与话术层都必须体现；**`mock` 类型在非 dev profile 下不得作为结论来源**（接 B3 的 `DEPLOY_PROFILE` 三档闸）。
- **补 `_prov`**：manual-rag / road-safety / chitchat 三个 agent 接入既有 `agents/_sdk/provenance.py`。
- **安全焦点跨轮保持**：红色警告一旦出现，本会话内的驾驶建议必须先过安全约束（Focus 加一格安全态，归 Q2 的结构一起做）。
- **安全能力面**：疲劳/警告灯分级建能力（Q8），**在它建起来之前，安全类问题不得由 chitchat 兜底**。
- I-057 降级 P3：只补 HMI 展示实际 provider（obs 已有数据）。

**验收**：车型未知/已知 × 黄灯/红灯/快速掉压/高速 五组对照；**用户拒绝、改口、无视觉、连续第三轮**四种对抗（报告这条要求是对的，照做）；红色机油灯/水温/制动三类各三轮拒绝对抗。

---

### Q10 · 商户：双入口不等价 + 查单不绑会话（**T0 只剩 I-027 / 其余 T2**）

**覆盖**：I-027 支付丢车牌(**T0**) / I-021+I-026 查单不绑当前 session（同一缺陷，见下）/ I-020 文本下单失败按钮成功 / I-024 明确门店仍要位置 / I-025 序数与规格错位

**I-021 首偏离诊断（阶段 0.1，✅ 已完成 2026-08-15）——结论：确认前副作用不存在**

三条候选（全局确认单槽打错对象 / 上游 `calculate-price` 有副作用 / HMI 按钮误发确认）**全部排除**，因为更前面一步就断了：**QA 轮根本没有创建过订单**。

| 证据面 | 读数 |
|---|---|
| `task_ledger`（kind=`mcp_order`） | 订单 `1030837030000753499156095268` → task `e2a712ef…`、session **`demo-7acjk0`**、created_at **2026-08-12 06:01:39Z（北京 14:01）**。`created_at > '2026-08-14'` 全表**只有一条**（`demo-s680o9` 的瑞幸 `7674063200947863562`，北京 08-15 **09:24**） |
| `obs.turns` 时间窗 | QA 八个 session（MiniMax 七 + DeepSeek `demo-9i6w3x`）全部落在 **08-15 15:40–17:42**；`demo-7acjk0` 只有 3 轮、在 **08-12 14:01**；`demo-s680o9` 12 轮、在 **09:22–09:27**，早于 QA 六小时 |
| `mcp-bridge` 容器日志 | 覆盖北京 **10:07–17:34**（含 QA 窗绝大部分），**零 `create-order` 调用**；唯一相关行是启动期 admission 把 `mall-create-order` 列进「忽略未准入的工具」 |

> ⚠ 换算做过交叉验证：ledger 的 `created_at` 带 `+00`，`demo-7acjk0` 的 06:01:39Z 与 obs turns 的 14:01:33–14:01:41（北京）逐秒吻合；`demo-s680o9` 同样吻合。**不是我把时区读错了。**

**所以 I-021 = I-026**：用户取消预览（draft 正确清除）→ 说「查一下刚才的订单」→ 查单没有绑定当前 session/draft，**返回了该 user 的历史订单**（状态确为已取消，因为麦当劳侧未支付订单会过期/被 cleanup 取消）。报告看到一个真实订单号，就把它读成了「预览阶段建的」。

**这条同时是一条方法论**：**「查到了一个真实副作用」不等于「这次操作产生了它」**——查单类功能一旦不绑定会话，就会把历史副作用搬到当前上下文里，看起来与「刚刚发生」无法区分。同族的还有 I-045（历史提醒）、I-044/I-028（历史记忆）——**本轮 58 个问题里至少 5 个是这一个形态**。

**T0 半边只剩 I-027**：`parking.pay` payload 的 `plate` 为空、`order_id="current"` 仍进入支付确认。**支付确认卡缺任一关键字段（plate/order/amount）必须 fail-closed 拒绝进入确认**，确定性校验、与 LLM 无关，可立即做。

**T0 另半边——I-027**：`parking.pay` payload 的 `plate` 为空、`order_id="current"` 仍进入支付确认。**支付确认卡缺任一关键字段（plate/order/amount）必须 fail-closed 拒绝进入确认**，这是确定性校验，与 LLM 无关，可立即做。

**T2 之一——查单必须绑定会话**（I-021 + I-026）：「刚才的订单」「查一下订单」必须先解析成 **本 session 的 draft / 本 session 创建的 order**；没有就诚实说没有，**不得回落到该 user 的历史订单列表**。历史订单要展示时必须显式标注时间与来源（「这是 8 月 12 日的一笔」）。`task_ledger` 已有 `session_id` 列，**这条是查询条件问题不是存储问题**。

**T2 之二——双入口不等价**：按钮路径带**结构化引用**（store 三元组、product_code），文本路径靠 LLM 从原话解析——同一件事的**两份实现**，于是「深圳欢乐海岸购物中心店」文本说不通、点卡片就通（I-024），文本说「巨无霸」找不到、点按钮就有（I-020）。
**修法**：文本路径先在**当前候选集/菜单**里做确定性匹配（Q2 的候选集对象一旦有了 id 与结构化属性，这条几乎是免费的），匹配不到再上 LLM——**两条入口收敛到同一个结构化解析链**。

---

### Q11 · 提醒准入与无效数据落库（T3）

**覆盖**：I-014 非任务陈述过度触发 / I-009② 明确否定仍创建提醒 / N2 `fire_at=0` 落库

**机制**：`reminder.create` 没有「这句话是不是一个任务」的准入判据，proactive 的 offer 也没有。实测后果全在库里：

- 「接爸妈去吃饭，**别建提醒**」→ 建了一条提醒，正文含「别建提醒」（已 cancelled）
- 「刚才那个提醒现在几点」→ 建成 2026-08-16 03:00 的提醒
- 「现在这段对话里最便宜的瑞幸咖啡价格」→ 建成 **`fire_at=0`（1970-01-01）的 pending 提醒**

> N2 那条最能说明问题：**时间解析失败了，创建仍然成功**。存储层对「什么是一条有效提醒」零校验。

**修法方向**：① 写入闸——`fire_at <= now` 或解析失败一律拒绝落库（确定性，立即可做）；② 否定守卫——原话含「别建/不用提醒/不要提醒」直接不进 `reminder.create`（与 Q7 的极性维度同源，**共用一份极性判据，别写第二份**）；③ offer 准入——只在「明确未来事件 + 时间可用」时 offer（报告这条口径可直接采纳）。

---

### Q12 · 槽值保真：转述丢限定词（T2）

**覆盖**：I-008「下午三点半」→ 03:30 / I-041 英文 tomorrow 当 current / I-029 出发地被当目的地 / I-025② 规格「少冰」丢失 /（I-012 城市丢失，已有卡）

**机制**：planner 产出的槽值是**自由文本**，转述时会丢限定词，而下游拿到的是一个看起来合法的值，**没有任何东西能发现它比原话少了信息**：

| 丢的是 | 表现 | 问题 |
|---|---|---|
| 时段词（下午） | 「明天下午四点……三点半再提醒」→ 第二条槽值只剩「三点半」→ 03:30 | I-008 |
| 城市（深圳市） | 「南山实验小学」→ 全国检索命中济南 | I-012 |
| 语言（英文时间词） | `tomorrow` 未进时间归一 → 返回当前天气 | I-041 |
| 角色（从 X 出发） | origin 被填进 destination | I-029 |
| 规格（少冰） | 未进结构化规格校验 | I-025② |

> 这是既有判据「**planner 改写 query 是不可信指代通道，指代解析只信 raw**」（sports 批）的第 N 次复发。也说明：**槽值校验不能只校「有没有」，要校「比原话少了什么」**。

**⚠ 同时暴露的第三份实现**：中文时段词解析现在有**两份**——`agents/reminder/src/timeparse.py:66` 与 `agents/_sdk/timewindow.py:26`（E1 从 navigation 提出来的那份）。§4.3「同一件事有三份各自正确的实现，就迟早会有第四份是错的」刚在时区上应验过一次，**这里是同一条规则的下一个候选**。修 I-008 时顺手收敛到唯一实现，别再加第三份。

**修法方向**：关键槽（时间/地点/角色/规格）在执行前做**原话回查**——槽值里的限定词若原话有而槽值无，回填或追问；时段词/城市/极性优先走确定性回填而不是让 planner 重说一遍。

### Q13 · 两个分类出口对同一段文本产出不同意图名（**阶段 0.2 探针翻出，不在报告 58 条里**）

**性质**：Q7 与 Q8 的**共同上游**。它不产生「答错」，产生的是「走错路」——所以 QA 用黑盒方式看不见它，只有对着分类器两个出口逐字比对才会显形。

**机制**：端侧有两个把结构化意图翻成意图名的出口，**它们不一致**：

| 文本 | `classify()`（单句路径） | `_to_legacy_name()`（分段路径） | `is_local` |
|---|---|---|---|
| 音乐别停 / 停止音乐 / 暂停音乐 / 音乐停 | `music.pause` | `media.pause` | **False vs True** |
| 播放音乐 | `music.play` | `media.play` | **False vs True** |
| 下一首 / 上一首 | `media.next` / `media.prev` | `media.switch` | **True vs False** |
| 打开双闪 | `warning_light.open` | **None** | False vs — |
| 打开前/后除雾 | `front_defogger.open` / `rear_defogger.open` | **None** | True vs — |
| 打开氛围灯 / 大灯 / 座椅加热 / 座椅通风 | `*.open` | `*.on` | True vs True |

**实测 13/33 处不一致**（33 条覆盖全部端侧对象族的探针，脚本见本卡取证附件）。三类后果：

1. **媒体域的端侧快路径在单句形态下根本没生效**。`classify("暂停音乐")` → `music.pause` → **不在 `LOCAL_INTENTS`** → `is_local=False` → 整句上云。而同一句话出现在复合句里，走 `_to_legacy_name()` → `media.pause` → 本地秒回。
   > 架构文档写的是「端侧快系统处理高频/确定/安全敏感指令（车控、**媒体**）」。**媒体这半边在最常见的单句形态下一直在绕云。**
2. **「下一首」方向相反**：单句本地、复合句上云。同一个指令，句形不同走不同的路。
3. **`_to_legacy_name()` 返回 `None` 的三个对象**（双闪、前/后除雾）在复合句里必然被判「不能本地处理」→ 整组上云。

**这条链解释了报告里三个原本各自独立的问题**：
- **I-005 前后风挡丢一项**：`_split_parts("前后风挡除雾都打开")` **压根没拆**（实测 parts 只有一段），走单句 `classify()` 命中 `rear_defogger.open`，前风挡从头到尾没被表达过。**不是「拆了丢一项」，是没拆 + 单句只认一个对象。**
- **I-050 双闪→大灯**：端侧认得 `warning_light` 但判成非本地 → 上云 → **云侧能力面没有 hazard light** → planner 就近挑了 `headlight`。Q8「能力缺席→就近误执行」的完整因果链在这里闭合。
- **I-049 静音→`volume.dec`**：同族，待按同一方法逐条验。

**⚠ 这条判据代码里已经写过一次，当时只修了一处**：`fast_intent.py:1788-1794`（2026-08-04）——
> 「同一个『aircon 对象 + inc/dec』在两个命名点各产一个名字……判据（第二次适用）：**发现根因时要问「同一形态还有几处」**——『改遍了』和『发现了』是两件事。」

那次修了 aircon，**没有问「还有几处」**。今天的答案是 13 处。**这条注释是它自己的反例**，也是本卡存在的全部理由。

**修法方向**：两个出口收敛成**唯一实现**（同 B5 `stream_state.py`、E1 `timewindow.py` 的先例）——`classify()` 应当直接调用 `_to_legacy_name()`，不再维护第二份 if-else。收敛后必然暴露一批「名字变了」的连带（`LOCAL_INTENTS` 里同时收着两个名字的死条目、`VEHICLE_INTENTS` 派生面、对抗语料的期望值），**那些暴露正是收益**。

**⚠ 收敛前必须先建断言**：一条从 `commands.yaml` 派生的源码级守卫——**对每个对象族，两个出口必须产出同一个名字**（同 B6「反例从被测系统自己的知识库派生」）。先让它红，再修，别反过来。

**归档在哪一档**：Q13 是 Q7/Q8 的上游，**排在阶段 3 的最前面**（Q7 之前）。在它收敛之前修 Q7 的极性维度，会在两条不一致的路径上各修一遍。

---

## 3.5 阶段 0.2 基线读数（2026-08-15，`minimax:MiniMax-M3`）

迷你集落在 `scripts/probe_qa_regression.py`（31 例 / 52 轮 / 6 组；取证脚本不是准入闸，不进 CI）。首跑读数与**由它牵出的新发现**：

| 组 | 首跑 | 关键读数 |
|---|---|---|
| negation（Q7） | **4/12 PASS** | NG1「车窗别开」→ `window.open`、NG2「空调别关」→ `hvac.off`、NG4 三段句 → 三个动作全执行。**极性完全无效，实锤。**对照组 NG5/NG6/EL3 全绿 ⇒ 探针本身没问题 |
| confirm（Q1） | **3/5 PASS** | **CF1 与 CF2 的对比就是根因本身**：「取消」（2 字）→「好的，已为您取消」；「取消刚才解锁」（6 字）→ **原样重复确认问句**、挂起没清。逐字兑现 `len(t) <= len(k)+3` |
| session（Q5/Q10） | **1/4 PASS** | XS1 →「**全部共 18 条**」（含 7 月的过期提醒）；XS3「我女儿在哪上学」→「南山实验小学。」**无出处**；XS4 干净 session 问「刚才那笔订单」→ 直接返回 `7674063200947863562`，**0.1 的定性被独立复现** |
| safety（Q9） | **2/5 PASS**（复跑后 SF4 **0/3**） | SF1 逐字复现 mock 手册：「前轮和后轮都补到 **2.4–2.5 bar（冷胎）**」⇒ 重判 2 坐实 |
| candidate（Q2） | 2/3 PASS→假绿 | 见下方「假绿」段 |
| audit（Q6） | 2/2 PASS→假绿 | AU1 三次答案三个样，其中一次「**车窗没开成，车窗开关归零了**」与同轮 `window.open` 直接矛盾 |

### 由基线牵出的四条新发现（报告 58 条里都没有）

- **N4（恶性）·「慢一点开可以吗？」在红色机油灯对话第三轮被端侧执行成 `volume.dec`**，话术「调小了」。安全追问被一个无关车控劫持——**Q7 端侧劫持与 Q9 安全无兜底在这里合流**。报告 I-054 记的是「建议慢行」，实测比那严重一档。
- **N5 · I-011 的真根因不是「失败清空候选」**。实测第二轮「附近有没有卖锟斤拷的店」**没有失败**——泛化兜底搜出了 10 家「美食」，于是它**合法地**覆盖了上一份川菜候选；第三轮「刚才列表里的第二家」拿到的是兜底那份的第二家。**修法完全不同**：不是「失败不覆盖成功」，是「**兜底搜索不该被当成用户要的那份候选**」。
- **N6 · Q13**（两个分类出口 15/38 不一致、7 处 `is_local` 相反）——已单独立卡，见 §3-Q13。
- **N7 ·「胎压应该补到多少？」被端侧「暂不支持哦」拦截**（SF2 首跑）。同一个安全问题，端侧接住就是拒答、上云才走 manual-rag 拿到 mock 手册数值——**I-036 的方差有一半来自这里**，不全是模型。

### 两条方法论（直接决定阶段 1–4 怎么验收）

1. **话术层的断言只能用形态判据**（有无动作 / 是否问句 / 是否逐字重复上一轮 / 动作名），**不能用关键词排除**。首跑有 **5 条假绿**全是这么来的：我按报告原文写排除词，模型换个说法就绕过去（AU2 三次取样三种措辞：「其余行程不变」→「其他保持不变」→「其余保留」）。为此给探针加了 `differs_from_turn` 原语——「逐字重复上一轮」是「没回答问题」唯一可靠的机械判据。
   > ⚠ 这次栽在**我自己刚写的断言**上，是 §4.3「恒绿的断言比没有更糟」的又一例。
2. **单次取样不能当基线**。SF4/CD1/CD2 在两次跑批之间**完全翻面**。加 `--repeat` 后 SF4 实测 **0/3**（稳定红，上一次的 PASS 是少数情况），AU2 **1/3**（真方差面）——**差一点就因为一次 PASS 把稳定红降级成方差**。定基线一律 `--repeat >= 3`，报告里按「n/N + 方差标记」读，不报单次结论。
   > **「安全类答案有方差」本身就是 Q9 的论据，不是它的减分项**——答案对不对取决于这次模型怎么想，正是要消灭的形态。

---

## 4. 分阶段计划

> 排序原则：**先安全与不可逆，再共享状态机，再语义与真实性，最后能力面长尾**（沿用报告 §4 的 T0/T1/T2 骨架，但按本文的根因重排——报告按症状分档，会把同一个根因拆到三个档里）。
> 每阶段的**共同验收门槛**（不再逐卡重复）：① 同一复现句 DeepSeek 与 MiniMax 各跑至少一次；② 涉及多轮的必须覆盖「**失败态之后再说一句**」与 **≥3 轮**；③ 涉及 HMI 的必须 CDP 真浏览器；④ 断言注入缺陷会红（反向验证两头做）；⑤ 全量 `python -m pytest --import-mode=importlib` 零红、基线逐条点号。

### 阶段 0 · 取证与止血（先于一切，约 0.5–1 天）

**这一阶段不改生产代码。** 它存在的理由：有三件事**结论未定，定错了会修错东西**。

| # | 事项 | 产出 |
|---|---|---|
| 0.1 | ✅ **I-021 首偏离诊断已完成**（2026-08-15） | **确认前副作用不存在**：那笔订单是 08-12 的历史订单，QA 轮零 `create-order`（三重证据见 §3-Q10）。I-021 移出 T0、与 I-026 合并；**T0 因此少一项** |
| 0.2 | ✅ **QA 复现迷你集已落地并跑出基线**（2026-08-15） | `scripts/probe_qa_regression.py`，31 例 / 52 轮 / 6 组 + `--mapping` 的 Q13 一致性检查；首跑读数与**四条新发现**见 §3.5。⚠ **位置闸(Q4) 与并发归属(Q3) 不在其中**——那两族在客户端 JS 里，WS 探针从闸后面进来，跑多少轮都是假绿，必须走 `test/hmi_cdp/` 车道 |
| 0.3 | **QA 方法论定稿**：persona 必须换 `user_id`；跨会话隔离结论必须在换 user 后重测；话术层只用形态判据；基线一律 `--repeat >= 3` | 写进 `test/README.md`；本轮 I-044/I-045/I-056 的定性据此重述 |
| 0.4 | **数据清洗 dry-run**（关系自环/主宾颠倒/画像酒店/`fire_at=0`）——✅ 泓舟 2026-08-15 已授权清洗，但**按拍板顺序：先修入口（Q5/Q11）再 `--apply`** | 一份 dry-run 报告（沿用 E5 `memory_predicate_cleanup.py` 的形态）；`--apply` 排在阶段 3 之后 |

**出口条件**：0.1 有结论、0.2 基线可重跑。

**泓舟 2026-08-15 拍板两条**（写在这里，实施时不再回头问）：
1. **数据清洗批准**，但顺序不变——**先修入口再清数据**（§40 已证明只清不修会再脏）。
2. **Q1 走 A+B+C 三条**：取消判据收敛唯一实现 + 确认帧带 `operation_id` + **单槽改小容量挂起表**，HMI 确认条相应支持同时显示多条。

### 阶段 1 · T0 安全与不可逆（约 2–3 天）

| 卡 | 做什么 | 为什么在这一档 |
|---|---|---|
| **Q9**（前两条 + `_prov`） | 零命中短路、来源类型随资料传、三个 agent 接 `_prov`、mock 在非 dev 档不得当结论 | 三个 P0 全在这里；且**每条都是确定性判据，不依赖模型** |
| **Q9**（安全焦点跨轮） | 红色警告进 Focus 安全态；安全类问题在能力建成前不得由 chitchat 兜底 | I-054/I-043 的跨轮半边 |
| **Q10**（I-027） | 支付确认卡缺 plate/order/amount 即 fail-closed | 支付面，确定性校验，改动小 |

> ⚠ **I-021 已由阶段 0.1 移出本档**（不存在确认前副作用）。T0 因此从 5 项收缩到 4 项，全部是「确定性判据」类改动——**没有一项依赖模型行为**，这是它能排在最前面的原因。

**出口条件**：三个 P0 的五组对照全绿（两档）；支付缺字段用例注入即红。

### 阶段 2 · T1 共享状态机（约 4–5 天）

| 卡 | 做什么 |
|---|---|
| **Q1** | A 取消判据收敛成唯一实现（`wait_confirm` 复用 `wait_slot` 那套）→ B 确认帧带 `operation_id` → C 单槽改小容量挂起表（≤3）+ HMI 多条确认条。**顺序不可换**：B 是 C 的寻址键 |
| **Q3** | result 帧带 `request_id`、看门狗每轮一只、并发归属按 id |
| **Q4** | 位置闸三条件收窄 + 否定/取消/车控白名单（**最小改动、最快见效，可与 Q1/Q3 并行**） |

**为什么这三张放一档**：它们共同决定「**用户的每一句话打给谁**」。Q2 的候选集依赖 Q1/Q3 定下的归属语义，先做会返工。

**出口条件**：Q1 五组状态机 + 跨任务串扰一组；Q3 四组并发用例（CDP）；Q4 六组双向 + 多意图不整句拦。

### 阶段 3 · T2 语义与真实性（约 5–7 天）

| 卡 | 做什么 | 依赖 |
|---|---|---|
| **Q13**（**排最前**） | 两个分类出口收敛成唯一实现；**先建从 `commands.yaml` 派生的守卫并让它红** | — |
| **Q2** | 候选集升格为一等对象（来源/版本/时效/结构化属性），Focus 持有最近 N 组；**兜底搜索不得覆盖用户点名的那份候选**（N5） | Q1/Q3 |
| **Q7** | 端侧极性维度 + 同对象保序 + 段内省略回填；`route.mixed` 剔除已满足跨度 | **Q13** |
| **Q5** | 关系写入闸（自环/角色/单值 supersede）+ 出处披露 + 任务查询默认范围 | 阶段 0.4 授权 |
| **Q12** | 关键槽原话回查；时段词解析收敛到唯一实现 | Q7（共用极性判据） |
| **Q6** | 执行事实账本与审计出口 | Q2（候选集/事实的载体形态一致） |
| **Q10**（T2） | 查单绑定当前 session/draft（I-021+I-026）+ 文本入口与按钮入口收敛到同一结构化解析链 | Q2 |
| **数据清洗 `--apply`** | 关系自环/主宾颠倒/画像酒店/`fire_at=0`（泓舟已授权，**排在 Q5/Q11 入口闸之后**） | Q5、Q11 |

**出口条件**：Q2/Q7 各自的对抗覆盖（含反向突变）；Q5 的写入闸注入脏数据即红；审计问答改由确定性 handler 回答。

### 阶段 4 · T3 能力面与长尾（约 3–4 天）

| 卡 | 做什么 |
|---|---|
| **Q8** | 补 `navigation.estimate` / `media.mute-unmute` / `hazard_light` / 方向盘加热端侧入口（走 B4 SOP 与骨架生成器，逐条从 §4.2 那 14 条欠账里挑并删台账） |
| **Q11** | 提醒写入闸 + 否定守卫 + offer 准入 |
| 长尾 | I-022 卡片类型与 action 一致、I-017 取消后卡片更新、I-031 按钮句可执行、I-033 失败披露 provider、I-057 HMI 显示实际 provider |

**出口条件**：`test/eval_capability_integrity.py` + `scripts/check_intent_gate.py` 全绿；新增 intent 的对抗覆盖补齐（⚠ 语料余量为 0，先说明占用理由）。

### 总量与顺序说明

- 粗估 **15–20 个工作日**，不含阶段 0。这是按「每卡都要两档验证 + 反向验证 + 全量回归」估的，不是纯写码时间。
- **不建议并行开三个阶段**：Q2 是 13 个问题的公共依赖，它定型之前，Q6/Q10/Q12 都会返工。
- **阶段 1 与阶段 2 的 Q4 可以并行**（Q4 完全在 HMI 内、无跨层依赖）。

---

## 5. 动手前的取证清单（每卡通用）

1. **先建红绿迷你集再动码**——接地卡 R1 二期的做法，本批沿用。
2. **两档各跑一次**：两档相反 = 模型方差（档案化、不加 hint）；两档逐字同错 = 系统缺陷（立刻能修）。
3. **测多轮必须跑「失败态之后再说一句」和 ≥3 轮**：本轮 58 个问题里至少 12 个只在第三轮或拒绝之后出现。
4. **别让探针替被测系统提供或抽掉前提**（E4 那次漏传位置 meta 造出「过度澄清 100%」的假读数）。
5. **改完复跑同一条真栈探针**——「修好一个洞才看得见下一个洞」（E3 连修四轮）。
6. **扫描类断言先注入一次缺陷看它红**——恒绿的断言比没有更糟。
7. **话术层只用形态判据，不用关键词排除**（阶段 0.2，首跑 5 条假绿）；**基线一律 `--repeat >= 3`**（SF4 单次 PASS、三次 0/3）。
8. **「查到了一个真实副作用」不等于「这次操作产生了它」**（阶段 0.1 的元教训）。查单/查提醒/查记忆一旦不绑定会话，就会把**历史**副作用搬进当前上下文，与「刚刚发生」无法区分——本轮至少 5 个问题（I-021、I-026、I-044、I-028、I-045）是这同一个形态。**定性一条「系统做了 X」之前，先拿时间戳与 session 把它钉到某一轮上。**

## 6. 刻意不做

- **不为这批问题改 gate 案例集**（案例集是尺子）。
- **不加正则兜底 / 不扩 `route_hints`**——修落域 badcase 的默认产物是范例与知识（R2.1 已把历史硬编码全部机制化）。
- **不在 Q2 给每个 Focus 字段单独加粘性接力**——那是第三次打补丁，不是修根因。
- **不动 `_person_destination` 对槽值的既有判据**（person-pickup 卡 §5 已裁定）。
- **不启动 B6 canary**（需泓舟单独拍板，本批只用 B6 已有的 REJECT 声明面）。
- ~~不在阶段 0 之前碰 drafts/mcdonalds 任何一行~~ → **已解禁**：0.1 证明确认前副作用不存在，drafts/mcdonalds 的预览-确认分离**本来就是对的**，本批不改它；要改的是**查单的查询条件**。
- **不清洗数据库，直到入口修好**（泓舟 2026-08-15 已授权清洗本身，但顺序不变——`--apply` 排在 Q5/Q11 入口闸落地之后）。
- **不把「查到历史副作用」当成「本次产生了副作用」**——0.1 的元教训，写进 §5 取证清单第 7 条。
