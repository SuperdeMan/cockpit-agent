name: charging-strategy
type: guide
description: 充电找桩与长途补能策略的分流判据（附近找桩补电/跨城长途怎么充电、路上电够不够/目的地附近充电）
priority: 55
capability_dependencies: [charging.find, charging.plan, charging.status, navigation.navigate_to]
keywords: [充电, 充电桩, 充电站, 快充, 补能, 没电, 耗尽, 耗空, 电量不, 怎么充]
# 依据：charging-planner manifest 三能力语义 + journeys target_b B2 族校准记录
# （「去X怎么充电」真栈探针 6/6 稳定走 charging.plan；charging 无 route_hints，路由纯 LLM
# ——这正是本 guide 存在的原因）。「去X的路上找个充电站」形态刻意不写死：校准记录认定
# navigation 途经点流对该句也是合理体验，知识只分流清晰形态。
knowledge: |
  **充电分流**（能力语义不同，按出行形态选，别混用）：
  - 车辆已经没电的**耗尽陈述**就是补能求助 → charging.find，**不是 charging.status**；
  未耗尽但带续航焦虑并要求就近补电，同样归 charging.find（快慢充偏好放 prefer 槽）。
  纯设施发现（『附近有没有充电站』，只是看看有什么）→ 周边发现能力（nearby）。
  - 一次性询问跨城/长途怎么补能（『去惠州怎么充电』『去外地要不要中途补能』）→
  charging.plan 带 destination——要的是现在就给沿途分段策略，不是找一个桩。
  **条件补能分两轮**：『先看电够不够到 X，不够再补能』首轮只出 charging.status，且
  complexity=adaptive。第二轮只读最近观察：例目标『续航不足到南京就补能』，观察
  `status=ok, intent=charging.status, data.range_km=45` 已明确不足时，必须 `done=false` 且只出
  charging.find/charging.plan；目的地只是续航判据，绝不扩成 navigation.navigate_to。
  只有观察明确够用才 `done=true`；不要重查状态，也不要提前执行条件分支。
  - 导航去某地并在那附近充（『导航去深圳湾，附近找个充电桩』）→ navigation.navigate_to
  与 charging.find(destination=深圳湾) 并列两步。
  只问电量/续航本身（『现在电量多少』『还能跑多远』）→ charging.status，不要出找桩步。
  **只有车的补能归充电域**：手机/耳机/笔记本/充电宝等随身设备快没电，不是车辆充电意图
  ——绝不要出 charging.find/charging.plan 步（那是找商场咖啡馆或买充电宝的场景，
  交给周边发现或闲聊应答）。
few_shots:
  - user: 去惠州怎么充电
    plan: {"steps":[{"id":"s1","agent_id":"charging-planner","intent":"charging.plan","slots":{"destination":"惠州"},"depends_on":[],"slot_refs":{}}]}
  - user: 动力电池已经耗尽
    plan: {"steps":[{"id":"s1","agent_id":"charging-planner","intent":"charging.find","slots":{},"depends_on":[],"slot_refs":{}}]}
golden:
  - text: 去惠州怎么充电
    expect_intents: [charging.plan]
    expect_not: [charging.find]
  - text: 快没电了，附近找个快充
    expect_intents: [charging.find]
    expect_not: [navigation.navigate_to, nearby.search]
  - text: 动力电池彻底耗空了
    expect_intents: [charging.find]
    expect_not: [charging.status, nearby.search]
  # holdout：避开 few-shot 原句与全部 keywords 字面（测泛化不测自证）。
  # 「电快见底」句已被 charging.find hint 覆盖（canonical 哨兵，知识归因看下面两条）
  - text: 电快见底了，就近找个地方充上
    expect_intents: [charging.find]
    expect_not: [nearby.search]
    holdout: true
  # hint 够不到的真 paraphrase（消融车道的知识归因载体：无 SOC 词形、无「怎么充电」）
  - text: 去趟惠州中间要不要补个电
    expect_intents: [charging.plan]
    expect_not: [nearby.search]
    holdout: true
  # 条件补能：非对抗原句；初始轮只查状态，消费分支由 observation 后的 replan 决定。
  - text: 先看当前续航能不能撑到苏州，撑不到再找地方补上
    expect_intents: [charging.status]
    expect_not: [charging.find, charging.plan]
    expect_complexity: adaptive
    holdout: true
  # 知识边界负例：设备充电绝不出车辆充电步（hint guard 挡确定性层，这条守 LLM 层）
  - text: 手机快没电了，附近找个地方充一下
    expect_not: [charging.find, charging.plan]
    holdout: true
owner: charging-planner
version: 4
