# M-A 验收余项暂停交接

更新时间：2026-07-30 19:15（Asia/Shanghai）

## 当前范围与边界

- 当前工作树：`D:\Personal\AI\Claude Code\产品\car-agent\.worktrees\acceptance-m0a-m4-residuals-impl`
- 当前分支：`codex/acceptance-m0a-m4-residuals`
- 目标：完成 M-A 验收、提交、推送并合并后暂停；尚未完成，当前按用户要求提前在安全检查点暂停。
- 根工作区有其他 M5 修改，不得触碰、暂存或回退。
- 根 `.env` 是唯一运行时环境来源；不要读取、复制、修改或提交它。E2E 通过 `E2E_STACK_ROOT` 使用根真栈。
- 暂停时不得 push、删除或启动新的长时间命令。本检查点未执行 push 或删除。

## 已完成事项

### 此前已提交到当前分支

- 当前分支在暂停前已有 34 个本地提交未推送，最新三个为：
  - `57a1263 fix(e2e): harden external provider evidence`
  - `9933b88 fix(tts): verify provider-consistent streaming`
  - `59ac595 fix(e2e): preserve shared-stack profile inputs`
- MiniMax TTS 真栈批量合成和语音回环已通过，`e2e_voice_loop` 4/4。
- MiniMax 流式 TTS 可用，但未达到旧的“相对同供应商 batch 至少快 2 倍”阈值；严格流式性能证据由同供应商 Qwen 给出：首包约 682ms，batch 约 1797ms，2.6×。
- 真实外部 provider 12/12、S2S provider 直连探针 12/12 已通过。

### 本检查点新增但尚待最终总验

1. `scripts/run_e2e.py`
   - 从授权的运行根加载唯一 `.env` 到 E2E 子进程环境；
   - 显式进程变量优先，不覆盖；
   - 限制文件大小、校验变量名；
   - 新加载的敏感值自动进入 runner 脱敏集合，端到端泄漏测试覆盖。
2. `test/e2e_degrade.py`、`test/e2e_resilience.py`
   - Docker Compose 操作固定使用 `E2E_STACK_ROOT`，不再误在 worktree 项目目录执行。
3. `test/e2e_protocol_smoke.py`
   - 嵌套 runner 清除父级 `E2E_*` 内部状态，避免共享根/canonical 元数据污染。
4. `llm-gateway/s2s/provider.py`、`llm-gateway/s2s/session.py`
   - Compose 注入空字符串时，S2S model、WS URL、VAD、会话轮数、超时均正确回落代码默认值。
5. `test/e2e_memory_graph.py`
   - Redis 探针与 GDPR 清理固定使用 `E2E_STACK_ROOT`，不再连接错误的 Compose project。

## 已取得的新鲜验证证据

- runner 单测：`95 passed`
- 剩余 E2E 协议单测：`167 passed`；新增 Redis 根路径定向测试另行 `1 passed`
- S2S 单测：`71 passed`
- 共享根下协议烟测：PASS
- `e2e_s2s`（Qwen real profile，milestone）：`26/26 passed`
- `e2e_degrade`：`4/4 passed`
- `e2e_resilience`：`2/2 passed`
- `e2e_memory_graph` 最新：`24/30 passed`
  - GDPR 五类目标（memory item、relation、voiceprint、profile identity、session history）的 seed/count/read/delete/control fingerprint 均已通过；
  - 剩余 6 项均属于记忆抽取/关系构建未发生：
    `preference_stored`、`preference_single_current`、`relations_stored`、
    `family_relation`、`place_relation`、`person_place_resolution`。

## 首轮 M-A canonical 结果

运行：`e2e-20260730095512-8db9c147876a`

- 22 项 PASS；
- 11 项 FAIL；
- `e2e_journeys` 因子进程未继承根 `.env` 而 33 项跳过。

已确认被本轮共因修复消除：

- `e2e_protocol_smoke`
- `e2e_degrade`
- `e2e_resilience`
- `e2e_s2s`
- `e2e_memory_graph` 中的 GDPR/Redis 部分
- `e2e_journeys` 的凭证环境继承根因（尚待完整 35 项复跑）

## 未完成事项

1. 记忆抽取剩余 6 项：
   - 能力门运行时已确认 `E2E_CAPABILITY_ENABLED=true` 且 capability secret 存在；
   - `AppendTurn` 后没有 `consolidate` 日志，强烈指向 capability 绑定校验未通过或触发条件未进入；
   - 下一步应在不记录 token/secret 的前提下，为 `_allows_synthetic_extraction` 增加可枚举的拒绝原因日志/测试，核对 run_id、user_id、session_id 三个绑定量；
   - 不要通过移除能力门或放宽 synthetic session 隔离来让测试变绿。
2. 尚未复跑/处理：
   - `e2e_memory` 主动 routine；
   - `e2e_scene` 的低电量建议卡；
   - `e2e_voiceprint` 的模型可用性；
   - `e2e_ws` 的交易确认断言；
   - `e2e_planner_toolcall` 的 MiniMax `addressed=false` named tool 方差；
   - `e2e_journeys` 35 项完整真栈；
   - canonical 第二轮。
3. 完成所有失败后还需：
   - 全量 Python、HMI、dashboard、Go 验证；
   - 更新验收报告、M-A 测试真实性设计、AGENTS 与 S2S RFC；
   - 拉取并合并最新 `origin/main`；
   - 经验证后推送功能分支并合并 main；
   - 恢复并确认根真栈默认配置；
   - 到此才算 M-A 完成并暂停。不要开始 M-B。

## 下一步建议顺序

1. 记忆图谱 runner PID `8332` 已在写出最终汇总后自然退出，无需处理。
2. 复现并定位 synthetic memory capability 的拒绝原因，修复后单跑 `e2e_memory_graph` 至 30/30。
3. 依次单跑剩余失败族，先处理共因，再处理业务断言。
4. 提交当前批次后运行完整 M-A canonical；要求无 FAIL、无 SKIP 并成功 promotion。
5. 完成全量回归与文档同步，再合并、推送；随后恢复默认真栈并暂停。

## 暂停时任务状态

- 后台 Python PID `8332` 已自然退出；命令为
  `scripts/run_e2e.py --id e2e_memory_graph --lane milestone`，最终结果 `24/30`。
- 历史 Python PID `98548` 是 VS Code isort LSP，不是本轮任务，不处理。
- 历史 Docker CLI PID `35928` 是 `docker compose -f compose.yaml up -d --build`，
  CPU 为 0；不是本轮新启动，不处理。
- Docker 容器的精确列表与健康状态见本次暂停回复中的状态快照。
