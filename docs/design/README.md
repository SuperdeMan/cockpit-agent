# docs/design/ — 前瞻设计与问题分析

> 本目录放**面向落地的设计提案与问题分析**，区别于：
> - `docs/architecture/` —— 架构唯一真相源（已定型的设计）
> - `docs/reviews/` —— review 修复交接清单
>
> 这里的文档是"给后续执行者（人或 Agent）照着做"的蓝图：先讲清现状与问题，再给目标、方案、分阶段落地步骤与验收标准。**一旦某提案落地并稳定，应把定稿内容并入 `docs/architecture/` 并在此处标注「已归档」。**

## 命名与结构约定

- 文件名：`YYYY-MM-DD-<topic-kebab>.md`
- 每篇开头声明：`状态`（草案/评审中/已批准/落地中/已归档）、`交付对象`（谁来实现）、`关联`（相关代码与文档）
- 推荐骨架：**现状/证据 → 问题 → 目标 → 方案 → 分阶段落地 → 验收 → 风险**
- 证据要可核验：引用 `文件:行号`、命令、实测结果，不空谈。

## 当前文档

| 文档 | 主题 | 状态 |
|---|---|---|
| [2026-06-13-vehicle-control-command-architecture.md](2026-06-13-vehicle-control-command-architecture.md) | 车控域升级到「公版语音指令表」统一 schema | P1 已落地：知识库三件套（62对象/150意图）+ VAL + fast_intent + 飞书全量导入脚本；P2/P3 待做，P2 缺口已量化见下方 2026-07-03 意图覆盖分析 |
| [2026-06-13-multi-intent-and-context.md](2026-06-13-multi-intent-and-context.md) | 多意图拆分 + 对话上下文/指代消解 | 已全部落地：上下文 + M1云侧DAG + M2端侧切分 + M3黄金用例 |
| [2026-06-13-asr-pipeline-analysis.md](2026-06-13-asr-pipeline-analysis.md) | ASR 收音失败根因分析与修复链 | 已全部落地：前端竞态 + 后端转码 + E2E + HTTPS提示 |
| [2026-06-13-open-domain-latency.md](2026-06-13-open-domain-latency.md) | 开放域响应慢：模型分层 + 流式贯通 + 即时反馈 | 已落地：流式+模型分层+chitchat兜底+即时反馈；降规划延迟待做 |
| [2026-06-14-cloud-central-orchestrator.md](2026-06-14-cloud-central-orchestrator.md) | 云端中枢：理解→规划→异构调度（车端快思考/Agent/工具）；T0/T1/T2 分级 + 有界 Agentic 循环 | 已落地：P0-P3、DispatchToEdge、T2、工具、权限/可观测；已补混合意图语义分组、句子级增量 TTS 与慢意图完整性回归 |
| [2026-06-15-observability-dashboard.md](2026-06-15-observability-dashboard.md) | NATS 可观测出口 + collector + 独立 Dashboard：车辆 diff、端云链路、Agent 运行态与 debug 对照实验 | 已归档：P0-P3 全部落地并完成 20 服务全栈验收；**2026-07-10 被 badcase 排查重构扩展**（会话/轮次/日志贯通 + SQLite 持久化 + 四视图，见下方 2026-07-10 行）|
| [2026-06-20-info-agent-expansion.md](2026-06-20-info-agent-expansion.md) | info 扩展：联网搜索/新闻/股票（只读聚合）+ 票务独立成交易 Agent | 草案：`info.weather` 已落地，余下规划中 |
| [2026-06-20-standalone-agents-roadmap.md](2026-06-20-standalone-agents-roadmap.md) | 独立 Agent 扩展路线完整设计：充能规划/场景编排/路况安全/行程增强/交易类——含 manifest、Provider 接口、交互流程、代码骨架、打通契约 | 已落地（P0-P3：charging-planner/scene-orchestrator/road-safety + trip-planner 增强）；§8 两处缺口已闭环（scene 命令对齐 VAL；road-safety 主动播报 Agent 侧已实现，**仅余 HMI 投递一跳**）|
| [2026-06-20-new-agents-detailed-design.md](2026-06-20-new-agents-detailed-design.md) | （已被 standalone-agents-roadmap.md 合并，保留作为历史参考） | 已合并 |
| [2026-06-20-search-news-redesign.md](2026-06-20-search-news-redesign.md) | WS6 补充：搜索/新闻从"罗列链接"重设计为"LLM 结论+摘要卡片"——Agent/HMI/Provider 三层方案 | 已落地但**卡片方案被 2026-06-22 取代**（search_answer 复读结论问题） |
| [2026-06-22-search-quality-and-card-redesign.md](2026-06-22-search-quality-and-card-redesign.md) | 联网搜索质量重构（Exa 正文级检索 + 接地合成/引用/诚实弃权 + api-football 赛事 + 新闻 TTS 播报式速览）+ 信息卡片重设计（气泡给结论、卡片给证据，消除重复） | 已落地（P1-P5 + 二/三/四轮修复，真实 API 端到端验证） |
| [2026-06-22-charging-route-planning.md](2026-06-22-charging-route-planning.md) | 充电规划：高德沿真实路线取途经充电点（出发地→途经点→目的地）+ 泛地点高德候选二次确认（dest_choice）+ charging_route 时间线卡 + 聚合器卡片择优 + advisory（不车控/不发导航） | 已落地（多轮迭代，真实高德端到端验证） |
| [2026-06-20-ws2-registry-production.md](2026-06-20-ws2-registry-production.md) | WS2 Registry 生产化：PostgreSQL 持久化/多实例/语义路由/AgentClient 动态解析 | 已落地（P0+P1：PgStore/pgvector/AgentClient 动态解析/多实例） |
| [2026-06-20-ws8-security-permissions.md](2026-06-20-ws8-security-permissions.md) | WS8 安全与权限：权限动态解析/third-party 沙箱/LLM 注入防护/网络白名单/车控安全门控 | 已落地（P0+P1：注入检测集成/VAL 4 项门控/third-party 沙箱/HTTP_PROXY） |
| [2026-06-23-named-places.md](2026-06-23-named-places.md) | 常用地点（家/公司）：导航别名解析 + 未设置时二次交互引导设置 + Redis 持久化（memory 服务 profile.places）+ HMI 回显/修改 | 已落地 |
| [2026-06-23-navigate-landmark-and-charging-waypoint.md](2026-06-23-navigate-landmark-and-charging-waypoint.md) | 视觉地标经共享件解析地图官方名（name_matches 校验拒邻近无关 POI）+「导航去地标附近充电」按目的地搜站、聚合器并入 navigate.waypoints + 类目搜索不被整句多意图劫持 | 已落地（真实高德验证） |
| [2026-06-23-restaurant-waypoint-and-topscorers.md](2026-06-23-restaurant-waypoint-and-topscorers.md) | 顺路用餐途经点（navigate_to.stop_category→waypoint_choice 候选二次选择→route_plan 路线卡，navigation 接管因 food 恒 mock）+ 赛事射手榜 | 已落地 |
| [2026-06-23-sports-match-detail.md](2026-06-23-sports-match-detail.md) | 赛事进球详情/射手榜：api-football /fixtures/events（进球射手与分钟，剔除罚丢点球）+ topscorers 赛季回退标注 + 历史总榜走搜索接地合成 | 已落地 |
| [2026-06-24-trip-planner-multiturn-and-confirm-robustness.md](2026-06-24-trip-planner-multiturn-and-confirm-robustness.md) | 行程规划多轮闭环（规划→改某天→确认→第一站导航）：确定性 trip.plan/trip.modify 兜底（覆盖降级路径）+ trip-planner 有状态多轮 + 确认收尾取行程首日景点搜 POI + 确认词「占据整句」修复（行/可以/不要不误判）+ 孤儿确认护栏 + 跨 Agent meta 透传与 Struct→dict 修复 + 电量一致性 | 已落地（783 passed 实测 + 端到端验证） |
| [2026-06-24-complex-task-thinking-and-process-region.md](2026-06-24-complex-task-thinking-and-process-region.md) | 复杂任务动态思考（按统一 is_complex 判据对 LLM 开 thinking，Planner JSON 恒关，reasoning 不下发）+ 可折叠过程区（ProcessUpdate 事件 → 气泡内嵌折叠条「步骤+思考摘要」，行车/泊车双态门控；普通车控/闲聊零过程零延迟） | 落地中 |
| [2026-06-25-memory-system-redesign.md](2026-06-25-memory-system-redesign.md) | 记忆系统分层重构：L0 会话 + L1 车辆 + L2 语义画像(pgvector,自动抽取偏好) + L3 情景 + L4 主动雏形；多用户就绪(occupant)/时序-lite(superseded_by 不覆盖)/provenance+置信度/隐私端云分割/导出遗忘；复用 registry 的 bge-small-zh+pgvector，proto 向后兼容加 Remember/Recall/ForgetUser/ExportUser。配套 [research 调研](../research/2026-06-25-cockpit-and-agent-memory-systems.md) + [实施计划](../superpowers/plans/2026-06-25-memory-system-redesign-implementation.md) | 草案待评审 |
| [2026-06-25-context-system-redesign.md](2026-06-25-context-system-redesign.md) | 上下文系统重构：统一 `ContextManager` 脊柱——working/core 装配层(token 预算 + catalog 语义预筛) + 结构化焦点态(跨轮指代) + 上下文生命周期门面收敛 + 敏感上下文按 manifest `context_scopes` 下发。承接 memory 重构后裸着的 working/core 层 | 已落地（Phase 0-4，883 passed/6 skipped，零回归；§8 记两处取舍） |
| [2026-06-25-comms-link-hardening.md](2026-06-25-comms-link-hardening.md) | 通讯链路量产级加固：全链路 gRPC keepalive(共享 `runtime/grpcio.py` 工厂) + 优雅停机 + HMI 韧性(退避重连/断线发送队列/看门狗) + 熔断接线 + LLM 网关连接池/流式 stall + 依赖连接加固(Redis/PG/NATS)；含一处危险车控确认退化修复 + Go 网关换 IP 显式重连补强(dns:/// 单独不可靠) | 已落地（P0-P2，真栈自愈 2/2，不重启依赖方即恢复） |
| [2026-06-26-trip-planner-redesign.md](2026-06-26-trip-planner-redesign.md) | 行程规划结构化重构：LLM 提议骨架→确定性接地真实 POI/求解每日车程+按真实 SoC 沿路线编织充电点/出话术四段流水线(对症 TravelPlanner 纯 LLM 0.6% 通过率) + 每停靠点可导航 + 在途状态查询/「时间不够」自动精简 + 状态落 memory；护城河=车辆接地+在途编排 | 已落地（P0/P1/P2 全合并 main，真栈 e2e 6/6 真实 POI） |
| [2026-06-26-trip-planner-p0-implementation-plan.md](2026-06-26-trip-planner-p0-implementation-plan.md) | 行程规划重构 P0 实施计划：结构化模型 + 四段流水线 + 充电编织纯函数 + `trip_itinerary` 卡 + 落 memory 的逐项落地清单与验收标准 | 已落地（见 redesign 设计文档） |
| [2026-06-26-info-agent-deep-research-redesign.md](2026-06-26-info-agent-deep-research-redesign.md) | 信息域重构：新建**独立 `deep-research` Agent**（四段流水线：LLM 提议多视角子问题→确定性有界并行迭代检索→分节接地报告→渐进语音简报+可读报告卡，对症单轮检索多跳天花板）+ 检索/接地内核抽到 `_sdk` 共享（化解独立 Agent 的 provider 重复）+ info 联网查询编排层分层（quick/deep 路由）+ 接地「我」（位置/电量/行程/画像）+ 新闻个性化/深挖桥接；护城河=接地车辆+渐进语音+可落地产物，非「车机版 Perplexity」。用户拍板：独立 Agent / 语音简报+报告双态 / 先 P0 评审。P1=接地「我」(位置反查+画像召回)+多轮深挖(落 memory，「展开第N点」聚焦不重跑)+存记忆；增量=异步分钟级深调研(受理即返回+后台 deep 流水线越过同步 90s 上限+agent.proactive 推报告卡)+信源质量加权(域名权威分层重排+全局权威编号+学术兜底)+名单扩展(采纳信源研究报告)+新闻质量/时效/展示 | P0-P2 + 异步分钟级深调研 + 信源质量加权 + 名单扩展与新闻质量/时效/展示全落地（2026-06-27，全量 973 passed；真栈 e2e：固态电池调研→展开第1点聚焦上节、看新闻→详细讲讲第2条深挖桥接、异步「不急查完告诉我」→秒级受理→分钟后主动推送 9 节/3031 字报告卡、信源权威重排前5平均档位 1.00→2.80、综合新闻 Google News 头条 10 来源/今日/农场0/话题多样/繁体转简体；含端侧「电池」误匹配收窄 + 上线后实测修复 R1-R6：合成关思考防超时退化/去电量约束防漂移/去 livecrawl+简短子问题防 Exa 超时/清网页噪声/info.search 同源关思考/报告深度 985→2153字，见 §9；P2=新闻个性化+深挖某条桥接+主动早报雏形）|

| [2026-06-28-figma-hmi-dashboard-redesign-brief.md](2026-06-28-figma-hmi-dashboard-redesign-brief.md) | 座舱 HMI + Dashboard 视觉重设计 Figma 交接简报：产品语境/用户场景/设计原则与安全红线 + 横屏 1920×1080 信息架构骨架（左对话右"上下文舞台"场景机制）+ 全量组件与约 20 种卡片清单（字段/状态）+ design tokens 族 + 无障碍/车规可读性 + Figma 组织约定与移交 DoD + 实现映射。已锁定：横屏中控大屏 / **Aurora Glass 极光液态座舱**（液态玻璃+AI极光炫光+光球化身，提炼特斯拉/鸿蒙/小米/蔚来NOMI/理想/MBUX/iDrive+iOS Liquid Glass）/ HMI+Dashboard 同做 / 含建议 IA 骨架 | 已交接 → 落地中（见下方实施计划） |
| [2026-06-29-figma-hmi-dashboard-prompt.md](2026-06-29-figma-hmi-dashboard-prompt.md) | 分步喂给 Figma Make 的提示词块（A-1 设计系统 → A-2 主屏 → A-3~A-5 卡片族 → A-6 对话动态态 → A-7 设置；B-1~B-4 Dashboard）+ 每步「figma 输出物」（design/make 链接与版本号） | 工具输入（提示词索引，非落地文档） |
| [2026-06-29-figma-hmi-implementation-plan.md](2026-06-29-figma-hmi-implementation-plan.md) | 座舱 HMI Aurora Glass 重构实施计划：现状→目标 gap、token 迁移表、P0 设计系统 / P1 两栏外壳+右上下文舞台 / P2 卡片重建 / P3 对话动态态 / P4 设置侧栏 / P5 行车浅色 / P6 Dashboard 分阶段 + 验收 | 落地中：P0-P5（浅色）+ A-4 信息卡按源重建 + A-5 右舞台数据驱动地图 + A-8 图标库（39+21 图标，emoji 全替换，推回 Figma）全落地+push（commit 2ad83e3→39e65a4，按 Figma 源逐张重建非换皮）；**✅ 已重建容器 + 真后端全栈 e2e 验证（8 卡族真数据 + 过程区/确认条 + 地图舞台）+ 语音按钮换小舟光球 + 剩余 emoji 全替 A-8 图标 + ASR 流式上屏**；待做 P5 行车态变体、P6 Dashboard（均待 Figma 出帧）|
| [2026-06-30-asr-streaming-design.md](2026-06-30-asr-streaming-design.md) | ASR 流式识别上屏：WS 传输层（`/api/asr/stream` + 网关流式 ffmpeg）一次建好、引擎可换 + DashScope 实时（qwen3 走 `/realtime` OpenAI 协议、fun 走 `/inference` run-task 协议）+ MiMo 分块回退 + HMI 引擎/模型可切；边说边上屏、松手定稿自动发送、失败回退批处理 | 已落地（commit 184d48f→41fdd85；真栈 e2e：qwen3 5partial / fun 4partial / mimo 均出转写 + fake-mic 浏览器上屏→助手出天气卡。坑：qwen3 id 须**全小写**、fun 与 qwen3 端点/协议不同）|
| [2026-07-02-r2.2-permission-single-track.md](2026-07-02-r2.2-permission-single-track.md) | R2.2 权限单轨化：三处权限实现（planning 内联过滤/dispatch 内联校验/PermissionEngine 死壳）收敛为唯一决策 `security/permission.py::check_permission`（规划期+执行期同源）；fail-open 加 env `PERMISSIONS_FAIL_OPEN` 门控+结构化审计；纠偏「接 effective_scopes 会因扁平交集误拒 scene-orchestrator」→取零行为变化单轨 | 已落地并合并 main（`8999cba`/`0be9991`，全量 1014 passed + 真栈 e2e_ws 4/4） |
| [2026-07-02-r2.3-edge-cloud-persistent-channel.md](2026-07-02-r2.3-edge-cloud-persistent-channel.md) | R2.3 端云持久长连：`orchestrator/edge/cloud_client.py` 逐请求建流→进程内单条持久 bidi + corr_id 多路复用 + 15s 心跳 + 指数退避重连（dns:/// 重解析换 IP 自愈）+ 在途断连快速失败降级；删 Go 死代码 ChannelClient（~250 行）| 已落地并合并 main（`c7cdc01`/`ae8638d`，真栈 3 请求仅 1 hello + 换 IP 自愈探针）|
| [2026-07-02-r2.4-info-agent-split.md](2026-07-02-r2.4-info-agent-split.md) | R2.4 info agent 拆域：1269 行 `InfoAgent` 巨类按域拆 `handlers/{weather,search,sports,news,stock,briefing}` mixin + 共享 `_util`，`agent.py` 123 行；域方法经 self 靠 MRO 逻辑逐字不变 + 文件尾向后兼容重导出 | 已落地并合并 main（`def815a`/`18e6f73`，pytest agents/info 136 passed 零回归）|
| [2026-07-02-r3.1-session-auth.md](2026-07-02-r3.1-session-auth.md) | R3.1 会话鉴权最小闭环：静态 token 两层校验（HMI WS `?token=`→edge-gateway 解析身份+`meta.granted_scopes`、去 `user_id="u1"` 硬编码；Hello channel token→cloud-gateway），env 门控 `AUTH_REQUIRED` 默认关；未改编排核心/proto（R2.2 已备好 context.py 消费）| 已落地并合并 main（`f38b4db`，真栈无 token→401 + e2e_auth ALL PASS）|
| [2026-07-02-r3.2-service-mtls.md](2026-07-02-r3.2-service-mtls.md) | R3.2 服务间 mTLS：gRPC 双向 TLS，`GRPC_TLS` 门控默认关；单张共享 mesh 证书 + name override（`ssl_target_name_override`/`ServerName`）适配 agent 动态容器 hostname；Python `runtime/grpcio.py`（aio_channel secure + bind_port）+ Go `gateway/tlscfg`；`scripts/gen-certs.*` 生成证书（gitignore）| 已落地并合并 main（`37817c8`，全量 1030 passed + mTLS 模式 e2e_ws 加密链路 + insecure 探针被拒）。**至此 T3.1+T3.2 齐，安全链路无已知缺口** |
| [2026-07-03-r3.3-e2e-ci-gate.md](2026-07-03-r3.3-e2e-ci-gate.md) | R3.3 e2e 入 CI 门禁：nightly workflow 跑裁剪过的确定性 mock-safe 子集（`--case` 过滤 + `e2e_memory.py` 三处 SKIP 补丁，绕开 Mock 模式下 route_hints 缺失导致的必挂用例）+ `make e2e` 改为本地全量清单执行器（`scripts/run_e2e.{sh,ps1}`），零 GitHub secrets | 已落地并合并 main（`e54a914`/`cb70239`/`25b85aa`）：GitHub `workflow_dispatch` 二次实跑全绿（run 28639607108） |
| [2026-07-03-r3.5-degrade-matrix-e2e.md](2026-07-03-r3.5-degrade-matrix-e2e.md) | R3.5 降级矩阵自动化：新 `test/e2e_degrade.py` 刻画架构 §3.3 四行真实现状（断网/云Planner故障/单Agent故障/LLM超时，docker stop/pause 级故障注入 + `LLM_MOCK_DELAY_MS` 测试钩子），过程中发现 4 处真实缺口（研究阶段 3 处 executor 丢 error 信息/planner中途崩溃静默挂起/CompleteStream 无备用模型重试 + 真实跑又发现 1 处 edge-orchestrator pause/unpause 后不自愈）按用户决定只记录不修；Row 4 原设计经实测推翻重来 | 已落地并合并 main（`0355b1b`/`02a4896`）：GitHub `workflow_dispatch` 一次实跑全绿（run 28643924654） |
| [2026-07-03-r3.4-intent-eval-baseline.md](2026-07-03-r3.4-intent-eval-baseline.md) | R3.4 意图路由评测基线：`test/eval_fast_intent.py`（端侧 `classify_structured`）+ `test/eval_route_hints.py`（云侧 `RouteHintEngine`，复用生产同款 `PlanBuilder._validated_steps` 装配路径）产出准确率/召回率报告，基线入 `docs/reviews/eval/`；`ci.yml` 新增非阻塞 `intent-eval-baseline` job。飞书 1465 意图库标注语料不可得，改用现有 corpus + 历史回归转录；`route_hints_cases.yaml` 经 `--dump` 对真实 manifest 实测发现一处跨 hint 交互（"导航去第2天换一个"实际被 `trip.modify` 接管） | 已落地并合并 main，GitHub Actions `intent-eval-baseline` job 实跑确认全绿 |
| [2026-07-03-intent-coverage-gap-analysis.md](2026-07-03-intent-coverage-gap-analysis.md) | 意图覆盖缺口量化：飞书全量 1465 条意图/8683 条真实例句实测 `classify_structured()`，整体识别率 72.0%，给出分域/分对象明细与典型漏判样本，供「扩规则/上NLU模型/维持现状」三条后续路径决策参考 | 📋 纯调研，无代码改动；后续方向未定 |
| [2026-07-03-r3.6-observability-prometheus-otel-export.md](2026-07-03-r3.6-observability-prometheus-otel-export.md) | R3.6 Prometheus/OTel 导出：collector `GET /metrics`（手写 Prometheus 文本格式，零新依赖）+ `otel_bridge.py`（复用此前从未调用的 `tracing.py::setup_tracing()` 桥接真实 OTel span）+ compose 首次引入 `profiles` 机制门控 prometheus/grafana + 三面板 dashboard JSON。真栈数据链路已验证；Grafana 可视化因本机网络环境限制未验证（镜像/大文件下载不稳定，交叉验证确认是环境问题非代码问题） | ✅ 代码+真栈数据链路已验证；**Grafana 可视化已于 R4.0（2026-07-04）网络恢复后补验（三面板经数据源代理真实出数）**；R3 量产硬化至此全部完成 |
| [2026-07-04-r4.0-residual-cleanup.md](2026-07-04-r4.0-residual-cleanup.md) | R4.0 收尾包（清验收复审 §4 残留 + §3b 边界）：**K1** 端云持久通道 pause/unpause（同 IP 冻结再解冻）不自愈——真根因=app 心跳强制重连时 `_cancel_stream()` 令 `read()` 抛 `CancelledError`、被 `_run` 当任务取消 re-raise 打死重连循环（换 IP 走 grpc keepalive 的 `AioRpcError` 故不中招）→ `_run` 用 `_closing` 区分「流被取消/任务被取消」+ `_open()` 有界超时；**K2** `e2e_process_region.py` 默认泊车态断言在长期共享栈上因 VAL 调试态污染失败→断言前经 collector 复位泊车态使测试自足；**N1** `PermissionEngine` 死注入彻底删除；**K3** Grafana 面板本次网络恢复完成验证 | ✅ 全部完成并真栈验证（K1 解冻后 ~2s 自愈 + e2e_degrade 改回自愈断言 + 2 双向守护单测；K2 污染 fail→复位 pass；全量 1050 passed）；未改 proto/编排核心/架构 |
| [2026-07-04-r4.1-routing-quality.md](2026-07-04-r4.1-routing-quality.md) | R4.1 路由质量主题（T4.1+K6+D7-lite）：**P0** Registry 真语义路由（sha256 伪向量→llm-gateway→百炼真向量，按 capability 粒度，顺带修 §1.1 低频误路由 bug）+ **语义重排**修「语义被中文关键词打分遮蔽」（完整 ResolveAgents 路径 15/20→20/20）+ **P1** Resolve 评测基线 + **P2** 飞书 8590 条语料资产化+覆盖率报告（72.04% 复现自校验）+ **P3** 纯 pattern 扩规则（气象并入天气类 / 设置页族+修既有劫持）→ fast_intent 覆盖率 72.04%→75.74% | ✅ P0-P3+重排全落地并 push（`d596da9`…`317af48`）；真栈 registry 语义 20/20 + 全量 1069 passed；两真栈发现（语义遮蔽已修 / 82% 需对象化立 R4.1b 卡）见 §10 |
| [2026-07-04-r4.1b-edge-objectification-and-nlu-decision.md](2026-07-04-r4.1b-edge-objectification-and-nlu-decision.md) | R4.1b 端侧对象化 + K6 路径决策：量化「82% 目标缺口=辅助驾驶 ADAS 功能长尾等对象密集型工作」，摆开 A（继续扩规则对象化）/ B（端侧 NLU）两路。**关键洞察=NLU 只解决识别不解决执行，端侧对象化是 A/B 两路共用地基**。P0 补 3 个低成本对象（空气净化/导航播报/按键音，真栈 edge→VAL→action 6/6，覆盖率 75.74%→76.17%） | 🚧 P0 已落地并 push（`a094914`）；P1 ADAS（碰 ws8 安全门控）须 Plan Mode、建议独立聚焦会话 |
| [2026-07-04-r4.2-streaming-tts-bargein.md](2026-07-04-r4.2-streaming-tts-bargein.md) | R4.2 服务端流式 TTS + barge-in：DashScope CosyVoice 流式（复用 fun-asr 已破解的 `/api-ws/v1/inference` run-task 协议壳）+ WS `/api/tts/stream`（文本增量入→PCM 分片出）+ HMI AudioContext 调度播放（`pcmPlayer.mjs` node 可测）+ 失败无感回退句级批处理 + 打断 v1 确定性；P0 探针硬 gate（账号/增量回流/格式/首帧延迟四项全过才集成）；不改 proto/编排核心 | 📋 设计定稿待实施；§3.4 v2 语音打断已移交 R4.3 |
| [2026-07-04-r4.3-wake-vad-fullduplex.md](2026-07-04-r4.3-wake-vad-fullduplex.md) | R4.3 语音交互前端：唤醒词 + VAD + 全双工-lite——补 Phase 1 WS7 遗留「唤醒与打断」。全双工拆级钉死范围（L1 免唤醒连续对话 / L2 唤醒词 hands-free / L3 播报中打断；L4 真全双工=非目标，理由=绕开规划/执行分离车控安全链）；sherpa-onnx WASM 浏览器本地 KWS（「小舟小舟」pinyin 运行时配置，免训练）+ silero-vad 端点检测（fun/mimo 引擎首次获得端点能力），**唤醒前音频零上传**；`voiceLoop.mjs` 纯逻辑 FSM node 可测；两条必守边界=误唤醒静默回收 + 本地 dismiss 与云端 F1「取消」的分界（HMI 按 need_confirm 判定，承接 R4.1 §5.3 红线）；模型资产 gitignore+双源下载脚本（吸取 R3.6 大文件教训）；后端零改动、全部默认关 | ✅ **P1 大脑 + VAD 集成 + KWS 从源码构建全落地合 main**（`voiceLoop.mjs` FSM 20 例 + `sileroEndpoint.mjs` 10 例 node 绿，72/72）：**VAD=onnxruntime-web 单线程**（silero，无需跨源隔离）做端点+打断；**唤醒词「小舟小舟」=自建 sherpa-onnx KWS WASM**（生态无预构建→emscripten 从源码编，运行时 pinyin 免训练，`scripts/build-kws-wasm.sh` 重现）；**唤醒真麦验收通过**。🚧 **KWS 接进真 App** 在分支 `feat/r4.3-p2-kws-in-app`（主 app 开 `COOP`+`COEP:credentialless` 让 pthread KWS 能跑且不破坏跨源）；**已合 main**。**收尾（2026-07-05，真栈 Docker 容器端到端验证）**：唤醒人声播报（多提示音随机，TTS 关回退 beep）+ hands-free partials 实时上屏（ghost 气泡）+ 自定义唤醒词预设（4 词，token 对模型词表核验）+ 三路 mic 收敛单路共享流 + KWS 播报态 D6 抑制 + 流式 ASR 失败批处理兜底 + off→dashscope model 修复；后端契约护栏 `test/e2e_voice_loop.py`（ASR 流式 + TTS round-trip，真栈 PASS）；声学质量留真麦（§9）。取代 R4.2 §3.4 v2 |
| [2026-07-05-nearby-discovery-redesign.md](2026-07-05-nearby-discovery-redesign.md) | 周边发现 Agent 重构（food-ordering → nearby）：把 mock 点餐升级为**基于高德 POI 2.0 的通用周边发现** Agent——`nearby.search`（类目参数化，覆盖餐饮/酒店/景点/影院/停车/充电/加油，支持菜系·品牌·评分·人均·营业中过滤）+ `nearby.detail`（富详情：评分·人均·电话·营业时间·特色·图片）+ `nearby.order`（诚实预留桩，不假下单）。自持富数据 `AmapPlaceProvider`（补 navigation 薄 provider 丢掉的 business.cost/tel/opentime/photos），不动 navigation；与 navigation 按「**发现 vs 出行**」经 manifest `route_hints` 声明式切分、不改编排核心；新增 `place_list`/`place_detail` 卡并在 HMI 渲染（现 `restaurant_list`/`reservation` 卡未渲染）；含重命名 ripple 清单与真栈验收 | ✅ **P0 已落地并真栈验证（真高德端到端，2026-07-05）**：重命名+富 provider+三 intent+两卡渲染+全仓 food.*→nearby.* 迁移；全量 1087 passed 零回归、HMI build 过、registry resolve 基线重算 15/15；真栈实测「附近好吃的/酒店/火锅」出真实天安门周边 POI（评分·人均·营业·图片，缺字段不编造）+「XX 怎么样」出富详情卡。真机实测三修：①「看第八个详情」非锚定序号提取②充电/停车设施发现归 nearby 富卡（沿途充电规划仍归 charging）③价位/排序从原话兜底解析。安全：真栈发现 ws8 第三方出站代理是空壳（envoy 非可用正向代理）→ 换 `egress-proxy.py`（stdlib CONNECT 正向代理+域名白名单，实测 amap 200 / google 403），nearby 出站真正受白名单约束。**已合并 main（merge `b0ffac9`）**；余 P2 真实点单适配器（麦当劳/瑞幸 pre-order） |
| [2026-07-05-r4.3b-voice-loop-hardening.md](2026-07-05-r4.3b-voice-loop-hardening.md) | R4.3b 语音回路体验硬化：泓舟真麦反馈 5 问题根因全定位（首唤醒双份=StrictMode×enable 竞态孤儿控制器 / 处理期不可交互=THINKING 四种 FSM 死锁+网关 WS 串行无 cancel / 无「退下吧」退出 / 唤醒即说漏字=KWS 检测窗+recorder 等 ws.onopen / 语气词误收+qwen3 服务端 800ms 硬编码盖过客户端静音尾）+ 4 项审计隐患（陈旧 ASR 回调劫杀下一轮/pendingId 单槽/引擎 start 并发穿透/D6 回声指纹死代码）。P0 正确性包（epoch 护栏+死锁解除+回调代际+FIFO）/ P1 体验包（exit 词本地退场+filler 双门槛+端点宽限合并+recorder 先行）/ P2 链路包（PCM 直传+前滚缓冲根治漏字、vad_silence_ms 透传、网关 cancel 真打断）/ P3 obs+文档；后端仅 3 处加法（§7 清单制约束） | ✅ **P0-P4 全落地并真栈验证，已合并 main（`17e388e`）并 push**（P0 epoch 护栏/死锁解除/回调代际/FIFO；P1 退出词/filler/端点宽限合并/recorder 先行；P2 3 处后端加法**真栈 e2e 通过**——B1 PCM 直传 partial→final→done、B2 vad_silence_ms 透传、B3 网关 cancel「发 cancel→收 cancelled→新请求正常」，Go 编译过；P3 obs 计数+设置文案+D6 A4 勘误；**P4 两轮真麦反馈修复**——wake pre-roll=0 治「小周」误上屏/恢复唤醒提示音/退出词·filler 鲁棒化/语气词不退出聆听改进续问窗）；**110 node 例绿+tsc 零新错+build 过，三容器 --build 重建**；声学/真麦命中留 §10 泓舟验收 |
| [2026-07-07-r4.4-rejection-and-clarification.md](2026-07-07-r4.4-rejection-and-clarification.md) | R4.4 输入拒识 + 路由澄清：把「该不该处理、该给谁处理」显式建模为**置信度三段式**（高置信执行零改动 / 中置信路由级澄清 `intent_choice` 卡 / 低置信 hands-free 语音静默拒识）。受话判定与歧义申告**合并进 Planner 同一次 LLM 调用**（`addressed`/`clarify` 可选字段，fail-open、多 provider 通用）；拒识仅作用于 `meta.input_source=voice_*` 来源（push-to-talk/文本/补槽续接永不拒），fast_intent 端侧命中天然豁免；拒识轮不落对话历史不进画像；拒识压过 route_hints（电台「深入调研」碰瓷句代价不对称）、steps 压过 clarify（确定性优先）；澄清零会话状态（send_text 回发新请求，深度=1）+ `_fallback` 语义 top-1 加分数门槛（堵「低分硬执行 capabilities[0]」真 bug）；连续拒识端侧自适应收紧（续问窗减半→仅唤醒词→成功即恢复）；评测先行（rejection/clarify 语料照 R3.4 模式，先建尺再动 prompt） | ✅ **P0+P1+P2 全落地并各自 merge main（2026-07-08）**；真栈 e2e_rejection 2/2 + eval --live @ mimo 误拒 3.4%/拦截 88.9%、CLARIFY on 出卡+off 零变化+反例误澄清 0/17、rejectPolicy node 全路径；CLARIFY_ENABLED 默认 off 待真栈 CDP 验收翻 on，真麦嘈杂验收留泓舟 |
| [2026-07-07-r4.4-implementation-plan.md](2026-07-07-r4.4-implementation-plan.md) | R4.4 实施计划（可直接接手开工版）：P0 拒识主链（T0 语料先行→T1 planner schema/prompt→T2 engine D6 短路+落库跳过→T3 HMI 来源 meta+静默处理→T4 e2e/基线）/ P1 澄清主链（T5 fallback 门槛→T7 intent_choice 卡+序号选择→T8 语料）/ P2 自适应（T9 `rejectPolicy.mjs` 纯逻辑策略+FSM `setVadBargeInDisabled`）。含 §0 实施者必读 12 条坑（`_parse_and_validate` 空 steps 吞信号、build 重试条件、prefs 白名单制、拒识短路须在空计划分支之前、`_rejected` 内部键、HMI 必须补 `turnEnded()` 防 THINKING 死锁、prompt 按 env 拼接函数化等）+ 契约速查表（meta 键/卡 schema/env）+ 每期 DoD 命令块 | ✅ 已按计划全部执行（P0 merge → P1 merge → P2 merge，2026-07-08）|
| [2026-07-10-dashboard-badcase-observability-redesign.md](2026-07-10-dashboard-badcase-observability-redesign.md) | Dashboard badcase 排查重构（会话/轮次/日志贯通）：session_id 经 contextvar 贯通 + HMI 每轮自生成 trace_id（气泡角标复制→dashboard 搜索直达，零 proto 改动）+ 三个新 NATS 事件（`obs.turn` edge Handle 单点收口 / `obs.llm` 网关唯一出口 / `obs.log` 结构化日志激活+NatsLogHandler 防自激励）+ `cloud.planning` span 门控挂 plan/LLM raw + collector SQLite 持久化（重启不丢、badcase 豁免清理）+ dashboard 四视图重构（会话三级下钻默认页/总览/日志/badcase 收藏夹一键重放对照）+ 内容采集 `OBS_CONTENT_CAPTURE` 门控与统一脱敏 `redact.py` | ✅ P0-P2 全落地并合并 main（merge `e0795b9`，2026-07-10）：真栈 `test/e2e_obs.py` 16/16 + CDP 前端 12/12；实现差异与踩坑（llm-gateway 缺 nats-py 事件静默丢等）见 §12 |
| [2026-07-11-reminder-agent-design.md](2026-07-11-reminder-agent-design.md) | 智能提醒 Agent（reminder，50074）：自然语言创建日程/提醒/待办（确定性中文时间解析+LLM 兜底+回读确认）+ 到点主动触达（Agent 内 asyncio 调度轮询 PG `reminder_item` 原子领取 → 复用 NATS `agent.proactive`→HMI 播报+`reminder_card`）+ **右舞台 agenda 场景双形态**（单日=今日时间轴：当前时刻线/提醒节点/待办芯片；多日/全部=按天分组列表封顶一瞥，D7 拒周/月网格与舞台切换控件——维度切换在问句里）+ 序号/标题管理闭环（`REMINDERS_ACTIVE` 状态键）；独立 Agent 不并 info、不改编排核心/proto/网关；P1 update/snooze/重复/位置触发/任意日期区间，非目标=外部日历/端侧离线触发 | ✅ P0 + **P1a** 全落地（2026-07-11 同日：设计批准→P0→审查→泓舟批 P1a「直接开干」）：P1a=`reminder.update` 改时间（多条澄清+缺时间两轮续接）/ **snooze 改期原条目**根治 fired 尸体（e2e 4b 断言）/ **recur 重复规则**（每天/工作日/每周X，触发后滚动、完成不杀系列）/「下个月」区间 / 审查修复 B1 大后天截胡·B3 非法日期·N2 tz 守卫；reminder 单测 94、eval 26/26、真栈 e2e **7/7** + recur/update 探针；集成期修 `_POC_DEFAULT_SCOPES` 缺 profile.read/write 致被权限门剔除，详见 AGENTS.md §4 reminder 行；余 P1b 位置触发（独立启动）+ 错过补投 |
| [2026-07-11-reminder-cross-domain.md](2026-07-11-reminder-cross-domain.md) | 跨域提醒（真机 badcase trace 703f095f/b3ecd195：「第一场提醒我观看」路由/标题全对但反问时间，而开赛时间就在上一轮赛程数据里）：标准化「可提醒上下文」`REMINDABLE_ACTIVE` shared state——产未来事件的域按统一 schema opt-in 写入（本次 sports，trip/charging 即插），reminder 缺时间路径统一消费（序号/这场/开赛前指代 + `parse_lead` 提前量默认前10分钟 + 回读带事实锚）；A 类时间可推导（本设计）与 B 类事件触发（P1b）明确分界；不改编排核心 | ✅ P1c 已落地并真栈验证（2026-07-11 当日批+当日完成）：原 badcase 场景「第一场提醒我观看」从反问变一轮成单（开赛-10min）；reminder 单测 102 + sports 生产者 4 + eval 28/28；实现期修正=生产者写全部有 kickoff 场次保序号对齐、消费提到 LLM 兜底前（确定性优先） |
| [2026-07-11-reminder-agent-implementation-plan.md](2026-07-11-reminder-agent-implementation-plan.md) | 智能提醒 Agent P0 实施计划（可直接接手开工版）：Task 0-12 逐步 TDD——timeparse 黄金 40 例（固定时钟/固定 UTC+8 回退）→ store 双后端（claim_due 原子领取）→ scheduler（合并播报/失败不回滚）→ manifest+镜像（asyncpg 叠加层不动 _sdk）→ agent 四 handler（追问续接 REMINDER_PENDING/清空 meta.confirmed 契约）→ HMI types/纯逻辑 mjs/两卡/AgendaStage 双形态 → compose+conventions 登记 → 路由评测 7 例实测收敛 → e2e_reminder 6 断言真栈闭环；含实施者必读坑（依赖闭包/--build/时钟可测性） | ✅ 已按计划执行完毕（2026-07-11）：Task 0-12 全绿——reminder 单测 68 + HMI node 133 + eval_route_hints 17/17 + 全量 1268 passed + 真栈 e2e 6/6 |
| [2026-07-13-token-audit-config-and-badcase-fixes.md](2026-07-13-token-audit-config-and-badcase-fixes.md) | LLM 消耗排查（结论=栈内无闲置消耗入口，obs.db llm_calls 归属方法论）+ 两优化（合成会话跳过记忆抽取 session 前缀契约 §9.2、caller_service 归属补全）+ registry 长期不健康自动剔除（REGISTRY_EVICT_FAIL_COUNT，根治 food-ordering 残留刷告警）+ 批处理 ASR/TTS 去 MiMo 硬绑定（ASR_PROVIDER/TTS_PROVIDER auto 桥接流式引擎 + StreamBridge 双向适配）+ 真机 badcase 五修复（搜索兜底可读化/prompt 消毒/4xx 响应体可诊断、天气意图先答+地名收敛、HMI 右舞台车况动态化 vehicle_state 桥接、MiniMax new_sensitive 内容风控→权威 top-2 收窄重试+4xx→INVALID_ARGUMENT、「体感温度怎么样」误触空调→三层让路）| ✅ 当日全落地（2026-07-13）：全量 1406 passed/7 skipped、eval_fast_intent 57/57 零回归、真栈逐项复验（密钥轮换哈希比对/零抽取/vehicle_state 连上即推/意图先答/带引用真合成）|
| [2026-07-12-mode-routing-and-answer-quality.md](2026-07-12-mode-routing-and-answer-quality.md) | 四模式路由与回答质量重设计（直答/联网查询/新闻/深度调研）：模式进入判据全声明式（manifest 判别化 + Planner「时效与深度」通用段 + info.search/info.news 确定性 route_hints 护栏 priority 59）+ 新评测 `eval_mode_routing.py`（122 条五桶语料、`--live` 真 PlanBuilder 端到端 + 混淆矩阵）+ **引擎级 escalate 通用机制**（`AgentResult.data._escalate` 保留键，chitchat 误接时效题零播报自动改派搜索，conventions §9.1）+ chitchat 日期锚/depth 分档 + 搜索薄证据重试/新鲜度加权/top6 + 新闻 thinking 修复/话题 livecrawl + 深调研 cap 对齐/backtrack 放宽/深挖种子复用；§8 收尾续修（真机 badcase 四批）：MiniMax `<think>` 内联剥离、speech 出口统一剥 markdown（决策=不上渲染）、合成 JSON 截断/裸引号边界式抢救、赛事预测让路搜索+赛果带比赛阶段 | ✅ 9 切片 + 收尾续修全落地（2026-07-12，f210eb2 起连续提交）：基线 @mimo **175/177=98.9%**（受控对照 @MiniMax 84.2%→95.8%）、route_hints 47/47、确定性子集 57/57、全量 1360 passed/7 skipped、真栈四模式探针+badcase 原话复验全过；实施坑 8 条见 §7/§8；后续（2026-07-15）：日期锚扩为星期+时刻、纯钟点/日期/星期问句 chitchat `_clock_answer` 确定性直答 + 首个 route_hints 钉路由（墙钟 badcase，见 §9 与 journey 文档 §10） |
| [2026-07-14-scene-orchestrator-redesign.md](2026-07-14-scene-orchestrator-redesign.md) | 场景编排 Agent 重设计（静态预置激活器→「用户造场景 + 策略引擎」）：调研行业三代演进（预置模式/用户自定义 TCA[理想任务大师 120+动作·50+条件]/LLM 一句话生成[任务大师2.0·ChatIoT NL→TAP]）后定主线=**`scene.create` 一句话造场景**（LLM 仅创建期编译 NL→Scene DSL，白名单=构建期 COPY VAL `commands.yaml`，确定性校验+强制 require_confirm 改写+NEED_CONFIRM 回读；**激活/执行零 LLM**）+ PG `scene_item`（用户同名遮蔽预置）+ deactivate 真恢复（快照优先/反向默认表兜底）+ route_hints（guard 让路端侧 driving_mode/power_mode）；**v2 策略引擎（泓舟增补「按目标/环境/执行结果动态规划」）**=「聪明在编译期、可靠在运行期」两段式（D9）：Policy DSL v2（`guards/when/assert/on_fail` 平铺可选字段+key 白名单防幻觉）+ 激活期确定性 **Ground·Solve**（环境分支求值/幂等跳过已达成/空集诚实反馈，trip-planner 四段流水线同范式）+ **Verify-Repair** 失败闭环（后台对账 assert、诚实汇报/幂等重试建议卡/驻车补做 deferred，**repair 不新增执行通道**，对症 `_dispatch_cloud_actions` speech 逐条覆盖致多动作失败静默的真缺陷，D10）+ 与 Planner 边界「Planner 负责第一次的聪明，场景负责每一次的可靠」（D11）+ P3 询问式触发（触发/verify/补做共用一条 NATS 订阅）；**v2.1 评审修正（泓舟 4 指摘全成立）**：①DSL↔PG 字段一一同名对齐（补 goal/guards 列）②条件三态求值（unknown=exclude+告知·guard 缺数据降级 confirm，修互斥分支缺数据双发实 bug）③verify 竞态=activation_id 代际护栏（R4.3b epoch 同款）+对账清单闭包传参+单飞 cancel ④退出恢复基准改 `solved_actions`（when 跳过项不恢复，防覆盖用户手动调整） | ✅ **P0-P3 全部落地并真栈验证 + 全量代码评审收口**（`test/e2e_scene.py` **26/26**、全量 **1576 passed**、eval route_hints 70/70 + fast_intent 57/57、HMI CDP 真栈）：P0 造场景闭环 + P1 参数覆盖/会话沉淀/media 放开 + P2 Ground·Solve 三态求值与 Verify-Repair 后台对账 + P3 询问式触发（零执行权）。落地纠偏见文档 §0.5（4 处设计与代码事实不符 + 3 个被真栈揪出的既有真 bug：VAL 氛围灯 color 分支提前 return 导致亮度从未生效 / 端侧快路径劫持造场景句·激活句 / `_dispatch_cloud_actions` speech 逐条覆盖让失败静默）；**落地后全量评审六修复**见 §0.6（hour 时区 BUSINESS_TZ 同源 / once 触发器消费即熄 / 事件路径 10s 场景缓存 / verify 复合断言日志崩 / manifest 补 media.control / update·deactivate 两处 route_hints guard），随 `f436521` 入库、叙述 `4d77462` 补档 |
| [2026-07-14-journey-e2e-test-system.md](2026-07-14-journey-e2e-test-system.md) | 旅程级端到端验证体系（跨 Agent 自主执行 × 全场景连续对话 × HMI 二次交互）：在 L0-L2 存量测试之上新建 **L3 旅程层**（数据驱动 journey YAML 语料 + 统一 runner `test/e2e_journeys.py` + 能力记分卡：任务完成率/上下文保持率/二次交互成功率/诚实率/主动闭环率）与 **L4 HMI 交互层**（把历史临时 CDP 验证固化为 `test/hmi_cdp/`，`Network.webSocketFrameSent` 拦帧断言「点击→发出正确文本」）。两个关键协议事实定分层：HMI 二次交互=合成文本发送（`Cards.tsx` 五处，例外仅 `is_confirmation`/`meta.nearby_poi_id`）→ 协议层可高保真模拟；挂起确认遇无关轮被清（`engine.py:128`）→「中断-恢复」当前不存在。语料分**回归级**（必须绿，保护存量）与**目标级**（能力标尺，允许红——红灯清单=让系统具备两能力的工程 backlog）；测试集 A 组 14 条（单句多域/中间结果传递/交接改派/异步有始有终/失败诚实）+ B 组 13 条（跨域指代/中断恢复/域间信息迁移/快慢路径一致/20 轮通勤 showcase）+ C 组 12 条（CDP）+ G 全局门（时延基线/零泄漏/无断链话术）；失败自动 POST badcase 进 dashboard 收藏夹闭环 | ✅ **P0-P3 全落地 + 红灯修复收官（2026-07-14/15，§10 落地记录，泓舟拍板 9 项全按建议修+Q1-Q4 按建议）**：首跑抓到 scene custom_params 存量真 bug（`6470209`，路由变体依赖缺陷只有旅程层暴露）；目标级首跑 7/18 产出 9 项红灯工作项→当日三批修复（`96093ad`→`7e193c5`）——R1 POI 地标语义五连转正/R2 中断-恢复（含复验抓到的 wait_slot 疑问式黑洞次生 bug 三针修）/R3 焦点迁移/R7 导航 ETA→提醒一轮成单/R8 雨天确定性改室内/R9 故障层层诚实降级；**终态：回归级 15/15、目标级 16/18，B5-1「一次通勤」14 轮整旅程全绿**；L4 CDP 7 用例 6✅1⏭️（真帧实锤）；mock 车道挂 nightly、`make e2e` 已接入。**遗留卡清零（2026-07-15 收口，`3be7fdb`→`9c0843d`）**：A1-4=`_suspend` 增 prior 前缀前序步结论（防双播三道闸）+ timeparse 段位默认时刻（「明早」成单不追问），真栈理想形态「明天有雨…已设好明早8点带伞提醒」；B3-3=记忆三针（场景参数抽取黑名单/显式陈述立即抽取/谓词等价类 supersede），26 度落地终态校验过；B2-3 前提翻面照 B5-2 先例改语料（charging 例句形态，探针 6/6 选型）；**顺带修 B2-3 插话轮暴露的墙钟 badcase**（chitchat 锚缺时刻必编造 + planner 误路由 search→`_clock_answer` 确定性直答 + 锚补星期时刻 + manifest route_hints 钉路由）。全量 pytest 1627 |
| [2026-07-14-scene-orchestrator-implementation-plan.md](2026-07-14-scene-orchestrator-implementation-plan.md) | 场景编排重设计实施计划（可直接接手开工版）：P0 Task 0-9 逐步 TDD——基线+读实现依据（commands.yaml 结构/action_to_structured 判据/reminder 确认与续接样板）→ catalog 词表校验器（白名单/夹紧/DANGER 强制改写/RESTORE_DEFAULTS）→ store 双后端（**字段一一同名映射**）→ compiler（mock LLM/unsupported 剔除告知/全剔不可存/截断容错）→ agent 六 handler（create 确认与 SCENE_PENDING 续接/activate 写 activation_id+solved_actions+快照/deactivate 恢复基准=solved_actions/list 分组卡）→ HMI 两卡 → 镜像 COPY 词表+compose PG → manifest+route_hints eval 收敛+registry 基线 → e2e_scene ①②③④ → 回归登记；P1（update·delete/参数覆盖/会话沉淀/media 端侧分发单独确认）、P2 策略引擎（编译策略字段/solve 三态纯函数/NATS 多消费方订阅壳/verify 代际+单飞/e2e 行车拦截→驻车幂等补做）、P3 触发承接概要；实施者必读 14 条坑（词表唯一真相源/desc 长度即权重/重建等 10s/_POC_DEFAULT_SCOPES/裸 sys.path 禁插 src 等） | ✅ P0-P3 全部执行完毕（e2e_scene 26/26） |

> 接真实 provider 的标准流程见常青指南 [`docs/guides/provider-integration.md`](../guides/provider-integration.md)。
