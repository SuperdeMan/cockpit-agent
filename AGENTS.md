# AGENTS.md — 接手者入口

> 先读本文件，再动代码。工程规则最高权威是 [`CLAUDE.md`](CLAUDE.md)；架构唯一真相源是
> [`docs/architecture/cockpit-agent-architecture.md`](docs/architecture/cockpit-agent-architecture.md)。
> 当前 QA 与发布交接统一看
> [`docs/reviews/2026-08-30-qa-closeout-handoff.md`](docs/reviews/2026-08-30-qa-closeout-handoff.md)。
> 逐批历史只查 [`docs/agents-history.md`](docs/agents-history.md)，不要把历史流水抄回本文件。

## 1. 项目是什么

云边协同的智能座舱 multi-agent 系统。端侧快系统处理高频、安全敏感和离线能力；云侧
Planner 处理复杂、多域、多轮任务。Agent 统一使用 gRPC 契约 + Manifest，经 Registry 发现；
车控只经 VAL，LLM 只产意图/计划。用户端有两个：座舱 HMI（`hmi/`）与 Android 陪伴端
「小舟随行」（`mobile/`，React Native + Expo），同一后端大脑、同 `user_id` 共享记忆、各自
独立会话，两端共享的是判据不是 UI（架构 §2.4、约定 §9.33）。

当前阶段是 **Phase 1 工程化 PoC**。工程主干、云端中枢、真实 Provider、语音回路、记忆、
可观测、旅程验证、M0a→M4、M5 数据飞轮与探索式 QA 的编号开发批均已落地。PoC 已可运行，
但真实 CAN/SOME-IP、量产账号体系、完整隐私治理和部分外部能力仍是明确边界。

## 2. 文档地图

| 想了解 | 权威入口 |
|---|---|
| 当前 release、测试证据、QA 活项 | `docs/reviews/2026-08-30-qa-closeout-handoff.md` |
| 工程规则、目录、安全红线 | `CLAUDE.md` |
| 全局架构 | `docs/architecture/cockpit-agent-architecture.md` |
| Phase 1 计划与量产 DoD | `docs/architecture/phase1-implementation-plan.md` |
| 环境、端口、命名、错误码 | `docs/conventions.md` |
| 本地/云端开发与部署 | `docs/dev-guide.md` |
| 测试分层与 E2E | `test/README.md` |
| 意图对抗测试 | `docs/guides/intent-adversarial-testing.md` |
| 真实 Provider 接入 | `docs/guides/provider-integration.md` |
| MiniMax 原始 QA 问题 | `docs/reviews/2026-08-26-minimax-cloud-qa-findings.md` |
| MiniMax 根因与修复批 | `docs/design/2026-08-27-minimax-qa-root-cause-fix-plan.md` |
| 安全确认写闸 | `docs/design/2026-08-30-qa-safety-confirmed-write-guard.md` |
| Android App 当前入口 | [`剩余待办总表`](docs/design/2026-09-14-android-remaining-todos.md)（2026-09-14 起唯一入口） + `mobile/README.md` + [`完整评审`](docs/reviews/2026-09-07-android-ux-full-review.md) + [`分批处理建议`](docs/design/2026-09-07-android-review-remediation-batches.md) + [`性能/时延评审`](docs/reviews/2026-09-12-android-performance-latency-review.md) + [`端到端时延复盘 2026-09-16`](docs/reviews/2026-09-16-android-e2e-latency-location-wait.md)（App 比 HMI 慢 20s 的成因与修法；规划 LLM 快车道待裁决） + [`语音层三项体验修正 2026-09-17`](docs/design/2026-09-17-android-voice-sheet-focus-follow.md)（光球固定头区不随内容滚 / 内容区跟底 / 输入框「按住说话」提示；层高下限按头区 + 该档内容重算） + [`流式文字匀速上屏 + 长回答不被硬上限掐断 2026-09-18`](docs/design/2026-09-18-android-stream-text-pacing.md)（簇状 delta 原样上屏 ⇒ 显示按节拍追；chitchat `max_tokens` 只做兜底；真机 A/B 与清洁包因构建机内存耗尽未完成、服务端未 deploy） + [`GPT-6 Pro 外部评审逐条核对与分批待办 2026-09-19`](docs/design/2026-09-19-android-gpt6-review-remediation-batches.md)（七条 F01–F07 全部成立、F01 更糟；第一 / 二批当日落地，提交 `6931968f`…`0a6a19e6` 已 push、CI 8/8，候选包 `0a6a19e68` 为 OPPO 常驻包；服务端未 deploy；总表 §10 登记 E-16～E-22 / D-08 / D-09 / G-01～G-06）；启动前核对工作树与代码/设备版本 |
| Android 构建与跨工具交接 | [`操作指南`](docs/guides/android-build-and-device-validation.md)：共享镜像/设备串行占用、低内存参数、长任务接续与精确验包 |
| 历史流水 | `docs/agents-history.md`（只追加） |

服务子目录各有 README；改某个服务前先读该目录 README。

## 3. 不可违反的规则

### 3.1 运行环境

- 根目录 `.env` 是唯一运行时环境与密钥来源；不得复制、维护或依赖 `deploy/.env`。
- 本地真栈只用 `make up` 或根 `compose.yaml`；不得以 `deploy/docker-compose.yaml` 为首文件。
- 任何 E2E、Compose、部署或脚本/manifest 改动前，先从仓库根读取 `dev-stack.local`。
- `dev-stack.local` 只允许 `target=local|cloud`，不得保存 token、密码、私钥或 URL。
- 2026-09-20 起云端签名 E2E 身份车道已开（`shared/.env` 与根 `.env` 同一份 `E2E_IDENTITY_SECRET`）：干净用户实验用
  `scripts/probe_history_window.py` 这一路（`scripts.e2e_identity.sign_identity`），不再需要改 `AUTH_TOKENS`；秘密照旧不进代码 / 日志。
- `target=cloud` 时禁止启动本地 Compose；本地只做编辑、单测、静态检查和 Vite。
- 不停止其他 agent 正在使用的 Docker、Metro、Gradle、pytest 或真栈进程。

### 3.2 云端发布

- 真栈动作前先运行 `python scripts/dev_stack.py target show`。
- Windows 真栈、SSH 与 deploy 命令一律用 PowerShell；Git Bash/MSYS 会改写参数引号。
- cloud deploy 只接受 clean、已提交、main 可达的 SHA；先 dry-run，再经人工授权 `--apply`。
- deploy 不自动 commit、merge 或 push；`git push` 必须单独授权。
- push 前必须逐条展示 `origin/main..HEAD`，让用户看到会被一并推走的提交。
- 未标 `remote_safe` 的 E2E 不在 cloud 缺省运行；`remote_mutating=true` 仍需精确 `--id`、
  `--allow-mutating` 和本轮人工授权。
- 支付、商户写、真实车控、数据删除、系统配置不因通用 deploy/E2E 授权自动放行。
- 三存储迁云只用 `scripts/cloud_data_migration.py`；final 必须先取得停写授权。
- 不修改 `.env`、安全组、Tailscale、CI/CD、systemd、数据库 schema，除非用户逐项授权。

### 3.3 架构安全

1. 车控只经 VAL；任何组件不得直接碰 CAN/SOME-IP。
2. LLM 不直连车控：Planner 产计划，确定性 Executor 经权限与 VAL 执行。
3. 危险动作必须二次确认；`require_confirm` 权威来自 capability manifest/受控配置，不信 LLM。
4. 新 Agent 经 Registry 发现；不得为加 Agent 修改 orchestrator 核心路由分支。
5. secret/token/password 不进代码、commit、日志或文档。
6. 改 proto 先改 `proto/`，再 `buf generate proto`；绝不手改 `gen/`。
7. `Capability.response_only` 是只响应能力的权威；D0/T2/Executor 都必须 fail closed。
8. 安全问句的权威文本是服务端 `safety_origin_text`；LLM goal/reason 和补槽短句无授权权威。

## 4. 当前真实状态（2026-09-25）

### 4.0 发布快照

| 项目 | 当前值 |
|---|---|
| 真栈目标 | `target=cloud` |
| 远端 main / QA 文档 HEAD | 运行 `git rev-parse origin/main`；纯 docs/test 可领先 production release |
| 生产 release | `ee94c59534b1259cf447497101f380723e53d621`（2026-09-25 22:2x apply）= **评审四轮 §7 待办批一：位置词表合一；周边检索的中心接地**（用户「按你建议能直接修的接着处理」；[设计文档](docs/design/2026-09-24-conversation-review-round4-remediation.md) §5.6 / §5.6.1：`2c0c6663` 位置词表权威 `entities.yaml`、镜像与唯一扫描算法 `runtime/positions.py`（逐条对账 + 源码级「第三份词表」断言），顺带修 A/B 读出的「位置 + 功能」简写截走点名对象、氛围灯「亮度调低」执行成调高、挡位只取一位数；`684bfeec` nearby 名字校验不过时先 geocode，本地半径外且用户没说那座城 ⇒「没找到「X」」，与导航 E-1 同一个半径（`agents._sdk.location`）；`ee94c595` 这个回答不带 data，不被结果校验念成「数据源没返回」）：status ok 5/5、verify `verified`（`20260925T142243Z-ee94c59.json`）；真栈 RS42（位置）×3 3/3、RS43（中心接地）×3 3/3，回归 9 例 8/9（RS33 红在两处已知方差）。再上一个 `684bfeec84a73b1976fb80a9f024d4f7afc6f572`（2026-09-25 22:0x apply）= 同批 ① + ②：status ok 5/5、verify `verified`（`20260925T140339Z-684bfee.json`）。再上一个 `4e8da9dd28e3ddfe8a719784be80d608dd7cb09e`（2026-09-25 20:2x apply）= **评审四轮 §5.5 b：需确认命令的写步方向以车端确定性解析为准**（用户「先 a 再 b」；[设计文档](docs/design/2026-09-24-conversation-review-round4-remediation.md) §5.5 / §5.5.1：车端快路径「需确认 ⇒ 上云」那条路盖 `_edge_confirm`，云侧只在计划里**同一对象的写步方向不同**时换成车端那条，不加步、不碰别的对象与读步、不进 prompt；依据是 collector 全量历史 266 轮——264 一致、方向冲突 1 轮车端对、计划选了别的能力 1 轮规划对）：status ok 5/5、verify `verified`（`20260925T122751Z-4e8da9d.json`）；真栈 RS34 / RS35 / RS38 / RS31 ×2 8/8，盖章端到端到达（10 轮 `edge_confirm`、零纠正），问句与多分句路径不盖章。再上一个 `746343140905009f6c47650fc3259db211c3522d`（2026-09-25 19:5x apply）= **评审四轮 §5.5 a：确认问句念出这一步真会执行的动作**（`responses.yaml` 八条 `<object>_<operate>_confirm`，能力完整性门禁逐条核）：status ok 5/5、verify `verified`（`20260925T120117Z-7463431.json`）；真栈 6/6，14 次确认问句全部念出方向。再上一个 `5ca289c770ce8be4b82fe36c570ab59a1aadb3d2`（2026-09-25 19:0x apply）= **评审四轮 §7 两条接着修**（用户「接着修」；[设计文档](docs/design/2026-09-24-conversation-review-round4-remediation.md) §5.4.6 / §5.4.7：补槽答案替换了挂起步的旧值 ⇒ 模型写的 goal 跟着改、T2 循环续接后追旧值的步确定性丢掉、续接的再规划批不再套首轮 adaptive 纠偏（判据一份 `orchestrator/cloud/superseded.py`）；挂起轮与完成轮用同一份抽取登记本轮已执行的事实，候选只收挂起步自己那张卡）：status ok 5/5、verify `verified`（`20260925T110559Z-5ca289c.json`）；新探针 RS40 4/5、RS41 4/5（红的都是第 1 轮规划方差），回归 16 例 ×2 27/32——五条红都在新会话的规划那一步，其中 RS34「关闭后备箱」被规划成 `trunk.open`、确认话术听不出方向（§7，已由 §5.5 a / b 修）。再上一个 `4438ea6ba40c215d511305d19fb8ace05715d9f5`（2026-09-25 18:0x apply）= **评审四轮 RS39 追加：NEED_SLOT 必须声明缺哪个槽**（[设计文档](docs/design/2026-09-24-conversation-review-round4-remediation.md) §5.4.4 / §5.4.5：规划把 navigate_to 的 `destination` 写到 search_poi 上时按它搜；search_poi / set_place 的追问补上 `missing_slots`，manual.query 不再挂补槽；新增静态契约 `agents/_sdk/tests/test_need_slot_declares_missing.py`）：status ok 5/5、verify `verified`（`20260925T101443Z-4438ea6.json`）；真栈（旧判据）19/21，单步 search_poi 的改派第一次取到（语音补槽 782 ms 导航过去），两趟红是模型逐字照抄规划器提示里的澄清示例；同批读出两条范围外缺陷（T2 循环续接后目标漂移、挂起轮丢掉已执行的路线会话，§7，已由 `5ca289c7` 修），RS39 判据已收紧。再上一个 `dabc2e2dd326eaf6cbcd42996bcbc946fdd4e167`（2026-09-25 17:3x apply）= **评审四轮 RS39 + 聚合直出**（§5.4.3：导航语境下 search_poi 的「没找到」改派 navigate_to；多步结果里只有一步有话就直出，不交聚合模型改写）：status ok 5/5、verify `verified`（`20260925T094206Z-dabc2e2.json`）；真栈 18/18，两步形态的语音补槽轮 696–1489 ms、零聚合调用（`f535c654` 同一形态 3293 ms、话术被改写）。再上一个 `f535c6547e716bea4d74939936cfb0aec2ad7b8c`（2026-09-25 16:4x apply）= **评审四轮 R4-07 第二步**（§5.4：语音说出的澄清选择与补槽回答也过轻量受话判定，取消刻意不接）：status ok 5/5、verify `verified`（`20260925T084651Z-f535c65.json`）；真栈 13/15（两条红都是 RS39：search_poi 的「没找到」没留挂起 ⇒ `dabc2e2d`）。再上一个 `4b16d8467fb1294284cd0283919a70abdd4894d1`（2026-09-25 15:5x apply）= **评审四轮 R4-07 轻量受话判定**（用户裁决；[设计文档](docs/design/2026-09-24-conversation-review-round4-remediation.md) §5.3：确认 / 焦点省略 / 纯偏好三条出口的受话判定从借一次完整规划换成专门的轻量调用——助手上一句 + 原话 → `addressed`，快档、温度 0、上限 3 s，判据与规划器同一组句子；切换前离线 A/B：出口形态 80/80 零误拒、黄金句拦截 28/36 vs 规划器 21/36）：status ok 5/5、verify `verified`（`20260925T075612Z-4b16d84.json`）；真栈 14/15（唯一红是文字轮的规划方差）；语音「关掉」「确认」服务端 819–1466 / 956 ms（未命中网关缓存），命中缓存约 20–230 ms，`6e64b767` 时约 3.1 s。再上一个 `6e64b76793e481004bcfedcfe22d8550b5d8c896`（2026-09-25 14:4x apply）= **评审四轮 R4-07 第一步**（用户裁决；[设计文档](docs/design/2026-09-24-conversation-review-round4-remediation.md) §5.2：语音来源的「关掉」（焦点省略开关）与「确认」执行前先过受话判定——唯一实现 `PlannerEngine._voice_admitted`，判非受话 ⇒ 拒识零执行、确认的挂起保留；文字 / 确认条按钮 / 拒识关零额外调用）：status ok 5/5、verify `verified`（`20260925T064950Z-6e64b76.json`）；真栈 15/15（RS38 语音「关掉」「确认」零误拒、每轮都有 `cloud.voice_admission`）；**代价**：这两类语音回合服务端中位 273 ms → 3166 ms、23 ms → 3142 ms（多一次完整规划，降时延方向待裁决，§5.2.2）。再上一个 `266ed937c4e5292524c0d5f627ca950f61d55e1a`（2026-09-25 02:2x apply）= **评审四轮批 E 追加 ④**（[设计文档](docs/design/2026-09-24-conversation-review-round4-remediation.md) §8.7：原话不是地标描述时，地标解析给的候选必须和原话沾边才算验证——`a7e664f3` 上「导航到第二家」被猜成苏州的「东方之门」、导去 1478 km 外）：status ok 5/5、verify `verified`（`20260924T182658Z-266ed93.json`）；真栈（批 C + E 那一整组 24 条 ×3；第一趟跑完 15 条后 WebSocket 握手超时中断、其余补跑）38/43 + 30/30：RS32 / RS34 / RS35 / RS36 / RS37 / RS23 / TF1 / CT4 / SL3 / SL4 / CA1 / CA5 / PU1–PU9 各 3/3，RS33 三趟的「第二个」都落在第二家（红只在第 1 轮澄清卡方差），CL1 1/3、RS14 2/3 为已知方差；S2S 真 provider ×2 四次移交全是转写原话。再上一个 `a7e664f39d8f243731652f11235dee763cdf0176`（02:1x apply）= **评审四轮批 E 追加 ③**（§8.6：规划器没交出可用计划的裸序数同样以确定性澄清收尾）：status ok 5/5、verify `verified`（`20260924T181104Z-a7e664f.json`）；真栈 8/12（RS33 出卡的两趟都是确定性澄清，CT4 一趟导去苏州 ⇒ ④）。再上一个 `976f174c1d2eff8eb3b3018f3e66c4637f39626b`（01:5x apply）= **评审四轮批 E 追加 ②**（§8.5：被拦下的裸序数以确定性澄清收尾——`8528df0b` 上兜底谈话拿着候选上下文编了一句「现在为您发起导航」、什么都没执行）：status ok 5/5、verify `verified`（`20260924T175635Z-976f174.json`）；真栈 9/12（RS36 / CT4 3/3，RS33 一趟落到「没听清」⇒ ③）。再上一个 `8528df0b53b5e7fbf8b98ae829888db3a62e9419`（01:3x apply）= **评审四轮批 E 追加**（§8.4：地标解析的宽松名字匹配只在本地半径内算验证——`2f8f92be` 上「云岚国际中心」仍有两趟经地标解析导去北京；裸序数长不出与所选那一项无关的写步——列表之后的「第二个」曾被规划成 `reminder.cancel {index: 2}`）：status ok 5/5、verify `verified`（`20260924T173805Z-8528df0.json`）；真栈 30/33（RS36 3/3、CL1 出卡的两趟不再导去北京）。再上一个 `2f8f92be6ee114b7c29114f3b7e46c860a379114`（2026-09-25 00:5x apply）= **评审四轮批 C + 批 E**（§4.1 / §8：S2S 重建摘要按完整问答对取 4 对、长消息保留首尾与每个带否定 / 限定词的分句、助手那条带真实执行过的动作、读不到说实话；名字对不上又在本地半径（150 km）之外的弱匹配不当目的地——修前深圳说「云岚国际中心」出发去 1940 km 外北京的美容院，`search_poi` 零结果不再套「找到 0 个…推荐前三个」；端侧位置长词优先、按原话全取——修前「副驾驶位」开的是主驾那扇、「主驾和副驾」只开一扇）：status ok 5/5、verify `verified`（`20260924T165346Z-2f8f92b.json`）；真栈 58/72（RS37 修前 0/3 → 3/3；RS36 1/3、RS33 0/3 逼出 §8.4；RS23 0/3 是执行声称判据的已知误报面）。发布前远端可用盘 31.2 GB 低于 30 GiB 构建闸：按用户批准的保留最近 6 份口径删旧镜像集 / 源码 / 上传目录只多出 0.1 GB，再删不在保留范围内的 130 个 `builds/<sha>`（sudo 下 20 GB，回滚不读它们）⇒ 51.6 GB；同机 drone-agent 的镜像与共用构建缓存未碰。再上一个 `bda5af716e640945070fcaf200dbf8c1b8a0f202`（2026-09-24 23:2x apply）= **评审四轮批 B**（§3：普通应答「好的」只在挂起就是最近那个提示时确认，修前挂着「打开后备箱」插话听笑话之后的「好的」3/3 真把后备箱打开；「打开后备箱，再告诉我空调有哪些模式」按步骤归属保留后备箱确认；告警在场时拒绝建议出确定性立场）：status ok 5/5、verify `verified`（`20260924T153139Z-bda5af7.json`）；真栈 47/51（RS34 / RS35 / SF4 / RS32 各 3/3，RS33 1/3 且为假绿 ⇒ 批 E）。再上一个 `b48d76d7812d48b432a0e74f654023095138ec1f`（2026-09-24 22:2x apply）= **评审四轮批 A**（§2：S2S 移交出去的是转写原话、模型改写只记 `interpretation`；「关掉」带回上一轮的目标位置；旧澄清不抢更新提示的序数）：status ok 5/5、verify `verified`（`20260924T143039Z-b48d76d.json`）；真栈 52/57（RS32 修前 0/3 → 3/3、RS33 0/3 → 2/3，S2S 5 次移交全是原话）。再上一个 `ecbeed289dc96a8e65c9f9bc5de8407c0c704ad3`（2026-09-24 17:0x apply）= **评审三轮追加批 N**（设计文档 §17：规划器把空数组连同工具编码的收尾标签吐进 steps（`["[]</steps>"]`，collector 35 个 trace）按「零步」认，不再当解析失败白烧一次重试、第二轮再坏就落技术失败；「帮我查查卤牛肉怎么做」这类查询 + 疑问框架认成求信息的请求；回归当场揪出 N-3——「推荐三部适合全家看的电影」第一轮掉档编出瑞幸下单 + 导航、第二轮如实说零步，回落规则却执行了编出来的导航——求信息的请求 + 重试说零步 + 抢救计划带写 ⇒ 不回落；「首轮零步 + 之后坏 ⇒ 一律闲聊」的泛化量过后不做）：status ok 5/5、verify `verified`（`20260924T090453Z-ecbeed2.json`）；真栈规范化事件 2 次均正常收尾，N-3 未被真栈触发（离线按 trace 复现）；回归 + 行车安全组 ×3 **36/36**（首发 `f14d1e43` 上 35/36，唯一红即 N-3 那一趟）。其间首发 `f14d1e43`（16:2x apply）。再上一个 `b7a49361247927e0fc9bce933fae718b21bc3100`（2026-09-24 15:0x apply）= **评审三轮追加批 M**（设计文档 §16：犯困 / 饮酒 / 不适登记后追问能不能继续开，答该状态自己的话术，不再是「在它排除之前…熄火，并联系救援」的车辆故障口吻；「当前高速路段」这类正在走的路不当目的地，天气 / 预报 / 路线一样都没查到时不让模型综合——修前模型拿着全是「暂无」的数据建议「开启双闪谨慎驾驶」）：status ok 5/5、verify `verified`（`20260924T070235Z-b7a4936.json`）；真栈 M-1 修前 3/3 车辆口吻 → 首发 9/9、终版 3/3 状态话术；被填了目的地的追问首发 6 趟接住 4、漏 2 种写法（当轮改写重发），终版 2/2；回归 + 行车安全组 ×3 **36/36**。其间首发 `6b7335c0`（14:3x apply）。再上一个 `8278121587288b903a5584e4aadf81ebd1b89196`（2026-09-24 13:3x apply）= **评审三轮追加批 L**（设计文档 §15：原计划「问句一律不执行声明为写的云侧步」先量——collector 426 轮声明写步里被判成问句的 13 轮全是正当的（条件指令、「去惠州怎么充电」→ 充电规划…），命中 0，不做；收成**问规范 / 注意事项 / 条件**且每个分句都是提问时才扩闸（历史 27 轮命中 2、误伤 0），并补上问句判据漏收的「注意些什么」——「开长途前要注意些什么」修前真栈被规划成 `scene.create`「长途自驾模式，共 11 个动作」挂确认卡、`scene.activate`、F09-b）：status ok 5/5、verify `verified`（`20260924T053253Z-8278121.json`）；真栈这句 ×10 场景写 0、F09-b 0（这 10 趟模型没往场景走，新闸未被真栈触发），对照「去惠州怎么充电」2/2 照常充电规划；回归 + 行车安全组 ×3 **36/36**。再上一个 `624142233634856137f6d9c79d7d3b92af9e8b8b`（2026-09-24 12:4x apply）= **评审三轮追加批 K**（设计文档 §14：告警在场时整句只是在拒绝安全建议——「别提醒我，继续开就行」「我没事，继续开」——一律由拿着告警「立场不改」的兜底谈话作答，不管规划器出了澄清卡、被「无动作」重试催出一步查天气、还是催坏成技术失败；驾驶员状态分清「此刻自述」与「提到这类风险」——否定句不算、话题问句与假设句答同类安全常识且不登记会话告警、补上「我好困 / 困了怎么办」这类自述）：status ok 5/5、verify `verified`（`20260924T044947Z-6241422.json`）；真栈拒绝轮 10/10 守住立场（其中 4 轮是新规则从技术失败救回）、否定 / 话题 / 假设句登记告警 0/24（修前 8/8）、自述照常登记；回归 + 行车安全组 ×3 **36/36**。其间首发 `36a92009`（12:2x apply）的 K-1 只接了澄清卡一种形态，真栈 10 轮冒出另两种丢安全线的形态，当轮改写重发。再上一个 `8cee1699f45ad50a94a11ac89959ec9d422236b1`（2026-09-24 11:0x apply）= **评审三轮追加批 J**（设计文档 §13：road-safety 的知识型问句——「怎么缓解开车时的疲劳」「开长途前要注意些什么」——按问题给通用建议、不再报天气，状态自述与告警仍确定性优先；全由只回答步组成的计划不再被「如果 / 若」升成多批——「如果你能去旅行，你想去哪里」不再进 T2 超时念失败）：status ok 5/5、verify `verified`（`20260924T030200Z-8cee169.json`）；真栈落到 road-safety 的知识问句 5/5 按问题答（修前 3/6 答天气）、假设句 5/5 simple（修前 2/3 升 adaptive）；回归 + 行车安全组 ×3 35/36（唯一红 SF4 是规划器澄清卡方差，与本批无关）。再上一个 `fc1f5ddef8826d3e9dff0b5b1820ae71f36a6d51`（2026-09-24 10:3x apply）= **Android N-04 / N-01**（[总表 §11](docs/design/2026-09-14-android-remaining-todos.md)：S2S 签名身份闸只认带了 token 的会话——云端实验开关常开后真 App 的端到端挡位全部 1008，现与 Edge WS / 设计同口径；内置工具失败话术改中文，英文诊断串只进 `error.message`）：status ok 5/5、verify `verified`（`20260924T023225Z-fc1f5dd.json`）；真栈 App 同形握手修前 3/3 1008 → 修后 3/3 `ready`，无签名冒充测试命名空间 2/2 仍 1008；英文问时间听到英文诊断串 2/5 → 0/5。再上一个 `b6afd59b0be0250dc8fc2ddc260628ec1142a65c`（2026-09-24 00:1x apply）= **评审三轮追加批 I**（设计文档 §12：纯应答长不出写步——原话只是应答（「好的 / 可以」）或复述助手口吻的执行声称（「已为您执行」）、上一轮助手又没有提问时，计划里的端侧写 / 需确认 / 声明为写的云侧步整份换成谈话；应答词与语气面下沉到 `runtime.affirmation`，engine 确认授权词表成员不变；原方案「槽值没有落点就拦」先量误伤后收窄）：status ok 5/5、verify `verified`（`20260923T161753Z-b6afd59.json`）；真栈「可以，已为您执行」×30 零动作，新闸拦下 `volume.mute`、批 G 的 G-3 拦下 `hvac.inc` / `volume.dec`；回归 RS31 / RS30 / RS29 / RS28 / RS21 / RS25 / RS26 ×3 **21/21**。再上一个 `a4bb73bfb3ea84e223c6ee0f058022a5c4e6b06c`（2026-09-23 23:1x apply）= **评审三轮追加批 H**（设计文档 §11：「有几个 / 能放几个 / 分几个」计数问进问句判据——此前 23 句里 13 句被端侧执行成写车控，「空调有几个风量档」调风量、「大灯有几个模式」开大灯；区别在「几」前面的句法头，「开几个车窗」「放几首歌」仍是指令）：status ok 5/5、verify `verified`（`20260923T151349Z-a4bb73b.json`）；真栈 RS31 修前 0/1（5 句 3 句被执行）→ 修后 ×3 **3/3**；回归 RS30 / RS29 / RS28 / RS21 / RS25 / RS26 ×3 **18/18（RS21 一趟为探针误判，判据修正后重判 / 重跑 3/3）**。再上一个 `31943d47404c803bb0d607dd7daead2432331b39`（2026-09-23 22:1x apply）= **评审三轮追加批 G**（设计文档 §10：显式输入的「不受话」重试——第二轮如实答「受话、零步」且原话没有操作证据时由兜底谈话作答，不再让用户听「没能拆成步骤」；被这条重试催出来的写车控 / 需确认步，原话没有操作证据就整份作废，不再执行编出来的关双闪 / 停媒体；纠正话术改写在生产容器里 A/B 证伪、未改）：status ok 5/5、verify `verified`（`20260923T141814Z-31943d4.json`）；真栈修前 / 修后同一组 24 趟：失败话术 9 → 0、编造执行 1 → 0；回归 RS30 / RS29 / RS28 / RS21 / RS25 / RS26 ×3 **18/18**。再上一个 `2c38446d7c2cbc2c58457ab85a628cb2c5707f15`（2026-09-23 20:1x apply）= **评审三轮追加批 F**（设计文档 §9：F-1 能力编号笔误只朝「只回答不执行」的能力归位——四条证据 + 求信息请求的混槽形态，真栈 `cap_0107`（瑞幸下单）带着 chitchat 的 `depth` 被归位；F-2 求信息的请求规划失败 /不受话时由兜底谈话作答，不再是「没能拆成步骤 / 没听清」；F-4 原因问句只看问词之后的操作动词、列举问只看另起的分句——「空调为什么不制冷」「车窗有哪些开启方式」不再被执行成开空调 / 开车窗；F2 规划知识真栈 A/B 无收益已撤回）：status ok 5/5、verify `verified`（`20260923T121222Z-2c38446.json`）；真栈 RS30 / RS29 / RS28 / RS21 / RS25 / RS26 ×3 **18/18**。批 F 链 `7b0ee8de`（F1 代码）→ `bb609ef5`（F2 知识）→ `01564cc2`（F2 撤回）→ `a59b1621` → `07e9ea5c` → `2c38446d`，各自 status ok 5/5、verify verified。再上一个 `f9bec42371b6d36dae613ea41123a9ebfa500939`（2026-09-23 16:3x apply）= **评审三轮追加批 E**（设计文档 §8：问句判据补列举问法——「天窗有什么用」不再打开天窗、「空调有什么模式」不再开空调、「有什么好看的电影」不再放视频；视频「推荐」让路给云端；端侧出口第五维——原因问句的查询让路给云端，「续航为什么会下降」不再答「电量72%」；navigation 只说了从哪出发时不替用户定目的地）：status ok 5/5、verify `verified`（`20260923T083310Z-f9bec42.json`）；真栈 RS21 3/3、RS28 3/3（判据层；推荐与列举问交到云端后 planner 接不住，§8.1.1 / §8.2）。再上一个 `1255f7b2513f2c0519257bd7d218e0d1ca27d519`（2026-09-23 14:4x apply）= **评审三轮收口**（§4.2 reminder 拆步修正第二版：这一步的分句按与槽标题的最长公共子串认、原话拆开之后事件分句自己没给时间就拒绝，不看 planner 的时间槽）：status ok 5/5、verify `verified`（`20260923T064644Z-1255f7b.json`）；真栈 RS22 ×3 3/3、RS27 收紧判据后 ×3 **3/3**（两趟正好是此前出错的两种 planner 写法）；R3-04 闸首次放行等待 n=24 p50 247 ms / p90 598 ms / 最大 830 ms（设计文档 §5.1.1）。再上一个 `c99a9a748aa2fbeab07c62f1a02482885dd00a8f`（14:2x apply）= **评审三轮批 D**（R3-04 流式闸只由文本决定释放、任意切包逐字相同，D0 / T2 发布即权威，span 记 `claim_gate_hold_ms`；连同 §4.2 第一版 `a2d03dde`）：status ok 5/5、verify `verified`（`20260923T063207Z-c99a9a7.json`）；真栈 RS22 3/3、RS21 1/3（红在落域：第二句被规划成导航）、RS27 2/3（判据漏判了 planner 换写法后的事件定时提醒，已收紧）。再上一个 `5c729fbce12e6166c92d0f331d5cfafd0ee92400`（2026-09-23 13:5x apply）= **评审三轮批 C**（R3-03 被拒诉求按原话来源分句认同一性、共用城市名的独立诉求照做；R3-06 T2 再规划批用运行时步骤 ID `t<批次>-<局部>`，不再被当成已完成跳过 / 串读上一批结果）：status ok 5/5、verify `verified`（`20260923T055455Z-5c729fb.json`）；真栈 RS27/RS22/RS13/RS14 ×3 = 7/12（四条红在初次规划层、一条是 reminder 执行层缺陷，逐条见设计文档 §4.1.1；RS27 逼出 reminder 拆步后按整句补标题 / 借时间的真缺陷，§4.2）。再上一个 `926d4c17e55e118fa1506e14da85bc085425ad69`（2026-09-23 13:3x apply）= **评审三轮批 B**（R3-02 约束补丁带墓碑交给跨轮合并、同句 SET + DELETE 撤掉的那一维不再复活；R3-05 挂起删除四态 + 进程内墓碑，删不掉的取消说「X 不会执行了」、确认 / 续接复活不了，关旧开新一次整表写）：status ok 5/5、verify `verified`（`20260923T053248Z-926d4c1.json`）；真栈 RS26 / RS7 / RS24 / RS16 ×3 **12/12**。再上一个 `416e47bc917e8c66f7da2181de2ea1c350220c43`（2026-09-23 13:0x apply）= **评审三轮批 A**（R3-01 确认授权的两个必要条件：只有语气面的「啊 / 唉 / 请 / 那 / 。」不再是裸确认；点名确认拆成召回 + 裁决，裁决只认挂起那一刻服务端生成的已校验步骤摘要（`SessionState.action_summary`），端侧解析只做否决——「确认关闭后备箱」不再把挂着的「打开后备箱」确认掉，落 `system.pending_mismatch`）：status ok 5/5、verify `verified`（`20260923T051015Z-416e47b.json`）；真栈 RS25 ×3 **3/3**。再上一个 `86c439985b6d8eb95feaf6cc581387a7cccb38c0`（2026-09-22 21:5x apply）= **评审二轮批 C**（R6 历史裁剪只裁助手那半、用户那条放不下就整对舍弃并记 `history_omitted`；R7 焦点里私有两格 `session_constraints` / `active_task` 按乘员归属（`by_occupant`），共享车辆 / 路线状态仍共享；R8 挂起存储读写各三态，读不到不说「没有」、存不下不说「正在清除你的数据」）：status ok 5/5、verify `verified`（`20260922T135947Z-86c4399.json`）；真栈 RS23 / RS24 / RS7 各 3/3（**RS24 活体**：A 说「我不吃辣」后 B 回问三趟都没听到「您这次说过」）。再上一个 `06ad7ae4627263ebe22e08842bb1a93e42ec3345`（21:2x apply）= **评审二轮批 B 的真栈追加**（reminder 事件触发的「有没有给时间」改问事件短语所在的那个分句——RS22 2/3 逼出：「以后有堵车就提醒我，另外列出明天的提醒」里另一个诉求的「明天」把诚实拒绝挡成了「什么时候提醒你？」）：status ok 5/5、verify `verified`（`20260922T132905Z-06ad7ae.json`）；真栈 RS11 3/3、RS13 3/3、RS22 改判据后 3/3（三趟 T2 都是「先念出明天那条提醒 + 诚实拒绝事件触发」）。再上一个 `8af9b8bd74cece811b632898e49ccd5eba54214b`（20:1x apply）= **评审二轮批 B**（R4 执行性声称在首次释放前拦——`ExecutionClaimGate` 句级有界闸挂在 D0 / T2 流式与 E 路径逐步播报上；R5 `unsupported` 终止的是那一个诉求而不是整个领域，被丢步的下游记 `blocked` 不删边）：status ok 5/5、verify `verified`（`20260922T102118Z-8af9b8b.json`）；真栈 RS21 3/3、EC1 3/3、RS13 3/3。再上一个 `d0170329e3623abe0572c6923aaea72b4b6d2fcf`（17:4x apply）= **对话评审第二轮批 A**（R1 裸确认语气面一份——「行程 / 确认函 / 可以改」不再授权、「好的，确认吧」真确认；R2 取消极性与问句——「不要取消」`pending_kept`、「怎么取消」`pending_state`、两条挂起并存时点名绑定；端侧撤回否决——「取消刚才打开空调」不再执行成开空调；R3 语音来源的纯偏好陈述先过受话判定；R9 「怎么把 X 打开」「请告诉我怎么…」「请问…」是提问）：dry-run 零阻断（基线 `5818d136`）→ apply `submitted` → status ok 5/5 零 warning、`release_sha` = `running_release_sha` → verify `verified`（`20260922T094509Z-d017032.json`，minimax / MiniMax-M3）；真栈 RS15–RS20 ×3 **18/18**（RS15/16/17/19/20 `[det]`，RS18 `[var]` 只是手册 / chitchat 措辞）；R3 受话判定读数「我不吃辣」：voice_followup 1/3 受话、ptt 2/3、voice_wake 3/3——被拒那几趟 rejected 卡 + 零登记，受话的致谢 + 约束读出口逐字相同。再上一个 `5818d1369f520d712ae6189c2abff33faff010a9`（2026-09-22 13:3x apply，隔离 worktree——主树带着未提交文档时 apply 会 `safety_rejected`）= **批 8 第二个 release**（④ `remote-data-migration.sh` 迁移预检的 Serve 判据复用 `verify_tailscale_serve`；基础设施批准锚 `86edd25e → bd0671c4`，材料 `.artifacts/infrastructure-approval/5818d136…/`，只读 precheck → prepare-upload + scp 四文件 → trusted entry `infrastructure_approved` → 只读 postcheck 三摘要齐）。再上一个 `016bd8b1ea18077f20e6d318dee7949aa7b46e7f`（13:0x apply）= **批 8 第一个 release**（① T2 完成轮收口与 E 路径同一份——此前 adaptive / reactive 出口不写焦点、不清续接挂起、不补挂起软提醒，PU7「路线会话没盖上」的真因；② 安全闸二第二臂：焦点里未解除的告警在场时 planner 空手轮改走兜底谈话；③ chitchat「看不到这辆车的任何实时读数」类别否定；RS14）：status ok 5/5、verify `verified`（`20260922T050629Z-016bd8b.json`）；真栈 RS14 ×3 = 2/3（两趟 T2 里的 navigate 之后「取消导航」→「已结束到深圳湾公园的导航」，第三趟 planner adaptive 却零步）、continuity 70/71（唯一红 SF4 T65 chitchat 拒绝过短不给替代）、window g18 ×2 未落 chitchat。再上一个 `c98fb087d2aeb552f3e0c34a184969c4ddb021ae`（2026-09-22 11:0x apply）= **批 7 追加第五个 release**（nearby：与原话「X 附近」矛盾的陈旧 `location` 槽让路给原话）。追加链 `267b5d8b`（预检问候 smoke 冷启重试 + `input_source=release_probe`，**apply 后 `ROLLBACK_FAILED`**）→ `1a8ff180`（verify 回环端口有界等待，同样 `ROLLBACK_FAILED`）→ `c4c1186d`（10:4x，`VERIFIED`：Serve 判据改「我们的五条一条不少」——两次回滚的真因是同机 drone-agent 加的第六条 `:8447` 条目，旧判据「恰好五条」把每次验收都判红；三轮基础设施批准锚 `499fc97c → 1ce1525f → 36364333 → 86edd25e`）→ `93564e19`（10:4x，continuity 两检查点 + nearby `_candidate_place`）→ `c98fb087`。再上一个 `74f334f452eb3e1dc99d17030727218a221c7038`（2026-09-21 18:0x apply）= **对话评审批 7 第四个 release**（聚合器：拿掉拒绝步时告知 LLM「那部分已由系统另行作答」，真栈 RS13 ×3 路况 + 诚实拒绝零猜测）。批 7 链 `3f9b18e4`（17:2x，①②③④ 主体：`unsupported` 拒绝终止 T2 再规划 / `safety.road_condition` 接高德真路况（路线 tmcs + 路名态势，navigation 内部意图 `route_traffic`）/ reminder 事件短语剥前缀 / 端侧回指主语查询让路）→ `1e892f1b`（17:4x，态势接口城市名优先——真栈证否「adcode 优先」，`UNKNOWN_ERROR 20003`）→ `551dd66c`（17:5x，连接词形态先求值 + 按分句剥前缀）→ `74f334f4`。再上一个 `eb1dd6bd`（2026-09-21 14:5x apply，隔离 worktree；区间含另一会话经用户批准的 CI 改动 `mobile-apk.yml`，按 dev-guide 一次性 digest 批准流程放行）——**缺省历史视窗 2 → 4 对**（用户裁决，依据 W19-c；`_HISTORY_EXCHANGES`，取回 10 条，预算 1400 不动）+ 视窗装置的不 pin 臂；真栈不 pin 复验 `history_exchanges=4` 10/10、槽解出 8/10（另 6 格最后一轮没进规划）。再上一个 `eb55e502`（13:0x；用户批准云端清理 ①②后 26 个镜像无缓存全量重建约 75 min）——问句闸补「几档 / 几级 / 几种 / 多少档 / 什么条件 / 什么情况」（W19-c 真栈逼出：「它有几档」曾被规划成 `seat.heating.on` 并执行）；真栈复验 w6 g05 / g14 / g29 三格零动作（`manual.query` / `chitchat.talk` / `manual.query`）。再上一个 `b0d79dbe`（09-20 17:4x）——W19-c 装置 + 设计文档 §8.2（这次发布同时把云端 `shared/.env` 新加的签名身份车道 `E2E_IDENTITY_ENABLED=true` 带进 edge-gateway，日志 `e2e_identity=true`；用户授权「切换干净用户」）。W19-c 装置 `scripts/probe_history_window.py` + 设计文档 §8.2（这次发布同时把云端 `shared/.env` 新加的签名身份车道 `E2E_IDENTITY_ENABLED=true` 带进 edge-gateway，日志 `e2e_identity=true`；用户授权「切换干净用户」）。再上一个 `eb87b571`（17:0x）= 批 6 第二个 release：（W19-b：条件提醒 guide 第四分「持久订阅」+ 范例两句 `ca2725b0`、两条流式路径的 `raw_text_from` span 格 `eb87b571`）；批 6 首个 release `cc331315`（16:5x）= W16-b 步级起点原话（`Step.origin_text` / `resumed`、`step_raw_text` 一份判据、三条执行路径接线）。再上一个 `cf1d0f96`（15:4x）= 批 5 第三个 release：nearby「X 附近」中心锚定 `31e8fefc` + 本批文档，连同另一会话的四个 mobile 提交（`59088760` / `8df1d117` / `6faa3c75` / `e7a83638`，用户裁决「授权推送」）。批 5 前两个 release `485fccd1`（13:5x）→ `8e403d5c`（14:3x）：W17 读取三态 / W18 候选墓碑 + 约束读出口 / W19 视窗 pin / reminder 事件触发拒绝 + 真栈 continuity 逼出的两条修。再上一个 `9ebaa5c3`（12:3x）= P2 收尾 |
| 上一生产基线 | `684bfeec84a73b1976fb80a9f024d4f7afc6f572`（评审四轮待办批一 ① + ②）；再上一个 `4e8da9dd28e3ddfe8a719784be80d608dd7cb09e`（评审四轮 §5.5 b：方向以车端确定性解析为准）；再上一个 `746343140905009f6c47650fc3259db211c3522d`（评审四轮 §5.5 a：确认问句念出动作）；再上一个 `5ca289c770ce8be4b82fe36c570ab59a1aadb3d2`（评审四轮 §7 两条接着修）；再上一个 `4438ea6ba40c215d511305d19fb8ace05715d9f5`（评审四轮 RS39 追加：NEED_SLOT 必须声明缺哪个槽）；再上一个 `dabc2e2dd326eaf6cbcd42996bcbc946fdd4e167`（评审四轮 RS39 + 聚合直出）；再上一个 `f535c6547e716bea4d74939936cfb0aec2ad7b8c`（评审四轮 R4-07 第二步）；再上一个 `4b16d8467fb1294284cd0283919a70abdd4894d1`（评审四轮 R4-07 轻量受话判定）；再上一个 `6e64b76793e481004bcfedcfe22d8550b5d8c896`（评审四轮 R4-07 第一步）；再上一个 `266ed937c4e5292524c0d5f627ca950f61d55e1a`（评审四轮批 E 追加 ④）；再上一个 `a7e664f39d8f243731652f11235dee763cdf0176`（评审四轮批 E 追加 ③）；再上一个 `976f174c1d2eff8eb3b3018f3e66c4637f39626b`（评审四轮批 E 追加 ②）；再上一个 `8528df0b53b5e7fbf8b98ae829888db3a62e9419`（评审四轮批 E 追加）；再上一个 `2f8f92be6ee114b7c29114f3b7e46c860a379114`（评审四轮批 C + 批 E）；再上一个 `bda5af716e640945070fcaf200dbf8c1b8a0f202`（评审四轮批 B）；再上一个 `b48d76d7812d48b432a0e74f654023095138ec1f`（评审四轮批 A）；再上一个 `ecbeed289dc96a8e65c9f9bc5de8407c0c704ad3`（评审三轮追加批 N）；再上一个 `f14d1e43ab046177fcbe83151d6997762bb16c2b`（批 N 首发，回归揪出 N-3）；再上一个 `b7a49361247927e0fc9bce933fae718b21bc3100`（批 M）；再上一个 `6b7335c019865271ec9f74563361ae783c9c21ce`（批 M 首发，M-2 漏两种写法）；再上一个 `8278121587288b903a5584e4aadf81ebd1b89196`（批 L）；再上一个 `624142233634856137f6d9c79d7d3b92af9e8b8b`（批 K）；再上一个 `36a92009809110ac295108f8ebb2b15188008b00`（批 K 首发，K-1 只盖澄清卡）；再上一个 `8cee1699f45ad50a94a11ac89959ec9d422236b1`（批 J）；再上一个 `fc1f5ddef8826d3e9dff0b5b1820ae71f36a6d51`（Android N-04 / N-01）；再上一个 `b6afd59b0be0250dc8fc2ddc260628ec1142a65c`（批 I）；再上一个 `a4bb73bfb3ea84e223c6ee0f058022a5c4e6b06c`（批 H）；再上一个 `31943d47404c803bb0d607dd7daead2432331b39`（批 G）；再上一个 `2c38446d7c2cbc2c58457ab85a628cb2c5707f15`（批 F 终版）；再上一个 `07e9ea5c1fc1583141540835a25a7109fa936f4a`（批 F）；再上一个 `a59b16219c76aeb54a53ee4f8a48d0a7e98a44bb`、`01564cc2974a222c77fcc88ab9bfd592bdacd390`（F2 撤回）、`bb609ef533a7998e91193523dfb48e4b737b26a4`（F2 知识，已撤回）、`7b0ee8dea896f960627a09c026c210f2b743e3e6`（F1）、`f9bec42371b6d36dae613ea41123a9ebfa500939`（追加批 E）；再上一个 `1255f7b2513f2c0519257bd7d218e0d1ca27d519`（评审三轮收口）；再上一个 `c99a9a748aa2fbeab07c62f1a02482885dd00a8f`（评审三轮批 D）；再上一个 `5c729fbce12e6166c92d0f331d5cfafd0ee92400`（评审三轮批 C）；再上一个 `926d4c17e55e118fa1506e14da85bc085425ad69`（评审三轮批 B）；再上一个 `416e47bc917e8c66f7da2181de2ea1c350220c43`（评审三轮批 A）；再上一个 `86c439985b6d8eb95feaf6cc581387a7cccb38c0`（评审二轮批 C）；再上一个 `06ad7ae4627263ebe22e08842bb1a93e42ec3345`（批 B 真栈追加）；再上一个 `8af9b8bd74cece811b632898e49ccd5eba54214b`（评审二轮批 B）；再上一个 `d0170329e3623abe0572c6923aaea72b4b6d2fcf`（批 A）；再上一个 `5818d1369f520d712ae6189c2abff33faff010a9`（批 8 第二个）；再上一个 `93564e19f0cf6a289ce03725de67e77f59b4eacd`（批 7 追加第四个）；追加链之前的基线 `74f334f4`；再上一个 `551dd66c1d9cd108f25bcba127b67b07ebce6489`（批 7 第三个 release）；批 7 之前的基线 `eb1dd6bd`（缺省视窗 4 对）；再上一个 `eb55e5026ac42ddcca2156d1adf12083c5d13d10`（问句闸修复）；再上一个 `b0d79dbe`（W19-c 装置）；本轮未回滚。云端只保留最近 6 份 release（见设计文档 §8.2.2，2026-09-21 清理 ④） |
| status | 2026-09-25 22:2x：`ok`、5/5 endpoint healthy、零 warning，`release_sha` = `running_release_sha` = `ee94c595`；22:0x 同样 ok 5/5（`684bfeec`）；20:2x 同样 ok 5/5（`4e8da9dd`）；20:0x 同样 ok 5/5（`74634314`）；19:0x 同样 ok 5/5（`5ca289c7`）；18:1x 同样 ok 5/5（`4438ea6b`）；17:4x 同样 ok 5/5（`dabc2e2d`）；16:4x 同样 ok 5/5（`f535c654`）；15:5x 同样 ok 5/5（`4b16d846`）；14:4x 同样 ok 5/5（`6e64b767`）；02:2x 同样 ok 5/5（`266ed937`）；02:1x 同样 ok 5/5（`a7e664f3`）；01:5x 同样 ok 5/5（`976f174c`）；01:3x 同样 ok 5/5（`8528df0b`）；00:5x 同样 ok 5/5（`2f8f92be`）；2026-09-24 23:3x 同样 ok 5/5（`bda5af71`）；22:3x 同样 ok 5/5（`b48d76d7`）；17:0x 同样 ok 5/5（`ecbeed28`）；16:2x 同样 ok 5/5（`f14d1e43`）；15:0x 同样 ok 5/5（`b7a49361`）；14:3x 同样 ok 5/5（`6b7335c0`）；13:3x 同样 ok 5/5（`82781215`）；12:4x 同样 ok 5/5（`62414223`）；12:2x 同样 ok 5/5（`36a92009`）；11:0x 同样 ok 5/5（`8cee1699`）；10:3x 同样 ok 5/5（`fc1f5dde`）；00:1x 同样 ok 5/5（`b6afd59b`）；2026-09-23 23:1x 同样 ok 5/5（`a4bb73bf`）；22:1x 同样 ok 5/5（`31943d47`）；20:1x 同样 ok 5/5（`2c38446d`）；批 F 链其余五个 release 切换后同样 ok 5/5；16:3x 同样 ok 5/5（`f9bec423`）；15:2x 复读同样 ok 5/5（`1255f7b2`）；14:4x（`1255f7b2`）与 14:2x（`c99a9a74`）切换后同样 ok 5/5；13:5x 同样 ok 5/5（`5c729fbc`）；13:3x 同样 ok 5/5（`926d4c17`）；13:0x 同样 ok 5/5（`416e47bc`）。再上一个 2026-09-22 21:5x：`ok`、5/5 endpoint healthy、零 warning，`release_sha` = `running_release_sha` = `86c43998`。再上一个 2026-09-22 21:2x：`ok`、5/5 endpoint healthy、零 warning，`release_sha` = `running_release_sha` = `06ad7ae4`（`8af9b8bd` 20:1x 同样 ok 5/5）。再上一个 2026-09-22 17:4x：`ok`、5/5 endpoint healthy、零 warning，`release_sha` = `running_release_sha` = `d0170329`。再上一个 2026-09-22 11:0x：`ok`、5/5 endpoint healthy、零 warning，`release_sha` = `running_release_sha` = `c98fb087`（`c4c1186d` / `93564e19` 切换后各独立复核一次同样 ok）。再上一个 2026-09-21 18:0x：`ok`、5/5 endpoint healthy、零 warning，`release_sha` = `running_release_sha` = `74f334f4`（批 7 四个 release 切换后各独立复核一次，均 ok 5/5 零 warning）。再上一个 2026-09-21 14:5x 独立复核：`ok`、5/5 endpoint healthy、零 warning，`release_sha` 与 `running_release_sha` 均为 `eb1dd6bd`（`eb55e502` 13:1x 同样（切换后事务未释放锁的几分钟里 status 报 `degraded` + `remote release lock is unavailable`，是锁不是故障；`b0d79dbe` / `eb87b571` / `cc331315` 同样 5/5 零 warning） |
| verify | `verified`；artifact `20260925T142243Z-ee94c59.json`；上一 release `20260925T140339Z-684bfee.json`；上一 release `20260925T122751Z-4e8da9d.json`；上一 release `20260925T120117Z-7463431.json`；上一 release `20260925T075612Z-4b16d84.json`；上一 release `20260925T064950Z-6e64b76.json`；上一 release `20260924T182658Z-266ed93.json`；上一 release `20260924T181104Z-a7e664f.json`；上一 release `20260924T175635Z-976f174.json`；上一 release `20260924T173805Z-8528df0.json`；上一 release `20260924T165346Z-2f8f92b.json`；上一 release `20260924T153139Z-bda5af7.json`；上一 release `20260924T143039Z-b48d76d.json`；上一 release `20260924T090453Z-ecbeed2.json`；上一 release `20260924T082655Z-f14d1e4.json`；上一 release `20260924T070235Z-b7a4936.json`；上一 release `20260924T063747Z-6b7335c.json`（首跑紧跟 apply 撞锁窗口给全空 `-unknown.json`，重跑 verified）；上一 release `20260924T053253Z-8278121.json`；上一 release `20260924T044947Z-6241422.json`；上一 release `20260924T042120Z-36a9200.json`；上一 release `20260924T030200Z-8cee169.json`；上一 release `20260924T023225Z-fc1f5dd.json`；上一 release `20260923T161753Z-b6afd59.json`；上一 release `20260923T151349Z-a4bb73b.json`；上一 release `20260923T141814Z-31943d4.json`；上一 release `20260923T121222Z-2c38446.json`；批 F 链 `20260923T094643Z-7b0ee8d.json` / `20260923T101645Z-bb609ef.json` / `20260923T105112Z-01564cc.json` / `20260923T111708Z-a59b162.json` / `20260923T114229Z-07e9ea5.json`；上一 release `20260923T083310Z-f9bec42.json`；上一 release `20260923T064644Z-1255f7b.json`；上一 release `20260923T063207Z-c99a9a7.json`；上一 release `20260923T055455Z-5c729fb.json`；上一 release `20260923T053248Z-926d4c1.json`；上一 release `20260923T051015Z-416e47b.json`（minimax / MiniMax-M3，lock e2e）；上一 release `20260922T135947Z-86c4399.json`；上一 release `20260922T132905Z-06ad7ae.json`；上一 release `20260922T102118Z-8af9b8b.json`；再上一个 `20260922T094509Z-d017032.json`（`minimax:MiniMax-M3`，lock `e2e`，`e2e_remote_safe` passed）；上一 release `20260922T053020Z-5818d13.json` / `20260922T050629Z-016bd8b.json`；再上一个 `20260922T030906Z-c98fb08.json`（`minimax:MiniMax-M3`，lock `e2e`，passed；紧跟 apply 的首次 verify 给了全空 `-unknown.json`，锁窗口，重跑即 verified）；追加链 `20260922T024242Z-93564e1.json` / `20260922T004405Z-c4c1186.json`；服务端验收证据 `c4c1186d`：`tailnet_entries: 6`、`listener_ready_s: 0`、问候 smoke `attempt 1 pass`；上一 release `20260921T100657Z-74f334f.json`；批 7 前三个 release `20260921T092914Z-3f9b18e.json` / `20260921T094436Z-1e892f1.json` / `20260921T095613Z-551dd66.json` 均 verified；上一 release `20260921T065448Z-eb1dd6b.json`；上一 release `20260921T051142Z-eb55e50.json`；上一 release `20260920T110205Z-b0d79db.json`（首验 `20260920T094206Z-b0d79db.json`）；批 6 第二个 release `20260920T090840Z-eb87b57.json`；首个 `20260920T085748Z-cc33131.json`；批 5 第三个 release `20260920T074422Z-cf1d0f9.json`；批 5 第二个 release `20260920T063543Z-8e403d5.json`；批 5 首个 release `20260920T055706Z-485fccd.json`；P2 收尾 `20260920T043927Z-9ebaa5c.json`（`e2e_remote_safe` passed）；批 4 首个 release `20260920T032437Z-e9044da.json`；上一 release `20260919T170707Z-88a8945.json`（`e2e_remote_safe` 8/8）；批 3 前两个 release `20260919T170108Z-fb98150.json` / `20260919T165227Z-4bfebdd.json`；2026-09-19 的四个见 history。⚠ 紧跟 apply 的 verify 与**任何仍在跑的 verify 循环**都会给全空 `…-unknown.json`（远端锁），先 `TaskStop` 再在 PowerShell 前台重跑 |
| 代码验证 | **评审四轮待办批一 `ee94c595` 工作树：全量固定口径 9754 passed / 0 failed / 32 skipped / 10 warnings，336 s（-n 6）**；`684bfeec` 工作树 9753 / 0 / 32，`2c0c6663` 工作树 9742 / 0 / 32；新增 `runtime/tests/test_positions.py` 27、`test_position_extraction` +29、`test_ambient_brightness_and_levels` 15、`test_control_target_positions` +7、`agents/nearby/tests/test_center_grounding.py` 11、`test_amap_provider` +1；16 + 10 处变异各判红；四门禁 + smoke_edge 13/13；`eval_fast_intent` 69/69；端侧规则新旧 A/B 10 071 条语料 275 条变化逐类读过。再上一个 **评审四轮 §5.5 b `4e8da9dd` 工作树：全量固定口径 9928 passed / 0 failed / 32 skipped / 10 warnings，622 s（-n 6）**；`test_edge_authority` 4、`test_edge_confirm_stamp` 2、源码级「不进 prompt」断言扩到 `edge_confirm`；6 处变异各判红；四门禁 + smoke_edge 13/13。再上一个 **§5.5 a `74634314` 工作树：9922 / 0 / 32，574 s**；`test_edge_call` +2；4 处变异 + 门禁变异各判红。再上一个 **评审四轮 §7 两条接着修 `5ca289c7` 工作树：全量固定口径 9920 passed / 0 failed / 32 skipped / 10 warnings，602 s（-n 6）**；判据模块 5、循环 +3、补槽续接进 T2 端到端 +1、`replan_batch` 持久化往返 +1、候选集 +2、确认 2 条改钉新契约；12 处变异各判红；四门禁 + smoke_edge 13/13。再上一个 **评审四轮 RS39 追加 `4438ea6b` 工作树：全量固定口径 9908 passed / 0 failed / 32 skipped / 10 warnings，557 s（-n 6）**；导航 +3、manual_rag 1 条改形、静态契约 2；3 + 3 处变异各判红；四门禁 + smoke_edge 13/13。再上一个 **RS39 + 聚合直出 `dabc2e2d` 工作树：9903 / 0 / 32，609 s**；导航接地 3 条重写、改派契约 +1、聚合器 +1 与 2 条改形、确认用例改断言订单原话；5 + 4 处变异各判红。再上一个 **R4-07 第二步 `f535c654` 工作树：9898 / 0 / 32，529 s**；`test_engine_voice_admission` +6、`test_admission` +1；3 + 18 处变异各判红。再上一个 **评审四轮 R4-07 轻量受话判定 `4b16d846` 工作树：全量固定口径 9891 passed / 0 failed / 32 skipped / 10 warnings，290 s（-n 6）**；新增 `test_admission` 16、`test_engine_voice_admission` 改为 21；18 处变异各判红；四门禁 + smoke_edge 13/13。再上一个 **R4-07 第一步 `6e64b767` 工作树：全量固定口径 9869 passed / 0 failed / 32 skipped / 10 warnings，371 s（-n 6）**；新增 `test_engine_voice_admission` 14、`test_execution_focus` +2；10 处变异各判红；四门禁 + smoke_edge 13/13。再上一个 **批 E 追加 ④ `266ed937` 工作树：全量固定口径 9853 passed / 0 failed / 32 skipped / 10 warnings，266 s（-n 6）**；导航接地 +5（猜出来的名胜两个入口 + 中间入口 + 两条对照）；5 + 重锚 4 处变异各判红。再上一个 **批 E 追加 ③ `a7e664f3` 工作树：9848 / 0 / 32，265 s**；裸序数写步闸 30（+6：没交出可用计划的三种形态、模型自己的澄清保留、没有可选项时不动、技术失败落在非谈话步）；4 + 12 处变异各判红。再上一个 **批 E 追加 ② `976f174c` 工作树：9842 / 0 / 32，264 s**；裸序数写步闸 24（收尾改成确定性澄清）；12 处变异各判红。再上一个 **批 E 追加 `8528df0b` 工作树：9841 / 0 / 32，265 s**；新增地标宽松匹配 4、裸序数写步闸 23；3 + 9 处变异各判红（「依赖步不一并去掉」「括号注记不剥」首跑是绿的，各补能区分的断言 / 用例后判红）；四门禁 + smoke_edge 13/13。再上一个 **批 C + 批 E `2f8f92be` 工作树：9814 / 0 / 32 / 10 warnings，278 s**；新增导航接地 10、端侧位置抽取 14；12 处变异各判红；端侧全目录 998。再上一个 **批 C `0ac21055` 工作树：9790 / 0 / 32，285 s**（runtime `context_snapshot` 7、S2S 摘要 +4；5 处变异各判红）。再上一个 **批 B `bda5af71` 工作树：9779 / 0 / 32 / 11 warnings，273 s**（首版 `6e82bec0` 9770）；新增应答归属 21、问句闸分句归属 35、写步闸 +8、affirmation +32、安全立场 +9；17 + 2 处变异各判红（「提议句不剥引号」「平手也保留」首跑是绿的，各补能区分的用例后判红）。再上一个 **批 A `b48d76d7` 工作树：9672 / 0 / 32 / 11 warnings，365 s**；新增 S2S +7、端侧目标 11、云侧位置 23、回复归属 12、VAL 播报 2；18 处变异各判红。再上一个 **评审三轮追加批 N `ecbeed28` 工作树：全量固定口径 9620 passed / 0 failed / 32 skipped / 10 warnings，329 s（-n 6）**；新增规范化 12、规划出口 7、抢救回落 4、F-2 3、问句判据 10；7 + 4 处变异各判红；首跑全量红 1 条是 D14 契约用例（字符串 addressed 不规范化，那一半撤回）；四门禁 + smoke_edge 13/13（首发 `f14d1e43` 工作树 9616 / 0 / 32）。再上一个 **评审三轮追加批 M `b7a49361` 工作树：全量固定口径 9584 passed / 0 failed / 32 skipped / 10 warnings，308 s（-n 6）**；新增 road-safety 18、runtime 6；首发 9 处 + 改写 5 处变异各判红（首发 `6b7335c0` 工作树 9579 / 0 / 32）；四门禁 + smoke_edge 13/13。再上一个 **评审三轮追加批 L `82781215` 工作树：全量固定口径 9560 passed / 0 failed / 32 skipped / 10 warnings，294 s（-n 6）**；新增问句判据 15、F-2 1、规划出口 8；五处变异各判红；四门禁 + smoke_edge 13/13。再上一个 **追加批 K `62414223` 工作树：9536 passed / 0 failed / 32 skipped，321 s**；新增 runtime 安全信号 63、安全闸二 10、road-safety 8、chitchat 5；首发 34 处 + 改写 17 处变异各判红（首发 `36a92009` 工作树 9528 / 0 / 32）；四门禁 + smoke_edge 13/13。再上一个 **评审三轮追加批 J `8cee1699` 工作树：全量固定口径 9450 passed / 0 failed / 32 skipped / 10 warnings，365 s（-n 6）**；新增 road-safety 8、`test_planning_answer_only_not_adaptive` 6（另修 toolcall 测试替身的 `response_only` 恒真）；七处变异各判红；四门禁 + smoke_edge 13/13。再上一个 **Android N-04 / N-01 `fc1f5dde` 工作树：全量固定口径 9436 passed / 0 failed / 32 skipped / 10 warnings，347 s（-n 6）**；新增 S2S 身份 6、工具失败话术 9；七处变异各判红；四门禁 + smoke_edge 13/13。再上一个 **评审三轮追加批 I `b6afd59b` 工作树：全量固定口径 9421 passed / 0 failed / 32 skipped / 10 warnings，326 s（-n 6）**；新增 `test_affirmation` 22、`test_planning_ack_write_guard` 11；八处变异各判红（另一处等价变异已换掉）；四门禁 + smoke_edge 13/13。再上一个 **评审三轮追加批 H `a4bb73bf` 工作树：全量固定口径 9388 passed / 0 failed / 32 skipped / 10 warnings，327 s（-n 6）**；新增 `test_question_shape` +26、端侧 `test_count_question_gate` 18、探针 RS31 与执行声称判据的引号复述排除 +6；变异 8 + 5 处各判红；四门禁 + smoke_edge 13/13。再上一个 **评审三轮追加批 G `31943d47` 工作树：全量固定口径 9338 passed / 0 failed / 32 skipped / 10 warnings，322 s（-n 6）**；新增 `test_planning_explicit_not_addressed` 17、`test_question_shape` +17、对抗评测分类器 +7；十二处变异各判红；四门禁 + smoke_edge 13/13。再上一个 **评审三轮追加批 F `2c38446d` 工作树：全量固定口径 9297 passed / 0 failed / 32 skipped / 10 warnings，309 s（-n 6）**；批 F 新增 `test_planning_ref_rehome` 13、`test_planning_info_request_no_action` 18、`test_question_shape` +19、端侧 +8；四门禁 + smoke_edge 13/13；批 F 各轮变异合计 24 处各判红（F1 十四、细化二 + 三 + 五）。再上一个 **评审三轮追加批 E `f9bec423` 工作树：全量固定口径 9239 passed / 0 failed / 32 skipped / 10 warnings，307 s（-n 6）**；新增 `test_question_shape` +31、`test_enumeration_reason_gate` 32、`test_unspoken_destination` 13；四门禁 + smoke_edge 13/13；十二处变异各判红。再上一个 **评审三轮收口 `1255f7b2` 工作树：全量固定口径 9162 passed / 0 failed / 32 skipped / 10 warnings，318 s（-n 6）**；reminder 拆步四种 planner 写法参数化（+3）、五处变异各判红；探针 RS27 列表轮收紧（`card_text_not`）`scripts/tests` 绿、拿掉判据即红。**批 D `c99a9a74` 工作树（含 §4.2 第一版）：9159 / 0 / 32 / 10 warnings，312 s**；新增 `test_claim_gate_invariance` 13（585 次随机切包逐字等于一次喂入）、reminder +5；批 D 六处 + 第一版三处变异各判红；四门禁 + smoke_edge 13/13。再上一个 **评审三轮批 C `5c729fbc` 工作树：全量固定口径 9141 passed / 0 failed / 32 skipped / 10 warnings，295 s（-n 6）**；新增 `test_refused_goal_identity` 10、`test_loop_step_identity` 9；四门禁 + smoke_edge 13/13；七处变异各判红。再上一个 **评审三轮批 B `926d4c17` 工作树：全量固定口径 9122 passed / 0 failed / 32 skipped / 10 warnings，301 s（-n 6）**；新增 `test_constraint_patch` 10、`test_pending_clear_states` 14；四门禁 + smoke_edge 13/13；六处变异各判红。再上一个 **评审三轮批 A `416e47bc` 工作树：全量固定口径 9098 passed / 0 failed / 32 skipped / 10 warnings，314 s（-n 6）**；新增 `test_engine_confirm_authority` 62；四门禁 + smoke_edge 13/13；八处变异各判红。再上一个 **评审二轮批 C `86c43998` 工作树：全量固定口径 9033 passed / 0 failed / 32 skipped / 10 warnings，257 s（-n 6）**；新增 `test_pending_store_states` 10、`test_focus_owner_scope` 7、`test_context_budget_fidelity` +5；四门禁 + smoke_edge 13/13；七处变异各判红。再上一个 **评审二轮批 B 真栈追加 `06ad7ae4` 工作树：全量固定口径 9011 passed / 0 failed / 32 skipped / 10 warnings，274 s（-n 6）**；批 B `8af9b8bd` 工作树 9006 / 0 / 32（305 s）；新增 `test_stream_claim_gate` 10、`test_planning` +5、`test_loop` +1、reminder `test_agent` +4；四门禁 + smoke_edge 13/13；六处变异各判红（D0 / T2 / E 不挂闸、按域拦、删边、时间信号回整句）。再上一个 **对话评审第二轮批 A `d0170329` 工作树：全量固定口径 8990 passed / 0 failed / 32 skipped / 10 warnings，295 s（-n 6）**；定向 cloud + edge + runtime + scripts 4777、agents + test + security + memory 3570、agents 四个 + contrast 647；四门禁 + smoke_edge 13/13；新增 `test_engine_cancel_polarity` 24、`test_engine_confirm` +20、`test_pending_cancel` +7、`test_engine_reject` +5、`test_question_shape` +23、`test_question_write_guard` +3、`test_polarity` +8；十一处变异各判红。再上一个：批 7 追加（2026-09-22）只跑定向：`test_e2e_stack_lease` `-n 8` 三趟 0 红（修前 5/3 红）、deploy 资产 + release 库 365、cloud + nearby 1655、`test_probe_qa_long_sessions` 90、nearby 125；L0 门禁绿；六处变异各判红。上一轮 **批 7 `3f9b18e4` 工作树：全量固定口径 8861 passed / 0 failed / 32 skipped / 12 warnings，272 s；四门禁 + smoke_edge 13/13 全过**（L0 门禁第一趟被 `_REPLAN_SYSTEM` 新句里的「取消」二字判成 cohort-leakage，改措辞后绿）；新增 `test_route_traffic` 11、`test_anaphora` 20、`test_anaphora_gate` 9、road_safety +9、amap provider +5、loop +4、planning +3、reminder +4、outcome +1；九处变异各判红；边缘目录 905 passed。后三个 release 只跑定向：navigation + road_safety 265 / reminder 234（+2）/ cloud + reminder 1767 + `test_aggregator` +1；L0 门禁复跑绿。再上一个 **`eb1dd6bd` 工作树：全量固定口径 8782 passed / 0 failed / 32 skipped，270 s；四门禁 + smoke_edge 全过**。`eb55e502` 工作树 8781 / 0 / 32（新增 `test_probe_history_window` 38、`test_question_shape` +7）。批 6：全量（含批 6 两包的工作树）8733 / 0 / 32，248 s；新增 `test_step_origin_text` 19、`test_skills` +1；六处变异各判红；四门禁 + smoke_edge 13/13 + 架构守卫全过；cloud 目录 `eb87b571` 工作树 1525 passed。批 5：全量（`485fccd1` 工作树）8692 / 0 / 32；`8e403d5c` 工作树 8710 / 1 / 32（那 1 红是 `test_cloud_deploy_assets` 真实 bash 子进程的秒级时钟边界，串行 185/185 ×2 绿）；新增 `test_memory_read` 23、`test_degraded_read` 8、`test_context_read_state` 9、`test_engine_memory_read_state` 10、`test_context_history_pin` 5、candidate_sets +4、shortcut +3、reminder +8、nearby +5、runtime +15；四门禁 + smoke_edge 13/13 三趟全过；`go build/vet` 零错；十六处变异各自判红。真栈（`minimax:MiniMax-M3`）：RS7 / RS8 / RS9 / CD9 ×2 **8/8**（`485fccd1`）；`continuity` persona `485fccd1` 41/43 中止 → `8e403d5c` **59/61 跑完整趟**（含 600 s 沉默 + 两次断连重连；两条红 = 三个地名的 nearby 列表逐字相同，已修 `31e8fefc` 并在 `cf1d0f96` 上 CD9/RS7 ×3 **6/6**：planner 填 `keyword=万象城` 那趟列表照样锚在万象城）；W19 视窗单变量 2 对 4/8 vs 4 对 7/8（仪器读数，不改缺省）。上一轮：**全量固定口径（`e3528ee7` 工作树，`TZ=UTC0` `-n 8`）：8593 passed / 0 failed / 32 skipped / 11 warnings，232 s**；P2 收尾新增 `test_goal_ledger` 15、`test_contrast_pairs` 7、`test_question_shape` +17；九处变异各自判红；真栈 A/B（45 句 × 2，MiniMax-M3）84/90 vs 81/90、工具通道 82 → 54 ⇒ `PLANNER_GOALS` 缺省 off；探针 contrast 10/10、RS6/EC1/CL1 6/6。批 4 首个 release 的读数：**全量固定口径（`b0b39c7d` 工作树，`TZ=UTC0` `-n 8`）：8548 passed / 0 failed / 32 skipped / 11 warnings，237 s**（那条 OS-lock 用例本趟绿，隔离债未修）；分层新增 `test_engine_outcome` 16、`test_outcome` 6、`test_manifest_effect` 6、task_frame +3、route_hints +3、obs_spans +5、collector +3；dashboard vitest 2 + tsc 0；`go build/vet ./gateway/...` 零错；四门禁 + smoke_edge 13/13；九处变异各自判红。真栈（`minimax:MiniMax-M3`，`--repeat 2`）：RS6 纯偏好致谢 2/2、EC1 零动作零声称 2/2；collector `turns.outcome` 上线即有读数。上一轮：**全量固定口径（`4bfebdd1`，`TZ=UTC0` `-n 8`）：8485 passed / 1 failed / 32 skipped**——那 1 红仍是 `test_e2e_stack_lease` 的 OS-lock 用例（串行 61/61 绿，隔离债未修）；后两个 navigation 提交只跑 navigation 197 / reroute 29。分层新增：`test_engine_clarify_pending` 14、`test_engine_task_frame` 13、reroute +3；mobile 111 suites / 1143 + tsc/eslint 0；hmi 338 + tsc 零新错；四门禁 + smoke_edge 全过；五处变异各自判红（label/send_text 通道、进展闸、correct 闸、读写优先、时刻守卫）。真栈（`minimax:MiniMax-M3`）：CL1 澄清续接 2/3 ×2（差的那次是 planner 没出澄清卡）、TF1 改口 `4bfebdd1` 0/3 → `fb98150d` 3/3（07:30）→ `88a89456` **3/3「到达时限改为19:30」**。上一轮读数：**全量固定口径（`0d4d795e`，`TZ=UTC0` `-n 8`）：8453 passed / 0 failed / 31 skipped / 11 warnings，249s**（`1f5dfd27` + 探针改动那趟 8437 / 0 / 31；上一基线 8397 / 1 / 31 那条 OS-lock 用例本次两趟都绿，隔离债未修）。后续 `40edaad1` / `49dacc1f` / `e8e6c949` 三个小提交只跑 cloud 目录（1386–1388 passed）。分层：`test_engine_confirm` 58、`test_context_budget_fidelity` 15、`test_focus_lifecycle` 8、`test_candidate_sets` 59、runtime 28、nearby 114、planning +3 / loop +1；四门禁 + smoke_edge 13 全过；八处变异各自判红（问句否决 / 歧义退回最新 / 预算软化 / 撤销通道 / 时态框架 / 判重键忽略 slots / 合并键无签名 / 不过期短时引用）。真栈（`minimax:MiniMax-M3`）：`confirm` 组 CF1/3/4/5/7/9 ×2 **12/12**（`744fb655`）、CF7 ×2 **2/2** 走确定性出口（`0d4d795e`）、RS4/RS5 ×2 **4/4**（约束改口撤销端到端）、CD8 ×3 **3/3**（`e8e6c949`，同能力两批共存、点名旧批绑旧批）、W09 沉默 660 s（含一次发布断连重连）后「第二家评分多少」仍由候选台账确定性答出第 1 轮第 2 项。历史读数：**全量固定口径（`82c8c7fa` = `ca4bf370` + docs，`TZ=UTC0` `-n 8`）：8397 passed / 1 failed / 31 skipped / 12 warnings，272s。** 那 1 红 `test_e2e_stack_lease.py::test_parallel_owner_cleanup_failure_overrides_both_passes`：同文件串行 61 passed / 2 skipped、单独 `-n 8` 三趟各红 2–3 条不同用例——parallel-owner 那组用例拿**真仓库根**当 `repo_root` 去抢同一把仓库级身份 OS 锁（`identity_lock_path`），xdist 多 worker 同跑必撞（§6.1 点名的「OS lock 污染读数」形态；测试隔离债，未修）。31 skipped = 32 基线 − 1（Docker Desktop 此刻在跑）。12 warnings = Starlette ×8 + WordPiece ×2 + audioop ×1 + 一条 stack-lease 子进程读线程 GBK 解码告警。分层：`test_route_hints` 37、`eval_route_hints` 119/119、范例 328 条契约 OK、四门禁 + smoke_edge 13、runtime 13、`test_safety_focus` 25、`test_engine_input_facts` 4、cloud 1345、agents+registry+security 2103、mobile `voiceTts` 22 + tsc/eslint 0；十处变异各自判红。真栈（`minimax:MiniMax-M3`，collector 逐 trace）：T24「红色机油灯亮了还能继续开吗」干净会话 3/3 `safety.driving_advice` + deterministic 卡、对照「机油灯亮了怎么办」仍 `manual.query`、safety 组 `--repeat 3` 15/15（`1eb25a70`）；T47 persona 同形序列 ×3 解除轮零动作、「规划去广州路上的补能」3/3 `charging.plan`（`0d414816`）；带 `dest_choice` 挂起的六轮序列 ×4 解除句 0/4 被吞、落技术失败出口那趟也清掉了告警（`ca4bf370`）。逐个 release 的读数见收口页 |
| manual-rag | 整本范围生产证据仍绑定历史 `9a3b6f2f08657464c5049a5abf8f6e989e398bce`：独立章节187/187、视觉35/35、雨刮/背宝剑各3/3。本次未重跑整本，详情见 QA 交接页 §4.6 |
| 自然问法边界 | 原36题完整真栈只在 `434a046` 闭合；`9a3b6f2f` 当轮有7条旧表述被安全预检拒绝。本次未宣称新 release 36/36 |
| 证据边界 | 评审四轮待办批一：RS42 / RS43 的读数绑定 **`ee94c595`**（`.artifacts/probe-round4-backlog1-ee94c595.json`），首发 `684bfeec` 的新探针与回归 9 例绑定 `684bfeec`（`.artifacts/probe-round4-backlog1-new-684bfeec.json`、`…-regress-684bfeec.json`）；回落规则「26 次里约 24 次抢救计划对」是 collector规划 span 截至 2026-09-25 的只读查询（4498 次规划），「约」= 人工逐条读判；A/B 的 275 条是端侧规则对语料的离线重算，不是真栈复跑。评审四轮 §5.5：a 的真栈读数绑定 **`74634314`**（`.artifacts/probe-round4-confirm-speech-74634314.json`），b 的绑定 **`4e8da9dd`**（`.artifacts/probe-round4-edge-authority-4e8da9dd.json`）；b 的「266 轮历史」是 collector 截至 2026-09-25 的留存（只读检索，对象词 6 个）；方向翻转在真栈上造不出来（伪造键在车端入口剥掉），「换成车端那条」只有引擎端到端用例证明。评审四轮 §7 两条接着修：修后读数绑定 **`5ca289c7`**（`.artifacts/probe-round4-drift-focus-5ca289c7.json`、`…-regression-5ca289c7.json`）；修前形态引自 `4438ea6b` 那一趟 adaptive（§5.4.5）；T2 续接这条路真栈只取到 1 趟（规划形态方差），「追旧值的步被丢掉」由端到端用例证明；RS34 那趟方向翻转只在 `5ca289c7` 回归里取到一次。评审四轮 RS39 与 R4-07 第二步：修前 RS39 的两种形态、补槽轮 3.3 s 与聚合改写绑定 **`f535c654`**（`.artifacts/probe-round4-admission-step2-f535c654.json`）；改派 / 聚合直出修后绑定 **`dabc2e2d`**（`.artifacts/probe-round4-rs39-escalate-dabc2e2d.json`、`…-rs39-variants-dabc2e2d.json`、`…-rs39-x6-dabc2e2d.json`）；NEED_SLOT 修后读数、单步 search_poi 改派的真栈样本与两条范围外缺陷绑定 **`4438ea6b`**（`.artifacts/probe-round4-rs39-*-4438ea6b.json`、`…-rs35-36-4438ea6b.json`）——规划形态有方差，42 趟里单步 search_poi 只取到 2 趟（一趟改派、一趟走 T2 循环）；离线补槽 / 澄清 ×3 绑定当时的 MiniMax-M3（`.artifacts/admission-slots-20260925.*`）；语音澄清选择没有真栈用例（澄清卡本身有模型方差）。评审四轮 R4-07 轻量受话判定：离线 A/B 绑定当时的 MiniMax-M3 与 collector 截至 2026-09-25 的 53 条语音轮（`.artifacts/admission-ab-light-vs-planner-20260925.*`，本机直连厂商）；修后真栈与时延绑定 **`4b16d846`**（`.artifacts/probe-round4-admission-light-4b16d846.json`），未命中缓存的样本只有 3 个。评审四轮 R4-07 第一步：修前 RS38 与时延基线绑定 **`266ed937`**，修后 15/15 与时延绑定 **`6e64b767`**（`.artifacts/probe-round4-admission-*`）；真栈只证明受话的真指令不被误拒，「真的非受话」那一支由单测与变异证明（真栈造不稳）。评审四轮：修前基线 RS32–RS35 + S2S 绑定 **`ecbeed28`**（代码同冻结 `42596fd3`），RS36 / RS37 绑定 **`bda5af71`**；批 A 修后绑定 **`b48d76d7`**，批 B 修后绑定 **`bda5af71`**，批 C + E 修后绑定 **`2f8f92be`**，E 追加修后绑定 **`8528df0b`** / **`976f174c`** / **`a7e664f3`** / **`266ed937`**（`.artifacts/probe-round4-*`、`.artifacts/s2s-escalate-*`）；S2S 取样每轮新会话，只覆盖会话起始注入摘要，**带真实历史的 20 轮重建没有真栈证据**；R4-07 只有源码路径表（设计文档 §5.1），没有真实音频证据。评审三轮追加批 N：技术失败 132 轮 / 空数组残片 35 个 trace / 抢救回落 121 次的量表绑定 collector 截至 `b7a49361` 在线时的留存；修前四句 ×8 绑定 **`b7a49361`**；修后四句 ×8、规范化事件与 RS28 那趟红绑定 **`f14d1e43`**；回归 36/36 绑定 **`ecbeed28`**（`.artifacts/ack-probe-nbefore*` / `nafter*`、`.artifacts/probe-round3-batchN-*.json`）。评审三轮追加批 M：修前 M-1 ×1 与单轮 ×6 绑定 **`82781215`**；首发 M-1 ×3 与两轮形态 ×2 绑定 **`6b7335c0`**；终版 M-1 ×1、两轮形态 ×2 与回归 36/36 绑定 **`b7a49361`**（`.artifacts/k-probe-m*`、`.artifacts/ack-probe-m2before-*`、`.artifacts/probe-round3-batchM-b7a49361.json`）；「当前高速路段」修前读数引自批 K 的追问（`36a92009` / `62414223`，24 趟里 6 趟走进目的地分支）。评审三轮追加批 L：修前 ×5 绑定 **`62414223`**、修后 ×10 + 对照 ×2 + 回归 36/36 绑定 **`82781215`**（`.artifacts/ack-probe-lbefore-*` / `lafter-*` / `lctrl-*`、`.artifacts/probe-round3-batchL-82781215.json`）；误伤量表（426 轮声明写步、27 轮规范问法）绑定 collector 截至 `62414223` 在线时的留存；新闸在真栈上未被触发（修后 10 趟模型没往场景能力走），证据是离线按真栈原样复现。追加批 K：修前 K-2 ×1 绑定 **`8cee1699`**；首发 K-1 ×5 / K-2 ×2 绑定 **`36a92009`**；终版 K-1 ×5 / K-2 ×1 / 回归 36/36 绑定 **`62414223`**（`.artifacts/k-probe-*`、`.artifacts/probe-round3-batchK-62414223.json`）。评审三轮追加批 J：修前（×3）绑定 **`fc1f5dde`**、修后（×5）与回归 35/36 绑定 **`8cee1699`**（`.artifacts/ack-probe-j1a-*` / `j1b-*` / `j2-*`、`.artifacts/probe-round3-batchJ-8cee1699.json`）。Android N-04 / N-01：握手修前 3/3 1008 与英文问时间修前 2/5 原样报错绑定 **`b6afd59b`**、修后 3/3 `ready` + 冒充 2/2 1008 + 0/5 绑定 **`fc1f5dde`**（`.artifacts/s2s-handshake-*.json`、`.artifacts/ack-probe-n01*.json`）；S2S 只验到握手，完整语音轮次未在真机重走。评审三轮追加批 I：「可以，已为您执行」×30（零动作、新闸拦 1、G-3 拦 2）+ 回归 21/21 绑定 **`b6afd59b`**（`.artifacts/ack-probe-ack-1790180301.json`、`.artifacts/probe-round3-batchI-b6afd59b.json`）；误伤量表与历史回放（417 轮写步、3325 轮纯应答）绑定 collector 截至 `a4bb73bf` 的留存（`.artifacts/cloud-write-history.json`）。评审三轮追加批 H：RS31 ×3 3/3 + 回归 18/18（RS21 一趟为探针误判，判据修正后重判 / 重跑 3/3） 绑定 **`a4bb73bf`**（`.artifacts/probe-round3-batchH-a4bb73bf.json`；RS21 判据修正后重跑 `.artifacts/probe-round3-batchH-rs21-a4bb73bf.json`，判据来自本地工作树、被测服务端 = `a4bb73bf`）；RS31 修前 0/1 绑定 **`31943d47`**（`.artifacts/probe-round3-batchH-before-31943d47.json`）。评审三轮追加批 G：修后 24 趟（失败话术 0、动作 0）+ 回归 18/18 绑定 **`31943d47`**（`.artifacts/g-probe-after-1790173107.json`、`.artifacts/probe-round3-batchG-31943d47.json`）；修前 24 趟（失败话术 9、编造执行 `media.stop` 1）绑定 **`2c38446d`**（`.artifacts/g-probe-before-1790172112.json`）；纠正话术 A/B（生产 planner 容器内只规划不执行，12 句 × 8 × 2 臂）绑定 **`2c38446d`** 镜像（`.artifacts/eina-ab-1790171273.json`）。评审三轮追加批 F：18/18 绑定 **`2c38446d`**（`.artifacts/probe-round3-batchF-2c38446d.json`）；知识 A/B 四组（A `7b0ee8de` / B `bb609ef5` / C `01564cc2` / 终态 `07e9ea5c`，`.artifacts/kn-ab-*.json`）——B 臂新范例一次都没被检回、K4 未扭动 ⇒ F2 撤回；澄清卡 0 / 3 / 0 / 2 是方差；RS21「可以，已为您执行」一趟被重试逼出关双闪绑定 **`a59b1621`**（与本批无关，§9.3）。评审三轮追加批 E：RS21 ×3 3/3 + RS28 ×3 3/3 绑定 **`f9bec423`**（`.artifacts/probe-round3-batchE-f9bec423.json`）——RS28 判的是零写操作与「原因句不整句等于电量模板」，话术质量逐句见设计文档 §8.1.1（推荐 2/3 规划失败 + 1/3 编号错位到瑞幸下单、「空调有什么模式」3/3 落场景列表）。评审三轮收口：RS22 ×3 3/3 + RS27 ×3 0/3（旧判据，三条红都在规划层：只规划会议 + 清理句落取消闸 / 事件订阅被改写成查天气 + 带伞、T2 三批重复建提醒 / `unresolved_object`）绑定 **`1255f7b2`**（`.artifacts/probe-round3-reminder-1255f7b2.json`）；RS27 收紧判据 ×3 3/3 绑定 **`1255f7b2`**（`.artifacts/probe-round3-rs27-strict-1255f7b2.json`，判据来自本地工作树，被测服务端 = `1255f7b2`）；R3-04 闸时延 n=24 绑定 **`1255f7b2`**（`.artifacts/gate-latency-*.json`）。评审三轮批 D：RS21 1/3、RS22 3/3、RS27 2/3（判据漏判）绑定 **`c99a9a74`**（`.artifacts/probe-round3-batchD-c99a9a74.json`）。评审三轮批 C：RS27 / RS22 / RS13 / RS14 ×3 = 7/12 绑定 **`5c729fbc`**（artifact `.artifacts/probe-round3-batchC-5c729fbc.json`）——五条红里四条在初次规划（澄清卡 / `unresolved_object`）、一条是 RS27 逼出的 reminder 执行层缺陷（§4.2），R3-03 的再规划路径这组句子走不到，确定性证据是离线用例；R3-06 在真栈 trace 上可见（`t2.iter local_ids`）。评审三轮批 B：RS26 / RS7 / RS24 / RS16 ×3 12/12 绑定 **`926d4c17`**（artifact `.artifacts/probe-round3-batchB-926d4c17.json`）；R3-05 三条新出口在云端无触发场景，证据只有离线用例与变异。评审三轮批 A：RS25 ×3 3/3 绑定 **`416e47bc`**（artifact `.artifacts/probe-round3-batchA-416e47bc.json`）。评审二轮批 D（独立验证，绑 **`86c43998`**）：长会话留出集 `continuity` **70/71**（唯一红 T63「接孩子后去万象城」= family 域落域方差，**不改 gold**）；L1 gate **107/117**、L2 **3/4**（诊断读数，`--repeat 1`）——与批 8 的 111/117 / 4/4 的差**逐条查过是采样方差**：六条 new-only 的红样本 `raw_intents` 全空或非法 ref，重跑 `--repeat 3` 32/36，且三个新判据对那几句原话全判 False；`forbidden_route_rate` 0/117、validator 后逃逸 0/117 与批 8 一致。评审二轮批 C：RS23 / RS24 / RS7 各 3/3 绑定 **`86c43998`**（artifact `.artifacts/probe-round2-batchC-86c43998.json`）；**R8 的三条新出口在云端没有触发场景**（Redis 正常），证据只有离线 10 条 + 三处变异判红。评审二轮批 B：RS21 / EC1 / RS13 各 3/3 绑定 **`8af9b8bd`**（artifact `.artifacts/probe-round2-batchB-8af9b8bd.json`）；RS11 / RS13 3/3 与 RS22 改判据后 3/3 绑定 **`06ad7ae4`**（`…-06ad7ae4.json` / `…-06ad7ae4-rs22.json`）。评审二轮批 A：RS15–RS20 ×3 18/18 与 R3 三来源受话读数绑定 **`d0170329`**（artifact `.artifacts/probe-round2-batchA-d0170329.json`，gitignore）。批 7 追加：continuity 三趟 `c4c1186d` 64/71（中止于导航清理）/ `93564e19` 69/71 / `c98fb087` 70/71（唯一红 SF4 是尺子词表缺「停下」，已补）——CONT-REFRESH 三趟 6/6、CONT-ENV 第二趟起 5/5；两条 nearby 命名修各绑逼出它的那趟；发布证据绑各自 SHA。批 7：`RS12,RS10 ×3` 6/6 + 窗口装置 run `0921c`（端侧 `battery.query` 0/6）绑定 **`3f9b18e4`**；`RS12,RS11 ×3` 6/6（路名态势真答）+ `RS13 ×3` 3/3（① 活体：`planner.loop`「batch is all unsupported refusals; not replanning」）绑定 **`1e892f1b`**；`RS13 ×2 + RS11 ×2`（事件短语「有堵车」）绑定 **`551dd66c`**；`RS13 ×3` 3/3 零猜测绑定 **`74f334f4`**；生产 `turns.outcome` 89 轮读数绑定 `3f9b18e4` 起。缺省视窗 4 对的部署 / status / verify 与不 pin 复验（run `0921b`）绑定 **`eb1dd6bd`**；问句闸修复的部署 / status / verify 与三格复验（run `0921a`）绑定 **`eb55e502`**；W19-c 视窗实验（run `0920b`，96 session）绑定 **`b0d79dbe`**；批 6 的部署/status/verify 绑定 **`cc331315`**（RS10 ×3 + RS9 A 臂 ×3：W16-b 活体在 RS10 第 3 趟）与 **`eb87b571`**（RS11/RS9/RS10/CT2 ×3 11/12：订阅句 reminder 首意图 8/9 + 9/9 + holdout 3/3）；批 5 的部署/status/verify 绑定 **`485fccd1`**（RS7/RS8/RS9/CD9 ×2、continuity 首趟）、**`8e403d5c`**（continuity 复跑、W19 A/B）与 **`cf1d0f96`**（nearby 锚定：CD9/RS7 ×3 6/6）；P2 收尾的部署/status/verify 与 contrast / RS6 / EC1 / CL1 真栈绑定 **`9ebaa5c3`**（A/B 的 B 臂绑 `e3528ee7`、A 臂绑 `e9044daf`）；批 4 首个 release 的 RS6/EC1 真栈绑定 **`e9044daf`**；T24 真栈复验绑定 **`1eb25a70`**；之前各批：部署/status/verify 曾绑 **`f1a99063`**（09-16 那批的读数绑 `97825faa`）；MiniMax ASR 的三面真栈核对绑 `40ccb9b6`；下面的语音采纳矩阵读数绑 **`9ced633b`**——该 release 发布后只读复跑 §4 矩阵 24 轮（`minimax:MiniMax-M3`，零动作零挂起、车态不变）：播报句 `本台记者报道…` ptt 3/3、voice_followup 3/3（此前 1/3、3/3），首轮失败原话 `欢迎收听今天的节目，本台记者为您报道新闻。` 端侧不再执行 media.play、云侧拒识 5/6（1 次模型规划成新闻摘要，按「模型真拆出步不动」放行），乘客句 1/6 仍归模型（无播报语域）；上一 release 的读数：直连 + 新 release 的 PC 探针与真机读数见评审 §9.6/§9.7（握手 1.4–3s → 0.6–0.8s、TTS 音频不再欠速、深调研首段文本 13s → 5.9s、对话页空闲 30s 后 150% → 9.5%）；OPPO 常驻包见 §9.7。路线几何的真栈对照仍绑 `e38cd75`；语音采纳的证据仍绑 `f8fd151` / OPPO 包 `f8fd15152`（APK SHA-256 `6fc093a9…66103`）。来源与拒识出口已贯通，但真实 MiniMax-M3 文字注入续测：正常请求6/6、背景静默拒识6/12；另有首轮播报句误命中端侧 `media.play`，已停止探针造成的模拟播放。**拒识仍未验收，声学未验**；[逐条结果](docs/reviews/2026-09-11-voice-input-acceptance-live-findings.md)。手册整本与旧 Android 验收继续保留各自历史 SHA |

`b3a2aed` 是 v2 首次生产 release；`434a046`、`7b594f37`、`805711cf` 是后续生产历史；
`a406e22` / `423ed23` 是 v1 发布历史。
当前 release 不借用 `a729b98` 的 Cloud Planner、Planner+Info 或新闻专项数字。

### 4.1 QA 状态

- 探索式 QA 的 Q1–Q13、MiniMax C1–C16、M1–M6、B1–B7 和 I-024 已完成；
- 安全专项在 `e9fa602` 上 5 例、15/15 PASS；
- 完整 information persona 在 `e9fa602` 上 57/59，提醒/导航清理、零挂起与 release 连续均证明；
- `limit:null → "None" → ValueError` 已由 `a729b98` 修复，新闻 3 个干净会话零 internal error；
- manual-rag 已在`9a3b6f2f`闭合整本范围：独立章节187/187、视觉35/35、点名泛化问法各3/3；
- **QA 仍非全绿**。剩余活项只看 QA 当前交接页 §5，不从历史批次表找。

当前主要活项（2026-09-19 逐条收口，过程见 [QA 轮剩余活项收口](docs/design/2026-09-19-qa-residual-closeout.md)）：

| 活项 | 性质 | 状态 / 入口 |
|---|---|---|
| 安全问句偶尔落 `info.search`（T24） | manifest 没声明 Agent 早已实现的告警续驾能力 ⇒ planner 看不见 | **已修并发布 `1eb25a70`，真栈复验 3/3 `safety.driving_advice` + deterministic 卡、safety 组 15/15**（`agents/road_safety/manifest.yaml`）；QA 交接页 §5 |
| safety focus 持续阻断后续 charging plan（T47） | 安全状态解除时机的产品裁决 | **已裁 A、已发布 `0d414816`、真栈 T47 格 3/3 `charging.plan`**（`alert_resolved` 一份判据四个消费方）；`dest_choice` 挂起吞安全陈述已修并发布 `b342e3bb`；五条提前出口跳过输入侧登记的漏点 `_register_input_facts` 已发布 `ca4bf370`（真栈：解除句落技术失败出口那一趟不再「未解除」）；收口页 §3.3–3.5 |
| MiniMax TTS RPM | 外部配额 | **已由 `a09c73a`（09-06）闭合并销账**；混合意图轮盲听独立开 |
| barge-in 残帧 | 全双工上「零字节」不可判 | **裁决完**：客户端丢弃（已在）、服务端限 1s 在途窗口（探针判据已改） |
| gRPC RuntimeWarning | test-only fixture 债务 | **已修**（trip 测试用不可达 LLM 替身）；第三方弃用告警留档不藏 |

### 4.2 当前活项与其他可接工作

| 主题 | 启动条件 / 入口 |
|---|---|
| **对话评审第四轮**（回复归属 / 步骤依据 / 事实来源，评审冻结 `42596fd3`）落地 | [评审原文](docs/reviews/2026-09-24-cockpit_conversation_review_round_4.md) + [逐条重证与分批落地](docs/design/2026-09-24-conversation-review-round4-remediation.md)（2026-09-24 起唯一入口）：R4-01～R4-06 本地逐条复现后分批 A / B / C 落地，另加批 E（批 A / B / C 真栈读出来的导航接地、端侧位置抽取、地标宽松匹配与猜出来的名胜、裸序数写步与确定性澄清），生产 `b48d76d7` → `bda5af71` → `2f8f92be` → `8528df0b` → `976f174c` → `a7e664f3` → **`266ed937`**。R4-07 **第一步已上线**（用户裁决：先给风险最高的确认 / 焦点省略两条出口接受话判定，`6e64b767`，§5.2）；判定随后按用户裁决换成轻量调用（`4b16d846`，§5.3：离线 A/B 过线后切换，语音「确认」约 1 s、命中缓存约 20 ms）；第二步按用户「按你建议的来」上线（`f535c654`，§5.4：澄清选择 / 补槽也接，取消刻意不接——误拒一句真「取消」会把危险操作的挂起留下）；它的真栈逼出的追加修复 `dabc2e2d`（search_poi「没找到」改派 navigate_to、多步里只有一步有话就直出）→ **`4438ea6b`**（NEED_SLOT 必须声明缺哪个槽 + 静态契约）；它的真栈读出的两条（T2 循环续接后目标漂移、挂起轮丢掉已执行的路线会话）按用户「接着修」改完 → **`5ca289c7`**；端侧准入三态这一轮不做（要真机真实音频）；它的回归读出的写操作方向翻转按用户「先 a 再 b」修完：`74634314`（确认问句念出动作）→ `4e8da9dd`（方向以车端确定性解析为准，§5.5）；§7 待办里能直接修的按用户「按你建议能直接修的接着处理」逐批做：位置词表合一 `2c0c6663`、周边检索的中心接地 `684bfeec` → **`ee94c595`**（§5.6）。未做且有触发条件：批 C 第二段会话事实快照（§4.1）；客户端整句锚定接不住「第二个。」、「右侧后视镜加热」落成后视镜展开、接人远距闸「就去这个」无人消费、RS35 问的那半落到场景清单；探针用户轨迹记忆被修前的错误导航污染（删记忆需单独授权）、规划器把内部判断写进澄清问句（§7 / §8.7） |
| **对话评审第三轮**（确认授权 / 撤销补丁 / unsupported 身份 / 流式分块 / 挂起删除 / T2 步骤身份，评审冻结 `7e41fcf2`）落地 | [评审原文](docs/reviews/2026-09-23-cockpit_conversation_review_round_3.md) + [逐条重证与分批落地](docs/design/2026-09-23-conversation-review-round3-remediation.md)（2026-09-23 起唯一入口）：R3-01–R3-06 对 HEAD 逐条复算全部成立（R3-01 B 还核出对象不同也授权：「确认打开车窗」把挂着的「打开后备箱」确认掉）。**批 A（R3-01，用户授权提交 / 推送 / 部署 / 真栈）已发布 `416e47bc`、批 B（R3-02 + R3-05）已发布 `926d4c17`、批 C（R3-03 + R3-06）已发布 `5c729fbc`、批 D（R3-04）已发布 `c99a9a74`、reminder 拆步修正第二版 `1255f7b2`（当前生产）——六条全部落地**（§2 / §2.1 / §2.1.1）：语气词不能独立授权；点名确认拆召回与裁决，裁决只认挂起那一刻服务端生成的已校验步骤摘要（能力描述 + 槽值，与确认卡同一句），数字整串比、单字不作数，端侧解析只做否决、客户端带来的 `_edge_nlu` 在端侧入口剥掉；点名取消在多条召回里按覆盖度取唯一最高；真栈 RS25 3/3。代价：点名措辞与确认卡不同（「确认解锁」对「打开车门锁」）改为确定性复述而不是直接执行。批 B（§3 / §3.1 / §3.1.1）：约束补丁带墓碑交给跨轮合并（同句 SET + DELETE 撤掉的一维不复活，真栈 RS26 3/3）；挂起删除四态 + 进程内墓碑（删不掉的取消说「不会执行了」而非「已取消」，确认 / 续接复活不了）、关旧开新一次整表写、澄清挂起存不下不再说成隐私清除——这三条云端无触发场景，证据是离线用例。批 C（§4 / §4.1 / §4.1.1）：被拒诉求按原话来源分句认同一性（共用「深圳」的会议提醒照做、空槽读能力不再被误拦）、T2 再规划批换运行时步骤 ID（真栈 trace `t1-r1<r1`）；真栈 RS27 逼出 reminder 拆步后仍按整句补标题 / 借时间（§4.2，随批 D 发布）。批 D（§5 / §5.1 / §5.1.1）：流式闸只由文本决定释放（≤ 160 字整句判、超长句只扣最后 15 个非空白字的保留区，585 次随机切包逐字等于一次喂入）、D0 / T2 发布即权威（final = 闸实际放出的那份）；真栈闸首次放行等待 n=24 p50 247 ms / p90 598 ms / 最大 830 ms（首音推迟不超过它）。reminder 拆步修正第二版（§4.2）：`c99a9a74` 真栈又逼出两种 planner 写法，改按最长公共子串认分句、拆开后事件分句没给时间就拒绝；RS27 列表轮判据收紧后 ×3 3/3（两趟正好是出错的两种写法）。留下（仅记录，设计文档 §7）：端侧「推荐…电影」执行视频播放、「续航为什么会下降」答电量、road-safety 疲劳问句答天气、假设句「如果你能…」走 T2 后 chitchat 超时重跑并把失败念出来、没说目的地的「从欢乐海岸出发，不走高速」2/3 被执行成导航（起终点相同 / 编出天安门）、RS27 上 planner 把事件订阅改写成查天气 + 带伞提醒；闸时延要再压的可选路（保留区放行用于所有句子）未做。**追加批 F（用户「接着做」，已发布 `2c38446d`，设计文档 §9）**：F-1 能力编号笔误只朝只回答能力归位（真栈 `cap_0107>cap_0007`）；F-2 求信息的请求规划失败 / 不受话 / 带澄清口吻的空计划都由兜底谈话作答；F-4 原因问句与名词短语列举问不再被执行（「空调为什么不制冷」开空调、「车窗有哪些开启方式」开车窗都已堵上）；F2 规划知识（范例 + `scene.list` 描述）真栈 A/B 无收益，已撤回。**追加批 G（用户「接着修」，已发布 `31943d47`，设计文档 §10）**：`explicit_input_not_addressed` 历史 80 次触发里 40 次是模型两次都说没事可做却报技术失败（「你好，请只回复一句问候」），2 次编出车控 / 提醒动作——第二轮零步且原话无操作证据时由兜底谈话作答，被催出来的写车控 / 需确认步在原话无操作证据时整份作废；纠正话术改写 A/B 证伪未改（「我有点冷」救回 8/8 → 5/8）。**追加批 I（用户「做」，已发布 `b6afd59b`，设计文档 §12）**：纯应答长不出写步——原方案「槽值没有落点就拦」先量误伤（417 轮历史写步里会误拦「第二个」选候选 ×4、「换条路走」×5、记忆补出的值和无槽的「取消导航」）后收窄成「原话是纯应答 + 上一轮助手没提问」；历史回放只命中那 3 轮编造，真栈 ×30 新闸拦下 `volume.mute`、G-3 拦下 `hvac.inc` / `volume.dec`。仍记录（§12.2）：只说起点时补能计划被补出目的地、被编出「取消导航」（各 1 例，无动作）。**追加批 N（用户「继续推进」，已发布 `ecbeed28`，设计文档 §17）**：空数组残片按零步认、查询 + 疑问框架是求信息的请求；回归揪出并当批修掉「求信息的请求被抢救计划编出导航、零步重试却被当成重试失败」；「首轮零步 + 之后坏 ⇒ 一律闲聊」量过后不做（会把裸地名改成闲聊）；回归 36/36。**追加批 M（用户「开始做」，已发布 `b7a49361`，设计文档 §16）**：驾驶员状态的会话告警答状态话术（修前车辆故障口吻 3/3）；正在走的路不当目的地、失败查询不算数据、没数据不让模型综合（修前「开启双闪」）；首发漏两种写法当轮改写；真栈 M-1 12/12、被填目的地的追问终版 2/2；回归 36/36。**追加批 L（同一指令的第 3 条，已发布 `82781215`，设计文档 §15）**：原方案「问句一律不执行声明写」量出来误伤 13 / 命中 0，不做；只对**问规范 / 注意事项 / 条件、每个分句都是提问**的句子把问句闸扩到声明为写的云侧步（命中 2 / 误伤 0），问句判据补「注意些什么」；回归 36/36。**待裁决（§15.4）**：带逗号的碎片句不接管、能力侧区分「改状态的写」与「为回答建的任务」。**追加批 K（用户「你按你建议的顺序做」，已发布 `62414223`，设计文档 §14）**：告警在场时整句只是在拒绝安全建议 ⇒ 兜底谈话作答（不管规划产出了什么；首发只接澄清卡，真栈又见重试催出查天气、催坏成技术失败两种，改写）；`runtime.safety_signal` 拆「提到这类风险」与「此刻自述」——否定不算、话题 / 假设句答 `topic_speech` 不登记告警；真栈拒绝轮 10/10、否定 / 话题 / 假设登记 0/24、回归 36/36。**待裁决（§14.2）**：问句摸到声明为写的云侧能力（已由批 L 收窄处理）、「无动作」重试把第二轮催坏的残余、road-safety 会话告警话术是车辆故障口吻、探针身份的记忆回流。**追加批 J（用户「继续修接下来的」，已发布 `8cee1699`，设计文档 §13）**：road-safety 知识型问句按问题答、不报天气；只回答的计划不再被「如果 / 若」升成多批。**待裁决（§13.2）**：状态判据把「疲劳驾驶」这类话题当自述、拒绝安全建议被问成澄清卡（这两条已由批 K 修）、问句摸到声明为写的云侧能力（「开长途前要注意些什么」→ `scene.activate`）、RS27 多批重复建提醒、英文问时间。**追加批 H（用户「接着修」，已发布 `a4bb73bf`，设计文档 §11）**：「有几个 / 能放几个 / 分几个」计数问进问句判据（句法头 + 几 + 量词，另起分句带操作动词仍是指令），端侧 13 句不再被执行；真栈 RS31 修前 0/1 → 修后 3/3。**待裁决（§9.3）**：「空调有什么模式」的设备模式范例需要 8 条人审对照用例；手册库缺项（RS29 / RS30 12/18「没查到」）；JSON 语法错的修补与否。**追加批 E（用户「现在开始」，已发布 `f9bec423`，设计文档 §8 / §8.1 / §8.1.1）**：前两条端侧与无目的地导航已修——问句判据补列举问法（只看列举词之后的操作动词，「空调」的「调」不再把列举问判成指令），视频「推荐」让路，端侧出口第五维原因问句让路，navigation `_unspoken_destination`（`navigate_to` / `estimate` 共用，会话已确立的目的地放行，追问带 `missing_slots`）；真栈 RS21 3/3、RS28 3/3。**待裁决（§8.2）**：推荐 / 列举问交到云端后 planner 给空计划、兜底谈话被 F09 判成技术失败、能力编号错位（`cap_0107` 带着 chitchat 的槽落成瑞幸下单）照常派发；方式问法按字配对（「空调为什么不制冷」执行 `hvac.on`）；road-safety 疲劳问句答天气、假设句 T2 超时两条仍只记录 |
| **对话评审第二轮**（落域 / 拒识 / 上下文 / 长会话，评审冻结 `f827ebdd`）落地 | [评审原文](docs/reviews/2026-09-22-cockpit_conversation_review_round_2.md) + [逐条重证与分批落地](docs/design/2026-09-22-conversation-review-round2-remediation.md)（2026-09-22 晚起唯一入口）：R1–R9 对 HEAD 逐条重证全部成立（R1 还核出「好的，确认吧」反向落空、R2 核出端侧「取消刚才打开空调」执行成开空调）。**批 A（R1 / R2 / R3 / R9 + 端侧撤回否决，用户授权提交 / 推送 / 部署 / 真栈）已发布 `d0170329`**（§2 / §2.1 / §2.1.1）：裸确认的语气面只留一份、剥完一个实质字都不剩才是授权；取消先判极性与问句再改状态（`pending_kept` 新出口、问句走 `pending_state`、点名绑定 / 点不到就问）；语音来源的纯偏好陈述先跑一次 planner 只取 `addressed`；`question_shape` 认「怎么把 X 打开」的把-框架、剥「请问」前缀、认句首解释元请求（「温度如何调高」合同不动）；真栈 18/18。**批 B（R4 / R5）已发布 `8af9b8bd` + 真栈追加 `06ad7ae4`**（§3 / §3.1 / §3.1.1）：`ExecutionClaimGate` 句级有界闸让谈话步的「已为您…」在**首次对外释放之前**就被拦下（D0 / T2 流式与 E 路径逐步播报共用一条，流出的 = final 的 = 落库的）；`unsupported` 的终止标记绑定「领域 + 被拒那步的槽值实质」，同域的独立诉求照做、被丢步的下游记 `blocked` 传递阻塞而不是删边变根节点；真栈逼出并修掉 reminder「有没有给时间」问整句的缺陷（另一个诉求的「明天」挡掉诚实拒绝）。**批 C（R6 / R7 / R8）已发布 `86c43998`**（§4 / §4.1 / §4.1.1）：历史裁剪只裁可再生成的助手回答、用户那条要么完整要么整对舍弃（`history_omitted`）；焦点里私有两格按乘员归属、共享车辆状态仍共享（边界见设计文档 §4 表）；挂起存储读写各三态，故障不再被说成「没有」或「正在清除你的数据」、也不再对客户端宣布挂起已关闭。**批 D（独立验证）已做**（§5 / §5.1 / §5.2 / §5.3）：反例集 RS15–RS24 钉成测试（改 id / 换组 / 只留话术判据都会红）；长会话留出集 `continuity` 70/71；L1 107/117 + L2 3/4 诊断读数并逐条归层（无一落在本轮修过的层）；**正式基线仍未重建**——L3 按设计拒绝 cloud 目标，要切 local 全栈（§10.2 的原因一字不变） |
| 对话状态评审（落域 / 拒识 / 上下文 / 长会话）落地 | [评审原文](docs/reviews/2026-09-19-cockpit_conversation_review.md) + [逐条重证与分阶段落地](docs/design/2026-09-19-conversation-review-remediation.md)（2026-09-19 起唯一入口）：F01–F12 对 HEAD 逐条重证全部成立；**批 1（P0 W01–W04 + W05-lite）、批 2（P1 W08/W09）、批 3（P1 W10 / W06 / W07）发布 `88a89456`；批 4（P2 W11–W15，2026-09-20 用户授权提交 / 推送 / 部署 / 真栈）已发布 `e9044daf`**——确认寻址 / 上下文预算硬约束 / 约束改口 / T2 判重键 / 候选批版本 / 焦点两层寿命 / 澄清进挂起表 / `acts` + 活动任务帧 / **终态账本 `cloud.outcome` → collector `turns.outcome`（受话 / 理解 / 支持 / 执行失败分账）** / 纯偏好陈述确定性致谢与「我听到了 X，但没听清要拿它做什么」两条新出口 / 谈话步零动作的执行性声称按句剥掉 / `Capability.effect` 声明（任务帧 kind 读它）/ route_hints 从完整注册表扫、不再靠 hint 拿目录保护。**P2 已收尾（`9ebaa5c3`）**：W12 planner 侧 `goals/covers` 账本已落但真栈 A/B 判定缺省 off（工具通道 82 → 54、账本 0 条漏承接判出）；礼貌尾词只在祈使框架上开口子；`effect` 不接问句闸（132 条云侧写 gold 里 3 条会误拦）；评审 §4 对比对七对离线 + 五对真栈 10/10。**批 5（P3）已发布 `485fccd1` → `8e403d5c` → `cf1d0f96`**（设计文档 §7 / §7.1）：读取三态（memory 服务自报 `degraded` + PG 退避重连；胶囊 `history_state` / `memory_state`；记忆问句读不到 ⇒ `memory_unavailable`、执行史读不到 ⇒「查不到」）、候选墓碑（点名被顶掉的那批 ⇒「已经不在手边」，不悄悄换批）、会话约束读出口（「我今天说过不吃辣吗」零 LLM）、reminder 事件触发诚实拒绝（`unsupported`）、请求级视窗 pin（W19 首个单变量读数 2 对 4/8 vs 4 对 7/8）、`continuity` 长会话 persona（沉默 600 s + 断连重连，`8e403d5c` 59/61 跑完整趟）；顺手修掉 `e9044daf` 起「帮我找家不辣的餐厅」被当纯陈述零搜索的生产缺陷；真栈逼出三条修（补槽下祈使开头判换题 + `task_title` 形状、`pending_missing` 点名关闭、nearby「X 附近」中心锚定 `31e8fefc`，`cf1d0f96` 上 CD9 ×3 证实）。留下的观察：问候句偶尔「需要澄清」。**批 6（2026-09-20 晚，批 5 留项）已发布 `cc331315` → `eb87b571`**（设计文档 §8 / §8.1）：W16-b 步级起点原话（`Step.origin_text` 服务端盖章 + 持久化 + 旧记录回填；判据 `models.step_raw_text`：续接的那一步看本轮原话、其余步看自己的起点原话；三条执行路径 + 槽值保真接线；span `raw_text_from=origin`）——真栈 RS10 第 3 趟活体：路况补槽答完路线后，T2 再规划出的 reminder 步读到「只要有堵车就提醒我」而诚实拒绝（修前「什么时候提醒你」）；W19-b 条件提醒 guide 第四分「持久订阅 ⇒ 只规划 reminder.create」+ 范例两句——A/B（MiniMax-M3）：「只要有堵车就提醒我」reminder 首意图 0/3 → 8/9（单步 unsupported 4/9、并列路况步 4/9、no_plan 1/9）、「之后一旦下雨就通知我」2/3 → 9/9、holdout 3/3、CT2 3/3。留下：T2 loop 收到 `unsupported` 后仍再规划（一次规划出 `reminder.cancel`）；road-safety 零结果空模板话术；**W19-c 已做（用户授权切干净用户，`b0d79dbe` / `69c63e36` / `eb55e502`）**：签名身份车道开通、32 组 × 3 臂视窗实验——T1 出视窗 0/48、在视窗 23/48（4 对 = 6 对，k=4 只有 6 对救回 7/16），闲聊插话时 W07 任务帧会替视窗把上一件任务的槽送到 planner；端侧代词追问劫持 4 组恒定；真栈逼出问句闸补「几档 / 什么条件」（「它有几档」曾执行 `seat.heating.on`）——用户批准清理 ①②（上传目录 5.1 GB + 构建缓存 25.4 GB，可用 31.9 → 55.1 GB）后 `eb55e502` 已部署并复验（g05 / g14 / g29 零动作）。**缺省视窗已抬到 4 对（用户裁决，`eb1dd6bd`，不 pin 真栈复验 `history_exchanges=4` 10/10、槽解出 8/10）**；清理 ④ 按建议保留最近 6 份 release（设计文档 §8.2.2），③ 那 415 个停止容器属于同机的 drone-agent 项目、不碰。**批 7（2026-09-21 下午，批 6 留项，用户授权提交 / 推送 / 部署 / 真栈）已发布 `3f9b18e4` → `1e892f1b` → `551dd66c` → `74f334f4`**（设计文档 §9 / §9.1）：① `_refused=unsupported` 是该诉求的终态——整批 unsupported ⇒ T2 不再 replan、再规划丢同域新步（真栈 RS13：拒绝之后只剩聚合那一次 LLM，修前 replan 出 `reminder.cancel`）；② `safety.road_condition` 接真路况——修前拿「X 路况」搜 POI 播「为您找到 0 个…推荐前三个：。需要导航过去吗？」，现在 navigation 内部意图 `route_traffic`（高德路线逐段 tmcs：「全程约24.2公里、预计42分钟，沿途缓行约2.4公里，其余畅通」；路名态势：「深南大道目前畅通，畅通路段约占95.6%」——城市名优先，adcode 被真栈证否）；③ reminder 事件短语剥前缀 + 连接词形态先求值（holdout「有暴雨预警」、整句「有堵车」）；④ 端侧回指主语（它 / 那它 / 那款…）的查询让路给云端（W19-c 三臂恒定的 `battery.query` 0/6）；聚合器在拿掉拒绝步时告知 LLM 不替那部分诉求猜测。记录不做：T1 出视窗后拿焦点城市 / 范例实体冒充指代物（视窗本体 / 一例不够立闸）、跨用户偏好隔离（`e2e-` 身份不做记忆抽取验不到）。留下：w6 g18 planner 落 chitchat 拿本车续航答「那它的纯电续航呢」（云侧落域方差）。**2026-09-22 追加（用户「1234 都做」）已发布 `267b5d8b` → `1a8ff180` → `c4c1186d` → `93564e19` → `c98fb087`**（设计文档 §9.2）：① 预检问候 smoke 带 `input_source=release_probe`、冷启形态退避重试；顺带修掉验收闸两条把两次发布打成 `ROLLBACK_FAILED` 的判据（同机 drone-agent 的第六条 Serve 条目 / 回环端口 1–2 s 竞态），三轮基础设施批准；③ continuity 加 CONT-REFRESH（同键新版本 = 最新）与 CONT-ENV（`vehicle_env` 改电量再问、`$baseline` 放回），三趟 64/71 → 69/71 → 70/71（唯一红 SF4 是尺子词表缺「停下」，已补），逼出并修掉 nearby 两条候选命名缺陷（planner 丢地名 ⇒ `_candidate_place`；planner 带上一轮 `location` ⇒ 与原话矛盾即陈旧）；④ `test_e2e_stack_lease` 隔离债闭合；② 正式基线**未重建**：L1/L2 要本地 gRPC 网关、L3 要 mock 车道，`target=cloud` 禁本地 Compose、本机 Docker 未起、内存 5.3 GB——步骤与预期见 §9.2。留下（批 7 追加）：两次失败发布的 builds / releases / 镜像残留未清理（诊断候选，需删除授权）。**批 8（2026-09-22 下午，批 7 追加留项，用户授权提交 / 推送 / 部署 / 真栈）已发布 `016bd8b1` → `5818d136`**（设计文档 §10 / §10.1 / §10.1.1）：① **T2 完成轮此前不写焦点、不清续接挂起、不补挂起软提醒**——engine 的 adaptive / reactive 两条出口在 `loop.run` 之后直接 return（PU7「路线会话没盖上」的真因：第一趟 T63 走了四轮 T2、两次 navigate 全没落 `active_route`，第三趟走 E 路径就正常——「偶发」= 路径方差）⇒ `loop.run(settle=)` 完成轮合成前回调，engine 做 E 路径那三件事；真栈 RS14（条件句确定性逼 T2）×3 = 2/3，两趟 T2 里的 navigate 之后「取消导航」→ `navigate_cancel`「已结束到深圳湾公园的导航」（修前只能答「当前没有正在进行的导航」），第三趟 planner「adaptive 却零步」交 chitchat 答天气。② SF4「别提醒我，继续开就行」落「没听清」的真因：planner `addressed=false, steps=[]`（文字输入下是合法空计划）⇒ 安全闸二补第二臂——焦点里未解除的告警在场、零步且无澄清卡 ⇒ 兜底谈话（首版写在 engine，发现 planner 早有第一臂后合回一处）；③ chitchat system 补「看不到这辆车的任何实时读数」类别否定（g18「纯电续航335km」两个数都是编的）；④ 迁移预检 Serve 判据复用 `verify_tailscale_serve`（`5818d136`，基础设施锚 `86edd25e → bd0671c4`）。continuity 70/71 跑完整趟、零 cleanup 失败、env 放回；唯一红 SF4 T65「抱歉，这个我不能撤回。」——立场守住但不给替代（chitchat 在告警 prompt 下过度简短）。**追问两件（同日下午，设计文档 §10.2）**：正式基线**不能在云端跑**（L3 `e2e_journeys` 签名身份 + 持久数据，runner 对 cloud 目标按设计拒绝），L1 / L2 诊断读数可以（gRPC 没发布到宿主、SSH 转发指容器 IP `172.18.0.6:50052`）：**L1 gate 111/117（94.9%，`--repeat 1`，2 进程 × 139 case，MiniMax-M3，代码 `a7989a20`）**——1 条 stable_fail「关掉音乐」`media.stop` vs gold `media.pause`（model-gold 分歧，案例集候选），5 条 unstable 全是 planner 方差形态；forbidden 0/117、validator 后逃逸 0/117、工具通道 96/117；**L2 4/4**（gate 里 l2 只有 4 个 A5 挂起单元）；报告 `_ci-run-intent-adversarial-{l1,l2}-minimax-016bd8b1`（gitignore），不是正式基线、不与 08-09 的 141/147 直比；两次失败发布的残留已按盘点清理（builds / releases / 104 个镜像 tag / 5 个暂存目录，证据与锚备份保留，status ok 5/5）。留下：正式基线未重建（§9.2，需切 local 全栈）；planner「adaptive 却零步」/「接孩子后去万象城」只给一步并把接人当途经点搜两种落域方差；chitchat 告警下拒绝过短；③ 等真栈样本；其余 13 个成功发布的暂存目录（~690 M）与 110 份 build record（16 G）仍是清理候选、未批 |
| Android App 剩余待办（2026-09-14 起的唯一入口） | [剩余待办总表](docs/design/2026-09-14-android-remaining-todos.md)（**2026-09-24 N-04 端到端挡位 1008 / N-01 工具英文报错已修并发布 `fc1f5dde`，§11**）：E 工程可独立 / D 只缺真机 / H 缺真人、外部条件或授权 / R 已裁决不改。本日推进七条（重发终态、支持页不再被常驻层盖住、播报语域受话边界端云两处、发送跟底、重发全文）落在 `c036fd14` / `696899b5` / `cbf17b96` / `883452d2`（+ docs `9ced633b`），**已 push、已 deploy `9ced633b`**（status 5/5、verify verified、矩阵复跑见 §4.0 证据边界）；OPPO 固定包 `696899b59` / `cbf17b96e` / `883452d29` 逐格证据见总表 §5。**GPT-6 Pro Android 评审（2026-09-19）**：[逐条核对 + 分批待办](docs/design/2026-09-19-android-gpt6-review-remediation-batches.md) 是该评审的唯一入口——F01–F07 两批当日落地；2026-09-20/21 第三批（§6.15）：D-08 慢解码替身真机闭合、D-09 核出 Android 上 routeChange 死监听（E-24）与焦点启动永久持有（E-25）两条真缺陷并修、主 TTS × 系统中断五维取到、G-05 首跑揭穿 Mobile APK 工作流自 M4 起没构建过（垫片拆出 + CI 一行）；G-07（收音期持焦点 + 系统抢占放弃收音）用户裁决「都做」当晚落地并真机闭合（`ec7e0fc0`）；**OPPO 常驻包 `b08579f6c`**；09-22 凌晨安静环境补到语音轮 SPEAKING / FOLLOWUP × 中断；**S2S 格被云端身份闸挡住（`E2E_IDENTITY_ENABLED` ⇒ `/api/s2s` 对真 App 一律 1008，端到端挡位当前不可用，总表 N-04）**；耳机断开 OS 层无可配对耳机。下面各行是历史入口，状态以总表为准 |
| Android App | 进行中：[AR05 实施方案](docs/design/2026-09-09-ar05-structured-contracts-implementation-plan.md)（步骤 0–6 已实施并发布 `d425b9c`：T0 授权闸、Registry 声明往返、四份契约端云贯通、F09 窄修复、会话摘要、Android 消费面、网关编译与测试、真栈契约证据与 OPPO 固定包 `d425b9c2d`；**步骤 7（2026-09-10）只补验证、零代码改动**，见方案 §12.4。**仍未签收**，逐条见 [§9.4 签收条件](docs/design/2026-09-09-ar05-structured-contracts-implementation-plan.md) 与 §9.5 V01–V12 逐格表：**全格闭合 3（V01/V04/V11）、部分 8、仅离线 1**（前一轮是 0 格全闭合）。剩余阻塞收敛成三个前提，都要人工授权或真人参与——① **一个无 `vehicle.control` 的受限 token**（卡 V05 负例、V07 的 `permission.scope_missing`、V02 的「新身份不继承」；云端 `AUTH_TOKENS` 只有一条全授权条目，改根 `.env` 是红线）；② **一个旧 release 的部署环境 + 一次 Redis 重启**（卡 V06 挂起恢复保真、V10 的新客户端+旧服务端）；③ **真人听音**（卡 V08）。原「缺中文输入通道」这条阻塞**对卡片渲染取证不再成立**：纯 ASCII 英文语料在真栈上走同一条确认/补槽闭环（与中文语料逐字同构），`adb shell input text` 即可注入，确认卡/补槽卡/issue 卡三种渲染都已在 `d425b9c2d` 固定包上取到。⚠ 这不等于中文输入不再需要——卡片渲染不经过按语种分支的代码，但 ASR/NLU/IME 与任何把中文文本当被测对象的流程仍需真正的中文通道（AR06 A06-3/V04 照旧），且英文语料确实会改变**路由**（`navigate to Hangzhou` 绕过了模糊目的地澄清）；AR04 的服务端多 operationId 实机组合也已闭合（真机「待处理事项（3）」三条真实 operationId 并存、取消末项不串账）。Planner 技术失败降级已在 AR05 步骤 2–4 修复并取到真栈实例与真机 issue 卡，但触发率 2/7 不可稳定复现，V09 不计闭合。实现与设备基线：[AR04 第十五节](docs/design/2026-09-08-ar04-presentation-ack-implementation.md) + `mobile/README.md`。Android 代码 `1c67807`：支持页浮动在场、闲置光球静帧、滚动内容留余量；OPPO 两轮证据与资源恢复见 AR04。AR02/AR03/AR10 与 Xiaomi 对照范围独立。 |
| Android App AR06～AR11 方案 | [接续路线与六份独立方案](docs/design/2026-09-09-android-ar06-ar11-execution-roadmap.md)：AR06 release/lint、AR07 KWS A/B、AR08 首音、AR09 UI 性能/观测、AR10 固定包 UX 验收、AR11 Android M5 交付规划。2026-09-09 已落盘，均为草案、未实施；附逐批 Goal 文本、前序未签收项与完成边界。2026-09-10 补[工程交付优先与集中验收安排](docs/design/2026-09-10-android-goal-delivery-and-acceptance-plan.md)，默认不让人工签收阻塞独立工程任务。**2026-09-10 工程交付批已实施**（`7481cb5`→`c5b2c9b`）：[AR06～AR09 实施记录](docs/design/2026-09-10-ar06-ar09-engineering-implementation.md)——AR06 的 lint 门禁（0/0，反向验证会红，**CI 已实跑通过**）、e2e dev/release 分轨、中文输入在 prod 固定包上闭合（推翻「CLI 未装」的旧判断）、验包（APK SHA-256 端本一致、非 DEBUGGABLE）；AR07/08/09 交付的是**仪器**（KWS 单变量入口与四分栏计数、轮次时间线与 nearest-rank 统计、只读有界诊断页），**唤醒率与首音仍然没有数字**；UI 性能已取 P0/P3/P5 首批读数。⚠ 其中的 **F1「内存泄漏、超门槛 55×」当日晚被单变量实验推翻**：那是取证装置自己的走法（深链一路前进、从不返回 ⇒ 30 轮后 120 个屏活在栈里），换成用户真实路径（进页面后按返回键）后 Views 静置回到冷基线 151、PSS 回到 256MB，**屏是会被正确释放的**；F2 的 99% 卡顿随之改写为「那个状态正常导航到不了」。逐条更正与真缺陷见下一行的余项收口。附录记两件真栈结果：受限 token 已加并可用（`edge-vehicle` 等 14/17 `scope_missing`）；受限身份端到端**安全面成立而解释面不成立**（6/6 零动作，但话术 6 种、有一条谎称已执行、0/6 提权限）——归 AR05 未修。另附[AR10 验收准备材料](docs/design/2026-09-10-ar10-acceptance-preparation.md)（入场条件/冻结表/五人脚本/计分/报告骨架，零参与者、零结论）与[AM5 交付计划](docs/design/2026-09-10-android-m5-delivery-plan.md)（五包可派工任务、DoD 与工作量依据；零生产能力实现）。⚠ 同日一次由本会话造成的生产回退（手动 compose 漏 `RELEASE_SHA` ⇒ edge-gateway 被换成 8 月镜像、`/api/session` 404 数分钟）已恢复并逐条记录；**`status` 5/5 全程没发现它**，验证面缺「运行镜像 tag == current SHA」的对账。 |
| Android App 页面打磨批 A→G | [2026-09-10 打磨批计划 + 回填](docs/design/2026-09-10-android-ui-polish-batches.md)（评审出处 [2026-09-10 打磨评审](docs/reviews/2026-09-10-android-ui-polish-review.md)）：A 对话页信息层级（+D 启动图、J2 首页示例）→ E 删 v1 回滚路径 → B 设置页五组 + 开发者选项（prod 隐藏、构建行连点 7 次）→ C 车辆页中文化 + 线性图标 + 最小字号 → F 聊天基线（持久化 50 条、时间分隔、回到最新、重发）→ G 承诺卡人话摘要（云侧取 Registry 能力描述，`trunk.open` → 「打开后备箱」）。提交链 `0aee251`→`bd71f89` 共 10 个，**已 push**；批 G **已 deploy `d532c6d`** 并真栈复验。真机三包：`7525784b6`（批 A，12 态）/ `7fc8d9894`（最终包，12/12 态 + 冷启动恢复）/ `e95d07725`（F 追加：FlashList v2 只在 data 变化时跟底，历史一恢复列表变长就把晚到的卡片 / 键盘下的新气泡压在 Composer 下 ⇒ `stickToBottom` 一处判据；Maestro 01/08 由红转绿）。Maestro 追加包 8 条 = 6 ✅ + 09 ❌（driver 在动画页检出问题，失败截图里目标已在屏）+ 03 ⛔（飞行模式段 driver 挂起，机制未钉死，见文档「批 F 追加」未达项 ③ 与集中处理表）。Xiaomi 对照四格 4/4 已在 `e95d07725` 上截到（MIX Fold 4 外屏 360×840dp / 横 840×360dp；横屏行车档下 Dock 落在 Composer 之下）。**未闭合**：「草稿为空 ⇒ 在听…」占位格、三条待办并存截图、「重发」真机截图（断网路径到不了：uncertain ∧ pending 没有出口，下一批裁决）、Maestro 03 的 driver 挂起（工具侧）。 |
| Android App AR01～AR11 余项收口 | [2026-09-10 余项收口](docs/design/2026-09-10-ar-residuals-closeout.md)（`ea509ff`→`046fb5c`，已在 main、随后续 release 上线——2026-09-24 核对 `git merge-base --is-ancestor`）：四件工程正题。① **推翻上一轮的 F1**——「30 次路由循环内存不回落、超门槛 55×」是探针只推不弹造成的，`Views` 那一列一眼可辨（151→7651、每轮 +500、`Activities` 恒 1）；加上返回键后静置逐字回到 151。真缺陷是**外部深链只推不弹且用户可达**：桌面 Shortcut「说话」点 20 次 ⇒ Views 151→3483、PSS 207→407MB，用户无法自救。修法＝`+native-intent` 把深链规范成「目的地」（voice 变成对话页参数、进目的地前先回栈底、只接管五个真入口、认不出的原样交回），并同时修掉它会引入的回归（voice 参数改为消费参数本身而非组件实例 ref）。② **AR05 解释面**：受限身份 6 次 6 种说法、一条谎称已执行 ⇒ 过滤 catalog 时把被挡下的那半留作理由，命中即出确定性 `permission.scope_missing`（**一次 LLM 都不调**，恢复出口指能力设置不指系统权限页）；路由复用 Registry top-1 与既有 `CLARIFY_FALLBACK_MIN`，不加新词表新阈值，分不清就退回今天的行为。③ **`status` 运行镜像对账**：健康检查答不了「活着的是哪一份代码」——远端 preflight 现在报运行容器的镜像 tag，不一致／彼此不一致／读不到三种分开报，任一 ⇒ degraded。④ p3 探针改走用户的路（`--nav back` 为默认），内存读数带上对象计数。⑤ **E-02 闭合**：Dock 前提两趟真栈跑通（`uxV2Dock` 回读 → 流 02 与流 06 各 RC=0，两条流都走取消、零车控执行）。四处判据均双向反向验证判红。**真机证据**绑固定包 `1f241c8c5`（APK SHA-256 `4c2c16bd…6985`，设备侧回读逐字节相同、非 DEBUGGABLE）：深链三臂 A 3483→**183** views、B 2490→**278**，而**故意不接管**的对照臂 C 修复前后都是 **16911**（逐字相同 ⇒ 装置照旧测得出「涨」）；AR09 P3 修正协议下 Views 151→**151**，第 1 趟（冷起）+88.7MB、第 2 趟（暖起）**+1.0MB** ⇒ 那 88.7MB 是首次触达成本不是累积，帧 4.10%/3.71% janky、p50 17/16ms。**本机全量固定口径 8231 passed / 32 skipped / 0 failed**（上一基线 8205/32）；mobile 876 + tsc/lint 0、hmi 333、`go build/vet/test ./gateway/...` 全绿、四道门禁 + smoke_edge 13 全过。⚠ 顺带定死一条操作判据：**Maestro 的 driver 与任何走 `uiautomator` 的工具互斥**（driver 不停就报 `UiAutomationService already registered!`，症状是整机 dump 全被 SIGKILL，看着像被测对象坏了）。⑥ **AR05 V05 / E-06 车控端到端负例闭合**（用户单独授权该用例）：受限身份发两种车控语料——`打开后备箱`（需确认）被**云侧新判据**拦下且 `confirm_policy`/`operation_id` 根本没生成（scope 闸先于确认闸），`打开空调26度`（无需确认、闸失效即当场执行）被**端侧 T0 闸**拦下，两次车态读数逐字段相同、零执行零残留。**②③ 已 deploy 并真栈复验**（生产 release 已从 `d425b9c` 推进到 `74852a7`，push/dry-run/apply 逐步单独授权，含 CI/CD digest；该次发布同时把上一轮已 push 未部署的 AR06～AR09 整批带上生产）：受限身份同题六次 **6/6 逐字相同**、6/6 `permission.scope_missing` 点名「导航助手」、恢复出口指能力设置（此前是 6 次 6 种、含一条谎称已执行、0/6 提原因）；`status` 新列 `running_release_sha` 与 `release_sha` 两次读数均一致，并在一次 apply 报 runtime 失败时**当场回答了「生产有没有被动过」**。**未闭合**：E-03 唤醒率（本轮未安排）／E-04 首音（缺外部时基与真人）、E-08 五人 UX（缺参与者）、E-07 旧服务端+Redis 重启、P1/P2/P4 与系统级动画缩放那一臂；AR05 的 V06/V08/V09/V10 照旧未闭合。 |
| Android 四项体验修正（2026-09-11，用户口述） | [设计与实施记录](docs/design/2026-09-11-android-sheet-controls-map-tabletop.md)：① 语音层整层任意位置下滑收起（滚动区在顶部才接管、横滑不吃、跟手 + 快甩）+ 泊车路径补内容下限（矮容器不再裁球，主力机读数逐 dp 不变）；② 可点控件两档制——按钮 = `TARGET`（48/56），胶囊/chip = `PILL`（36/44）经 `ui/Pill`，外框仍撑到触控目标；③ 地图路线——navigation / charging_planner 的 `route_plan` / `charging_route` 卡带 `origin_loc` / `destination_loc` / 途经点坐标 / 抽样折线（`hmi/src/types.ts` 加可选字段），客户端 `core/map/geometry.ts` 一份判据、地图页（高德 SDK）画折线与角色标注、舞台内嵌地图；④ 桌面姿态上半横排。本地：mobile jest / tsc / lint、navigation + charging 单测、hmi 333 全绿（计数见记录 §7）。**已 push（`979854a` + `e38cd75`）、已 deploy `e38cd75`（dry-run 零阻断 → apply submitted → status 5/5 healthy、running SHA 对齐 → verify verified）**，真栈同题对照证明 `route_plan` 卡从零几何变成带起终点坐标 + 240 点折线。首个候选包在 OPPO 上抓到一个真缺陷：Marker 自定义标注触发库的 `update` 命令、Fabric 互操作层把缺席的 args 当 null ⇒ 整个 App 退到桌面；修法是 patch-package 让命令永远带数组（`e38cd75`）。**未闭合**：折叠机桌面姿态横排要 Xiaomi 真机；整层下滑的手感只在 OPPO 上验过一次（深链升层 → 内容区下滑 → 层收起）。 |
| Android 语音输入采纳 | `f8fd151` 已推送、部署、verified；OPPO 同 SHA 包。输入来源与拒识终态已修；端侧新闻快捷规则误判与播报语域漏拒 2026-09-14 已修并发布（`696899b5`，随 `9ced633b`，QA 交接页 §5），**没有播报语域的乘客句仍分不出、归声学（总表 H-06）**。真实文字续测12/18符合预期（背景6/12、正常6/6），声学未验；[当前核实与后续范围](docs/reviews/2026-09-11-voice-input-acceptance-live-findings.md) |
| 支付余项 | 等支付宝沙箱恢复、微信商户号到位；不做最终付款 |
| 端侧能力台账 | `orchestrator/edge/knowledge/capability_exemptions.yaml` 与 reachability 测试 |
| `memory_item` 信息衰减 | 出现第二个可复现实例后再立项，不凭单例改 supersede |
| M5 后续 | catalog 再次裁剪或范例规模/真实流量达到文档触发条件后启动 |
| 订座/票务 | 有真实 Provider 再做，不为对标造假能力 |
| 可执行性 canary | shadow 分布人工裁定后再由用户拍板；入口 `docs/design/2026-08-10-b6-actionability-forward.md` |

### 4.3 读数纪律

- **记录缺陷不等于修复缺陷**：台账必须标“仅记录 / 已修 / 待触发”；修好后回归锁仍保留。
- **分布不代替逐条证据**：unstable 可能是边界方差，也可能是稳定低通过率；逐条看 trace。
- **扫描类断言必须做反向验证**：临时注入一处目标缺陷，证明它真的会红，再恢复实现。
- **先确认输出通道**：toolcall、salvage、流式和 deterministic handler 的分布不可混算。
- **总数跨趟通常不可比**：时间、商户营业、provider/QPS、判据版本都会改变分母；逐条对原红。
- **证据不跨 SHA**：本地全量、部署 release、真栈 artifact 与后续 docs/test 提交分栏记录。

## 5. 接手第一步

```powershell
python scripts/dev_stack.py target show
git status --short --branch
git log -5 --oneline --decorate
```

然后按任务读取：

- QA：`docs/reviews/2026-08-30-qa-closeout-handoff.md`；
- 云端迁移/发布：`docs/dev-guide.md` + `docs/reviews/2026-08-17-cloud-data-migration-handoff.md`；
- Planner/安全：架构 §5.2.13、约定 §9.40、安全专题设计；
- mobile：`mobile/README.md` + `docs/design/README.md` 中最新 mobile implementation plan。

引用任何 release、测试数或长会话结果前，先核对 SHA 与 artifact；不得从旧段落抄数字。

## 6. 改完怎么验证

### 6.1 全量固定口径

PowerShell：

```powershell
$env:TZ = 'UTC0'
Remove-Item Env:PYTHONIOENCODING -ErrorAction SilentlyContinue
python -X utf8 -m pytest -q -n 8 --dist worksteal
```

- `pytest.ini` 已固定 `--import-mode=importlib`；不要重复另造口径。
- 内存充足可用 `-n auto`；可用内存约 6GB 时用 `-n 8`，避免 worker 被 OOM 杀死。
- 并行偶发红先单文件/串行复跑；OS lock、真实子进程和其他 agent 会污染读数。
- 跑批期间不改工作树；读数属于 collect 时的树。
- `target=cloud` + 本地 Docker 停时，32 skipped 是当前 Windows 基线。

### 6.2 四道 blocking 门禁与端侧 smoke

```powershell
python test/smoke_edge.py
python test/eval_skills.py
python test/eval_exemplars.py
python scripts/check_intent_gate.py
python test/eval_capability_integrity.py
```

四道 CI blocking 门禁是 skills、exemplars、L0 strict、capability integrity；均为零 LLM、零网络。

### 6.3 云端验证

```powershell
python scripts/dev_stack.py target show
python scripts/dev_stack.py status
python scripts/dev_stack.py verify
```

- `submitted` 不等于部署成功；必须独立跑 status 与 verify。
- worktree 部署可用干净隔离克隆/工作树，只复制 `dev-stack.local`，不要复制 `.env`。
- status/verify 需要根 `.env` 的 Tailnet 端点与 token；deploy 本身不读根 `.env`。
- 长会话必须显式 `--expected-sha <完整40位release>`。
- 迷你集默认不 pin provider，运行前先确认实际 provider/model；迷你集没有清理段。

### 6.4 证据纪律

- release SHA、测试 SHA、artifact SHA 分栏记录；不把邻近提交的全量结果转借给 release。
- 自动 PASS 不等于业务正确：读 speech、actions、card、trace、cleanup 和 open operations。
- 单次采样不当基线；模型方差项至少 repeat 3。
- 回放是尺子重算，不是当前真栈复跑。
- 测试 fixture 的环境/顺序问题要隔离定性，不能用 skip 或注释绕过。

## 7. 常见工程任务

### 7.1 新增 Agent

1. 新建 `agents/<name>/manifest.yaml`、源码、README、tests；
2. 遵守 `proto/cockpit/agent/v1/agent.proto`；
3. 注册服务，不改 orchestrator 核心分支；
4. 加 capability 契约、权限、确认、provenance 与验证用例；
5. 跑服务测试、Cloud Planner、门禁和全量。

详细流程见 `CLAUDE.md` §3。

### 7.2 新增端侧车控能力

1. 改 `orchestrator/edge/knowledge/commands.yaml`；
2. 明确对象、operate、权限、`require_confirm`、drive/voice 限制；
3. 让生成器派生意图，不手写第二份集合；
4. 跑 capability integrity、intent gate 和 edge tests；
5. 确认规则产出的命令能通过 VAL，不只验证名字存在。

### 7.3 改 proto / manifest

- proto 先改真相源，再 codegen；generated 文件 gitignore，不手改、不 force-add；
- manifest 新字段要检查 YAML loader、Registry 持久化 round-trip、Step 装配、挂起恢复和执行出口；
- 可选 JSON null 在 map<string,string> 边界视为“未提供”，不得转成 `"None"`。

## 8. 协作与文档

- 默认中文；结论先行，代码/命令/变量用英文。
- 变更前读规则；大改先给方案，用户确认后实施。
- 修改后主动验证，不能用注释、skip 或宽松断言掩盖失败。
- 工作树可能有别人改动；只碰本任务文件，禁止 `git reset --hard`、rebase、force-push。
- 删除文件/目录、改 `.env`/密钥/CI/CD、数据库迁移、push、生产部署都要人工授权。
- `AGENTS.md` 是规则与当前入口，不是变更日志；逐批过程写 `docs/agents-history.md`。
- 已完成的 implementation plan 保留作实施证据，但不作为“下一步”入口。
- 文档中的“今天/最近”只用于引用原始用户话术；状态一律写绝对日期。
